# Custom run judge sample

This sample implements and registers an `IRunJudge`. Its sibling test project runs
the public run-judge contract against the implementation.

The projects reference packed NuGet artifacts, not Tracon source projects. Run them
through the repository's release gate as described in [`../README.md`](../README.md).

