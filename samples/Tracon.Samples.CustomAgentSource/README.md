# Custom agent source sample

This sample loads agent definitions from JSON and exposes them through
`IAgentSource`. Its sibling test project runs the public agent-source contract.

The projects reference packed NuGet artifacts, not Tracon source projects. Run them
through the repository's release gate as described in [`../README.md`](../README.md).

