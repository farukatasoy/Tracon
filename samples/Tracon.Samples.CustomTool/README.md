# Custom tool sample

This sample shows generated tools, a hand-built `AIFunction`, and factory-based
registration for tools that need scoped services. Its sibling tests exercise the
public registration paths and runtime behavior.

The projects reference packed NuGet artifacts, not Tracon source projects. Run them
through the repository's release gate as described in [`../README.md`](../README.md).

