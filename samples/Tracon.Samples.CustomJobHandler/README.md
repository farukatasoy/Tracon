# Custom job handler sample

This sample implements an `IJobHandler` and registers it under a stable key. Its
sibling test project runs the public job-handler contract.

The projects reference packed NuGet artifacts, not Tracon source projects. Run them
through the repository's release gate as described in [`../README.md`](../README.md).

