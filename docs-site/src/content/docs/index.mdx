---
title: Tracon — approach control for your agents
description: A .NET package family that adds a production control plane to Microsoft Agent Framework, with run recording, tenant boundaries, tool policies, and an embedded console.
template: splash
hero:
  title: Your agents fly. You own the airspace.
---

import '../../styles/landing.css';

import { Code } from '@astrojs/starlight/components';

<div class="landing-intro">
  <h2>Autonomy needs<br />an operating model.</h2>
  <p>Your agent can answer a question. Operating it asks different questions: which tenant owns the session, who may call a tool, how work resumes, and what happened during a run. Tracon gives those questions a place in your application.</p>
</div>

<div class="operation-strips">
  <section class="operation-strip">
    <div><span class="strip-id">01 / Boundary</span><h3>Separate.</h3></div>
    <p>One host can serve several tenants. Tenant-scoped stores keep their records apart; authentication and caller attribution determine which scope a request enters.</p>
    <a href="/getting-started/security/">Set the tenant boundary →</a>
  </section>
  <section class="operation-strip">
    <div><span class="strip-id">02 / Coordination</span><h3>Sequence.</h3></div>
    <p>A task can involve several agents and a human decision. Coordinate workflows with checkpoints and resume; schedule recurring work or queue a run for a worker.</p>
    <a href="/concepts/workflows/">Follow a workflow →</a>
  </section>
  <section class="operation-strip">
    <div><span class="strip-id">03 / Permission</span><h3>Clear.</h3></div>
    <p>A model proposing a tool call does not set its policy. Define tools in code, apply authorization, and require approval for the calls you choose to review.</p>
    <a href="/concepts/tools/">Define the tool policy →</a>
  </section>
  <section class="operation-strip">
    <div><span class="strip-id">04 / Evidence</span><h3>Record.</h3></div>
    <p>Inspect run events, tool calls, timing, and configured cost data. Catalog agents record by default. Recording is best-effort: a store failure is logged while the agent continues.</p>
    <a href="/concepts/runs/">Read a recorded run →</a>
  </section>
</div>

<p class="inventory-line"><a href="/packages/"><strong>20</strong> NuGet packages</a> · <a href="/http-api/"><strong>168</strong> generated HTTP operations</a> · <a href="/ui/"><strong>30</strong> embedded console screens</a></p>

## MAF runs the agent. Tracon provides the controls.

Keep MAF's `AIAgent`, `AgentSession`, `ChatMessage`, and `AIFunction`. Tracon adds
catalogs, policies, persistence, inspection, and HTTP access around those objects.
There is no parallel agent type hierarchy to adopt.

<span id="thirty-seconds"></span>

```mermaid
flowchart TD
    accTitle: Microsoft Agent Framework and Tracon responsibilities
    accDescr: Your ASP.NET Core application contains Tracon's control plane and the MAF agent runtime. Tracon supplies agent catalogs, policies, and recorded state around MAF execution. Your code and the embedded console use the control plane.
    HOST["Your ASP.NET Core application"] --> CONTROL["Tracon control plane<br/>catalog · policies · stores · HTTP API"]
    CONTROL --> MAF["Microsoft Agent Framework<br/>agent execution · model and tool interaction"]
    UI["Your code · embedded console · HTTP clients"] --> CONTROL
```

## The console ships in the assembly.

Add `Tracon.UI` and register `UseUI()` to embed the console in your host. It reads
the same management API that your own clients can call. The consuming application
does not need Node.js or a JavaScript build to serve it.

<figure class="product-shot">
  <a href="/screenshots/dashboard.png">
    <img src="/screenshots/dashboard.png" alt="The Tracon console showing recorded runs, tokens, and recent activity from the documentation sample" width="2880" height="1800" loading="lazy" decoding="async" />
  </a>
  <figcaption>The real console, captured from a local sample with seeded runs. This is sample data, not live telemetry.</figcaption>
</figure>

<Code lang="csharp" title="Program.cs · registration in a configured host" code={`using Tracon;

var builder = WebApplication.CreateBuilder(args);
builder.AddTracon().UseUI();

var app = builder.Build();
app.MapTracon("/tracon");
app.Run();`} />

This registers the control plane and console. An agent and model provider are
configured separately. Storage starts in memory; add persistence when the records
must survive a restart. Tracon is not yet published: the [source-based guide](/getting-started/first-agent/)
requires access to a checkout of the repository.

## Choose your path

<ul class="route-list">
  <li><a href="/getting-started/first-agent/"><strong>Build</strong><span>Configure a source checkout, run an agent, and inspect the result.</span><span aria-hidden="true">↗</span></a></li>
  <li><a href="/guides/openai-api/"><strong>Integrate</strong><span>Connect an OpenAI-compatible client to an existing agent.</span><span aria-hidden="true">↗</span></a></li>
  <li><a href="/guides/production/"><strong>Operate</strong><span>Plan persistence, authorization, workers, telemetry, and recovery.</span><span aria-hidden="true">↗</span></a></li>
  <li><a href="/capabilities/"><strong>Evaluate</strong><span>Review capabilities, package choices, defaults, and operating limits.</span><span aria-hidden="true">↗</span></a></li>
</ul>

<div class="boundary-note">
  <p><strong>Your host owns the boundary.</strong> Tracon is a .NET package family embedded in your application. You configure authentication, model providers, storage, and outbound connections. Tools are defined in code; the console cannot write tool code. <a href="/getting-started/">Read the architecture and limits</a> before choosing an integration.</p>
</div>

Public packages are not available yet. The ASP.NET Core integration also uses
preview MAF hosting dependencies. Review [compatibility](/reference/compatibility/),
[versioning](/reference/versioning/), and [licensing](/reference/licensing/)
as part of your evaluation.
