{
  "name": "tracon-ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Embedded management console for Tracon. Built into Tracon.UI as embedded resources.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/check-tokens.mjs && node scripts/check-modal-layer.mjs && node scripts/check-custom-classes.mjs && tsc --noEmit && vitest run && vite build && node scripts/postbuild.mjs && vite build --config vite.embed.config.ts && node scripts/postbuild-embed.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tracon/client": "file:../../../packages/tracon-client",
    "@tanstack/react-query": "^5.103.1",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "@testing-library/dom": "^10.4.2",
    "@testing-library/react": "^16.3.3",
    "@testing-library/user-event": "^14.6.7",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "jsdom": "^30.1.0",
    "tailwindcss": "^4.1.14",
    "typescript": "^5.9.3",
    "vite": "^8.3.0",
    "vitest": "^3.2.4"
  }
}
