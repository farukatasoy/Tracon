# Generated by AmbientWriteSiteTests. Refresh:
#   AGENTPRISM_AMBIENT_WRITE_REFRESH=1 dotnet test tests/AgentPrism.Core.UnitTests -c Release
# One line per <path>:<method>. The reason is at least 40 characters and explains
# why the write site is safe (see MEMORY.md's AsyncLocal rule). New entries this
# refresh added carry a placeholder reason that MUST be replaced by hand.
src/AgentPrism.AspNetCore/McpServer/CatalogToolCallHandler.cs:HandleAsync | using var tenantScope opens and restores the ambient tenant for exactly one MCP call in this method's own body, same pattern as JobWorkerBackgroundService
src/AgentPrism.AspNetCore/McpServer/McpTaskRunProvisioning.cs:FilterAsync | Both SetCurrent calls are in this method's own body around await next(...), never inside a nested async helper
src/AgentPrism.AspNetCore/McpServer/McpTaskRunProvisioning.cs:SetCurrent | This IS the wrapper's own definition, a synchronous setter, not an ambient-write call site itself
src/AgentPrism.AspNetCore/McpServer/RunBackedMcpTaskStore.cs:CloseIfStillQueuedAsync | using var tenantScope wraps one orphaned-row cleanup in this method's own body, not a loop; scoped to the run's own recorded owning tenant
src/AgentPrism.AspNetCore/McpServer/RunBackedMcpTaskStore.cs:SetCancelledAsync | using var tenantScope wraps this method's own single GetRunAsync/CompleteRunAsync pair, not a loop; no nested async helper involved
src/AgentPrism.Core/Compilation/CompactionUsageTrackingChatClient.cs:GetResponseAsync | Not an async iterator; a single non-streaming span opened in the method's own synchronous-continuation body (Faz 93 measured)
src/AgentPrism.Core/Recording/AgentPrismRunContext.cs:SetCurrent | This IS the wrapper's own definition, a synchronous setter, not an ambient-write call site itself
src/AgentPrism.Core/Recording/RunReconciliationService.cs:TryContinueAsync | Background reconciliation loop; scope opened with using(...) around one continuation attempt, not a streaming path
src/AgentPrism.Core/Recording/RunRecordingAgent.Lifecycle.cs:PrepareRun | Synchronous method (not async); attribution and span resolved here on purpose so the AsyncLocal write stays in the caller's own body
src/AgentPrism.Core/Recording/RunRecordingAgent.cs:RunCoreAsync | Non-streaming path; SetCurrent is called in this async method's own body, the documented correct pattern (Faz 6/11)
src/AgentPrism.Core/Recording/RunRecordingAgent.cs:RunCoreStreamingAsync | Streaming iterator; the write repeats immediately before every MoveNextAsync inside the loop (Faz 12 fix)
src/AgentPrism.Core/Scheduling/AgentRunJobHandler.cs:ExecuteAsync | using var attribution opens the scope in this method's own body and holds it across every await of the one job; not a streaming iterator, so no MoveNextAsync boundary is crossed
src/AgentPrism.Core/Scheduling/JobWorkerBackgroundService.cs:ExecuteJobAsync | using var tenantScope opens and restores the ambient tenant for exactly one job execution, not a streaming loop
src/AgentPrism.Core/Skills/Scripts/SandboxedSkillScriptRunner.cs:RunFileScriptAsync | using var activity opens the span in this method's own body; no streaming loop crosses a yield boundary here
src/AgentPrism.Core/Skills/Scripts/SandboxedSkillScriptRunner.cs:RunStoredScriptAsync | Same pattern as RunFileScriptAsync: using var activity in the method's own non-streaming body
src/AgentPrism.Workflows/Internal/WorkflowRunner.cs:PumpAsync | Streaming iterator; the ambient scope is rewritten immediately before every step inside the inner loop (Faz 15 fix)
src/AgentPrism.Workflows/Internal/WorkflowRunner.cs:RunGuardedAsync | Root span and scope opened once in this method's own body before the nested PumpAsync, which repeats its own write per step
