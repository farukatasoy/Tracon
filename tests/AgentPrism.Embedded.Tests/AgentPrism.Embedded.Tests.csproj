<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!--
      Tests the ACTUAL entry point (samples/AgentPrism.Embedded/Program.cs), not
      a reconstruction of its wiring: WebApplicationFactory<T> needs an entry
      point assembly, which the sample IS and the AgentPrism.* library is not —
      see the comment next to Microsoft.AspNetCore.Mvc.Testing in
      Directory.Packages.props for why every other test project uses TestHost
      instead.
    -->
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" />
    <PackageReference Include="Microsoft.AspNetCore.TestHost" />
    <ProjectReference Include="../../samples/AgentPrism.Embedded/AgentPrism.Embedded.csproj" />
  </ItemGroup>

</Project>
