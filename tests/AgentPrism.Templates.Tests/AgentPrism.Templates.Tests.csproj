<Project Sdk="Microsoft.NET.Sdk">

  <!--
    This project is NOT linked to AgentPrism.Templates as code: the template
    package's IncludeBuildOutput=false, there is no build output to link
    against. Tests run the `dotnet` CLI as a subprocess (pack, new install,
    new, build, and starting the generated app to verify it over HTTP).
  -->

</Project>
