<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="../../src/AgentPrism.SqlServer/AgentPrism.SqlServer.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Testcontainers.MsSql" />
    <!-- Pins to the patched version for GHSA-q939-rpr3-3284 (SSH.NET, a transitive dependency of Testcontainers). -->
    <PackageReference Include="SSH.NET" />
  </ItemGroup>

  <!--
    Shared contract tests (Phase 23). The SAME abstract classes also run in
    AgentPrism.PostgreSql.IntegrationTests. This phase's exact test was that:
    adding a third persistence implementation should NOT REQUIRE writing a new
    contract test.
  -->
  <ItemGroup Label="Shared contract tests (Phase 23)">
    <Compile Include="../Shared/**/*.cs" LinkBase="Shared" />
  </ItemGroup>

</Project>
