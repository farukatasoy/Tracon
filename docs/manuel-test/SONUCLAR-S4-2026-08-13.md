# Şerit 4 — Koşum Sonuçları (2026-08-13)

> Şerit 4 · port 5084 · şema `mt_s4` · worktree `../ap-s4` · dal `test/kosum-s4`
> Protokol: [`KOSUM-PLANI.md`](KOSUM-PLANI.md)

## Kurulum sapmaları (bir kez, tüm şerit için geçerli)

1. **§3.1 worktree komutu.** Plandaki `git worktree add ../ap-s4 -b test/kosum-s4 docs/manuel-test`
   `docs/manuel-test`'i dal adı sanıyor (yanlış — bir dizin). `git worktree add ../ap-s4 -b test/kosum-s4`
   (HEAD'den) kullanıldı.
2. **Tam çözüm derlemesi başarısız oldu.** `dotnet build AgentPrism.slnx -c Release`
   worktree'de `NU1903` (SSH.NET güvenlik danışma uyarısı, warn-as-error) ile
   restore aşamasında çöktü — `AgentPrism.PostgreSql.IntegrationTests` ve
   `AgentPrism.SqlServer.IntegrationTests` projelerinde. `main`'de bu iki proje
   zaten restore edilmiş olduğundan (global NuGet cache) sorun görünmüyordu;
   worktree'nin kendi `artifacts/` yolu taze restore istiyor ve NuGet Audit
   canlı danışma veritabanına her seferinde bakıyor. Şerit 4'ün kapsamı bu iki
   entegrasyon test projesini hiç kullanmadığından `dotnet build
   samples/AgentPrism.Api -c Release` ile dar derleme yapıldı — arayüz build'i
   dahil (npm/tsc/Vitest/Vite/Brotli), 0 hata. **Faz kapanışında** dört
   doğrulama kapısının tamamı (tam `dotnet build/test/pack/format`) yine de
   çalıştırılmalı; bu sapma yalnız koşum oturumları için geçerli.
3. **`user-secrets` yerine ortam değişkeni kullanıldı** — şerit izolasyonu
   (KOSUM-PLANI §2.2). Anahtarlar `dotnet user-secrets list --json` ile
   **salt okunur** çekildi, hiçbir dosyaya/loga yazılmadı.
4. **Reset yalnız şerit başında (S4-1 öncesi) uygulandı, dosya başına DEĞİL.**
   00-INDEKS §4'ün "bir dosyaya başlamadan önce" ifadesi ile `10-ARAYUZ-AGENT-
   PLAYGROUND.md`/`11-ARAYUZ-RUN-SESSION-SSE.md`'nin kendi ön koşulları
   ("`FIX-AGENT-01` sonraki koşumlarda SİLİNMEDEN bırakılır", dosya 11 dosya
   10'un agent'ını kullanır) çelişiyor. KOSUM-PLANI §3.3 zaten "şerit
   kapsamlı" reset öngördüğünden, `mt_s4` şeması yalnız S4-1'in başında bir
   kez drop+migrate edildi; sonraki oturumlar arasında veri BİLEREK korundu.
5. **Ortam değişkeni `unset` etmek, `dotnet user-secrets`teki değeri gizlemiyor**
   (config sağlayıcı sırası: env yalnız AYNI anahtar SET edilirse user-secrets'ı
   ezer). "Kayıtlı değil" davranışı için boş string gerekir
   (`export AgentPrism__Ui__AuthToken=""`), tam `unset` değil — KOSUM-PLANI
   §2.2'nin zaten belirttiği kural, ama pratikte gözden kaçırılması kolay.

---

## Devir notu — S4-2: `09` §3–§5 koşuldu, 4 kusur bulundu (2026-08-13)

**Sonuç: 17/17 koşuldu — 10 Geçti, 4 Kaldı, 3 Atlandı.**

| Case | Durum | Not |
|---|---|---|
| MT-UI-018 | ✅ Geçti | `⌘K` paleti açar, `insideText:true` Playground istem kutusunda da doğrulandı |
| MT-UI-019 | ✅ Geçti | Ok-aşağı + Enter, vurgulu komutun `perform()`'unu doğru çalıştırdı |
| MT-UI-020 | ❌ Kaldı | **`HATA-S4-004`** — `Esc` sonrası odak paleti açan `⌘K` düğmesine dönmüyor, `document.body`'de kalıyor |
| MT-UI-021 | ⏭ Atlandı | MT-UI-010/011 ile aynı yapısal engel — `Reader` oturumu tarayıcıda kurulamıyor |
| MT-UI-022 | ✅ Geçti | `g a` hızlı dizisi Agents'a gitti, `g` + 2.2sn + `a` hiçbir şey yapmadı |
| MT-UI-023 | ✅ Geçti | Metin alanında `merhaba` tam olarak yazıldı, `g` yutulmadı |
| MT-UI-024 | ❌ Kaldı | **`HATA-S4-005`** — `/` kısayolunun hedeflediği `input[data-search]` HİÇBİR ekranda yok (ölü özellik, `?` yardımında reklamı var) |
| MT-UI-025 | ✅ Geçti | `?` 11 satırlık kısayol tablosunu açtı, "Kapat" otomatik odaklandı, `Esc`/tıklama ikisi de kapattı |
| MT-UI-026 | ⏭ Atlandı | Tooling kısıtı — Playwright Chromium `prefers-color-scheme` OS'un gerçek Dark ayarını yansıtmıyor, emülasyon aracı yok |
| MT-UI-027 | ✅ Geçti | Tema düğmesi anında değişti, `localStorage` ve DOM kalıcılığı yeniden yüklemede doğrulandı |
| MT-UI-028 | ❌ Kaldı | **`HATA-S4-006`** — Ayarlar'daki tema `<select>`'i üst çubuk düğmesiyle state paylaşmıyor; SPA içi değişiklikte düğme donuk/yanlış kalıyor |
| MT-UI-029 | ✅ Geçti | Üst çubuk düğmesinden `system`'e asla dönülemediği canlı doğrulandı |
| MT-UI-030 | ✅ Geçti | Dil düğmesi TR↔EN'i anında, sayfa yenilemeden değiştirdi |
| MT-UI-031 | ⏭ Atlandı | Tooling kısıtı — tarayıcı dilini `tr-TR` yapacak bir araç yok; `matchLocale` mantığı ayrıca doğrulandı |
| MT-UI-032 | ❌ Kaldı | **`HATA-S4-007`** (kritik kapsam) — sunucunun `ProblemDetails` metinleri İngilizce DEĞİL, kodun içine gömülü sabit Türkçe; 28 kaynak dosyasını etkiliyor |
| MT-UI-033 | ✅ Geçti | 5 ekran, 2 dil, programatik tarama — çıplak placeholder yok, yatay taşma yok |
| MT-UI-034 | ✅ Geçti | `Total tokens` `en`'de `1,540`, `tr`'de `1.540` — `Intl.NumberFormat` doğru |

**Bulunan 4 kusur:**

### HATA-S4-004 — Komut paleti `Esc` sonrası odağı açan öğeye döndürmüyor

- **Case:** MT-UI-020
- **Önem:** Orta
- **İzlek:** B
- **Ortam:** macOS arm64 · net10 · PostgreSQL · yok (arayüz)

**Beklenen**
`Esc` ile palet kapandığında odak, paleti açan `⌘K` düğmesine (`data-testid="palette-open"`) döner.

**Gerçekleşen**
Palet kapanıyor ama odak `document.body`'ye düşüyor; hem klavye kısayoluyla
(`Cmd+K`) hem düğmeye tıklayarak açılan palet için aynı sonuç.

**Yeniden üretme**
1. `⌘K` düğmesine tıkla (veya `Cmd+K` bas).
2. `Tab`'a birkaç kez bas (odak tuzağı doğru çalışır, girişte kalır).
3. `Esc`'e bas.
4. `document.activeElement` kontrol et — `BODY`.

**Kanıt**
- `src/AgentPrism.UI/frontend/src/components/layout.tsx:157`
  `onClose={() => setPalette(false)}` yalnız state kapatıyor, focus-restore yok.
- `src/AgentPrism.UI/frontend/src/components/command-palette.tsx:117-122`
  `Escape` dalı da yalnız `onClose()` çağırıyor.
- Karşılaştırma: aynı dosyadaki `ShortcutHelp` (satır 376-378) KENDİ "Kapat"
  butonuna odaklanıyor ama bu da açılıştaki davranış, palet'i AÇAN öğeye
  dönme değil.

**Kapsam**
Yalnız bu case — paletin HER kapanışını etkiliyor (klavye veya buton, fark
etmiyor); klavye kullanıcıları için orta düzey ergonomi regresyonu.

---

### HATA-S4-005 — `/` kısayolu hiçbir ekranda hedef bulamıyor (ölü özellik)

- **Case:** MT-UI-024
- **Önem:** Düşük
- **İzlek:** B
- **Ortam:** macOS arm64 · net10 · PostgreSQL · yok (arayüz)

**Beklenen**
`/` tuşu, `input[data-search]` özniteliği taşıyan bir arama kutusuna
odaklanır (örnek ekran: Agents listesi).

**Gerçekleşen**
`src/AgentPrism.UI/frontend/src/screens/*.tsx` içindeki HİÇBİR ekran
`data-search` özniteliğini hiç kullanmıyor — `grep -rln "data-search"
screens/` sıfır sonuç veriyor. Agents listesi (kod VE canlı DOM ile
doğrulandı) arama kutusu içermiyor. Buna rağmen özellik `?` kısayol
yardımında kullanıcıya "Focus the search box on this screen" olarak
reklam ediliyor.

**Yeniden üretme**
1. Agents ekranına git.
2. `/` tuşuna bas.
3. `document.activeElement` `BODY`'de kalıyor, `input[data-search]`
   sayfada hiç yok.

**Kanıt**
- Tüketen taraf: `src/AgentPrism.UI/frontend/src/components/layout.tsx:202`.
- Reklam: `command-palette.tsx:394`, `locales/en.ts:135`, `locales/tr.ts:134`.
- `grep -rln "data-search" src/AgentPrism.UI/frontend/src/` → yalnız
  `layout.tsx`, sıfır üretici ekran.

**Kapsam**
Uygulama genelinde — `/` kısayolu tüm konsolda hiçbir zaman işlevsel değil;
kullanıcıya yanlış beklenti veren belgelenmiş ama uygulanmamış bir özellik.

---

### HATA-S4-006 — Ayarlar'daki tema seçici üst çubuk düğmesiyle state paylaşmıyor

- **Case:** MT-UI-028
- **Önem:** Düşük
- **İzlek:** B
- **Ortam:** macOS arm64 · net10 · PostgreSQL · yok (arayüz)

**Beklenen**
Ayarlar ekranındaki tema `<select>`'i "Sistemi izle" yapıldığında üst
çubuktaki düğmenin ikonu/etiketi O ANKİ çözümlenen temayı (`resolveTheme
('system')`) yansıtır.

**Gerçekleşen**
Önce üst çubuk düğmesiyle tema `dark` yapıldı, sonra SPA içi gezinmeyle
Ayarlar'a gidilip `<select>` "Follow system" yapıldı. Sayfanın GERÇEK teması
doğru şekilde `light`'a döndü (`data-theme="light"`, bu makinenin
`prefers-color-scheme` `false` olduğu için) ama üst çubuk düğmesi YANLIŞ
kaldı: `title="Theme: dark"`, `aria-label="Switch to light theme"`, ikon
hâlâ ay (`MoonIcon`) — sanki hâlâ karanlık temadaymış gibi.

**Yeniden üretme**
1. Üst çubuk düğmesiyle tema `dark` yap.
2. Ayarlar'a SPA içi git (tam sayfa yenilemeden).
3. Konsol panelindeki tema `<select>`'ini "Follow system" yap.
4. `document.documentElement.dataset.theme` = `"light"` (doğru) ama üst
   çubuk düğmesinin `title`/`aria-label`/ikonu hâlâ `dark`'ı gösteriyor.

**Kanıt**
- `src/AgentPrism.UI/frontend/src/components/layout.tsx:319-353` `ThemeToggle`
  kendi `useState<ThemePreference>`'ını yalnız MOUNT anında
  `readThemePreference()` ile dolduruyor, dışarıdan güncellenmiyor.
- `src/AgentPrism.UI/frontend/src/screens/settings.tsx:31,134-147` Ayarlar
  `<select>`'i BAĞIMSIZ bir `useState` taşıyor, `writeThemePreference`/
  `applyTheme`'i doğrudan çağırıyor, `ThemeToggle`'ı bilgilendirmiyor.
- Paylaşılan bir context/store yok; `ThemeToggle` kabuğun parçası olduğu
  için SPA gezinmesinde hiç unmount olmuyor.

**Kapsam**
Ayarlar'daki `<select>`'ten yapılan HER tema değişikliği üst çubuk
düğmesini bir sonraki TAM SAYFA YENİLEMESİNE kadar yanıltıcı bırakır.

---

### HATA-S4-007 — Sunucunun `ProblemDetails` metinleri İngilizce değil, koda gömülü sabit Türkçe (28 dosya)

- **Case:** MT-UI-032
- **Önem:** Yüksek
- **İzlek:** B
- **Ortam:** macOS arm64 · net10 · PostgreSQL · yok (arayüz + HTTP)

**Beklenen**
`ErrorNote`'un kendi yorumuna göre ("the API contract is single-language on
purpose") ve MT-UI-032'nin kendi beklenen sonucuna göre sunucudan gelen
`ProblemDetails`/hata metinleri İNGİLİZCE'dir; istemci onu çevirmeden
olduğu gibi gösterir.

**Gerçekleşen**
Dil Türkçeyken var olmayan bir agent'a gidildiğinde ekranda
`"Agent bulunamadi: 'does-not-exist-xyz' adinda bir agent yok."` göründü —
bu metin Türkçe (aksansız/ASCII harf çevirisi). `curl` ile doğrudan sunucu
sınandı: `Accept-Language` başlığı YOK, hatta açıkça `Accept-Language: en`
gönderilse de yanıt DEĞİŞMİYOR — `{"title":"Agent bulunamadi","status":404,
"detail":"'does-not-exist-xyz' adinda bir agent yok.",...}`. Yani bu istek
diline göre değil, sunucu KODUNUN İÇİNE SABİT yazılmış bir Türkçe string.

**Yeniden üretme**
1. `curl -H "Authorization: Bearer manuel-test-token-2026"
   http://localhost:5084/agentprism/api/agents/does-not-exist-xyz`
2. Yanıt gövdesindeki `title`/`detail`'i oku.

**Kanıt**
- `src/AgentPrism.AspNetCore/Endpoints/AgentEndpoints.cs:513-514` (ve aynı
  dosyada 217, 276, 345, 427, 450, 458, 473, 505, 562, 571, 579, 594, 602,
  852, 867, 986, 1001, 1013, 1021, 1031) HER `title`/`detail` SABİT Türkçe
  string olarak yazılı — lokalizasyon sistemi ya da `CurrentUICulture` yok.
- `grep -rlE '"[A-Za-z ]*(bulunamadi|kullanimda|gecersiz|desteklenmiyor|
  eksik|zorunlu|gerekli|basarisiz)[a-z]*"' src/` ile **28 kaynak dosyası**
  bulundu — pratik olarak `src/AgentPrism.AspNetCore/Endpoints/`'in TAMAMI
  (`AgentEndpoints`, `ApiKeyEndpoints`, `ApprovalEndpoints`,
  `AttachmentEndpoints`, `CatalogEndpoints`, `EvalEndpoints`,
  `ExperimentEndpoints`, `GovernanceEndpoints`, `KnowledgeEndpoints`,
  `ModelHealthEndpoints`, `ObservabilityEndpoints`, `QuotaEndpoints`,
  `RetentionEndpoints`, `RunEndpoints`, `SchedulingEndpoints`,
  `SessionEndpoints`, `SkillEndpoints`, `SkillScriptGrantEndpoints`,
  `WebhookEndpoints`, `WorkflowEndpoints`, `Voice/VoiceConversationEndpoint.cs`)
  artı `AgentPrism.Core`/`AgentPrism.Workflows`/`AgentPrism.Generators`'daki
  birkaç dosya. Hatta bir eşleşmeyen rota isteğinin ASP.NET Core'un KENDİ
  404 fallback'i bile Türkçe döndü (`{"title":"Bulunamadi",...}`), bu da
  büyük ihtimalle bir global exception/not-found handler'ın da aynı desende
  yazıldığına işaret ediyor (ayrıca doğrulanmadı, bu turun bütçesi dışında).

**Kapsam**
Yalnız bu case'e özgü değil — HTTP API yüzeyinin BÜYÜK ÇOĞUNLUĞU (en az 28
dosya) etkileniyor. CLAUDE.md'nin kendi tanımına göre bu bir "milyonlarca
geliştiricinin bağımlı olabileceği" NuGet paket ailesi; İngilizce konuşan
HERHANGİ bir API tüketicisi (yalnız bu arayüz değil, doğrudan HTTP/OpenAI-
uyumlu uçları kullanan HERKES) hata mesajlarını Türkçe alıyor. Şerit 1/2/3'ün
önceki HTTP case'lerinde muhtemelen fark edilmedi çünkü `detail`/`title`
alanları genelde yalnız VARLIĞI (durum kodu, alan adı) doğrulanmış, metnin
dili ayrıca kontrol edilmemişti — bu, toplama oturumunda (§8) diğer
şeritlerin sonuçlarıyla çapraz kontrol edilmeli.

---

## Devir notu — S4-1: `09` §1–§2 koşuldu, 3 kusur bulundu (2026-08-13)

**Sonuç: 17/17 koşuldu — 12 Geçti, 3 Kaldı, 2 Atlandı.**

| Case | Durum | Not |
|---|---|---|
| MT-UI-001, 002 | ✅ Geçti | Token kartı, `sessionStorage` kalıcılığı doğrulandı |
| MT-UI-003 | ❌ Kaldı | **`HATA-S4-001`** — yanlış token girilince reddedilme mesajı hiç görünmüyor |
| MT-UI-004 | ✅ Geçti | `AuthToken` kapalıyken kabuk doğrudan açılıyor |
| MT-UI-005 | ❌ Kaldı | **`HATA-S4-002`** — sunucu tamamen kapalıyken özel "ulaşılamıyor" kartı hiç görünmüyor |
| MT-UI-006, 007 | ✅ Geçti | Sekmeye özel token, "Forget" butonu |
| MT-UI-008 | ❌ Kaldı | **`HATA-S4-003`** — loopback dışı erişimde "Erişim reddedildi" kartı yerine ham JSON görünüyor |
| MT-UI-009 | ✅ Geçti | 17 nav öğesi doğru sırada, `pushState` doğrulandı |
| MT-UI-010, 011 | ⏭ Atlandı | Reader-rol test iskelesi (`X-Test-Role` başlığı) tarayıcının `Authorization: Bearer` giriş akışıyla uyumsuz — bkz. case notu |
| MT-UI-012..017 | ✅ Geçti | Aktif rota vurgusu, dar ekran, depolama rengi (kalıcı/bellek içi), 404, geri/ileri senkronu, sondaki `/` |

**Bulunan 3 kusur:**

### HATA-S4-001 — Yanlış token girilince reddedilme mesajı hiç görünmüyor

- **Case:** MT-UI-003
- **Önem:** Orta
- **İzlek:** B
- **Ortam:** macOS arm64 · net10 · PostgreSQL · yok (arayüz)

**Beklenen**
Kart kapanmaz; `role="alert"` taşıyan kırmızı bir satır (`access.token.rejected`) görünür.

**Gerçekleşen**
Form sessizce boş hâline döner; hiçbir hata mesajı görünmez.

**Yeniden üretme**
1. Token kartına `yanlis-token` yaz, "Continue"a tıkla.
2. Formun boş, mesajsız hâline döndüğünü gözlemle.

**Kanıt**
- Kök neden: `src/AgentPrism.UI/frontend/src/lib/api.ts:155-159` — `request()`
  HER 401 yanıtında koşulsuz `setToken(null)` çağırıyor (yorum: "known to be
  wrong" credential'ı bırakmak için tasarlanmış), bu yanlış tokenın YOL AÇTIĞI
  401'i de kapsıyor.
- `src/AgentPrism.UI/frontend/src/components/access-gate.tsx:53-55`'teki
  `TokenPrompt failed={token !== null}` render edildiğinde `token` zaten
  `setToken(null)` ile temizlenmiş oluyor; `failed` hiçbir gerçek akışta
  `true` olamıyor — `access.token.rejected` mesajı fiilen ölü kod.

**Kapsam**
Yalnız bu case — `AccessGate`'in 401-sonrası tüm dallarını etkiliyor
(yanlış token her zaman "neden reddedildiğini söylemeyen" bir sıfırlanmaya yol açıyor).

---

### HATA-S4-002 — Sunucu tamamen kapalıyken özel "ulaşılamıyor" kartı hiç görünmüyor

- **Case:** MT-UI-005
- **Önem:** Düşük
- **İzlek:** B
- **Ortam:** macOS arm64 · net10 · PostgreSQL · yok (arayüz)

**Beklenen**
"Sunucuya ulaşılamıyor" başlıklı özel bir kart (`access.unreachable.title`) görünür.

**Gerçekleşen**
Ne "önceden yüklü sekmeden yeniden yükle" ne "kapalıyken doğrudan aç" adımı
bu kartı üretti — ilk durumda tarayıcının KENDİ `net::ERR_CONNECTION_REFUSED`
sayfası görünüyor (React hiç çalışmıyor), ikincisinde zaten yüklü SPA'nın
`meta` sorgusu senkron `focus`/`visibilitychange` tetiklemesiyle yeniden
getirilmedi.

**Yeniden üretme**
1. Konsolu aç, `dotnet run` sürecini durdur.
2. Aynı sekmede tam sayfa yenile → `net::ERR_CONNECTION_REFUSED`.

**Kanıt**
- `GET /agentprism/` yanıtı `Cache-Control: no-cache` taşıyor — disk
  önbelleğinden de sunulamıyor.
- Aynı origin hem statik kabuğu (`index.html`/JS) hem API'yi sunuyor; sunucu
  tamamen durunca HER İKİSİ de erişilemez oluyor.

**Kapsam**
Same-origin statik+API sunumu ve service worker/offline kabuk olmadığı
sürece HER tarayıcıda geçerli bir mimari sınırlama — basit bir kod
düzeltmesi değil, altyapısal bir karar (ayrı statik host veya service
worker) gerektiriyor.

---

### HATA-S4-003 — Loopback dışı erişimde "Erişim reddedildi" kartı yerine ham JSON görünüyor

- **Case:** MT-UI-008
- **Önem:** Yüksek
- **İzlek:** B
- **Ortam:** macOS arm64 · net10 · PostgreSQL · yok (arayüz), `AllowRemoteAccess=false`

**Beklenen**
"Erişim reddedildi" kartı görünür; gövde metni sunucudan gelen ham metni
(`detail`) kart İÇİNDE taşır.

**Gerçekleşen**
Tarayıcı yalnızca sunucunun ham `ProblemDetails` JSON gövdesini düz metin
olarak gösteriyor; React hiç çalışmıyor.

**Yeniden üretme**
1. Uygulamayı `--urls "http://0.0.0.0:5084"` ile başlat.
2. LAN IP'sinden (`http://<lan-ip>:5084/agentprism/`) konsolu aç.

**Kanıt**
- `src/AgentPrism.AspNetCore/Security/AgentPrismEndpointFilter.cs:72-80`'deki
  loopback denetimi `_allowRemoteAccess` bayrağına bakıyor ve
  `requireBearerToken` parametresinden BAĞIMSIZ her uca (statik kabuk ucu
  dahil) aynı şekilde uygulanıyor.
- Sınıfın kendi XML yorumu (satır 42-49) analog bir sorunu bearer-token
  katmanı için ÇÖZDÜĞÜNÜ anlatıyor ("kabuk `requireBearerToken:false` ile
  çağrılır, yoksa kullanıcı token girebileceği ekranı hiç göremez") ama aynı
  çözüm loopback denetimine uygulanmamış.

**Kapsam**
Yalnız bu case değil — statik kabuk ucunun tamamını etkiliyor: loopback dışı
HERHANGİ bir istemci (yanlışlıkla LAN'dan açan bir geliştirici dahil) hiçbir
zaman React uygulamasını göremiyor, yalnız ham JSON görüyor.

---

## Kullanıcı eylemi bekleyen case'ler

*(Şerit 4 boyunca güncellenir.)*

| Case | Neden | Kullanıcıdan istenen |
|---|---|---|
| MT-UI-026 | Playwright MCP'nin Chromium'u `prefers-color-scheme`'i bu makinenin gerçek OS Dark ayarına göre yansıtmıyor (araç kümesinde emülasyon yok) | Gerçek bir tarayıcı sekmesinde (macOS görünümü zaten Dark) `http://localhost:5084/agentprism/` sıfırdan açılıp ilk karede açık tema yanıp sönmesi olup olmadığı gözlenir |
| MT-UI-031 | Playwright MCP'nin Chromium'unun `navigator.languages`'ı `en-US` sabit; tarayıcı dilini `tr-TR` yapacak bir araç yok | Chrome → Ayarlar → Diller'de `tr`/`tr-TR`'yi başa alıp `localStorage` temizlenerek sayfa sıfırdan açılır, konsolun doğrudan Türkçe açıldığı doğrulanır |
