# Şerit 1 — Koşum Sonuçları (2026-08-13)

> Şerit 1 · port 5081 · şema `mt_s1` · worktree `../ap-s1` · dal `test/kosum-s1`
> Protokol: [`KOSUM-PLANI.md`](KOSUM-PLANI.md)

## Devir notu — sonraki oturum bununla başlar

**Oturum S1-1, S1-2 ve S1-3 (tam) bitti.**

| Dosya | Durum |
|---|---|
| [`04-KALICILIK-DIGER.md`](04-KALICILIK-DIGER.md) | ✅ **Tamam — 40/40** |
| [`23-SAKLAMA-ARSIV-KOTA.md`](23-SAKLAMA-ARSIV-KOTA.md) | ✅ **Tamam — 26/26** |

**Sonraki oturum S1-4'ten başlar:** [`20-BELLEK-RAG-BAGLAM.md`](20-BELLEK-RAG-BAGLAM.md)
(tümü, 31 case). KOSUM-PLANI'nin kendi notu: **PostgreSQL + pgvector
zorunlu**, gerçek embedding çağrısı → OpenAI anahtarı gerekir. Bu, şeridin
şimdiye kadar SQLite ile koştuğu her şeyden **farklı bir kalıcılık
sağlayıcısına geçiş** demektir — ortam bloğunu `AgentPrism__PostgreSql__*`
dolu, `AgentPrism__Sqlite__ConnectionString` boş olacak şekilde değiştirin
(`AgentPrism__PostgreSql__SchemaName=mt_s1`, port 55432, `ap-pg` container'ı
zaten ayakta). SQLite dosyası (`agentprism-manuel.db`) bu geçişte silinmez —
S1-6/S1-7 (ses, 19 dosyası) tekrar SQLite'a dönebilir, o noktada mevcut
kalır.

**Bu oturumda (S1-3'ün kalanı, `23` §3–§5) bulunan yeni bulgu:**

`HATA-S1-006` — **Kritik.** Workflow çalıştırmaları (`/api/workflows/{ad}/run`)
kota muhasebesini **tamamen atlıyor**: `WorkflowEndpoints.cs` hiçbir yerde
`RunRecordingAgent`/`QuotaEnforcer`'a değmiyor. Ölçüldü: `ozetle-ve-cevir`
workflow'u iki kez art arda çalıştırıldı, `quota_usage` sayaçları **hiç
artmadı** (`FARK=0`, deterministik). Bir kiracı, tanımlı bir kotayı agent
uçları yerine workflow uçlarını kullanarak baştan sona atlatabilir. Ayrıntı
aşağıda, `MT-RET-033` altında.

**Önceki oturumlardan taşınan, hâlâ geçerli notlar:**

1. **`HATA-S1-004` ÇÖZÜLDÜ — kod düzeltildi (K-392).** `InvariantGlobalization`
   hem örnekten hem şablondan kaldırıldı; dört doğrulama kapısı yeşil.
   Düzeltme `docs/manuel-test` dalında (`aa64bc7`) ve `test/kosum-s1`'e merge
   edildi. **Şerit 2, 3, 4 bu düzeltmeyi almalı** — worktree'lerinde
   `git merge docs/manuel-test` çalıştırıp yeniden derlemeliler; almazlarsa
   SQL Server dokunan her case aynı duvara çarpar.
2. **`user-secrets` deposu kirli.** `AgentPrism:Sqlite:TablePrefix` önceki
   oturumdan enjeksiyon değeriyle kalmış. Şerit ortam değişkenlerini açıkça
   sabitliyor (aşağıdaki sapma notu); bu sabitleme kaldırılırsa uygulama açılmaz.
3. **SQL Server veritabanı:** `AgentPrism_S1` oluşturuldu ve şema kuruldu
   (`agentprism`, 45 tablo). Senaryo dosyasındaki `$MSSQL` `-d AgentPrism`
   kullanır; şerit `-d AgentPrism_S1` kullanmalıdır (KOSUM-PLANI §2.3).
4. `/health` bu şeritte taban durumda **`Degraded`** döner — sebep
   `Henuz saglikli oldugu dogrulanmis bir model saglayicisi yok.` Sağlayıcı
   çağrısı yapılmadığı için normaldir, kusur değildir. `Unhealthy` bekleyen
   case'ler migration gerekçesine göre değerlendirilmelidir.
5. **`23`'ün `Girilecek veri` script'lerinde iki tekrarlayan tuzak bulundu ve
   dosyada düzeltildi** (doküman sapması, ürün kusuru değil): (a) SQLite'a
   ham `datetime('now', ...)` ile satır eklemek `AgentPrism.Sqlite`'ın
   `created_at` için kullandığı `T`-ayraçlı ISO-8601 biçimiyle **sözlüksel
   olarak uyuşmuyor** — fixture'lar artık `strftime('%Y-%m-%dT%H:%M:%S.0000000Z', ...)`
   kullanıyor; (b) `xargs -P5 -I{} curl ... "$(uuidgen)"` deseni tek bir
   `Idempotency-Key`'i tüm paralel isteklere paylaştırıyor (dış kabuk komut
   satırını BİR KEZ kurduğu için) — düzeltme arka planda (`&`) ayrı alt-kabuk
   kullanmak. İkisi de `docs/manuel-test/23-SAKLAMA-ARSIV-KOTA.md` içinde
   düzeltildi, orijinal (bozuk) hâl not olarak bırakıldı.
6. `quotas/usage` yanıtı düz dizi **değil**, `{usage:[...], definitions:[...],
   ...}` nesnesidir; kiracı-geneli satırların `agentName`'i `null` değil
   `""`. `23`'ün birkaç `jq` script'i bunu varsaymıyordu, düzeltildi.

## Sapmalar

- **`user-secrets` yerine ortam değişkeni kullanıldı** — şerit izolasyonu
  (KOSUM-PLANI §2.2). Ek olarak şerit, paylaşılan depodaki kirlilik yüzünden
  `AgentPrism__Sqlite__TablePrefix`, `__CommandTimeoutSeconds`,
  `__AutoApplyMigrations` ve SQL Server karşılıklarını **varsayılan değerlerine
  açıkça sabitledi**. Depoya yazılmadı.
- SQL Server veritabanı `AgentPrism` yerine **`AgentPrism_S1`** (KOSUM-PLANI §2.3).
- MT-SQL-023 ikinci örneği doküman `5090` derken **5091** kullanıldı; 5090 başka
  şeride ayrılmamış olsa da şerit kendi aralığında kaldı.
- `MT-RET-023`/`041` çok kiracılık gerektirdiğinden `AgentPrism__Tenancy__Enabled`
  ve `AgentPrism__Tenancy__AllowHeaderResolution` ortam değişkenleriyle açıldı
  (uygulama bu ayarla yeniden başlatıldı, `Program.cs` değiştirilmedi —
  `AllowedTenants` beyaz listesi **kullanılmadı**, o yalnız `13-KIRACI-VE-GUVENLIK.md`'nin
  kendi case'lerinde gereken geçici kod değişikliğiyle ilgili, bu oturum onu
  tetiklemedi). `23`'ün kalan bölümlerinde de tenancy açık bırakıldı; başlıksız
  isteklerde varsayılan kiracıya (`default`) düşmeye devam etti, hiçbir case
  bundan etkilenmedi.

## Sayım

`04-KALICILIK-DIGER.md` tamamlandı.

| | |
|---|---|
| Dosyadaki toplam case | **40** |
| Bu oturumlarda koşulan | **37** (`001`/`020`/`021` önceki oturumda) |
| ☑ Geçti | **34** |
| ☑ Kaldı | **3** |
| ☑ Atlandı | **3** |
| ☐ Beklemede | **0** |

**Kaldı (3):**
- `MT-SQL-004` (SQLite) ve `MT-SQL-014` (SQL Server) → `HATA-S1-002`. Aynı kök
  neden iki sağlayıcıda da ölçüldü.
- `MT-SQL-005` → `HATA-S1-003`.

**Atlandı (3) — üçü de yöntem geçersizliği, kod kusuru değil:**
- `MT-SQL-001`, `MT-SQL-010`: örnek uygulama boş bağlantı dizesini
  "sağlayıcı yapılandırılmamış" sayıp validator'a hiç ulaşmıyor
  (`Program.cs:632-646`).
- `MT-SQL-071`: `chmod` çalışan sürecin ZATEN AÇIK dosya tanıtıcılarını
  etkilemiyor; `lsof` ile `.db`/`-wal`/`-shm` üzerinde `u` kipinde açık fd'ler
  doğrulandı. Gerçek salt-okunur bağlama (`mount -ur`) gerekir.

**Tüm SQL Server case'leri gerçek `mcr.microsoft.com/mssql/server:2022-latest`
ile koşuldu** (`MT-SQL-042`); `azure-sql-edge` ikamesi kullanılmadı.

## Doküman düzeltmesi gerektiren sapmalar (kusur değil)

| Case | Sapma |
|---|---|
| `MT-SQL-020`, `024`, `041`, `060` | Tablo sayısı beklentisi 44/44/45; ölçülen **45/45/46**. Doğrulama sorgusu `__migrations` defterini de sayar. İlişkisel iddia (fark = 1, yalnız `document_embeddings`) **tam doğrulandı**. |
| `MT-SQL-050` | `/health` `Healthy` için `GET /api/models/health` ön koşulu eksik — sağlayıcı sağlık önbelleği ısıtılmadan `Degraded` döner. İşletme notu: yeni açılmış örnek readiness probe'a `Degraded` bildirir. |
| `MT-SQL-073` | `sleep 15` yetersiz; SQL Server TCP'yi motor hazır olmadan kabul ediyor. Hazırlık yoklaması gerekir. |
| `MT-SQL-040` | `gpt-5.4-mini` model kataloğunda fiyatsız. Fiyat yolu `AgentPrism:Pricing:<saglayici>:<model>:Input` — `Providers` ara anahtarı **yok**, alan adı `InputCostPerMillionTokens` değil **`Input`**. |

## `23-SAKLAMA-ARSIV-KOTA.md` — tam sayım (26/26)

| | |
|---|---|
| ☑ Geçti | **25** |
| ☑ Kaldı | **1** (`MT-RET-033` → `HATA-S1-006`) |
| ☑ Atlandı | 0 |
| ☐ Koşulmadı | 0 |

Koşulan: `MT-RET-001`–`006` (§1), `MT-RET-010`–`015` (§2, önceki oturum),
`MT-RET-020`–`023` (§3), `MT-RET-030`–`035` (§4), `MT-RET-040`–`043` (§5,
bu oturum). Dosya **tamamlandı**.

**Bu dosyada bulunan doküman sapmaları:**

| Case | Sapma |
|---|---|
| `MT-RET-001` | Adım 1'deki `INSERT` güncel şemayla uyuşmuyor: `agentprism_runs`'ta `created_at`/`updated_at` yok, `started_at` + `is_streaming` var. Ayrıca `sleep 2` yetersiz — `run` bir iş kuyruğa yazar, iş uygulama ayaktayken işlenir. |
| `MT-RET-004` | `jq '{kind, targetName, status}'` yanlış yol; yanıt `{"job":{…},"items":[…]}` biçiminde sarmalı, doğrusu `jq '.job \| {…}'`. |
| `MT-RET-011` | İki ön koşul eksik: (1) `traces` hedefinin config anahtarı `Traces` değil **`Spans`**; (2) `AgentPrism:Retention:Enabled=true` olmadan hiçbir config varsayılanı devreye girmez (`RetentionPolicyResolver.cs:43`). |
| `MT-RET-015` | **Beklenen sonuç koda göre yanlıştı, düzeltildi.** İddia tersineydi: `run_inputs` config varsayılanını KULLANMAZ, `sessions` KULLANIR. |
| `MT-RET-020` | Fixture `datetime('now', ...)` (boşluk ayraçlı) kullanıyordu; `AgentPrism.Sqlite`'ın `AddTimestamp`'i `T`-ayraçlı ISO-8601 yazıyor. İkisi karışınca SQLite'ın metin karşılaştırması bozuluyor (`' ' < 'T'`) — düzeltilmeden ölçüldü: `matchingRows:150` (hepsi), düzeltilince `matchingRows:50` (doğru). `sleep 2` de yetersizdi, `4`'e çıkarıldı. |
| `MT-RET-031`, `034` | `jq '[.[] | ...]'` yanlış — yanıt düz dizi değil `{usage:[...], definitions:[...]}` nesnesi; doğrusu `.usage[]`. Kiracı-geneli `agentName` `null` değil `""`. |
| `MT-RET-035` | `xargs -P5 -I{} curl ... "$(uuidgen)"` tek `uuidgen` çağırır (dış kabuk komut satırını bir kez kurar) — 5 istek aynı `Idempotency-Key`'i paylaşıp `409`/`500` üretti, kota hiç ölçülmedi. Düzeltme: her istek ayrı alt-kabukta (`&`) çalışmalı. |

---

## HATA-S1-005 — Dört saklama hedefi config varsayılanını sessizce yok sayıyor

| | |
|---|---|
| **Önem** | Düşük |
| **Durum** | ⛔ Açık |
| **Case** | `MT-RET-015` |
| **Alan** | `AgentPrism.Core/Retention` |

`AgentPrismRetentionOptions.ForTarget` (`AgentPrismRetentionOptions.cs:85-101`)
16 saklama hedefinden yalnız **12'si** için bir ayar nesnesi döndürür. Şu dört
hedef `_ => null` dalına düşer ve config varsayılanı **hiç okunmaz**:

`run_inputs` · `voice_sessions` · `run_scores` · `document_embeddings`

`AgentPrism:Retention:RunInputs:MaxAgeDays` yazan bir tüketici ne hata, ne
uyarı, ne log alır — ayar sessizce etkisizdir. Ölçüldü: `Enabled=true` ve
`RunInputs:MaxAgeDays=60` verilmiş hâlde `preview?target=run_inputs` →
`{"maxAgeDays":null,"enabled":false}`. Aynı koşumda `sessions` için aynı desen
`{"maxAgeDays":60,"enabled":true}` döndü, yani mekanizma çalışıyor — yalnız bu
dört hedef bağlı değil.

Kasıtlı olabilir (bu hedefler sonraki fazlarda eklendi) ama hiçbir yerde
yazmıyor. Karşılaştırma: `MaxRows`'un config yüzeyine çıkmaması bilinçli bir
karardır **ve** `RetentionPolicyResolver.cs:50-51`'de yorumla belgelenmiştir.

**Önerilen düzeltme:** ya dört hedef `ForTarget`'a eklensin, ya da bilinçliyse
`ForTarget`'ın üstüne hangi hedeflerin config yüzeyi taşımadığı ve nedeni
yazılsın. Sessiz yok sayma seçeneği kalmasın.

---

## HATA-S1-006 — Workflow çalıştırmaları kota muhasebesini tamamen atlıyor

| | |
|---|---|
| **Önem** | **Kritik** |
| **Durum** | ⛔ Açık |
| **Case** | `MT-RET-033` |
| **Alan** | `AgentPrism.AspNetCore/Endpoints/WorkflowEndpoints.cs` |
| **İlgili faz** | Faz 21 |

**Belirti**
Bir kiracı-geneli günlük kota (`maxRuns`, `maxTokens`, `maxCost`) tanımlıyken
`/api/workflows/{ad}/run` üzerinden çalıştırılan işler kotayı **hiç
görmüyor** — ne sayaç artıyor, ne `429` dönüyor.

**Kanıt**
`ozetle-ve-cevir` workflow'u (iki agent adımlı: `ozetleyici`, `cevirmen`)
iki bağımsız kez çalıştırıldı. Her ikisinde de öncesi/sonrası
`quota_usage.runs` **birebir aynı** kaldı (`ONCE=2 SONRA=2 FARK=0`,
`updatedAt` de değişmedi). `usage[]`'te `ozetleyici`/`cevirmen` için yeni
bir satır da oluşmadı — yalnız sayaç artmamakla kalmıyor, hiç izlenmiyor.

**Kök neden**
`grep -rn "RecordQuotaAsync\|QuotaEnforcer" src/AgentPrism.AspNetCore/
Endpoints/WorkflowEndpoints.cs` sıfır sonuç. `RecordQuotaAsync` yalnız
`RunRecordingAgent.cs:779`'dan (agent çalıştırma yolu) çağrılıyor.
`WorkflowEndpoints.RunAsync` kendi çalıştırma motorunu (`IWorkflowHost`)
kullanıyor ve bu yol `RunRecordingAgent`'a hiç uğramıyor — kota, saklama
dışındaki tek "run tabanlı" muhasebe mekanizması, workflow'lar için
tasarlanmamış bir varsayıma (her run `RunRecordingAgent` üzerinden geçer)
dayanıyor.

**Etkisi**
Kota, maliyet/hız kontrolü için var olan bir güvenlik mekanizmasıdır. Bir
tüketici (kötü niyetli ya da yanlışlıkla) `/api/agents/{ad}/run` yerine aynı
işi bir tek-adımlı workflow'a sararsa tanımlı **her** kota (run sayısı,
token, maliyet) baştan sona etkisiz kalır — sessizce, hata/uyarı vermeden.
`MaxRuns=1` gibi sıkı bir kısıtlama bile workflow yoluyla sınırsız çağrıya
açık.

**Önerilen düzeltme**
`WorkflowEndpoints.RunAsync`'in kök seviyesi (workflow'un tamamı, tek
`Depth=0` çağrısı sayılarak — `MT-RET-033`'ün agent tarafında zaten sorduğu
"her workflow adımı mı yoksa workflow'un kendisi mi" sorusuyla aynı
tasarım kararı) `QuotaEnforcer`'ın kontrol/kayıt çağrılarına bağlanmalı.
İkinci seçenek: workflow'un kendi motoru her adımı `RunRecordingAgent`
üzerinden çalıştıracak şekilde yeniden kablolanmalı — bu aynı zamanda
saklama/gözlemlenebilirlik tarafında da tutarlılık sağlar (workflow
adımlarının kendi `quota_usage` satırı olup olmaması ayrı bir tasarım
kararı, ama SIFIR muhasebe kabul edilebilir değil).

---

# Hatalar

## HATA-S1-004 — `InvariantGlobalization=true` SQL Server kalıcılığını tamamen kırıyor

| | |
|---|---|
| **Önem** | **Kritik** |
| **Durum** | ✅ **ÇÖZÜLDÜ** — karar `K-392`, commit `aa64bc7` (`docs/manuel-test`) |
| **Case** | `MT-SQL-014`, `015`, `024`, `025`, `026`, `027` (6 case bloklanmıştı, düzeltmeden sonra hepsi koşuldu) |
| **Alan** | Örnek uygulama + **paket şablonu** |
| **İlgili faz** | Faz 23 |

**Belirti**
`UseSqlServer()` yapılandırıldığında uygulama hiç açılmıyor. Süreç
`Hosting failed to start` + `Unhandled exception` yazıp sonlanıyor;
`Now listening` hiç yazılmıyor.

```
fail: Microsoft.Extensions.Hosting.Internal.Host[11]
      Hosting failed to start
      System.NotSupportedException: Globalization Invariant Mode is not supported.
         at Microsoft.Data.SqlClient.SqlConnection.TryOpen(TaskCompletionSource`1 retry, SqlConnectionOverrides overrides)
         at Microsoft.Data.SqlClient.SqlConnection.InternalOpenAsync(SqlConnectionOverrides overrides, CancellationToken cancellationToken)
         at System.Data.Common.DbDataSource.OpenDbConnectionAsync(CancellationToken cancellationToken)
```

**Kök neden**
`samples/AgentPrism.Api/AgentPrism.Api.csproj:6` →
`<InvariantGlobalization>true</InvariantGlobalization>`. Bu ayar derlenmiş
`AgentPrism.Api.runtimeconfig.json` içine
`"System.Globalization.Invariant": true` olarak geçiyor.
`Microsoft.Data.SqlClient` Globalization Invariant Mode'u **desteklemez** ve
`SqlConnection.Open` çağrısında koşulsuz `NotSupportedException` fırlatır.

**Neden Kritik — örnekle sınırlı değil**
Aynı ayar paket şablonunda da var:
`src/AgentPrism.Templates/content/AgentPrism.Starter/AgentPrism.Starter.csproj:9`.
`dotnet new` ile proje kuran ve `UseSqlServer()` çağıran **her tüketici** bu
duvara çarpar. AgentPrism bir NuGet paket ailesidir; şablon kusuru doğrudan
tüketiciye taşınır. `AgentPrism.SqlServer` paketi, kendi şablonuyla kurulan bir
projede kullanılamaz durumdadır.

**Kanıt / ayırt edici kontrol**
- Geçerli bağlantı dizesiyle (port 51433, `AgentPrism_S1` mevcut): açılmadı.
- Geçersiz host'la (port 1): **aynı** istisna — yani hata bağlantı hedefine
  bağlı değil, `SqlConnection.Open`'ın kendisinde.
- `DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=0` ortam değişkeni **işe yaramıyor**:
  `runtimeconfig.json` içindeki `configProperties` ortam değişkenini ezer.
- SQLite ve bellek içi izlekler etkilenmiyor; `Microsoft.Data.Sqlite` bu moda
  bağımlı değil.
- Ayar doğrulamasıyla kapanan SQL Server case'leri (`011`, `012`, `013`, `016`)
  bağlantıya hiç ulaşmadıkları için normal koştu ve geçti.

**Uygulanan düzeltme** (kullanıcı kararıyla koşum sırasında kodlandı)
`InvariantGlobalization` hem `samples/AgentPrism.Api/AgentPrism.Api.csproj`
hem `src/AgentPrism.Templates/content/AgentPrism.Starter/AgentPrism.Starter.csproj`
dosyasından kaldırıldı. İki dosyaya da ayarın NEDEN açılmadığını yazan bir
yorum bırakıldı ki sonraki oturum geri eklemesin. Karar `K-392` olarak
`docs/KARARLAR.md`'ye yazıldı.

Ayarın kayıtlı bir gerekçesi yoktu (karar defterinde hiç geçmiyordu); ICU
bağımlılığından kaçınma amacıyla konmuş olmalı — doğruluk bu kazancı yener.

**Doğrulama**
- Dört kapı yeşil: `build` 0 uyarı/0 hata · `dotnet test` tüm paketler 0
  başarısız · `pack` exit 0 (85 `.nupkg`) · `format` exit 0.
- İlk denemede `AgentPrism.Templates.Tests` 3/10 kaldı: eklediğim XML yorumu
  `--UseSqlServer` içeriyordu, XML yorumları `--` taşıyamaz (`MSB4025`).
  Yorum düzeltildi, 10/10 geçti. Şablon testleri kusuru yakaladı.
- Derlenmiş `AgentPrism.Api.runtimeconfig.json` artık
  `System.Globalization.Invariant` anahtarını hiç taşımıyor.
- Bloklanan 6 case gerçek SQL Server bağlantısıyla yeniden koşuldu:
  5'i geçti, `MT-SQL-014` `HATA-S1-002`'yi SQL Server'da da doğruladı.

---

## HATA-S1-002 — `AutoApplyMigrations=false` + hazır olmayan şema uygulamayı kapatıyor

| | |
|---|---|
| **Önem** | Yüksek |
| **Durum** | ⛔ Açık — düzeltilmedi (KOSUM-PLANI §8 toplama oturumuna kaldı) |
| **Case** | `MT-SQL-004` (SQLite) **ve** `MT-SQL-014` (SQL Server) — ikisi de koşuldu ve kaldı |
| **Alan** | `AgentPrism.Sql.Shared` + `AgentPrism.AspNetCore` |
| **İlgili faz** | Faz 23/24, Faz 42 |

**Belirti**
`AutoApplyMigrations=false` ve şema henüz kurulmamışken uygulama açılıyor
(`Now listening` yazıyor), hemen ardından **kendini kapatıyor**. `/health`
isteği bağlantı kuramıyor (`HTTP:000`).

```
crit: AgentPrism.A2AApprovalGuardFilter[0]
      A2A disa acik yuzey denetimi basarisiz oldu; uygulama durduruluyor.
      Microsoft.Data.Sqlite.SqliteException: SQLite Error 1: 'no such table: agentprism_agent_definitions'.
         at AgentPrism.A2AApprovalGuardFilter.RunCheckAsync(...) A2A/A2AApprovalGuardFilter.cs:58
crit: AgentPrism.McpApprovalGuardFilter[0]
      MCP disa acik yuzey denetimi basarisiz oldu; uygulama durduruluyor.
         at AgentPrism.McpApprovalGuardFilter.RunCheckAsync(...) McpServer/McpApprovalGuardFilter.cs:86
```

**Kök neden**
`MigrationHostedService.StartAsync`, `AutoApplyMigrations` kapalıyken
`SchemaReadyGate.MarkReady()` çağırır — bu **bilinçli** bir karardır
("semanin hazir olmasi tuketicinin sorumlulugundadir"). Ancak `SchemaReadyGate`
"migration'lar bitti" ile "şema gerçekten hazır" arasında ayrım yapmaz.
`A2AApprovalGuardFilter` ve `McpApprovalGuardFilter` kapıyı bekler ve açılır
açılmaz `catalog.ListAsync` ile `agent_definitions` sorgular. Şema yoksa sorgu
patlar, `catch (Exception)` bloğu
`lifetime.ApplicationStarted.Register(lifetime.StopApplication)` çağırır.

**Etkisi**
Dokümante edilen sözleşme erişilemez hâle geliyor: `AutoApplyMigrations=false`
kurulumunda operatörün "migration'lar bekliyor" durumunu görmesi için
tasarlanmış `/health` → `Unhealthy` + `pendingMigrations` sinyali hiç
okunamıyor. Yerine kök nedeni gizleyen bir "no such table" kapanma döngüsü
oluşuyor. İlk kurulumda ya da migration adımı sırayı kaçırdığında tam olarak bu
yaşanır.

**Ayırt edici kontrol**
Şema önce `AutoApplyMigrations=true` ile kurulup (45 tablo) sonra `false` ile
açıldığında uygulama sorunsuz ayakta kaldı (`Application is shutting down`
satırı yok, `/health` → `Degraded`). Kusur ayarda değil, **kapının hazır
olmayan şema üzerinde açılmasında**.

**İki sağlayıcıda doğrulandı**
`HATA-S1-004` düzeltildikten sonra `MT-SQL-014` SQL Server'da yeniden koşuldu
ve SQLite ile **birebir aynı** davranışı verdi: `Now listening` → `crit:
AgentPrism.A2AApprovalGuardFilter` → `SqlException: Invalid object name
'agentprism.agent_definitions'.` → `Application is shutting down`,
`/health` `HTTP:000`, şema tablo sayısı 0. Kök nedenin sağlayıcıdan bağımsız
(`AgentPrism.Sql.Shared`) olduğu artık tahmin değil, ölçüm.

**Önerilen düzeltme**
İki yoldan biri: (a) onay denetimleri şema hazır değilken `crit` + kapatma
yerine uyarı verip denetimi ertelesin — sağlık ucu zaten durumu bildiriyor;
(b) `SchemaReadyGate` "hazır" ile "atlandı" durumlarını ayırsın ve bekleyenler
atlandı durumunda SQL'e dokunmasın. (a) daha küçük ve gözlemlenebilirlik
kuralına ("depo hatası çalıştırmayı kesmez") daha yakın.

---

## HATA-S1-003 — `Data Source=:memory:` dokümante edildiği hâlde hiç çalışmıyor

| | |
|---|---|
| **Önem** | Yüksek |
| **Case** | `MT-SQL-005` |
| **Alan** | `AgentPrism.Sqlite` — public API XML dokümanı |
| **İlgili faz** | Faz 24 |

**Belirti**
`AgentPrism:Sqlite:ConnectionString` = `Data Source=:memory:` ile uygulama
açılmıyor. Migration'lar uygulanıyor, hemen ardından süreç ölüyor.

```
info: AgentPrism.MigrationRunner[0]
      AgentPrism 15 migration uyguladi. Sema: agentprism_.
fail: Microsoft.Extensions.Hosting.Internal.Host[11]
      Hosting failed to start
      Microsoft.Data.Sqlite.SqliteException: SQLite Error 1: 'no such table: agentprism_tenants'.
```

**Kök neden**
`src/AgentPrism.Sqlite/Internal/SqliteDataSource.cs:44` —
`CreateDbConnection()` her çağrıda **yeni** bir `SqliteConnection` üretir.
Çıplak `:memory:` veritabanı **bağlantıya özeldir**: `MigrationRunner` kendi
bağlantısında 15 migration uygular, bağlantı kapanır, veritabanı yok olur.
`EnsureDefaultTenantAsync` yeni bir bağlantı açar — boş bir in-memory
veritabanı — ve `agentprism_tenants` orada yoktur.

**Sözleşme ihlali**
`src/AgentPrism.Sqlite/AgentPrismSqliteOptions.cs:17-18` XML dokümanı:
`Data Source=:memory: desteklenir ama kalici DEGILDIR: baglanti kapaninca veri
gider.` Bu iddia yanlış — `:memory:` hiç desteklenmiyor, uygulama açılmıyor.
XML dokümanı tüketiciye IntelliSense'te gösterilir; paket ailesinin public
sözleşmesinin parçasıdır.

**Doğrulanan çalışan biçim**
`Data Source=file:apmem?mode=memory&cache=shared` ile uygulama sorunsuz açıldı
(15 migration, `/health` → HTTP 200). Paylaşımlı önbellek aynı süreçteki tüm
bağlantılara tek bir in-memory veritabanı verir.

**Önerilen düzeltme**
XML dokümanı `cache=shared` biçimini önersin ve çıplak `:memory:`'nin bir
`DbDataSource` ile çalışmayacağını açıkça yazsın. Alternatif olarak
`AgentPrismSqliteOptionsValidator` çıplak `:memory:`'yi anlaşılır bir mesajla
reddetsin — sessizce "no such table" ile ölmekten iyidir.

---

## HATA-S1-001 — Koşum hijyeni: `user-secrets` temizliği uygulanmamış

| | |
|---|---|
| **Önem** | Düşük (süreç kusuru, ürün kusuru DEĞİL) |
| **Case** | `MT-SQL-002` |

Şerit açılırken paylaşılan `user-secrets` deposu
`AgentPrism:Sqlite:TablePrefix` = `x_; DROP TABLE agentprism_tenants;--`
taşıyordu — `MT-SQL-002` 3. adımının temizliği önceki oturumda uygulanmamış.
Depo `UserSecretsId=agentprism-sample-api` ile makine genelinde tek olduğu için
bu değer **her şeridi** etkiler ve uygulamanın açılmasını engeller.

KOSUM-PLANI §2.2 gereği depoya yazılmadı. Şerit kendi ayar anahtarlarını ortam
değişkeniyle sabitleyerek ilerledi.

**Öneri:** Toplama oturumunda (KOSUM-PLANI §8) depo temizlenmeli:
`dotnet user-secrets remove "AgentPrism:Sqlite:TablePrefix"`. Ayrıca senaryo
dosyalarındaki "Temizlik" adımlarının koşum sırasında gerçekten uygulanması
gerekir; uygulanmayan temizlik sonraki oturumu bozuyor.

---

## Kullanıcı eylemi bekleyen case'ler

| Case | Neden | Kullanıcıdan istenen |
|---|---|---|
| — | — | Bu oturumda fiziksel eylem gerektiren case çıkmadı. |
