# Şerit 1 — Koşum Sonuçları (2026-08-13)

> Şerit 1 · port 5081 · şema `mt_s1` · worktree `../ap-s1` · dal `test/kosum-s1`
> Protokol: [`KOSUM-PLANI.md`](KOSUM-PLANI.md)

## Devir notu — sonraki oturum bununla başlar

**Oturum S1-1, S1-2, S1-3, S1-4, S1-5 ve S1-6 (tam) bitti.**

| Dosya | Durum |
|---|---|
| [`04-KALICILIK-DIGER.md`](04-KALICILIK-DIGER.md) | ✅ **Tamam — 40/40** |
| [`23-SAKLAMA-ARSIV-KOTA.md`](23-SAKLAMA-ARSIV-KOTA.md) | ✅ **Tamam — 26/26** |
| [`20-BELLEK-RAG-BAGLAM.md`](20-BELLEK-RAG-BAGLAM.md) | ✅ **Koşuldu — 31/31** (13 Geçti · 10 Kaldı · 8 Beklemede — bkz. aşağı) |
| [`25-SAGLIK-TESHIS-OPENAPI.md`](25-SAGLIK-TESHIS-OPENAPI.md) | ✅ **Koşuldu — 28/28** (24 Geçti · 3 Kaldı · 1 Beklemede — bkz. aşağı) |
| [`19-COK-MODLULUK-VE-SES.md`](19-COK-MODLULUK-VE-SES.md) §1–§8 | ✅ **Koşuldu — 37/37** (35 Geçti · 1 Kaldı · 1 Atlandı — bkz. aşağı) |

**Sonraki oturum S1-7'den başlar:** [`19-COK-MODLULUK-VE-SES.md`](19-COK-MODLULUK-VE-SES.md)
§9–§13 (24 case, gerçek zamanlı konuşma/WebSocket, ElevenLabs **ve** OpenAI
kredisi harcar). **Gerçek bir ElevenLabs anahtarı bu oturumda kullanıcıdan
alındı** ve ortam değişkeni olarak set edildi (`user-secrets`'a YAZILMADI,
KOSUM-PLANI §2.2). `AgentPrism__Voice__ApiKey` ve
`AgentPrism__Voice__DefaultVoiceId="hpp4J3VqNfWAUOO0d1Us"` (Bella — MT-MM-038
listesinden doğrulanmış) S1-7 açılışında **aynen tekrarlanmalı** —
paylaşılan `user-secrets` deposundaki `AgentPrism:Voice:DefaultVoiceId` BAŞKA
bir hesaba ait geçersiz bir kimlik taşıyor (bkz. HATA notu aşağıda, kod kusuru
değil). Sunucu süreci bu oturumun sonunda durduruldu (port 5081 boş);
S1-7 KOSUM-PLANI §3.2'deki ortam bloğunu bu iki ek değişkenle birlikte
kurup yeniden başlatmalı. §9'daki Python WebSocket istemcisi
(`~/agentprism-manuel-test/voice_client.py`) henüz kurulmadı, S1-7'nin ilk
case'i (MT-MM-059) bunu yapacak.

**S1-6'da bulunan yeni kusur:**

`HATA-S1-014` — **Orta.** Eşlenmemiş `api/`-önekli bir yola GEÇERLİ statik
bearer token ile istek yapılırsa `404` yerine yanıltıcı `401` döner
(`Kimlik dogrulanamadi`, sanki token hiç verilmemiş gibi). Kök neden: UI
yakalayıcısı (`UiEndpoints.cs`) `requireBearerToken:false` filtresiyle
korunuyor; başlık BOŞ değilse filtre statik `AuthToken`'ı hiç denetlemeden
doğrudan `IApiKeyStore`'da arıyor, bulamayınca genel `401` üretiyor. Ayrıntı
aşağıda.

**S1-6'da tekrar öğrenilen bir kalıp (yöntem hatası, kod kusuru değil, iki
kez oldu):** `pgrep -f "AgentPrism.Api.dll"` macOS'ta `dotnet run` apphost
sürecini bulamıyor — kill komutu SESSİZCE hiçbir şeyi öldürmüyor, eski
süreç (eski ortam değişkenleriyle) port 5081'de yaşamaya devam ediyor ve
yeni `dotnet run` denemesi arka planda görünmez şekilde başarısız oluyor.
Güvenilir yöntem: `lsof -iTCP:5081 -sTCP:LISTEN -t` ile GERÇEK dinleyen PID'i
bulup doğrudan `kill -9 <pid>` ile öldürmek, sonra `ps eww <yeni-pid> | grep
AgentPrism__` ile yeni sürecin GERÇEKTEN beklenen ortam değişkenleriyle
açıldığını doğrulamak. Bu iki kez atlandı (MT-MM-013/014 tenancy açma
denemesinde ilk kez fark edilmeden geçildi, MT-MM-038/046'da servisin hâlâ
eski/sahte anahtarla çalıştığı keşfedilince kesin doğrulandı) — sonraki
şeritler bu deseni baştan uygulamalı.

**S1-5 için önemli ön koşul kuruldu (kalıcı, sonraki oturumlar için hazır):**
`~/agentprism-local-feed` bu şeridin `dotnet pack` çıktısıyla (sürüm
`0.0.0-preview.0.78`) tazelendi; `~/agentprism-manuel/test-paketi` konsol
projesi `AgentPrism.Testing` + `AgentPrism.Sqlite` paket referanslarıyla
kuruldu. K-8/K-9'u (ortak kuyruk, `24-TEST-PAKETI-VE-SABLON.md`) koşacak ajan
bu iki kaynağı **zaten hazır** bulacak — kendi kurulumunu atlayabilir ama
sürümü kontrol etmeli (`SURUM` değişkeni güncel commit'e göre değişebilir).

**S1-5'te KOSUM-PLANI §2.1 istisnası uygulandı (kod değil, doküman
düzeltmesi):** Dört case'in (`MT-DIAG-002/005/007/008`) "Girilecek veri" kodu
hiç çalışmıyordu — `GetServices<IHealthCheck>().OfType<AgentPrismHealthCheck>()`
deseni hem derlenmiyor (`AgentPrismHealthCheck` `internal`) hem de
`AddHealthChecks()`'in DI'a `IHealthCheck` **kaydetmediği** için boş
dönüyordu. Doğru yol `HealthCheckService.CheckHealthAsync()` — dört case'in
kodu da bununla düzeltildi, ayrıntı ilgili case'lerin notunda.

**🚨 S1-4 için önemli ön koşul: OpenAI anahtarının embedding erişimi
doğrulanmalı.** Bu oturumun kullandığı anahtarın bağlı olduğu proje
(`proj_0iwMbkX0bZWgw9Yqq4XK3U4K`) HİÇBİR embedding modeline erişemiyor
(`text-embedding-3-small`/`-large`/`ada-002` üçü de `403 model_not_found`,
doğrudan OpenAI API'sine karşı doğrulandı). Bu, `20`'nin 8 case'ini
(`MT-MEM-015/016/017/018/020/026/027/030`) tamamen bloke etti — `Beklemede`
bırakıldı, aşağıdaki "Kullanıcı eylemi bekleyen case'ler" tablosuna yazıldı.
Eğer S1-5/S1-6/S1-7 aynı anahtarı kullanacaksa ve embedding gerektiren bir
case varsa aynı duvara çarpabilir — önce `curl
https://api.openai.com/v1/embeddings -d '{"model":"text-embedding-3-small",...}'`
ile hızlı doğrulama önerilir.

**PostgreSQL geçişiyle ilgili öğrenilen kritik ders (kendi hatam, düzeltildi):**
bir bağlantı dizesini "kapatmak" için ortam değişkenini **`unset`
ETMEYİN** — `UserSecretsId=agentprism-sample-api` makine genelinde
paylaşıldığı için (KOSUM-PLANI §2.2) `unset`, uygulamayı `user-secrets`'taki
paylaşılan değere (payı `agentprism` şemasına işaret eder, `mt_s1`'e değil)
GERİ DÜŞÜRÜYOR. Doğru yöntem KOSUM-PLANI §2.2'nin zaten söylediği gibi
`export AgentPrism__PostgreSql__ConnectionString=""` — açıkça BOŞ değer.
Bu oturumda bir kez yanlış yapıldı (`MT-MEM-025`'in ilk denemesi), paylaşılan
`agentprism` şemasına 4 istek gitti; SQL ile doğrulandı, şemada hiçbir veri
yok/değişmedi (zarar yok), ama sonraki şeritler bu tuzağa DÜŞMEMELİ.

**Sonraki oturum S1-5 şu ortam bloğuna döner:** `AgentPrism__PostgreSql__*`
dolu (mt_s1), `AgentPrism__Sqlite__ConnectionString` boş. SQLite dosyası
(`agentprism-manuel.db`) bu geçişte silinmedi — S1-6/S1-7 (ses, 19 dosyası)
tekrar SQLite'a dönebilir, o noktada mevcut kalır.

**Bu oturumda (S1-3'ün kalanı, `23` §3–§5) bulunan yeni bulgu:**

`HATA-S1-006` — **Kritik.** Workflow çalıştırmaları (`/api/workflows/{ad}/run`)
kota muhasebesini **tamamen atlıyor**: `WorkflowEndpoints.cs` hiçbir yerde
`RunRecordingAgent`/`QuotaEnforcer`'a değmiyor. Ölçüldü: `ozetle-ve-cevir`
workflow'u iki kez art arda çalıştırıldı, `quota_usage` sayaçları **hiç
artmadı** (`FARK=0`, deterministik). Bir kiracı, tanımlı bir kotayı agent
uçları yerine workflow uçlarını kullanarak baştan sona atlatabilir. Ayrıntı
aşağıda, `MT-RET-033` altında.

**Önceki oturumlardan taşınan, hâlâ geçerli notlar:**

1. **`HATA-S1-004` ÇÖZÜLDÜ — kod düzeltildi (K-392).** `InvariantGlobalization`
   hem örnekten hem şablondan kaldırıldı; dört doğrulama kapısı yeşil.
   Düzeltme `docs/manuel-test` dalında (`aa64bc7`) ve `test/kosum-s1`'e merge
   edildi. **Şerit 2, 3, 4 bu düzeltmeyi almalı** — worktree'lerinde
   `git merge docs/manuel-test` çalıştırıp yeniden derlemeliler; almazlarsa
   SQL Server dokunan her case aynı duvara çarpar.
2. **`user-secrets` deposu kirli.** `AgentPrism:Sqlite:TablePrefix` önceki
   oturumdan enjeksiyon değeriyle kalmış. Şerit ortam değişkenlerini açıkça
   sabitliyor (aşağıdaki sapma notu); bu sabitleme kaldırılırsa uygulama açılmaz.
3. **SQL Server veritabanı:** `AgentPrism_S1` oluşturuldu ve şema kuruldu
   (`agentprism`, 45 tablo). Senaryo dosyasındaki `$MSSQL` `-d AgentPrism`
   kullanır; şerit `-d AgentPrism_S1` kullanmalıdır (KOSUM-PLANI §2.3).
4. `/health` bu şeritte taban durumda **`Degraded`** döner — sebep
   `Henuz saglikli oldugu dogrulanmis bir model saglayicisi yok.` Sağlayıcı
   çağrısı yapılmadığı için normaldir, kusur değildir. `Unhealthy` bekleyen
   case'ler migration gerekçesine göre değerlendirilmelidir.
5. **`23`'ün `Girilecek veri` script'lerinde iki tekrarlayan tuzak bulundu ve
   dosyada düzeltildi** (doküman sapması, ürün kusuru değil): (a) SQLite'a
   ham `datetime('now', ...)` ile satır eklemek `AgentPrism.Sqlite`'ın
   `created_at` için kullandığı `T`-ayraçlı ISO-8601 biçimiyle **sözlüksel
   olarak uyuşmuyor** — fixture'lar artık `strftime('%Y-%m-%dT%H:%M:%S.0000000Z', ...)`
   kullanıyor; (b) `xargs -P5 -I{} curl ... "$(uuidgen)"` deseni tek bir
   `Idempotency-Key`'i tüm paralel isteklere paylaştırıyor (dış kabuk komut
   satırını BİR KEZ kurduğu için) — düzeltme arka planda (`&`) ayrı alt-kabuk
   kullanmak. İkisi de `docs/manuel-test/23-SAKLAMA-ARSIV-KOTA.md` içinde
   düzeltildi, orijinal (bozuk) hâl not olarak bırakıldı.
6. `quotas/usage` yanıtı düz dizi **değil**, `{usage:[...], definitions:[...],
   ...}` nesnesidir; kiracı-geneli satırların `agentName`'i `null` değil
   `""`. `23`'ün birkaç `jq` script'i bunu varsaymıyordu, düzeltildi.

## Sapmalar

- **`user-secrets` yerine ortam değişkeni kullanıldı** — şerit izolasyonu
  (KOSUM-PLANI §2.2). Ek olarak şerit, paylaşılan depodaki kirlilik yüzünden
  `AgentPrism__Sqlite__TablePrefix`, `__CommandTimeoutSeconds`,
  `__AutoApplyMigrations` ve SQL Server karşılıklarını **varsayılan değerlerine
  açıkça sabitledi**. Depoya yazılmadı.
- SQL Server veritabanı `AgentPrism` yerine **`AgentPrism_S1`** (KOSUM-PLANI §2.3).
- MT-SQL-023 ikinci örneği doküman `5090` derken **5091** kullanıldı; 5090 başka
  şeride ayrılmamış olsa da şerit kendi aralığında kaldı.
- `MT-RET-023`/`041` çok kiracılık gerektirdiğinden `AgentPrism__Tenancy__Enabled`
  ve `AgentPrism__Tenancy__AllowHeaderResolution` ortam değişkenleriyle açıldı
  (uygulama bu ayarla yeniden başlatıldı, `Program.cs` değiştirilmedi —
  `AllowedTenants` beyaz listesi **kullanılmadı**, o yalnız `13-KIRACI-VE-GUVENLIK.md`'nin
  kendi case'lerinde gereken geçici kod değişikliğiyle ilgili, bu oturum onu
  tetiklemedi). `23`'ün kalan bölümlerinde de tenancy açık bırakıldı; başlıksız
  isteklerde varsayılan kiracıya (`default`) düşmeye devam etti, hiçbir case
  bundan etkilenmedi.

## Sayım

`04-KALICILIK-DIGER.md` tamamlandı.

| | |
|---|---|
| Dosyadaki toplam case | **40** |
| Bu oturumlarda koşulan | **37** (`001`/`020`/`021` önceki oturumda) |
| ☑ Geçti | **34** |
| ☑ Kaldı | **3** |
| ☑ Atlandı | **3** |
| ☐ Beklemede | **0** |

**Kaldı (3):**
- `MT-SQL-004` (SQLite) ve `MT-SQL-014` (SQL Server) → `HATA-S1-002`. Aynı kök
  neden iki sağlayıcıda da ölçüldü.
- `MT-SQL-005` → `HATA-S1-003`.

**Atlandı (3) — üçü de yöntem geçersizliği, kod kusuru değil:**
- `MT-SQL-001`, `MT-SQL-010`: örnek uygulama boş bağlantı dizesini
  "sağlayıcı yapılandırılmamış" sayıp validator'a hiç ulaşmıyor
  (`Program.cs:632-646`).
- `MT-SQL-071`: `chmod` çalışan sürecin ZATEN AÇIK dosya tanıtıcılarını
  etkilemiyor; `lsof` ile `.db`/`-wal`/`-shm` üzerinde `u` kipinde açık fd'ler
  doğrulandı. Gerçek salt-okunur bağlama (`mount -ur`) gerekir.

**Tüm SQL Server case'leri gerçek `mcr.microsoft.com/mssql/server:2022-latest`
ile koşuldu** (`MT-SQL-042`); `azure-sql-edge` ikamesi kullanılmadı.

## Doküman düzeltmesi gerektiren sapmalar (kusur değil)

| Case | Sapma |
|---|---|
| `MT-SQL-020`, `024`, `041`, `060` | Tablo sayısı beklentisi 44/44/45; ölçülen **45/45/46**. Doğrulama sorgusu `__migrations` defterini de sayar. İlişkisel iddia (fark = 1, yalnız `document_embeddings`) **tam doğrulandı**. |
| `MT-SQL-050` | `/health` `Healthy` için `GET /api/models/health` ön koşulu eksik — sağlayıcı sağlık önbelleği ısıtılmadan `Degraded` döner. İşletme notu: yeni açılmış örnek readiness probe'a `Degraded` bildirir. |
| `MT-SQL-073` | `sleep 15` yetersiz; SQL Server TCP'yi motor hazır olmadan kabul ediyor. Hazırlık yoklaması gerekir. |
| `MT-SQL-040` | `gpt-5.4-mini` model kataloğunda fiyatsız. Fiyat yolu `AgentPrism:Pricing:<saglayici>:<model>:Input` — `Providers` ara anahtarı **yok**, alan adı `InputCostPerMillionTokens` değil **`Input`**. |

## `23-SAKLAMA-ARSIV-KOTA.md` — tam sayım (26/26)

| | |
|---|---|
| ☑ Geçti | **25** |
| ☑ Kaldı | **1** (`MT-RET-033` → `HATA-S1-006`) |
| ☑ Atlandı | 0 |
| ☐ Koşulmadı | 0 |

Koşulan: `MT-RET-001`–`006` (§1), `MT-RET-010`–`015` (§2, önceki oturum),
`MT-RET-020`–`023` (§3), `MT-RET-030`–`035` (§4), `MT-RET-040`–`043` (§5,
bu oturum). Dosya **tamamlandı**.

**Bu dosyada bulunan doküman sapmaları:**

| Case | Sapma |
|---|---|
| `MT-RET-001` | Adım 1'deki `INSERT` güncel şemayla uyuşmuyor: `agentprism_runs`'ta `created_at`/`updated_at` yok, `started_at` + `is_streaming` var. Ayrıca `sleep 2` yetersiz — `run` bir iş kuyruğa yazar, iş uygulama ayaktayken işlenir. |
| `MT-RET-004` | `jq '{kind, targetName, status}'` yanlış yol; yanıt `{"job":{…},"items":[…]}` biçiminde sarmalı, doğrusu `jq '.job \| {…}'`. |
| `MT-RET-011` | İki ön koşul eksik: (1) `traces` hedefinin config anahtarı `Traces` değil **`Spans`**; (2) `AgentPrism:Retention:Enabled=true` olmadan hiçbir config varsayılanı devreye girmez (`RetentionPolicyResolver.cs:43`). |
| `MT-RET-015` | **Beklenen sonuç koda göre yanlıştı, düzeltildi.** İddia tersineydi: `run_inputs` config varsayılanını KULLANMAZ, `sessions` KULLANIR. |
| `MT-RET-020` | Fixture `datetime('now', ...)` (boşluk ayraçlı) kullanıyordu; `AgentPrism.Sqlite`'ın `AddTimestamp`'i `T`-ayraçlı ISO-8601 yazıyor. İkisi karışınca SQLite'ın metin karşılaştırması bozuluyor (`' ' < 'T'`) — düzeltilmeden ölçüldü: `matchingRows:150` (hepsi), düzeltilince `matchingRows:50` (doğru). `sleep 2` de yetersizdi, `4`'e çıkarıldı. |
| `MT-RET-031`, `034` | `jq '[.[] | ...]'` yanlış — yanıt düz dizi değil `{usage:[...], definitions:[...]}` nesnesi; doğrusu `.usage[]`. Kiracı-geneli `agentName` `null` değil `""`. |
| `MT-RET-035` | `xargs -P5 -I{} curl ... "$(uuidgen)"` tek `uuidgen` çağırır (dış kabuk komut satırını bir kez kurar) — 5 istek aynı `Idempotency-Key`'i paylaşıp `409`/`500` üretti, kota hiç ölçülmedi. Düzeltme: her istek ayrı alt-kabukta (`&`) çalışmalı. |

---

## `20-BELLEK-RAG-BAGLAM.md` — tam sayım (31/31)

| | |
|---|---|
| ☑ Geçti | **13** |
| ☑ Kaldı | **10** |
| ☐ Beklemede | **8** (embedding erişimi yok — bkz. devir notu) |
| ☑ Atlandı | 0 |

Koşulan: `MT-MEM-001`–`031` (tümü). Dosya **tamamlandı** (embedding erişimi
gerektiren 8 case dışında).

**Kaldı (10):**
- `MT-MEM-004` → `HATA-S1-007` (Orta).
- `MT-MEM-011`, `012` → `HATA-S1-008` (Yüksek).
- `MT-MEM-013`, `014` → `HATA-S1-009` (Orta, şüpheli davranış).
- `MT-MEM-021`, `022`, `023`, `024` → `HATA-S1-010` (Düşük).
- `MT-MEM-031` → `HATA-S1-011` (Yüksek).

**Beklemede (8, embedding erişimi yok):**
`MT-MEM-015`, `016`, `017`, `018`, `020`, `026`, `027`, `030`.

**Bu dosyada bulunan doküman sapmaları:**

| Case | Sapma |
|---|---|
| `MT-MEM-006/008/009/011/013/027` | "Girilecek veri" script'leri yeni bir fixture agent'ı `PUT /api/agents/{ad}` ile kaydediyordu — kod `PUT`'u yalnız var olan bir tanımı güncellemek için kabul ediyor (`404` verir). Doğrusu `POST /api/agents`. Dosyada düzeltildi, bkz. dosya başındaki not. |
| `MT-MEM-006/008` | `d.get('text')` çıkarma script'i yanıt gövdesinde böyle bir alan olmadığı için hep `None` bastı — gerçek metin `response.messages[0].contents[0].text` altında. Kolaylık script'i sorunu, hiçbir `Beklenen sonuç` bu alana bağlı değil. |
| `MT-MEM-031` | `KEY_JSON`'ın alanı `rawKey` değil `plaintextKey`. Ayrıca adım 4'ün örnek gövdesinde zorunlu `model` alanı eksikti — `HATA-S1-007` ile aynı köke (JSON body-binding istisnası yakalanmıyor) çarpıp `500` veriyordu; `model` eklenince asıl ölçmek istediği `403` gözlemlendi. |
| `MT-MEM-025` | `user-secrets remove`/`set` yerine (§2.2) ortam değişkeni kullanıldı — bu case'i "kapatmak" için `unset` DEĞİL, `=""` gerekir (bkz. devir notu, kendi hatam). |

---

## `25-SAGLIK-TESHIS-OPENAPI.md` — tam sayım (28/28)

| | |
|---|---|
| ☑ Geçti | **24** |
| ☑ Kaldı | **3** |
| ☐ Beklemede | **1** (`MT-DIAG-022` — geçici kod değişikliği gerekiyor, aşağıda) |
| ☑ Atlandı | 0 |

Koşulan: `MT-DIAG-001`–`008` (§1, tümü), `MT-DIAG-020`–`030` (§2, tümü),
`MT-DIAG-040`–`048` (§3, tümü). Dosya **tamamlandı**.

**Kaldı (3):**
- `MT-DIAG-005`, `MT-DIAG-027` → **HATA-S1-003'ün tekrarı** (yeni numara
  değil — `Data Source=:memory:` `AgentPrismTestHost` üzerinden de çöküyor,
  `04-KALICILIK-DIGER.md`'nin `MT-SQL-005`'iyle aynı kök neden).
- `MT-DIAG-042` → `HATA-S1-012` (Yüksek, `VoiceConversationEndpoint`'in
  OpenAPI etiketi eksik).
- `MT-DIAG-043` → `HATA-S1-013` (Orta, iki A2A ucunun `operationId`si yok).

**Beklemede (1):**
`MT-DIAG-022` — `13-KIRACI-VE-GUVENLIK.md` §8'in geçici `RoleTestAuthHandler`
kurulumu gerektiriyor (`samples/AgentPrism.Api/Program.cs` değişikliği +
yeni dosya) — KOSUM-PLANI §2.1'in "kod değiştirilmez" kuralına girer, S1-3'ün
aynı gerekçesiyle atlandı.

**Bu dosyada bulunan doküman sapmaları:**

| Case | Sapma |
|---|---|
| `MT-DIAG-002/005/007/008` | "Girilecek veri" `GetServices<IHealthCheck>().OfType<AgentPrismHealthCheck>()` kullanıyordu — tip `internal`, üstelik `AddHealthChecks()` `IHealthCheck`'i DI'a hiç kaydetmiyor. `HealthCheckService.CheckHealthAsync()` ile düzeltildi. |
| `MT-DIAG-005/027` | `Data Source=:memory:` (paylaşımsız) HATA-S1-003 nedeniyle çöküyor; K-183/sayaç davranışını ayrıca sınamak için `Data Source=file::memory:?cache=shared` kullanıldı. |
| `MT-DIAG-006` | "Adımlar" (geçersiz model) ile "Girilecek veri" (geçerli `openrouter-destek` fixture'ı) çelişiyordu. Geçici `manuel-diag-bozuk` (geçersiz modelli) agent'ı `POST /api/agents` ile kaydedilip kullanıldı, sonra silindi. |
| `MT-DIAG-047` | Karşılaştırma canlı örnek uygulama (`AgentPrism.Api`) çıktısını işlenmiş `docs/openapi/agentprism.json` (`AgentPrism.AspNetCore.FunctionalTests` host'undan üretilmiş) ile kıyaslıyor — iki farklı host yapılandırması, fark **beklenen**. |

---

## `19-COK-MODLULUK-VE-SES.md` §1–§8 — kısmi sayım (37/183)

| | |
|---|---|
| ☑ Geçti | **35** |
| ☑ Kaldı | **1** |
| ☑ Atlandı | **1** |
| ☐ Beklemede | 0 |

Koşulan: `MT-MM-001`–`006` (§1), `010`–`014` (§2), `020`–`023` (§3), `026`–`028`
(§4), `031`–`035` (§5), `038`–`043` (§6), `046`–`050` (§7), `053`–`055` (§8).
§9–§13 (gerçek zamanlı konuşma) **koşulmadı** — bkz. devir notu.

**Kaldı (1):**
- `MT-MM-031` → `HATA-S1-014` (Orta) — geçerli statik bearer token'lı,
  eşlenmemiş `api/` yoluna istek `404` yerine yanıltıcı `401` döndü.

**Atlandı (1):**
- `MT-MM-047` — `sesli-asistan` fixture'ının model ayarı
  `maxOutputTokens=1024`; 6000 karakterlik bir `speak` tool argümanı hiçbir
  zaman tek tamamlamaya sığmaz, model kaç kez denenirse denensin sınıra asla
  ulaşamaz (yapısal, model isteksizliği değil). Aynı sınır HTTP ucu
  tarafında `MT-MM-042`'de (model araya girmeden) doğrudan zaten kanıtlandı.

**Bu dosyada bulunan doküman sapmaları (kusur değil):**

| Case | Sapma |
|---|---|
| `MT-MM-013`/`014`/`022`/`067` (§10, koşulmadı) | Örnek `Girilecek veri` kodu `X-Tenant-Id` başlığını kullanıyordu; gerçek başlık adı `X-AgentPrism-Tenant`dır (`HttpTenantContext.cs:50`). Dosyada dört yerde de düzeltildi. |
| `MT-MM-014` | "Ön koşul" yalnızca bir ek yüklemeyi yeterli sayıyordu; `DELETE /api/sessions/{id}` gerçek bir `sessions` kaydı arıyor (`SessionEndpoints.cs:188`) ve salt bir ek yüklemek böyle bir kayıt oluşturmuyor. Önce gerçek bir agent çalıştırması (`POST /api/agents/support/run`) eklenerek düzeltildi. |
| `MT-MM-046` | Doğrulama SQL'i `attachments.tool_name` sütununa başvuruyordu — böyle bir sütun yok (`\d attachments` ile doğrulandı). Sorgudan çıkarıldı. |

---

## HATA-S1-014 — Eşlenmemiş `api/` yollarına geçerli statik bearer token ile istek `404` yerine yanıltıcı `401` döner

| | |
|---|---|
| **Önem** | Orta |
| **Durum** | ⛔ Açık |
| **Case** | `MT-MM-031` |
| **Alan** | `src/AgentPrism.AspNetCore/Endpoints/UiEndpoints.cs`, `src/AgentPrism.AspNetCore/Security/AgentPrismEndpointFilter.cs` |

**Belirti**
`UseVoiceConversation()` çağrılmadığında (`AgentPrism:Voice:ApiKey` boş)
`/api/voice/sessions/{id}/stream` gerçekten kayıtlı değildir ve dokümana göre
`404` dönmesi gerekir. Başlıksız istek gerçekten `404` döner
(`"'...' yolunda bir uc veya arayuz varligi yok."`) — ama AYNI istek geçerli
bir `Authorization: Bearer <dogru-statik-token>` başlığıyla yapılırsa `401`
döner (`"Kimlik dogrulanamadi", "Gecerli bir 'Authorization: Bearer <token>'
basligi gerekiyor."`), sanki geçerli bir token hiç sağlanmamış gibi. Sorun bu
uca özgü değil: rastgele herhangi bir eşlenmemiş `api/*` yolu (örn.
`/api/totally-made-up-path-xyz`) aynı `401`i verir; yanlış (geçersiz) bir
token da AYNI `401`i verir — doğru token ile yanlış token arasında hiçbir
davranış farkı yoktur.

**Kanıt**
```
curl "$APU/api/voice/sessions/deneme/stream"                              → 404 "Bulunamadi"
curl "$APU/api/voice/sessions/deneme/stream" -H "$APB" (DOGRU token)      → 401 "Kimlik dogrulanamadi"
curl "$APU/api/voice/sessions/deneme/stream" -H "Authorization: Bearer wrong-token-xyz" → 401 (ayni mesaj)
curl "$APU/api/totally-made-up-path-xyz" -H "$APB" (DOGRU token)          → 401 (ayni mesaj, ucla ilgisiz)
curl "$APU/api/voice/health" -H "$APB" (kayitli bir uc, kontrast)         → 501 (dogru token normal calisiyor)
```

**Kök neden**
Eşlenmemiş `api/*` yolları `UiEndpoints`'in yakalayıcı rotasına düşer
(`{**path}`, `UiEndpoints.cs:44-46`) ve `api/`/`v1/` önekliyse `ServeAsync`
doğrudan `404 Bulunamadi` üretir (`UiEndpoints.cs:66-74`) — AMA yalnız
`AgentPrismEndpointFilter`'dan geçebilirse. Bu grup
`AgentPrismEndpointFilter(options, requireBearerToken: false)` ile kurulu
(`AgentPrismEndpointRouteBuilderExtensions.cs:294`, tarayıcının statik
varlıklara `Authorization` başlığı ekleyemediği için bilinçli bir tasarım).
`requireBearerToken:false` olunca kurucu `_authToken`'ı `null` yapar
(`AgentPrismEndpointFilter.cs:57`). Filtrenin akışı (`InvokeAsync`,
`AgentPrismEndpointFilter.cs:78-133`):

1. Başlık BOŞSA ve `_authToken` `null` → doğrudan `Proceed()`, `ServeAsync`
   çalışır, `404` döner. (Gözlenen "başlıksız → 404" davranışı buradan gelir.)
2. Başlık BOŞ DEĞİLSE → `_authToken is {Length: >0}` koşulu `_authToken` zaten
   `null` olduğu için HER ZAMAN yanlıştır; statik token karşılaştırması
   (satır 93) hiç çalışmaz. Kod doğrudan `IApiKeyStore`'da bir API anahtarı
   arar (satır 100-131); statik `AuthToken` kayıtlı bir API anahtarı
   OLMADIĞI için arama boş döner ve son satırdaki genel `Unauthorized()`
   (satır 133) tetiklenir.

Sonuç: bu endpoint grubu için "geçerli statik token" diye bir kavram yok —
ya başlık tamamen boş olacak ya da geçerli bir API anahtarı taşıyacak; üçüncü
bir olası girdi (uygulamanın kendi statik `AuthToken`'ı) her zaman reddedilir
ve mesaj bunu "hiç token verilmedi" gibi yanlış yansıtır.

**Etkisi**
Güvenlik açığı değildir (fazla kısıtlayıcı, eksik değil) ama teşhisi
yanıltır: bir istemci yanlış yazılmış/henüz açılmamış bir uca doğru
kimlik bilgisiyle istek attığında "kimlik doğrulanamadı" mesajı görür ve
token'ı şüphelenir, oysa asıl sorun "böyle bir uç yok"tur. `13-KIRACI-VE-GUVENLIK.md`
ve bu dosyanın kendisi (MT-MM-013/031) "yok" ile "yetkisiz" arasındaki ayrımı
bilinçli bir tasarım ilkesi olarak test ediyor (404 vs 401/403, bilgi
sızdırmama) — bu davranış o ilkeyi eşlenmemiş yollarda tersine çeviriyor.

**Önerilen düzeltme**
`AgentPrismEndpointFilter`'a, `requireBearerToken:false` iken de statik
`AuthToken` ile eşleşen bir başlığı KABUL edip `Proceed()`'e izin verecek bir
yol eklensin (yalnız reddetmesin) — ya da en azından başlık boş olmadığında
API anahtarı da statik token da eşleşmezse dönen mesaj "bu uç kimlik
doğrulaması gerektirmiyor, sağladığınız değer bir API anahtarı değil" gibi
daha doğru bir ayrım yapsın.

---

## HATA-S1-005 — Dört saklama hedefi config varsayılanını sessizce yok sayıyor

| | |
|---|---|
| **Önem** | Düşük |
| **Durum** | ⛔ Açık |
| **Case** | `MT-RET-015` |
| **Alan** | `AgentPrism.Core/Retention` |

`AgentPrismRetentionOptions.ForTarget` (`AgentPrismRetentionOptions.cs:85-101`)
16 saklama hedefinden yalnız **12'si** için bir ayar nesnesi döndürür. Şu dört
hedef `_ => null` dalına düşer ve config varsayılanı **hiç okunmaz**:

`run_inputs` · `voice_sessions` · `run_scores` · `document_embeddings`

`AgentPrism:Retention:RunInputs:MaxAgeDays` yazan bir tüketici ne hata, ne
uyarı, ne log alır — ayar sessizce etkisizdir. Ölçüldü: `Enabled=true` ve
`RunInputs:MaxAgeDays=60` verilmiş hâlde `preview?target=run_inputs` →
`{"maxAgeDays":null,"enabled":false}`. Aynı koşumda `sessions` için aynı desen
`{"maxAgeDays":60,"enabled":true}` döndü, yani mekanizma çalışıyor — yalnız bu
dört hedef bağlı değil.

Kasıtlı olabilir (bu hedefler sonraki fazlarda eklendi) ama hiçbir yerde
yazmıyor. Karşılaştırma: `MaxRows`'un config yüzeyine çıkmaması bilinçli bir
karardır **ve** `RetentionPolicyResolver.cs:50-51`'de yorumla belgelenmiştir.

**Önerilen düzeltme:** ya dört hedef `ForTarget`'a eklensin, ya da bilinçliyse
`ForTarget`'ın üstüne hangi hedeflerin config yüzeyi taşımadığı ve nedeni
yazılsın. Sessiz yok sayma seçeneği kalmasın.

---

## HATA-S1-006 — Workflow çalıştırmaları kota muhasebesini tamamen atlıyor

| | |
|---|---|
| **Önem** | **Kritik** |
| **Durum** | ⛔ Açık |
| **Case** | `MT-RET-033` |
| **Alan** | `AgentPrism.AspNetCore/Endpoints/WorkflowEndpoints.cs` |
| **İlgili faz** | Faz 21 |

**Belirti**
Bir kiracı-geneli günlük kota (`maxRuns`, `maxTokens`, `maxCost`) tanımlıyken
`/api/workflows/{ad}/run` üzerinden çalıştırılan işler kotayı **hiç
görmüyor** — ne sayaç artıyor, ne `429` dönüyor.

**Kanıt**
`ozetle-ve-cevir` workflow'u (iki agent adımlı: `ozetleyici`, `cevirmen`)
iki bağımsız kez çalıştırıldı. Her ikisinde de öncesi/sonrası
`quota_usage.runs` **birebir aynı** kaldı (`ONCE=2 SONRA=2 FARK=0`,
`updatedAt` de değişmedi). `usage[]`'te `ozetleyici`/`cevirmen` için yeni
bir satır da oluşmadı — yalnız sayaç artmamakla kalmıyor, hiç izlenmiyor.

**Kök neden**
`grep -rn "RecordQuotaAsync\|QuotaEnforcer" src/AgentPrism.AspNetCore/
Endpoints/WorkflowEndpoints.cs` sıfır sonuç. `RecordQuotaAsync` yalnız
`RunRecordingAgent.cs:779`'dan (agent çalıştırma yolu) çağrılıyor.
`WorkflowEndpoints.RunAsync` kendi çalıştırma motorunu (`IWorkflowHost`)
kullanıyor ve bu yol `RunRecordingAgent`'a hiç uğramıyor — kota, saklama
dışındaki tek "run tabanlı" muhasebe mekanizması, workflow'lar için
tasarlanmamış bir varsayıma (her run `RunRecordingAgent` üzerinden geçer)
dayanıyor.

**Etkisi**
Kota, maliyet/hız kontrolü için var olan bir güvenlik mekanizmasıdır. Bir
tüketici (kötü niyetli ya da yanlışlıkla) `/api/agents/{ad}/run` yerine aynı
işi bir tek-adımlı workflow'a sararsa tanımlı **her** kota (run sayısı,
token, maliyet) baştan sona etkisiz kalır — sessizce, hata/uyarı vermeden.
`MaxRuns=1` gibi sıkı bir kısıtlama bile workflow yoluyla sınırsız çağrıya
açık.

**Önerilen düzeltme**
`WorkflowEndpoints.RunAsync`'in kök seviyesi (workflow'un tamamı, tek
`Depth=0` çağrısı sayılarak — `MT-RET-033`'ün agent tarafında zaten sorduğu
"her workflow adımı mı yoksa workflow'un kendisi mi" sorusuyla aynı
tasarım kararı) `QuotaEnforcer`'ın kontrol/kayıt çağrılarına bağlanmalı.
İkinci seçenek: workflow'un kendi motoru her adımı `RunRecordingAgent`
üzerinden çalıştıracak şekilde yeniden kablolanmalı — bu aynı zamanda
saklama/gözlemlenebilirlik tarafında da tutarlılık sağlar (workflow
adımlarının kendi `quota_usage` satırı olup olmaması ayrı bir tasarım
kararı, ama SIFIR muhasebe kabul edilebilir değil).

---

# Hatalar

## HATA-S1-012 — `VoiceConversationEndpoint`'in OpenAPI etiketi eksik

| | |
|---|---|
| **Önem** | Yüksek |
| **Durum** | ⛔ Açık |
| **Case** | `MT-DIAG-042` |
| **Alan** | `src/AgentPrism.AspNetCore/Voice/VoiceConversationEndpoint.cs` |
| **İlgili faz** | Faz 40 (K-40.3) |

**Belirti**
Yayımlanan OpenAPI belgesinde 147 işlemin 146'sı en az iki etiket taşıyor
(`AgentPrism` + özellik etiketi); `GET
/agentprism/api/voice/sessions/{sessionId}/stream`
(`operationId: AgentPrismVoiceStream`) yalnız `["AgentPrism"]` taşıyor.

**Kök neden**
`VoiceConversationEndpoint.Map()` (`VoiceConversationEndpoint.cs:38-52`) hiç
`.WithTags(...)` çağırmıyor; yalnız `voiceGroup`'un
(`AgentPrismEndpointRouteBuilderExtensions.cs:252`) grup düzeyindeki tek
`"AgentPrism"` etiketini devralıyor. Karşılaştırma: aynı özelliğin diğer
uçları (`VoiceEndpoints.cs:48,55,61,70`) hepsi `.WithTags("AgentPrism",
"Voice")` çağırıyor.

**Yeniden üretme**
```bash
curl -s http://localhost:5081/openapi/v1.json | \
  jq -r '.paths."/agentprism/api/voice/sessions/{sessionId}/stream".get.tags'
# ["AgentPrism"]  -- "Voice" eksik
```

**Önerilen düzeltme**
`agentGroup.MapVoiceConversation(...)`'ın döndürdüğü builder'a (veya
doğrudan `Map()` içindeki `MapGet` zincirine)
`.WithTags("AgentPrism", "Voice")` eklemek.

**Kapsam**
Yalnız bu tek uç etkileniyor (1/147 işlem). Fonksiyonel etki yok — yalnız
etiket bazlı istemci üreteci gruplaması bu ucu doğru kategoriye koyamıyor.

---

## HATA-S1-013 — İki A2A ucunun `operationId`si yok

| | |
|---|---|
| **Önem** | Orta |
| **Durum** | ⛔ Açık |
| **Case** | `MT-DIAG-043` |
| **Alan** | `src/AgentPrism.AspNetCore/A2A/AgentPrismA2AExtensions.cs` |
| **İlgili faz** | Faz 40 |

**Belirti**
`POST /agentprism/a2a/ozetleyici` ve
`GET /agentprism/a2a/ozetleyici/.well-known/agent-card.json`'ın OpenAPI
belgesinde `operationId` alanı yok (`null`). jq'nin `group_by` mantığında
iki `null` "tekrar eden" sayılıyor; toplam işlem sayısı (`147`) ile benzersiz
`operationId` sayısı (`146`) eşleşmiyor.

**Kök neden**
`AgentPrismA2AExtensions.cs:122-126`:
```csharp
agentGroup.MapA2A(handler, "/");
agentGroup.MapWellKnownAgentCard(card, "");
```
İkisi de üçüncü taraf A2A SDK uzantı metotları; ne kendileri ne de çağıran
kod `.WithName(...)` çağırıyor. `group` yalnız `.WithTags("AgentPrism",
"A2A")` alıyor (satır 103) — `AgentPrismEndpointRouteBuilderExtensions.cs`'teki
diğer tüm gruplar her tekil uçta ayrıca `.WithName(...)` çağırırken A2A grubu
bunu atlıyor.

**Yeniden üretme**
```bash
curl -s http://localhost:5081/openapi/v1.json | \
  jq -r '.paths."/agentprism/a2a/ozetleyici".post.operationId'
# null
```

**Önerilen düzeltme**
`AgentPrismA2AExtensions.cs:125-126`'ya `.WithName($"AgentPrismA2A_{agentName}")`
ve `.WithName($"AgentPrismA2AAgentCard_{agentName}")` eklemek (agent adı
başına benzersiz olacak şekilde).

**Kapsam**
Yalnız dinamik A2A yüzeyi etkileniyor (2/147 işlem, kayıtlı A2A agent sayısı
kadar çoğalır). Fonksiyonel etki yok — kod-üretici istemciler bu iki uç için
kararsız/otomatik ad üretir.

---

## HATA-S1-011 — `KnowledgeEndpoints` API anahtarı kapsam denetimi hiç uygulamıyor

| | |
|---|---|
| **Önem** | **Yüksek** |
| **Durum** | ⛔ Açık |
| **Case** | `MT-MEM-031` |
| **Alan** | `src/AgentPrism.AspNetCore/Endpoints/KnowledgeEndpoints.cs` |
| **İlgili faz** | Faz 51, 53 |

**Belirti**
`KnowledgeEndpoints`'in hiçbir ucu `RequireApiKeyScope` çağırmıyor —
yalnız `RunsRead` kapsamlı (okuma amaçlı) bir API anahtarı bilgi tabanı
belgesini serbestçe siliyor.

**Kanıt**
`scopes:["RunsRead"]` ile üretilen bir anahtarla `DELETE
/api/knowledge/{collection}/documents/{sourceId}` çağrıldı: `HTTP 204`.
Kontrol grubu: aynı anahtarla `PUT /api/agents/{name}` (geçerli `model`
alanıyla) çağrıldığında `HTTP 403`, `detail: "Bu uc 'AgentsAdmin' kapsamini
gerektiriyor; anahtar bu kapsami tasimiyor."` — yani kapsam sistemi
`AgentEndpoints`'te çalışıyor, `KnowledgeEndpoints`'te HİÇ devrede değil.
Yazma ucu (`POST .../documents`) bu oturumda embedding erişimi eksikliğinden
`500` verdi, ama istek kapsam filtresinden GEÇİP ingestion mantığına ulaştı
— `403` hiç üretilmedi, yani yazma da aynı şekilde açık.

**Kök neden**
`grep -n "RequireApiKeyScope" src/AgentPrism.AspNetCore/Endpoints/KnowledgeEndpoints.cs`
boş döner; karşılaştırma `AgentEndpoints.cs`/`RunEndpoints.cs` her ucuna
`RequireApiKeyScope(ApiKeyScope.AgentsAdmin/RunsWrite/...)` ekliyor. Doküman
bunu `WorkflowEndpoints`/`SchedulingEndpoints`/eval-experiment yüzeyi/
`GovernanceEndpoints`'in ardından beşinci bilinen tekrarı olarak not ediyor
— sistematik bir denetim boşluğuna işaret ediyor (`docs/KARARLAR.md` 53.3,
kapsam ∩ rol ilkesi).

**Etkisi**
Yalnız-okuma amaçlı (`RunsRead` gibi) bir otomasyon anahtarı, sızdırılırsa
veya kötüye kullanılırsa bilgi tabanı içeriğini yazabilir/silebilir —
embedding maliyeti dahil, geri alınamaz veri kaybı (silme) riski.

**Önerilen düzeltme**
`KnowledgeEndpoints`'in yazma/silme uçlarına uygun bir `ApiKeyScope`
(örn. `KnowledgeAdmin` veya mevcut bir kapsamın genişletilmesi) eklenip
`RequireApiKeyScope` çağrılmalı; okuma uçları (`GET`/`search`) ayrı, daha
gevşek bir kapsamla korunabilir.

---

## HATA-S1-010 — Bilgi tabanı doğrulama hataları `.NET ArgumentException`'ın iç parametre adını sızdırıyor

| | |
|---|---|
| **Önem** | Düşük |
| **Durum** | ⛔ Açık |
| **Case** | `MT-MEM-021`, `022`, `023`, `024` |
| **Alan** | `src/AgentPrism.Core/Knowledge/KnowledgeIngestionService.cs`, `src/AgentPrism.AspNetCore/Endpoints/KnowledgeEndpoints.cs:85,150` |
| **İlgili faz** | Faz 51 |

**Belirti**
Dört negatif senaryonun `ProblemDetails.detail` metni dokümante edilen
"tam olarak" beklenen metinle BAŞLIYOR ama sonunda `.NET`'in otomatik
eklediği `" (Parameter 'x')"` soneki taşıyor (ör. `"...ikisi birden ya da
hicbiri olamaz. (Parameter 'text')"`).

**Kanıt**
`MT-MEM-021`/`022`/`023`/`024`'ün dördü de bu deseni gösterdi (sırasıyla
`'text'`, `'text'`, `'collection'`, `'chunks'` parametre adlarıyla).

**Kök neden**
`KnowledgeIngestionService` doğrulama hatalarını `throw new
ArgumentException(message, nameof(param))` ile atıyor;
`KnowledgeEndpoints.cs:85` ve `:150` bunu `catch (ArgumentException ex) =>
Invalid(ex.Message)` ile yakalayıp doğrudan HTTP `detail` yapıyor. .NET'in
`ArgumentException.Message` getter'ı `paramName` verildiğinde mesaja her
zaman `"\n(Parameter 'x')"` ekler — yaygın bir "footgun", API tüketicisine
iç .NET parametre adını sızdırır.

**Etkisi**
İşlevsel değil (istek yine doğru şekilde `400` ile reddediliyor), ama (a)
dokümante edilen tam metinle uyuşmuyor, (b) internal parametre adı dış API
sözleşmesinin parçası hâline geliyor (ileride `nameof` yeniden adlandırılırsa
API tüketicisi fark eder).

**Önerilen düzeltme**
`Invalid(ex.Message)` yerine mesajı `ex.Message` değil özel bir alan
(`ArgumentException` alt sınıfında ayrı bir `UserMessage` özelliği, ya da
`ex.Message.Split('\n')[0]`) ile temizlemek; ya da `ArgumentException`
yerine `paramName` almayan bir istisna kullanmak.

---

## HATA-S1-009 — `EnableTextSearch` (`TextSearchProvider`) sorguyla eşleşen dosya bellek içeriğini hiç bulamıyor (şüpheli)

| | |
|---|---|
| **Önem** | Orta |
| **Durum** | ⛔ Açık (şüpheli davranış, kod seviyesinde doğrulanmadı) |
| **Case** | `MT-MEM-013`, `014` |
| **Alan** | `src/AgentPrism.Core/Compilation/AgentDefinitionCompiler.cs` (`CreateTextSearchProvider`, `SearchFileStoreAsync`) |
| **İlgili faz** | Faz 13 |

**Belirti**
`EnableTextSearch: true` olan bir agent, dosya belleğinde HARFİYEN mevcut
olan bir dizgiyi (`FILE-7841`) iki farklı sorgu biçiminde de bulamadı.

**Kanıt**
`manuel-dosya-bellek` `/default/user-notes.md` dosyasına `"kayit kodu
FILE-7841"` yazdı (SQL ile doğrulandı). `manuel-dosya-arama` (aynı kiracı,
farklı agent/oturum, `EnableTextSearch: true`) iki ayrı denemede de
(`"FILE-7841 ile ilgili bir kayit var mi?"` ve `"search tool unu kullanarak
FILE-7841 ara"`) hiçbir sonuç bulamadı — birinde model "eşleşen kayıt
bulamadım" dedi, diğerinde "bağlı bir arama aracına erişimim yok" dedi.
`GET .../events` çıktısında hiçbir ikisinde de `ToolInvoked`/arama olayı
görünmedi. Bu, dosyanın kendi notunun beklediği durumun (F-105 nedeniyle
aynı-kiracı içi izolasyon eksikliği yüzünden "bulmalı ama izole etmemeli")
TAM TERSİ.

**Kök neden**
Belirlenemedi — siyah kutu HTTP testi bunun ötesine geçemedi.
`TextSearchProvider` bir `AIContextProvider` (MAF, `Microsoft.Agents.AI`);
modele açık bir "tool" olarak sunulmuyor, sorguya göre otomatik bağlam
enjekte etmesi bekleniyor. `AgentDefinitionCompiler.SearchFileStoreAsync`
`fileStore.SearchAsync("/", query)` çağırıyor — sorgu metninin TAMAMI
arama girdisi olarak kullanılıyor (anahtar kelime çıkarımı yok), bu yüzden
doğal dil sorguları literal dosya içeriğiyle eşleşmeyebilir. Bu bir
hipotez, kod içi `AgentFileStore.SearchAsync`'in eşleştirme mantığı
(substring mi, regex mi, ne kadar toleranslı) ayrıca okunmadı.

**Etkisi**
`EnableTextSearch` yeteneği, test edilen iki sorgu biçiminde de işlevsiz
görünüyor — bu doğruysa dokümante edilen bir yetenek gerçekte çalışmıyor.
`MT-MEM-014`'ün kiracı yalıtımı sonucu da bu yüzden güvenilir kanıt
SAYILAMAZ (negatif sonuç, izolasyondan mı yoksa arama arızasından mı
kaynaklandığı ayırt edilemiyor).

**Önerilen düzeltme**
Kod seviyesinde `AgentFileStore.SearchAsync`'in eşleştirme davranışı
incelenmeli; `TextSearchProvider`'a geçirilen sorgu metninin ön işlemden
geçip geçmediği (anahtar kelime çıkarımı, embedding tabanlı benzerlik vb.)
doğrulanmalı. Bu HATA çözülmeden `MT-MEM-014` yeniden koşulmalı.

---

## HATA-S1-008 — Bellek sağlayıcıları (dosya belleği okuma, todo) mesaj serileştirmesinde `500` ile çöküyor

| | |
|---|---|
| **Önem** | **Yüksek** |
| **Durum** | ⛔ Açık |
| **Case** | `MT-MEM-011`, `012` |
| **Alan** | `Microsoft.Agents.AI` (MAF, harici paket) mesaj attribution + `src/AgentPrism.AspNetCore` (`AgentPrismJsonContext`, AOT source-generated JSON) |
| **İlgili faz** | Faz 13 |

**Belirti**
`EnableFileMemory`/`EnableTodo` açık bir agent'ın bellek sağlayıcısını
KULLANDIĞI bir turda (`FileMemoryProvider` için okuma/hatırlama turu,
`TodoProvider` için ilk yazma turu dahil) HTTP yanıtı her zaman `500` ile
çöküyor.

**Kanıt**
`manuel-dosya-bellek`: 1. tur (yazma) `HTTP 200`, veritabanında doğru
yazıldı (`agent_files` tablosunda `FILE-7841` doğrulandı). 2. tur (geri
okuma, AYNI oturum) 3 bağımsız denemede de `HTTP 500`. `manuel-todo`: 1.
tur (ilk yazma) doğrudan `HTTP 500`. Uygulama logunda her seferinde aynı
istisna:
```
System.NotSupportedException: JsonTypeInfo metadata for type
'Microsoft.Agents.AI.AgentRequestMessageSourceAttribution' was not
provided by TypeInfoResolver of type 'AgentPrism.AgentPrismJsonContext'.
```
Run sunucu tarafında TAMAMLANIYOR (span/idempotency kaydı yazılıyor, model
çağrısı ve maliyeti gerçekleşiyor) ama istemci hiçbir zaman yanıt alamıyor
— sessiz maliyet kaybı da var.

**Kök neden**
MAF, bellek sağlayıcılarının (FileMemoryProvider okuma yolu, TodoProvider)
enjekte ettiği içeriğe bir `AgentRequestMessageSourceAttribution` nesnesini
`ChatMessage.AdditionalProperties`'e ekliyor (kaynak/provenance bilgisi).
AOT uyumluluğu için kullanılan kaynak-üretimli `AgentPrismJsonContext`
(`System.Text.Json` source generator) bu tipi TANIMIYOR — HTTP yanıtı
serileştirilirken `System.Text.Json`, polimorfik `AdditionalProperties`
sözlüğünde bu tiple karşılaşınca `NotSupportedException` fırlatıyor ve bu
hiçbir yerde yakalanmıyor.

**Etkisi**
`EnableFileMemory`'nin "hatırlama" (okuma) yolu ve `EnableTodo`'nun TAMAMI
kullanılamaz durumda — dokümante edilen iki bellek sağlayıcısı yeteneği
API üzerinden fiilen çalışmıyor. Ayrıca her başarısız çağrı gerçek bir
model çağrısı yapıp ücretlendiriyor, sonucu hiçbir zaman istemciye
ulaşmıyor.

**Önerilen düzeltme**
`AgentPrismJsonContext`'e `Microsoft.Agents.AI.AgentRequestMessageSourceAttribution`
için `[JsonSerializable]` eklenmeli (tipin `Microsoft.Agents.AI` NuGet
paketinde public olduğu doğrulanmalı); polimorfik `AdditionalProperties`
sözlüğü serileştirilirken beklenmedik/bilinmeyen tipler için genel bir
fallback (ör. `ToString()` ya da atlama) düşünülebilir, AOT kısıtı altında
her yeni MAF sürümünün attribution tipini önceden bilmek kırılgan.

---

## HATA-S1-007 — `/api/agents/validate`, bilinmeyen `enum` string değerinde `400` yerine `500` veriyor

| | |
|---|---|
| **Önem** | Orta |
| **Durum** | ⛔ Açık |
| **Case** | `MT-MEM-004` (aynı desen `MT-MEM-031` adım 4'te de gözlemlendi) |
| **Alan** | `src/AgentPrism.AspNetCore/Endpoints/AgentEndpoints.cs` (`ValidateAgentAsync`) |
| **İlgili faz** | Faz 13 |

**Belirti**
`compaction.strategy` alanına `CompactionStrategyKind` enum'ında olmayan
bir string (`"Sihirli"`) gönderildiğinde `ValidateAgentAsync`'in kendi XML
dokümanının vaat ettiği "gövde ayrıştırılamıyorsa 400" davranışı yerine
`500` (`ProblemDetails`, generic "An error occurred...") dönüyor.

**Kanıt**
`curl -X POST /api/agents/validate -d '{"compaction":{"strategy":"Sihirli"},...}'`
→ `HTTP 500`. Uygulama logu:
```
Microsoft.AspNetCore.Http.BadHttpRequestException: Failed to read parameter
"AgentDefinitionRequest request" from the request body as JSON.
 ---> System.Text.Json.JsonException: The JSON value could not be converted
 to AgentPrism.CompactionStrategyKind.
```
Aynı desen `MT-MEM-031` adım 4'te de gözlemlendi (orada gövde eksik
`model` alanı yüzünden — farklı tetikleyici, aynı kök neden: minimal
API'nin body-binding aşamasında fırlayan `JsonException` hiçbir yerde
yakalanmıyor).

**Kök neden**
`CompactionStrategyKind` `JsonStringEnumConverter<T>` ile işaretli (K-040).
ASP.NET Core minimal API, tipli gövde parametresini endpoint gövdesine
girmeden ÖNCE (filtre/handler'dan önce) bağlıyor; enum dönüştürme hatası bu
aşamada `JsonException` olarak fırlıyor ve `ValidateAgentAsync`'in kendi
try/catch'i bu noktaya hiç erişemiyor — hata global exception handler'a
düşüp genel `500`'e dönüşüyor.

**Etkisi**
Metodun kendi XML dokümanının verdiği sözleşme ("yalnızca gövde
ayrıştırılamıyorsa 400") kısmen yanlış: bazı ayrıştırma hataları (enum,
eksik zorunlu alan) `400` değil `500` üretiyor. Bu tutarsızlık,
`/validate`'i bir CI/otomasyon aracı olarak kullanan bir tüketicinin
ağ hatasıyla girdi hatasını ayırt edememesine yol açar (metodun kendi
belgelediği amaç tam olarak bunun önlenmesiydi).

**Önerilen düzeltme**
Body-binding'i minimal API'nin otomatik JSON binding'i yerine elle
(`HttpContext.Request.ReadFromJsonAsync` + `try/catch (JsonException)`)
yapıp `400` döndürmek; ya da global bir `IExceptionHandler`/middleware
`JsonException`'ı yakalayıp `400 ProblemDetails`'e çevirmeli (bu, tüm
minimal API uçları için tutarlı bir düzeltme olurdu, yalnız `/validate`
için değil — `MT-MEM-031` adım 4'ün de aynı düzeltmeden faydalanacağı
gösteriyor bunu).

---

## HATA-S1-004 — `InvariantGlobalization=true` SQL Server kalıcılığını tamamen kırıyor

| | |
|---|---|
| **Önem** | **Kritik** |
| **Durum** | ✅ **ÇÖZÜLDÜ** — karar `K-392`, commit `aa64bc7` (`docs/manuel-test`) |
| **Case** | `MT-SQL-014`, `015`, `024`, `025`, `026`, `027` (6 case bloklanmıştı, düzeltmeden sonra hepsi koşuldu) |
| **Alan** | Örnek uygulama + **paket şablonu** |
| **İlgili faz** | Faz 23 |

**Belirti**
`UseSqlServer()` yapılandırıldığında uygulama hiç açılmıyor. Süreç
`Hosting failed to start` + `Unhandled exception` yazıp sonlanıyor;
`Now listening` hiç yazılmıyor.

```
fail: Microsoft.Extensions.Hosting.Internal.Host[11]
      Hosting failed to start
      System.NotSupportedException: Globalization Invariant Mode is not supported.
         at Microsoft.Data.SqlClient.SqlConnection.TryOpen(TaskCompletionSource`1 retry, SqlConnectionOverrides overrides)
         at Microsoft.Data.SqlClient.SqlConnection.InternalOpenAsync(SqlConnectionOverrides overrides, CancellationToken cancellationToken)
         at System.Data.Common.DbDataSource.OpenDbConnectionAsync(CancellationToken cancellationToken)
```

**Kök neden**
`samples/AgentPrism.Api/AgentPrism.Api.csproj:6` →
`<InvariantGlobalization>true</InvariantGlobalization>`. Bu ayar derlenmiş
`AgentPrism.Api.runtimeconfig.json` içine
`"System.Globalization.Invariant": true` olarak geçiyor.
`Microsoft.Data.SqlClient` Globalization Invariant Mode'u **desteklemez** ve
`SqlConnection.Open` çağrısında koşulsuz `NotSupportedException` fırlatır.

**Neden Kritik — örnekle sınırlı değil**
Aynı ayar paket şablonunda da var:
`src/AgentPrism.Templates/content/AgentPrism.Starter/AgentPrism.Starter.csproj:9`.
`dotnet new` ile proje kuran ve `UseSqlServer()` çağıran **her tüketici** bu
duvara çarpar. AgentPrism bir NuGet paket ailesidir; şablon kusuru doğrudan
tüketiciye taşınır. `AgentPrism.SqlServer` paketi, kendi şablonuyla kurulan bir
projede kullanılamaz durumdadır.

**Kanıt / ayırt edici kontrol**
- Geçerli bağlantı dizesiyle (port 51433, `AgentPrism_S1` mevcut): açılmadı.
- Geçersiz host'la (port 1): **aynı** istisna — yani hata bağlantı hedefine
  bağlı değil, `SqlConnection.Open`'ın kendisinde.
- `DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=0` ortam değişkeni **işe yaramıyor**:
  `runtimeconfig.json` içindeki `configProperties` ortam değişkenini ezer.
- SQLite ve bellek içi izlekler etkilenmiyor; `Microsoft.Data.Sqlite` bu moda
  bağımlı değil.
- Ayar doğrulamasıyla kapanan SQL Server case'leri (`011`, `012`, `013`, `016`)
  bağlantıya hiç ulaşmadıkları için normal koştu ve geçti.

**Uygulanan düzeltme** (kullanıcı kararıyla koşum sırasında kodlandı)
`InvariantGlobalization` hem `samples/AgentPrism.Api/AgentPrism.Api.csproj`
hem `src/AgentPrism.Templates/content/AgentPrism.Starter/AgentPrism.Starter.csproj`
dosyasından kaldırıldı. İki dosyaya da ayarın NEDEN açılmadığını yazan bir
yorum bırakıldı ki sonraki oturum geri eklemesin. Karar `K-392` olarak
`docs/KARARLAR.md`'ye yazıldı.

Ayarın kayıtlı bir gerekçesi yoktu (karar defterinde hiç geçmiyordu); ICU
bağımlılığından kaçınma amacıyla konmuş olmalı — doğruluk bu kazancı yener.

**Doğrulama**
- Dört kapı yeşil: `build` 0 uyarı/0 hata · `dotnet test` tüm paketler 0
  başarısız · `pack` exit 0 (85 `.nupkg`) · `format` exit 0.
- İlk denemede `AgentPrism.Templates.Tests` 3/10 kaldı: eklediğim XML yorumu
  `--UseSqlServer` içeriyordu, XML yorumları `--` taşıyamaz (`MSB4025`).
  Yorum düzeltildi, 10/10 geçti. Şablon testleri kusuru yakaladı.
- Derlenmiş `AgentPrism.Api.runtimeconfig.json` artık
  `System.Globalization.Invariant` anahtarını hiç taşımıyor.
- Bloklanan 6 case gerçek SQL Server bağlantısıyla yeniden koşuldu:
  5'i geçti, `MT-SQL-014` `HATA-S1-002`'yi SQL Server'da da doğruladı.

---

## HATA-S1-002 — `AutoApplyMigrations=false` + hazır olmayan şema uygulamayı kapatıyor

| | |
|---|---|
| **Önem** | Yüksek |
| **Durum** | ⛔ Açık — düzeltilmedi (KOSUM-PLANI §8 toplama oturumuna kaldı) |
| **Case** | `MT-SQL-004` (SQLite) **ve** `MT-SQL-014` (SQL Server) — ikisi de koşuldu ve kaldı |
| **Alan** | `AgentPrism.Sql.Shared` + `AgentPrism.AspNetCore` |
| **İlgili faz** | Faz 23/24, Faz 42 |

**Belirti**
`AutoApplyMigrations=false` ve şema henüz kurulmamışken uygulama açılıyor
(`Now listening` yazıyor), hemen ardından **kendini kapatıyor**. `/health`
isteği bağlantı kuramıyor (`HTTP:000`).

```
crit: AgentPrism.A2AApprovalGuardFilter[0]
      A2A disa acik yuzey denetimi basarisiz oldu; uygulama durduruluyor.
      Microsoft.Data.Sqlite.SqliteException: SQLite Error 1: 'no such table: agentprism_agent_definitions'.
         at AgentPrism.A2AApprovalGuardFilter.RunCheckAsync(...) A2A/A2AApprovalGuardFilter.cs:58
crit: AgentPrism.McpApprovalGuardFilter[0]
      MCP disa acik yuzey denetimi basarisiz oldu; uygulama durduruluyor.
         at AgentPrism.McpApprovalGuardFilter.RunCheckAsync(...) McpServer/McpApprovalGuardFilter.cs:86
```

**Kök neden**
`MigrationHostedService.StartAsync`, `AutoApplyMigrations` kapalıyken
`SchemaReadyGate.MarkReady()` çağırır — bu **bilinçli** bir karardır
("semanin hazir olmasi tuketicinin sorumlulugundadir"). Ancak `SchemaReadyGate`
"migration'lar bitti" ile "şema gerçekten hazır" arasında ayrım yapmaz.
`A2AApprovalGuardFilter` ve `McpApprovalGuardFilter` kapıyı bekler ve açılır
açılmaz `catalog.ListAsync` ile `agent_definitions` sorgular. Şema yoksa sorgu
patlar, `catch (Exception)` bloğu
`lifetime.ApplicationStarted.Register(lifetime.StopApplication)` çağırır.

**Etkisi**
Dokümante edilen sözleşme erişilemez hâle geliyor: `AutoApplyMigrations=false`
kurulumunda operatörün "migration'lar bekliyor" durumunu görmesi için
tasarlanmış `/health` → `Unhealthy` + `pendingMigrations` sinyali hiç
okunamıyor. Yerine kök nedeni gizleyen bir "no such table" kapanma döngüsü
oluşuyor. İlk kurulumda ya da migration adımı sırayı kaçırdığında tam olarak bu
yaşanır.

**Ayırt edici kontrol**
Şema önce `AutoApplyMigrations=true` ile kurulup (45 tablo) sonra `false` ile
açıldığında uygulama sorunsuz ayakta kaldı (`Application is shutting down`
satırı yok, `/health` → `Degraded`). Kusur ayarda değil, **kapının hazır
olmayan şema üzerinde açılmasında**.

**İki sağlayıcıda doğrulandı**
`HATA-S1-004` düzeltildikten sonra `MT-SQL-014` SQL Server'da yeniden koşuldu
ve SQLite ile **birebir aynı** davranışı verdi: `Now listening` → `crit:
AgentPrism.A2AApprovalGuardFilter` → `SqlException: Invalid object name
'agentprism.agent_definitions'.` → `Application is shutting down`,
`/health` `HTTP:000`, şema tablo sayısı 0. Kök nedenin sağlayıcıdan bağımsız
(`AgentPrism.Sql.Shared`) olduğu artık tahmin değil, ölçüm.

**Önerilen düzeltme**
İki yoldan biri: (a) onay denetimleri şema hazır değilken `crit` + kapatma
yerine uyarı verip denetimi ertelesin — sağlık ucu zaten durumu bildiriyor;
(b) `SchemaReadyGate` "hazır" ile "atlandı" durumlarını ayırsın ve bekleyenler
atlandı durumunda SQL'e dokunmasın. (a) daha küçük ve gözlemlenebilirlik
kuralına ("depo hatası çalıştırmayı kesmez") daha yakın.

---

## HATA-S1-003 — `Data Source=:memory:` dokümante edildiği hâlde hiç çalışmıyor

| | |
|---|---|
| **Önem** | Yüksek |
| **Case** | `MT-SQL-005` |
| **Alan** | `AgentPrism.Sqlite` — public API XML dokümanı |
| **İlgili faz** | Faz 24 |

**Belirti**
`AgentPrism:Sqlite:ConnectionString` = `Data Source=:memory:` ile uygulama
açılmıyor. Migration'lar uygulanıyor, hemen ardından süreç ölüyor.

```
info: AgentPrism.MigrationRunner[0]
      AgentPrism 15 migration uyguladi. Sema: agentprism_.
fail: Microsoft.Extensions.Hosting.Internal.Host[11]
      Hosting failed to start
      Microsoft.Data.Sqlite.SqliteException: SQLite Error 1: 'no such table: agentprism_tenants'.
```

**Kök neden**
`src/AgentPrism.Sqlite/Internal/SqliteDataSource.cs:44` —
`CreateDbConnection()` her çağrıda **yeni** bir `SqliteConnection` üretir.
Çıplak `:memory:` veritabanı **bağlantıya özeldir**: `MigrationRunner` kendi
bağlantısında 15 migration uygular, bağlantı kapanır, veritabanı yok olur.
`EnsureDefaultTenantAsync` yeni bir bağlantı açar — boş bir in-memory
veritabanı — ve `agentprism_tenants` orada yoktur.

**Sözleşme ihlali**
`src/AgentPrism.Sqlite/AgentPrismSqliteOptions.cs:17-18` XML dokümanı:
`Data Source=:memory: desteklenir ama kalici DEGILDIR: baglanti kapaninca veri
gider.` Bu iddia yanlış — `:memory:` hiç desteklenmiyor, uygulama açılmıyor.
XML dokümanı tüketiciye IntelliSense'te gösterilir; paket ailesinin public
sözleşmesinin parçasıdır.

**Doğrulanan çalışan biçim**
`Data Source=file:apmem?mode=memory&cache=shared` ile uygulama sorunsuz açıldı
(15 migration, `/health` → HTTP 200). Paylaşımlı önbellek aynı süreçteki tüm
bağlantılara tek bir in-memory veritabanı verir.

**Önerilen düzeltme**
XML dokümanı `cache=shared` biçimini önersin ve çıplak `:memory:`'nin bir
`DbDataSource` ile çalışmayacağını açıkça yazsın. Alternatif olarak
`AgentPrismSqliteOptionsValidator` çıplak `:memory:`'yi anlaşılır bir mesajla
reddetsin — sessizce "no such table" ile ölmekten iyidir.

**Ek — S1-5'te üçüncü tekrar (2026-08-13, `25-SAGLIK-TESHIS-OPENAPI.md`,
`MT-DIAG-005`/`MT-DIAG-027`).** Aynı çökme `AgentPrismTestHost.UseSqlite("Data
Source=:memory:")` üzerinden de doğrulandı — hem K-183 çoklu-kayıt
senaryosunda (`MT-DIAG-005`) hem de **tek** kayıtla (`MT-DIAG-027`, K-183'ten
tamamen bağımsız). Kök neden aynı (`SqliteDataSource.CreateDbConnection`),
etki alanı `AgentPrism.Testing`'i de kapsıyor — yalnız örnek uygulamanın
yapılandırması değil, kütüphanenin test host'u da aynı duvara çarpıyor. İki
case, önerilen çalışan biçimin `AgentPrismTestHost` altında da geçerli
olduğunu doğruladı: `Data Source=file::memory:?cache=shared`.

---

## HATA-S1-001 — Koşum hijyeni: `user-secrets` temizliği uygulanmamış

| | |
|---|---|
| **Önem** | Düşük (süreç kusuru, ürün kusuru DEĞİL) |
| **Case** | `MT-SQL-002` |

Şerit açılırken paylaşılan `user-secrets` deposu
`AgentPrism:Sqlite:TablePrefix` = `x_; DROP TABLE agentprism_tenants;--`
taşıyordu — `MT-SQL-002` 3. adımının temizliği önceki oturumda uygulanmamış.
Depo `UserSecretsId=agentprism-sample-api` ile makine genelinde tek olduğu için
bu değer **her şeridi** etkiler ve uygulamanın açılmasını engeller.

KOSUM-PLANI §2.2 gereği depoya yazılmadı. Şerit kendi ayar anahtarlarını ortam
değişkeniyle sabitleyerek ilerledi.

**Öneri:** Toplama oturumunda (KOSUM-PLANI §8) depo temizlenmeli:
`dotnet user-secrets remove "AgentPrism:Sqlite:TablePrefix"`. Ayrıca senaryo
dosyalarındaki "Temizlik" adımlarının koşum sırasında gerçekten uygulanması
gerekir; uygulanmayan temizlik sonraki oturumu bozuyor.

---

## Kullanıcı eylemi bekleyen case'ler

| Case | Neden | Kullanıcıdan istenen |
|---|---|---|
| `MT-MEM-015`, `016`, `017`, `018`, `020`, `026`, `027`, `030` | OpenAI anahtarının bağlı olduğu proje hiçbir embedding modeline erişemiyor (`text-embedding-3-small`/`-large`/`ada-002` üçü de `403 model_not_found`) | Embedding erişimi olan bir OpenAI API anahtarı sağlanmalı (veya mevcut projeye embedding modeli erişimi eklenmeli), sonra bu 8 case tekrar koşulmalı. |
