# Analyzer ve Kaynak Ureteci Yazimi

> Roslyn `IIncrementalGenerator` ve `DiagnosticAnalyzer` yazarken cikan tuzaklar.
> Bir analyzer'i TUKETMEK (RS00xx, `NoWarn`, paketleme) `build-ve-analyzer.md`'dedir.
>
> Bu dosya `MEMORY.md`'nin alan dosyasidir. Yalnizca bu alana
> dokunurken okunur. Yeni not buraya eklenir, `MEMORY.md`'ye degil.

## Kaynak üreteci (Faz 52)

- **`netstandard2.0`'da `record`/`init` için `IsExternalInit` yok** (2026-08-08): net5.0+ BCL'si taşır. Çözüm: `namespace System.Runtime.CompilerServices { internal static class IsExternalInit; }` polyfill'i — derleyici yalnız VARLIĞA bakar.
- **🚨 `SymbolDisplayFormat.FullyQualifiedFormat`, `UseSpecialTypes` yüzünden ilkel tipleri anahtar kelimeyle yazar** (`int`), `global::System.Int32` DEĞİL (2026-08-08): tip adına göre `switch` yapan kod (dönüştürücü seçimi) sessizce hiç eşleşmez; yalnız GERÇEK bir `int`/`bool` parametreli tool ile ortaya çıktı, `string`-only birim testleri yakalamadı. Çözüm: `UseSpecialTypes` bayrağı çıkarılmış özel format.
- **🚨 `OutputItemType=Analyzer`, çok-sıçramalı `ProjectReference` zincirinde YAYILMAZ**: `A → B → Generator` ise `A` üreteci YÜKLEMEZ — yalnız `A` PAKET (`PackageReference`) tükettiğinde NuGet'in `analyzers/dotnet/cs` yayılımı çalışır. Aynı çözümdeki bir örnek/tüketici (ProjectReference) Analyzer referansını AYRICA almalı; gerçek dış (paket) tüketici etkilenmez.
- **Çok-hedefli (net8/9/10) projede `@(Analyzer)` yalnız İÇ derlemede doludur**: `dotnet pack`'in dış derlemesinde `BeforeTargets="_GetPackageFiles"` BOŞ döner. Çözüm `TargetsForTfmSpecificContentInPackage` + `TfmSpecificPackageFile`; TFM-bağımsız dosyayı üç kez eklemek `NU5118` verir, `Condition="'$(TargetFramework)'=='netX.0'"` ile tek TFM'e sabitlenir.
- **`namespace X;` dosya başına TEK ad alanıyla sınırlıdır** (`CS8954`): iki ad alanı gereken üretilmiş dosyada blok biçimi (`namespace X { }`) kullanılır.
- **3. taraf soyut/sanal üye override ederken NRT imzasını TAHMİN ETME** (`Microsoft.Extensions.AI.AITool.Description`): reflection dökümü nullable ek açıklamasını GÖSTERMEZ; derleyici `CS8764` ile gerçek imzayı söyler. `NullableAttribute` YOKLUĞU genelde NON-nullable demektir.

## Tanı yazımı (Faz 73)

- **🚨 `Tracon.Core` KENDİ analyzer'ını KENDİ ÜZERİNDE koşturur** (2026-08-19): Core, `Tracon.Generators`'ı `OutputItemType=Analyzer` ile referanslar, yani üreteçle birlikte oradaki her `DiagnosticAnalyzer` Core'un kendi derlemesinde çalışır. Faz 73'te yeni bir tanının ilk hâli `dotnet pack`'i kırdı: kural `RunRecordingAgent` ve `ReplayMismatchGuard`'ı işaretledi. Yeni bir `APG` tanısı yazarken Core'un kendi kodunu da tarayacağını hesaba kat — bu bedava bir dogfood turudur. Analyzer referansı çok sıçramalı `ProjectReference` zincirinde yayılmaz — **Faz 93'ten beri `Tracon.Workflows` da kendi `OutputItemType=Analyzer` referansını taşır** (Core'un desenini tekrarlar), yani yeni bir `APG` tanısı yazarken **hem** Core **hem** Workflows üzerinde `dotnet build -c Release` ile sıfır-yanlış-pozitif ölçülmelidir — iki ayrı dogfood turu.
- **🚨 `DiagnosticSeverity.Info` tanısı `dotnet build` çıktısına DÜŞMEZ** (2026-08-19, ölçüldü): `-v:normal` ve `-t:Rebuild` ile de görünmez. Yalnız IDE'de ve `.editorconfig` ile seviye yükseltilirse çıkar. Bir agent'ın veya CI günlüğünün okumasını istediğin tanı `Warning` olmalıdır; bedelini `NoWarn`'a çeviren bir MSBuild özelliğiyle sınırla (K-506).
- **🚨 Bir tanının önerdiği DÜZELTMENİN uygulanabilir olduğunu ölç, yalnız tetiğini değil** (2026-08-21, Faz 78 denetim 🔴): `TRC0402` doğru koşulda ötüyor, mesajı doğru, testleri geçiyordu — ve hiçbir özellik açmamış tüketiciye **var olmayan** bir dosyayı adlandırmasını söylüyordu. Sorulacak soru: *bu tanının dediğini harfiyen yapan tüketici ne elde eder?* Her yapılandırmada sor, yalnız mutlu yolda değil.
- **Tüketicinin MSBuild özelliğini analyzer'a `CompilerVisibleProperty` taşır** (2026-08-21): `<CompilerVisibleProperty Include="X" />` → `context.Options.AnalyzerConfigOptionsProvider.GlobalOptions["build_property.X"]`. 🚨 `AdditionalFiles` opt-in'e bağlı DEĞİLDİR (`Tracon.Core.targets`); bir tanı "tüketici bunu açtı mı" bilgisine ihtiyaç duyuyorsa onu **ayrıca** almalıdır. Birim testinde `AnalyzerOptions`'ın ikinci parametresi bir `AnalyzerConfigOptionsProvider` ister — `AnalyzerTestHelper.RunWithPropertiesAsync` bunu kurar.
- **Analyzer diskten dosya OKUYAMAZ** (`RS1035`): tüketicinin bir dosyasına bakan tanı, dosyayı `buildTransitive` hedefinden `<AdditionalFiles>` ile alır. `AdditionalFiles` öğeleri **evaluation** anında toplandığı için, `.targets` dosyasının projenin sonunda import edilmesi sorun değildir — ama aynı build içinde ÜRETİLEN bir dosya o listeye giremez.
- **🚨 "Metot gövdesinde herhangi bir yer" tetikli bir döngü/akış kuralı, KONTEYNER döngülerde ve `finally` bloklarında yanlış pozitif üretir** (2026-08-24, Faz 93, ölçüldü): `TRC0501` (async iterator'da ambient yazımın döngü içinde tekrarlanıp tekrarlanmadığını denetleyen kural) ilk hâliyle `WorkflowRunner.RunGuardedAsync`'i (bir `await foreach` passthrough'u, kendi kendine güvenli) VE `WorkflowRunner.PumpAsync`'in `finally { await enumerator.DisposeAsync(); }` bloğunu (temizlik amaçlı await, nested call değil) yanlış işaretledi — ikisi de GERÇEK üretim kodu, ikisi de doğru. Çözüm iki daraltma: (1) bir döngü BAŞKA bir döngü içeriyorsa (konteyner), o döngü hiç değerlendirilmez, yalnız YAPRAK (en iç) döngüler bağımsız kontrol edilir; (2) `finally` klozları await taramasının dışında tutulur (`SyntaxNode.DescendantNodesAndSelf(descendIntoChildren:)` ile). Genel ders: bir akış-analizi kuralı yazarken **gerçek kod tabanındaki HER async iterator'ı** (`grep -rn "async.*IAsyncEnumerable" src/`) tek tek gözden geçir, yalnız planın önceden ölçtüğü vakalarla yetinme — plan yeni bir `ProjectReference` eklendiğinde ortaya çıkan vakaları ölçemez.
- **🚨 "Sonucu discard edildi mi" analizini SÖZDİZİMSEL (`ExpressionStatementSyntax`) değil `IOperation` (`IExpressionStatementOperation`) ile yaz** (2026-08-24, Faz 93, ölçüldü): `TRC0502`'nin ilk hâli yalnız `invocation.Parent is ExpressionStatementSyntax` kontrolü yapıyordu ve `void Run() => Begin(...);` (expression-bodied metot) şeklindeki discard'ı KAÇIRDI — bu satırın syntax ebeveyni `ArrowExpressionClauseSyntax`'tır, `ExpressionStatementSyntax` değil, ama semantik olarak AYNI şekilde atılır. `context.SemanticModel.GetOperation(invocation)?.Parent is IExpressionStatementOperation` her iki şekli de doğru yakalar (Roslyn'in operation ağacı void-dönen arrow body'yi bir `IExpressionStatementOperation` olarak modeller). Sözdizim tabanlı "bu ifadenin sonucu kullanılıyor mu" analizleri IOperation'a tercih edilmemeli.

## Şema description ekleme (Faz 125)

- **🚨 `netstandard2.0` hedefinde `System.Range`/`System.Index` YOK — `s[1..]` `CS0518` verir.** `Tracon.Generators` `netstandard2.0`'ı hedefler (Roslyn analyzer gereği); dizin aralığı operatörü BCL tipine ihtiyaç duyar, o tip bu TFM'de yok. Çözüm `s.Substring(1)`. `record`/`init` (`IsExternalInit`) için zaten bilinen aynı sınıftan bir tuzak — bu TFM'de "yeni C# sözdizimi" her zaman "yeni BCL tipi" varsayımıyla gelmez.
- **🚨 Bir `DiagnosticDescriptor` tanımlamak onu RAPORLAMAZ — `ToolRegistrationGenerator`'ın kendi `DescriptorsById` sözlüğüne de eklenmeli.** `ToolDiagnostics.cs`'e yeni bir tanı eklemek, `DiagnosticIntegrityTests`'i (help link, mesajdaki API adları, `troubleshooting.md`) geçirir ve `dotnet build` **sıfır uyarıyla** biter — ama tanı hiçbir zaman ÜRETİLMEZ. Sebep: `ToolRegistrationGenerator.ReportDiagnostic`, id'yi `DescriptorsById`'de bulamazsa sessizce `return` eder (`if (!DescriptorsById.TryGetValue(...)) { return; }`). TRC0009 eklenirken ölçüldü: kod derlendi, mesaj doğruydu, ve yalnız tanıyı ARAYAN bir test (`ToolSchemaDescriptionTests`) bunu yakaladı — `result.Diagnostics.ShouldBeEmpty()` bekleyen mevcut testler YEŞİL kaldı çünkü tanı gerçekten hiç raporlanmıyordu. Regresyon kapısı: `DiagnosticIntegrityTests.Every_DiagnosticInfo_routed_tool_diagnostic_is_wired_into_the_generators_dispatch_table` — private `DescriptorsById` alanını reflection ile okur, `ToolDiagnostics`'teki her `Tracon.Tools` tanısının (doğrudan `context.ReportDiagnostic` ile raporlanan `DuplicateName`/`NoToolsFound` hariç) tabloda olduğunu doğrular.

## Nesne parametresi (Faz 135)

- **🚨 Bir positional record parametresindeki attribute, `[property: ...]` hedefi VERİLMEDEN, KURUCU PARAMETRESİNE gider — generated PROPERTY'ye değil.** Roslyn ile ölçüldü: `record Rubric([property: Description("d")] string Name, int Weight)` → `Description` yalnız `Name` PROPERTY'sinde görünür, kurucu parametresinde YOK; ama `record Plain(string Name, [Description("d")] int Weight)` (hedef YOK) → `Description` yalnız kurucu PARAMETRESİNDE görünür, property'de YOK. Nesne üyesi modeli (`ParameterTypeValidator.TryCreateMember`) üyeleri kurucu parametrelerinden okur (`IParameterSymbol.GetAttributes()`) — tüketici `[property: ...]` hedefi yazarsa attribute generator'a hiç ULAŞMAZ ve üye sessizce description/kısıt kaybeder, derleme hatası vermez. Tüketici tarafına yazılan kural: attribute'u doğrudan positional parametreye koy, hedef belirtme.
- **Nesne grafındaki derinlik ve cycle kontrolü PATH LİSTESİ ile yapılır, `path.Count`'u "derinlik" olarak yeniden kullanma.** İkisi bu tasarımda çakışıyor (array sarmalama derinliğe eklenmiyor, path'e de eklenmiyor) ama ayrı tutuldu — gelecekte bir üye kaynağı eklenirse (ör. property tabanlı ikinci şekil) ikisinin senkron kalacağı garanti değildir.

## Şema kısıt ekleme (Faz 130)

- **🚨 Bir attribute'un sayısal argümanını doğrulamadan üretilen şemaya yazma — JSON Schema kendi anahtarına şart koyar.** `[MinLength(-1)]`/`[MaxLength(-1)]` derlenen, geçerli C#'tır; ama `minLength`/`maxLength`/`minItems`/`maxItems` JSON Schema draft 2020-12'de `nonNegativeInteger` olmak ZORUNDADIR. İlk hâl bu değeri hiç kontrol etmeden `Append` ediyordu — geçersiz bir şema **sessizce** üretiliyordu, hiçbir tanı ötmüyordu. Bağımsız denetim (Faz 130) buldu: 🔴 seviyesinde, çünkü fazın kendi tasarım ilkesiyle ("şema sessizce yanlış üretilmez") doğrudan çelişiyordu. Ders: bir attribute argümanını okuyup şemaya yazan her kod yolunda, hedef JSON Schema anahtarının KENDİ kısıtını (burada: negatif olamaz) ayrıca doğrula — attribute'un kendi ctor'unun tip kontrolü yetmez.
- **Bir attribute'un "argüman yok" hâli (`MaxLengthAttribute()`, EF Core'un "deposun kendi azamisini kullan" anlamı) sessizce hiçbir etki üretmemeli.** İlk hâl bu durumu `break`'le atlıyordu — ne şemaya bir anahtar giriyordu ne `TRC0010` veriyordu; aynı dosyadaki `Range(Type,...)` (değer yok → `TRC0010`) durumuyla tutarsızdı (denetim 🟡). Kural: bir attribute kullanımı şemaya HİÇBİR ŞEY katmıyorsa, bu durum diğer "uyumsuz" durumlarla AYNI tanıya (`TRC0010`) yönlendirilir — "sessizce hiçbir şey yapma" bir üçüncü davranış olmamalı.

## `<example>` derleme kapısı (Faz 79)

- **🚨 XML `<example>` bloğunun "yalnız iki yer tutucu yeter" iddiasını ölçmeden kabul etme.** Plan `app`/`tracon`'in yettiğini söylüyordu; küçük bir tarama `builder` (`IHostApplicationBuilder`) adının 45 bloğun 36'sında bağlanmamış geçtiğini gösterdi. Bir sarmalama (prelüd/harness) yazmadan önce **her** serbest tanımlayıcıyı say, sonra hangilerinin 2+ blokta tekrarlandığını (kalıcı placeholder) ve hangilerinin tek bloğa özgü olduğunu (örneği kendi kendine yeterli yap) ayır.
- **`<example>` her zaman C# taşımaz.** `<code language="json">` bir yapılandırma parçasıdır (`appsettings.json` fragmanı), Roslyn'e verilemez — bare bir JSON property'dir (`"X": {...}`), `{`+içerik+`}` sarılıp `JsonDocument.Parse` ile doğrulanır. Blok çıkarıcı `<code>` etiketinin `language` özniteliğini okumalı; okumazsa o bloklar sessizce atlanır (görüldü: 49 etiketten 4'ü kayboldu, bağımsız sayım çapraz kontrolü yakaladı).
- **🚨 Statik bir sınıf generic tip argümanı olamaz (`CS0718`) — iki ayrı `<example>` birbirini bu şekilde kırabilir.** Bir tool konteyner sınıfını `internal static class` gösteren örnek, `AddToolsFrom<T>()` gibi generic bir çağıranın örneğiyle BİRLİKTE kopyalanınca derlenmez. Tool metodu `static` kalmalı (TRC0007), konteyner sınıf kalmamalı.
