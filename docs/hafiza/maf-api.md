# MAF API Tuzaklari

> Microsoft Agent Framework tipleri, harness, context provider, skill.
>
> Oturum/gecmis saglayicisi AYRI dosyadadir: [`maf-oturum.md`](maf-oturum.md).
>
> Bu dosya `MEMORY.md`'nin alan dosyasidir. Yalnizca bu alana
> dokunurken okunur. Yeni not buraya eklenir, `MEMORY.md`'ye degil.

- **Tool çağrıları içeriklerden okunur** (2026-08-02): MAF ayrı bir kanca sunmaz. `FunctionCallContent` ve `FunctionResultContent` (Microsoft.Extensions.AI) hem `AgentResponse.Messages[*].Contents` hem `AgentResponseUpdate.Contents` içinde gelir. `RunRecordingAgent.WriteContentsAsync` bunu kullanır.
- **Token kullanımı akışta `UsageContent` ile gelir** (2026-08-02): akışlı çalıştırmada `AgentResponse.Usage` yoktur; güncellemelerin içeriklerinden `UsageContent.Details` toplanır.
- **Chat Completions yolu AOT'ta tamamen temiz** (2026-08-02): `GetChatClient(model).AsIChatClient().AsBuilder().UseFunctionInvocation().UseOpenTelemetry().Build()` → `IsAotCompatible=true` ile **0 uyarı**. Sorun çıkaran tek yol Responses (deneysel API tanısı, AOT tanısı değil).
- **🚨 `AIFunctionArguments.Services` bu repoda BOŞTUR — eski "örnek tool'ları için var" notu 2026-08-05'te ÖLÇÜLEREK çürütüldü** (K-218): MAF `EmptyServiceProvider` geçirir; tool bağımlılıkları kurulum anında alınır. Ayrıntı: `docs/hafiza/cekirdek-calistirma.md`.
- **`IChatClient.GetService(typeof(X))` boru hattında gezinir** (2026-08-02): testler `FunctionInvokingChatClient`, `OpenTelemetryChatClient` ve `ChatClientMetadata` varlığını böyle doğruluyor. `ChatClientMetadata.ProviderUri` yapılandırılan `Endpoint`'i yansıtır — `OpenAIClient.Endpoint` `OPENAI001` işaretli olduğu için doğrulama bu yoldan yapılır.
- **MAF tip adları tahmin edilemez** (2026-08-02): plan `AgentRunResponse` varsaydı; gerçek ad **`AgentResponse`**. Aynı şekilde `AgentResponseUpdate`. MAF'ın .NET dokümanı çoğu sayfada "Coming Soon" diyor. Yeni tip kullanmadan önce `.agents/skills/maf-api-kesfi/scripts/dump-api.sh` çalıştır.
- **`MAAI001` derlemeyi kırar** (2026-08-02): `HarnessAgentOptions` üyeleri "evaluation purposes only" işaretli. `TreatWarningsAsErrors` ile hata olur. Bastırma gerekçeyle ve tek dosyada yapılır.
- **`Microsoft.Agents.AI.Mcp` diye bir paket YOK** (2026-08-02): NuGet'te arandı, yayınlanmamış. MCP yolu resmî `ModelContextProtocol.Core` 2.0.0'dır; `McpClientTool` doğrudan `AIFunction` türetir. Tam `ModelContextProtocol` paketi sunucu barındırma bağımlılıkları da çeker — `.Core` seçilmeli (K-057).
- **Harness'ta shell diye bir üye yok** (2026-08-02): reflection ile ölçüldü. `HarnessAgentOptions`'ta sunucuda yüzey açan üyeler `FileAccessStore` ve `BackgroundAgents`'tır; ikisi de yalnız değer atandığında etkinleşir, atanmazsa kapalıdır (K-062).
- **MCP tool adında nokta kullanılamaz** (2026-08-02): OpenAI ve uyumlu sağlayıcılar fonksiyon adlarında yalnız `[a-zA-Z0-9_-]` kabul eder; `sunucu.tool` çağrı anında reddedilir. Biçim `{sunucu}_{tool}`.
- **🚨 MAF'ın `AgentFileSkillScriptRunner` delegesi `IServiceProvider?` (nullable) bekler** (2026-08-02, Faz 11): Metodu `IServiceProvider` ile yazmak `CS8622` verir. İmza tahmin edilmemeli, `maf-api-kesfi` skill'i ile doğrulanmalı.
- **MAF hiçbir skill script'ini kendisi çalıştırmaz** (2026-08-02, Faz 11): `AgentFileSkillScriptRunner` yalnız bir çağrı noktasıdır. Sandbox, zaman aşımı, kaynak sınırı ve denetim izi tamamen AgentPrism'in sorumluluğundadır.
- **`AgentInlineSkill.AddScript(name, Delegate, ...)` AOT temizdir** (2026-08-02, Faz 11): Temiz derleme ile doğrulandı — `IL2026`/`IL3050` üretmez. `MA0002` için `#pragma` gerekir (ordinal karşılaştırıcı uyarısı).
- **🚨 MAF alt agent'i `options = null` ile cagirir** (2026-08-02, Faz 12): calistirma ayarlarini alt cagriya TASIMAZ; tasinmasi gereken her sey (`SessionId` gibi) acikca kopyalanmalidir. Ayrinti: [`HAFIZA-GECMISI.md`](../arsiv/HAFIZA-GECMISI.md).
- **`BackgroundAgentsProvider` alti tool acar ve cagri ESZAMANSIZDIR** (Faz 12): model sirasiyla `start_task` → `wait_for_first_completion` → `get_task_results` cagirir; tek turda tamamlanan bir sahte istemci gercek yolu atlar. `FunctionResultContent` tool ADI tasimaz — adlar `FunctionCallContent.CallId` ile eslestirilir.
- **`ToolApprovalRequestContent.ToolCall` bir `ToolCallContent`'tir, `FunctionCallContent` degil** (2026-08-02, Faz 12): tool adini almak icin `is FunctionCallContent call ? call.Name : request.ToolCall.CallId` kaliplamasi gerekir. `ToolCallContent` yalnizca `CallId` tasir.
- **`AIAgent.Id` sanal DEGIL; `Name` ve `Description` sanaldir** (2026-08-02, Faz 12): `AIAgent` turetirken `Id` override edilemez. Ayrica bes uye zorunludur: `RunCoreAsync`, `RunCoreStreamingAsync`, `CreateSessionCoreAsync`, `SerializeSessionCoreAsync`, `DeserializeSessionCoreAsync`.
- **`Microsoft.Agents.AI.Workflows.Declarative` ALINMADI** (2026-08-03, Faz 16, K-129): +19 geçişli paket ve `ChatClientAgent` desteği yok. Ölçümün tamamı: `docs/arsiv/FAZ-GECMISI.md`, "Faz 16".
- **`AgentSkillsProvider`, `CompactionProvider`, `BackgroundAgentsProvider` üçü de `AIContextProvider` türevidir**: `ChatClientAgentOptions.AIContextProviders` ile **düz agent'a** takılır. Harness zorunlu değil — K-053'ün harness kusuru bu yolla aşılır. Planlarda öncelikli yol budur.
- **`AgentSkillsProviderOptions.Disable*Approval` varsayılanı `false`** (yani onay **açık**): skill yükleme, kaynak okuma ve script çalıştırma faz 6 onay akışından zaten geçiyor. Bu bayraklar açılmamalı.
- **`AgentFileStore` dosya sistemi değil, soyutlamadır** (`ReadAsync`/`WriteAsync`/`SearchAsync`). Veritabanı destekli uygulama, agent'a "dosya" verirken diske hiç dokunmaz — K-062'nin `FileAccessStore` endişesini ortadan kaldırır.
- **`WorkflowVisualizer.ToMermaidString(workflow)` var**; `Workflow.ReflectEdges()/ReflectExecutors()/ReflectPorts()` graf çıkarımı veriyor. Tarayıcıda mermaid.js render etmek ~100 KB gzip — waterfall gibi elle SVG tercih edilecek.
- **`Microsoft.Agents.AI.Workflows` 130 public tip içerir** ve `Directory.Packages.props`'ta sürümü zaten sabit (1.16.0). Ayrı paket (`AgentPrism.Workflows`) olarak planlandı.
- **🚨 `Microsoft.Agents.AI.Workflows.Declarative` sürümü çekirdekten ayrı ilerliyor** (aramada en yüksek seri 1.13.x, çekirdek 1.16.0). Kullanmadan önce 1.16.0 uyumlu sürüm olduğunu doğrulayın; yoksa **almayın**.
- **🚨 `Microsoft.Agents.AI.Foundry` 1.5.0'da kalmış** — çekirdek 1.16.0. Faz 27'nin Foundry bölümü bu doğrulamaya bağlı.
- **🚨 `Microsoft.Agents.AI.Harness` + tool cagrisi = kirik akis** (K-053): harness akisli yolda tool turlarini dogru tasimaz; bu yuzden kullanilmaz. Vaka: [`HAFIZA-GECMISI.md`](../arsiv/HAFIZA-GECMISI.md).
- **`CompactionProvider` tokenizer parametresi almaz** (2026-08-02, Faz 13): MAF içeride kendi tokenizer'ını çözüyor; `Microsoft.ML.Tokenizers.Data.*` gibi bir veri paketi gerekmedi. `TiktokenTokenizer.CreateForModel(...)`'i DOĞRUDAN çağırmak bu veri paketini ister ama `CompactionProvider`'ın kendisi bunu istemiyor — ikisini karıştırma.
- **🚨 `LocalEvaluator.EvaluateAsync(...).DetailedItems` HER ZAMAN bostur** (Faz 18, K-142): reflection dokumu alani listeler ama deger `null` — yalniz uzak raporlama arka uclariyla dolar. Gercek sonuc `Items[0].Metrics` ve ust duzey `AllPassed`/`Passed`/`Failed` sayaclaridir.
- **Kendi `AIContextProvider` alt sinifini yazmak icin** (Faz 22, ornek `McpResourceContextProvider`): `: base()` ile hicbir filtre verme. Ezilecek metot `ProvideAIContextAsync(InvokingContext, CancellationToken = default)` — `= default` KOYULMAZSA `MA0061`. `AIContext { Instructions = "..." }` metni baglama enjekte eder. Ayrinti: [`HAFIZA-GECMISI.md`](../arsiv/HAFIZA-GECMISI.md).
- **`ModelContextProtocol.Core`'un `ClientOAuthOptions.RedirectUri`'si `required`dir; SDK yalniz Authorization Code (+PKCE) destekler** (Faz 22): kullanici etkilesimsiz bir akis (client_credentials) SDK'da YOKTUR — bir OAuth akisi planlarken "etkilesimsiz" secenegi baslamadan reddedilir. Dogrulama yontemi: [`arsiv/HAFIZA-GECMISI.md`](../arsiv/HAFIZA-GECMISI.md). **🚨 Netlestirme (2026-08-20):** `ModelContextProtocol.Authentication.IdentityAssertionGrantProvider` pinlenmis 2.0.0'da BILE var (2.2.0 ile reflection'la karsilastirildi, ayni semboller) — ama saf `client_credentials` DEGIL, "var olan bir IdP kimlik token'inin degisimi"dir (Token Exchange + JWT Bearer Grant). Kullanicida onceden dogrulanmis bir IdP kimligi VARSA etkilesimsiz akis mumkundur; kimliksiz saf servis-servis senaryosu icin hala secenek yok.
- **`McpErrorCode.ResourceNotFound = -32002` ve `.InvalidParams = -32602`, `McpProtocolException.ErrorCode` uzerinden** uzak "bulunamadi" hatasini genel baglanti hatasindan ayirt etmek icin kullanilir (2026-08-04, Faz 22). Diger her `Exception` (iptal disinda) baglanti hatasi sayilir.
- **🚨 `FunctionInvokingChatClient` tool gövdesinden çıkan istisnayı YUTAR** (Faz 47, K-309): istisna bir tool sonucuna çevrilir, döngü devam eder ve istek `200` döner. Döngüyü kesmek `CurrentContext.Terminate = true` ile olur; hata ise çalıştırma bittikten sonra, model çağrısının DIŞINDA fırlatılmalıdır. Ölçüm: [`arsiv/HAFIZA-GECMISI.md`](../arsiv/HAFIZA-GECMISI.md).
- **`ChatClientAgentRunOptions` `sealed`dir; `AgentPrismRunOptions` TUREYEMEZ** (Faz 47): calistirma basina `ChatOptions` bindirmesi ile AgentPrism ayarlari BIRLIKTE kullanilamaz. Bindirme **tanim duzeyinde** yapilir: `AgentDefinition` kopyalanir, yeniden derlenir (`CompiledAgentCache`'e GIRMEZ). Ayrinti: [`HAFIZA-GECMISI.md`](../arsiv/HAFIZA-GECMISI.md).
- **🚨 `AgentFileStore` surec genelinde TEKTIR; yalitim SARMALAYICIDADIR** (F-105/K-434): kiraci oneki YETMEZ, onek `{tenantId}/{agentName}`'dir. Agent adi derleme aninda bilinir → `AsyncLocal` gerekmez. Oturum boyutu bilerek yalitilmaz. Ayrinti: [`HAFIZA-GECMISI.md`](../arsiv/HAFIZA-GECMISI.md).
- **`AIFunctionFactory.CreateDeclaration(...)` dogrudan `AIFunctionDeclaration` dondurur** (2026-08-18, F-108/K-436): sahte govde kurup `.AsDeclarationOnly()` ile atmaya GEREK YOK. Olculdu: `is AIFunction` → `false`, `is AITool` → `true`; `FunctionInvokingChatClient` cagriyi `FunctionCallContent` olarak dondurur, **calistirmaz**. Olcum: [`HAFIZA-GECMISI.md`](../arsiv/HAFIZA-GECMISI.md).
- **🚨 Istemciden gelen `FunctionResultContent` `ChatRole.Tool` altinda gonderilir, `ChatRole.User` DEGIL** (F-108/K-437): `ToolApprovalResponseContent` (FARKLI tip) burada `ChatRole.User` altinda gider — ikisini karistirma. Gercek cagriyla olculdu. Olcum: [`HAFIZA-GECMISI.md`](../arsiv/HAFIZA-GECMISI.md).
- **🚨 `AIFunctionDeclaration` `.JsonSchema` tasir, ciplak `AITool` tabani TASIMAZ** (F-108/K-435): tool sozlesmesini genisletirken hedef `AITool` degil `AIFunctionDeclaration` olmali; sema `AIFunction : AIFunctionDeclaration : AITool` zincirinde yalniz orta katmanda tanimli. `AITool`'a genisletmek `CS1061` verir (olculdu). Ayrinti: [`HAFIZA-GECMISI.md`](../arsiv/HAFIZA-GECMISI.md).
- **🚨 `ApprovalRequiredAIFunction.InvokeCoreAsync`'i DOĞRUDAN çağırmak defer ETMEZ** (2026-08-19, K-490): kısa devre `GetService<T>()` üzerinden çalışır (`IChatClient.GetService`'in `AIFunction` eşleniği) — onu saran sarmalayıcı `GetService`'i EZMEMELİ.
- **🚨 Surum yukseltmesinde imzayi DERLEME degil DIFF kanitlar** (2026-08-21): derleme yalniz BUGUN cagirdigimiz uyeleri gorur. Iki tam dump **tip kapsamli** diff'lenir — duz `diff` ayni uye satirini BASKA bir tipe eklenince kaciririr, ve reflection **davranisi gormez** (ayni imza, farkli istisna tipi). Yontem: `maf-api-kesfi` § *Surum yukseltirken*. 1.16→1.18 / 10.8.3→10.9.0 / 2.0.0→2.2.0 sonucu ve eklenen uyeler: [`MAF-GENISLEME-NOKTALARI.md`](../MAF-GENISLEME-NOKTALARI.md) § *Surum damgasi*.
- **🚨 `AIFunctionFactory.Create(...)` HER donus degerini (string/null dahil) `JsonElement`'e kutular** (Faz 89): kaynak ureteci (`[AgentPrismTool]`) ham CLR degeri DOGRUDAN dondurur, bu ikisi FARKLI. Test/olcumde tip esitligi (`result.ShouldBe("x")`) `JsonElement != string` yuzunden yanlislikla patlar; gercek metin `element.GetRawText()` (String-kind icin tirnaksiz `.ToString()` degil).

- **🚨 `WorkflowHostingExtensions.WithCheckpointing(AIAgent, CheckpointManager)` duz bir agent'ta SESSIZ PASSTHROUGH'dur** (2026-09-05, MAF 1.20.0 ile geldi): adi "herhangi bir agent'a checkpoint takar" gibi durur ve `AIAgent` alip `AIAgent` dondurur. Probe ile olculdu: `ChatClientAgent` verildiginde `ReferenceEquals(giren, cikan)` **True**, takilan `ICheckpointStore<JsonElement>`'e `Create=0 Retrieve=0 Index=0`, `WorkflowSessionCheckpointRecovery` oturumdan cozulmez ve **istisna atilmaz**. Yalniz workflow kokenli agent'lar icin anlamlidir. Reddetmedigi icin derleme, test ve gozle inceleme uclu de yesil kalir — kaybi yalniz calisma aninda "checkpoint yok" olarak gorursun. Dump bu tuzagi GOSTERMEZ; imza mukemmel gorunur. Kayit: [`MAF-GENISLEME-NOKTALARI.md`](../MAF-GENISLEME-NOKTALARI.md) § *Surum damgasi*.
- **🚨 `background_agents_wait_for_first_completion` tek cagride `BackgroundAgentsProviderOptions.WaitTimeout` kadar BEKLEMEZ — kisa bir yoklama gibi davranir** (2026-09-05, Faz 144, gercek `AgentPrismTestHost` + `background_agents_*` tool akisiyla olculdu): `WaitTimeout=150ms` verilip gorev hala calisirken TEK bir `wait_for_first_completion` cagrisi aninda "Task 1 is still running." donduruyor — MAF'in kendi "wait" davranisina GUVENILMEZ. Cozum bu belirsizligi OLCEREK degil ORTADAN KALDIRARAK verildi: `ChildAgentInvoker` katman 2'yi (sert kesme) MAF'a birakmadan KENDISI de ayni `WaitTimeout` degeriyle bagimsiz bir `Task.WhenAny` yarisi calistirir (K-621 deseni); `BackgroundAgentsProviderOptions.WaitTimeout` yalniz MAF'in KENDI ic mekanizmasiyla ayni sayiyi paylassin diye kurulur, davranis GARANTISI ondan beklenmez. Gercek modelin `wait_for_first_completion`'i TEKRAR TEKRAR cagirdigini varsay — tek cagriya guvenen bir test/entegrasyon "hala calisiyor" metnini gercek zaman asimi sanabilir.
