# Kod Haritasi

> Hangi is hangi dosyada. Bir alana dokunmadan once buraya bak.
>
> Bu dosya `MEMORY.md`'nin alan dosyasidir. Yalnizca bu alana
> dokunurken okunur. Yeni not buraya eklenir, `MEMORY.md`'ye degil.
>
> Graph/Compaction/Scheduling/Eval/Quota/Webhook/MCP alt sistemlerinin dosya
> haritasi AYRI dosyadadir: [`altyapi-haritasi.md`](altyapi-haritasi.md).

- **Build yapılandırması dört katmanlı** (2026-08-01; `bench` eklendi: Faz 116): kök `Directory.Build.props` (dil, kalite kapıları) → `src|tests|samples|bench/Directory.Build.props` (katman ayarları). Alt katmanlar kökü `[MSBuild]::GetPathOfFileAbove(...)` ile açıkça import eder — otomatik değil.
- **Paket sürümleri tek yerde** (2026-08-01): `Directory.Packages.props`. Projeler `Version` yazmaz. MAF GA sürümleri `$(MicrosoftAgentsAIVersion)` değişkeninden gelir; `maf-api-kesfi` script'i de bu değişkeni okur.
- **AOT bayrağı** (2026-08-01): `src/Directory.Build.props` içindeki `TraconAotCompatible`. Paket bazlı kapatmak için csproj'da `<TraconAotCompatible>false</TraconAotCompatible>`.
- **Public API takip anahtarı** (2026-08-01): kök `Directory.Build.props` içindeki `EnablePublicApiTracking`. `false` iken RS00xx tanıları `NoWarn` ile susturulur. Faz 7'de `true` yapılır.
- **Paket doğrulama kapısı** (2026-08-01): `Directory.Build.targets` içindeki `EnablePackageValidationGate`. Faz 7'de açılır; taban sürüm yayınlanmadan açılamaz.
- **Servis kayıtları tek dosyada** (2026-08-02): `src/Tracon.Core/TraconServiceCollectionExtensions.cs`. Hangi servisin nasıl kaydedildiğini görmek için önce buraya bak.
- **Harness kullanımı tek dosyada** (2026-08-02, taşındı: Faz 106): `AgentDefinitionCompiler.Agents.cs` → `CompileHarnessAgent`. MAF harness API'si değişirse yalnız orası etkilenir.
- **Tüm SQL metinleri tek dosyada** (2026-08-02): `src/Tracon.PostgreSql/Internal/SqlQueries.cs`. Şema adına göre bir kez kurulur, alan olarak saklanır.
- **`await using` + `ConfigureAwait` kalıbı tek dosyada** (2026-08-02): `PostgreSql/Internal/NpgsqlHelpers.cs`. `Store`'lar komutu kurar, yardımcı çalıştırır ve bırakır.
- **Store davranış sözleşmesi tek yerde** (2026-08-02): `tests/Tracon.PostgreSql.IntegrationTests/Contracts/`. Soyut sınıflar hem `InMemory*` hem `Postgres*` üzerinde koşar. `Store` davranışı değişecekse önce buraya bak.
- **Oturum kimliği damgası** (2026-08-02): `Core/Sessions/AgentSessionIdentity.cs` → `AgentSession.StateBag["Tracon.SessionId"]`. `RunRecordingAgent` ve `AgentSessionManager` buradan okur.
- **Deneysel API bastırmaları** (2026-08-02, `AgentDefinitionCompiler` kısmı güncellendi: Faz 106): `AgentDefinitionCompiler`'da bastırma artık üç dosyaya bölünmüştür — `.Agents.cs` (`CompileHarnessAgent`, `CreateBackgroundAgentsProvider`), `.Compaction.cs`, `.Memory.cs` (hepsi `MAAI001`) — ve `OpenAIChatClientFactory.CreateInnerChatClient` (`OPENAI001`+`MAAI001`). Repo genelinde `MAAI001` başka dosyalarda da vardır (`grep -rl MAAI001 src/` ile bul); bu not yalnız `AgentDefinitionCompiler` ailesini haritalar.
- **Tool yansıması tek dosyada** (2026-08-02): `Core/Tools/ToolMethodScanner.cs`. `AddToolsFrom` buraya delege eder; `Tracon.Core`'daki tek yansıma noktası.
- **HTTP uçları dört dosyada gruplu** (2026-08-02): `AspNetCore/Endpoints/` → `MetaEndpoints` (kimlik doğrulaması yok), `AgentEndpoints`, `SessionEndpoints`, `RunEndpoints`, `CatalogEndpoints` (tools+models+stats). OpenAI uyumlu uçlar ayrı: `AspNetCore/OpenAICompat/`.
- **`MapTracon` iki `MapGroup` kurar** (2026-08-02): meta grubu filtresiz + `AllowAnonymous()`, korumalı grup `TraconEndpointFilter` + opsiyonel policy. Dönen builder **yalnız korumalı grubu** temsil eder (K-042).
- **Çalıştırma özeti SQL'i iki sonuç kümesi döndürür** (2026-08-02): `SqlQueries.SelectRunStatistics` — önce toplam, sonra agent kırılımı. `reader.NextResultAsync()` ile okunur.
- **OpenAI hata biçimi tek yerde** (2026-08-02): `OpenAICompatSupport.Error(...)`. Agent seçimi ve kiracı sahiplik denetimi de aynı dosyada.
- **Sohbet geçmişi okuma tek dosyada** (2026-08-02): `AspNetCore/Internal/ChatHistoryReader.cs`. Hem `/api/sessions/{id}` hem `/v1/conversations/{id}/items` buradan okur; `MAAI001` bastırması da burada.
- **Konuşma kimliği = oturum kimliği** (2026-08-02): `/v1/conversations` ayrı bir `store` kullanmaz, `ISessionStore` üzerine kuruludur (K-043).
- **Üç dekoratör, sıra `Order` ile** (2026-08-02): kayıt 0 (en dışta) → telemetri 10 → tool onayı 20. `CompositeAgentCatalog` `OrderByDescending` ile sıralar, döngüde sarar; **küçük Order dışta kalır**. Yeni dekoratör eklerken bu ters mantığa dikkat.
- **🚨 `AgentDecoratorPipeline.Apply`'ın DÖRT tüketicisi var** (2026-08-28, Faz 122): `RunReplayService.Decorate`, `RunContinuationJobHandler.Decorate`, `EvalJobHandler` ve `AgentEndpoints.cs`'in parametreli-run yolu (`IAgentCatalog`'u atlayıp `CompileParameterizedAsync` kullanan dal) — dördü de `CompositeAgentCatalog`'un KENDİ decorate döngülerinden AYRI, paylaşılan statik yardımcıyı çağırır. Decorator zincirine dokunan bir değişiklik (normalizasyon, loglama, sıralama) bu dördünü de taramalı: `grep -rn "AgentDecoratorPipeline.Apply" src/`.
- **Onay sarmalaması `ToolRegistry` içinde** (2026-08-02): `RequiresApproval` işaretli tool `ApprovalRequiredAIFunction` ile orada sarılır. Derleyicide değil — defter, tool erişiminin tek kapısıdır; başka kod yolu sarmalamayı atlayamaz.
- **Span kimliği `TraceSpanIdentity.ForSpan`** (2026-08-02): `SHA-256(traceId + ":" + spanId)` ilk 16 baytı. Testler bu türetmeyi birebir tekrarlar (`TraceStoreContract.DeriveId`) — uretim kodu değişirse test de değişmeli.
- **MCP tool önbelleği kiracı bazlı** (2026-08-02): `McpToolCatalog._byTenant`, arka planda `McpDiscoveryService` tarafından tazelenir; `McpToolRegistry` `ITenantContext` ile dilim seçer. Bağlantılar tanım parmak izi değişmedikçe ayakta tutulur.
- **Kiracı çözümleme tek dosyada** (2026-08-02): `AspNetCore/Tenancy/HttpTenantContext.cs`. Singleton'dır ve `IHttpContextAccessor` kullanır — scoped olsaydı `ITenantContext` enjekte eden singleton `store`'lar yakalanmış bağımlılık hatası verirdi.
- **Sağlayıcı paketlerinin iç yerleşimi** (2026-08-05, Faz 27): dosya düzeyinde harita `docs/hafiza/openai-saglayici.md` içindedir — dört sağlayıcı paketi (`OpenAI`, `Anthropic`, `Google`, `Azure`) aynı şekli paylaşır. En kısa örnek `src/Tracon.Azure` (dokuz dosya; ayar sunmadığı için `*ProviderSettingsChatClient` dekoratörü yoktur, K-211).
- **Model sağlığı ve devre kesici üç dosyada** (2026-08-02, Faz 8): `Core/Models/ModelProviderHealthCache.cs` (TTL onbellek + devre durumu bindirmesi), `Core/Models/ModelProviderCircuitBreaker.cs` (CAS tabanlı üç durumlu makine, kilitsiz), `Core/Models/CircuitBreakingChatClient.cs` (`DelegatingChatClient` dekoratörü). Entegrasyon noktası `ModelProviderRegistry.CreateChatClient` — sağlayıcı paketlerinin içine girmez.
- **Denetim izi `Core/Audit/` altında altı dosyada** (Faz 9): `AuditActorContext` (AsyncLocal köprüsü), `AmbientAuditActorResolver`, `AuditSecretFilter`, `AuditRecorder` (tek yazma yolu, hata yutar), `Auditing*Store` decorator'ları. Kayıt her `store`'u tek tek sararak yapılır — genel bir `Decorate<T>` yardımcısı yoktur (K-077).
- **Rol politikaları `Tracon.AspNetCore/Security/` altında** (Faz 9): `TraconPolicies` (sabit adlar), `TraconRolePolicies` (Map anında `IAuthorizationPolicyProvider` ile çözülür), `RequireRole` (policy adı `null`'sa hiçbir şey eklemez). `MapTracon` bunu bir kez çözüp her `*Endpoints.Map(...)` çağrısına geçirir.
- **Skill zinciri `Core/Skills/` altında** (2026-08-02, Faz 10): `AgentSkillCatalog` kod kaydını tenant store'un önünde çözer; `TraconSkillsSource` tanımları `AgentInlineSkill`e çevirir. Cache anahtarı skill parmak izi içerir; edit sonrası agent cache'i bu yüzden yenilenir.
- **Skill kalıcılığı migration 0003'tedir** (2026-08-02, Faz 10): `agent_skills` + cascade bağlı `agent_skill_resources`; PostgreSQL store explicit `tenantId` parametresi alır. `UsePostgreSql()` kaydı `PostgresAgentSkillStore` ile değiştirir.
- **`run scope` tek yerde** (2026-08-02, Faz 12): `Core/Recording/TraconRunContext.cs` → `AgentRunScope` (RunId, RootRunId, Depth, AgentName, TenantId, Budget, Writer). `RunRecordingAgent` yayimlar; skill script calistiricisi ve alt agent sarmalayicisi okur. `SetCurrentRunId` KALDIRILDI, yerine `SetCurrent(scope)`.
- **Agac toplamlari SQL'de, okumada hesaplanir** (Faz 12): `SqlQueries.treeJoin` iki `LEFT JOIN LATERAL`; `RunRecord.ChildRunCount`/`TreeUsage` oradan gelir, saklanmaz. 🚨 Sutun indeksleri `ReadRun` icinde sabittir — sorguya sutun eklerken ikisini birlikte guncelleyin. Ayrinti: [`arsiv/HAFIZA-GECMISI.md`](../arsiv/HAFIZA-GECMISI.md).
- **Workflow yurutmesi ve graf/HITL dosyalari → [`workflows.md`](workflows.md)** (2026-08-15, Faz 58.0): `Tracon.Workflows/Internal/` altindaki on iki dosyanin haritasi o dosyanin "Nerede yasiyor" bolumundedir.
- **Ekler `Core/Attachments/` + `PostgreSql/Stores/Postgres{Attachment,AgentFile}Store.cs`** (Faz 14): `AttachmentTypeGuard`, `AttachmentUriReference`, `AttachmentResolvingChatClient` (devre kesicinin **icine** sarili). `PostgresAgentFileStore` agent adini ambient `scope`'tan okur (K-114). Ayrinti: [`arsiv/HAFIZA-GECMISI.md`](../arsiv/HAFIZA-GECMISI.md).
- **Maliyet `Core/Models/RunPricingResolver.cs` + `Core/Recording/RunCostRecalculationService.cs`** (Faz 20): `RunRecordingAgent.CompleteAsync` → `Resolve(...)` → `RunEventWriter.CompleteAsync(..., cost)` → `store`; saglayici `runs`'a yazilmaz (K-154). 🚨 `SqlQueries`'te yeni sutunlar HER ZAMAN sona eklenir, sabit-indeks okuyucular (`ReadRun`) renumber EDILMEZ.
- **İçerik filtresi tespiti Core'da tek dosyada** (2026-08-05, Faz 26): `Core/Models/ContentFilterDetectingChatClient.cs`. `ModelProviderRegistry.CreateChatClient` sarmalama sırasını belirler: gerçek istemci → ek çözme → devre kesici → **içerik filtresi (en dışta)**.
- **Ses ve konusma katmani → [`ses-ve-konusma.md`](ses-ve-konusma.md)** (2026-08-15, Faz 58.0): `Tracon.Voice/` (tool'lar, ElevenLabs istemcisi, token disi olcum) ve `Core/Voice/` (gercek zamanli konusma boru hatti) haritasi o dosyadadir.
- **Calistirma iptali `Core/Recording/RunCancellationRegistry.cs`** (2026-08-06, Faz 32): bellek ici, her zaman kayitli (K-244). Yazanlar: `RunRecordingAgent`, `WorkflowRunner.ExecuteAsync`. Kok kayittaki `TryCancel` ayni `RootRunId`'yi kaskad eder (K-243). Uc: `RunEndpoints.CancelRunAsync`.
- **Tanim dogrulama `Core/Compilation/AgentDefinitionValidator.cs`** (2026-08-06, Faz 34): bagimsiz denetimler + temizse `Compile()` dry-run. Dongu `Graph/AgentCallGraph.ValidateDetailed`'dandir (K-252). 🚨 MCP ulasilamazsa `unknown_tool` YAZILMAZ, dry-run atlanir. Uc: `AgentEndpoints.ValidateAgentAsync`.
