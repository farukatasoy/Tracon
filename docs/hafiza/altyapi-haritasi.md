# Altyapı Alt Sistemleri Haritası

> Graph/Compaction/Scheduling/Eval/Quota/Webhook/MCP alt sistemlerinin dosya
> haritasi. Genel kod haritasi icin: [`kod-haritasi.md`](kod-haritasi.md).
>
> Bu dosya `MEMORY.md`'nin alan dosyasidir. Yalnizca bu alana dokunurken okunur.
> Faz 156'da `kod-haritasi.md`'den ayrildi: dosya %5 bosluga dusmustu.

- **Cagri grafigi `Core/Graph/` altinda uc dosyada** (2026-08-02, Faz 12): `AgentCallGraph` (kaydetme anindaki statik dongu denetimi, acik yigin), `CallableAgentResolver` (katalogu ILK KULLANIMDA cozer — DI dairesini boyle kirar), `ChildAgentInvoker` (`AIAgent` turevi, `DelegatingAIAgent` degil; ic agent gec cozulur). Cagri sinirlarini degistirecekseniz once `ChildAgentInvoker.Refuse` metoduna bakin — dort kontrolun tamami orada.
- **Sikistirma/bellek `Core/Compilation/` altinda** (2026-08-02, Faz 13; dosya konumu güncellendi: Faz 106): `ObservedCompactionStrategy` (`HistoryCompacted` olayi, her strateji turu icin tekduze), `CompactionUsageTrackingChatClient` (`compact_history` span'i + ozet token'larini `ExtraUsage`'a yazar), `AgentDefinitionCompiler.Compaction.cs` → `BuildCompactionStrategy`, `AgentDefinitionCompiler.Memory.cs` → `CreateMemoryProviders` (dogrulama + kurulum). `TraconRunContext.ExtraUsage` (`CompactionUsageAccumulator`) `RunRecordingAgent.CompleteAsync`'te `MergeUsage` ile nihai `RunUsage`'a katilir.
- **Is kuyrugu `Abstractions/Scheduling/` + `Core/Scheduling/` + `PostgreSql/Stores/PostgresJob{Store,ScheduleStore}.cs`** (2026-08-03, Faz 17): `IJobStore` (kuyruk), `IJobScheduleStore` (tanim) ayrimi workflow definition/checkpoint ayrimiyla aynidir. `JobWorkerBackgroundService` tek `PeriodicTimer` dongusunde hem sirasi gelen zamanlamalari `jobs`'a dusurur hem `LeaseAsync` ile is kiralayip `IJobHandler` sozlesmesine dagitir. Migration 0008.
- **Harness dongusu (Faz 151) `Core/Compilation/`de uc dosyada**: `LoopEvaluatorRegistry` (bildirimsel `LoopSettings` → MAF dongusu; dort yerlesik `kind`, bilinmeyen `kind` `TraconCompilationException`), `RecordingLoopEvaluator` (MAF'a verilen TEK composite evaluator + `LoopIterationCompletedEventPayload`), `TraconLoopEvaluatorRegistration` (public kayit tipi, `TraconEvalCheckRegistration` ikizi — ikiz Faz 182'de `internal` oldu; Loop olani public `AgentDefinitionCompiler` ctor imzasinda gectigi icin public kalir). Baglama noktasi `AgentDefinitionCompiler.Agents.cs` `CompileHarnessAgent` sonu.
- **Eval (Faz 18) `Core/Evaluation/`de uc dosyada** (2026-08-03): `InMemoryEvalStore`, `EvalCheckRegistry` (JSON `checks` → `EvalCheck[]`, bilinmeyen tur `TraconException`), `EvalJobHandler` (`JobHandlerKeys.Eval`, Faz 17'nin AYNI `jobs` kuyrugunu kullanir — ayri kuyruk yok). Vaka kimligi `job_items.input`'ta metin (Guid.ToString) olarak tasinir.
- **Kota `Core/Quotas/`de dort dosyada** (2026-08-03, Faz 21): `QuotaPeriodCalculator` (saf mantik — gun/ay siniri, saat dilimi, yaz saati gecisi), `QuotaEnforcer` (denetim + tuketim + esik olaylari), `InMemoryQuotaStore`, `TraconQuotaOptions`. Akis: `QuotaGate.CheckAsync` (HTTP, calistirma ONCESI) → `QuotaEnforcer.CheckAsync`; `RunRecordingAgent.CompleteAsync` (kok calistirma, SONRASI) → `QuotaEnforcer.RecordAsync`. Hiz siniri **ayri** bir mekanizmadir: `AspNetCore/RateLimiting/TraconRateLimitFilter.cs`, bellekte, `PartitionedRateLimiter` uzerine ince katman (K-158).
- **Webhook `Core/Webhooks/` altinda yedi dosyada** (2026-08-03, Faz 21): `WebhookUrlValidator` (🚨 SSRF karari — `IsAllowedTarget` TEK dogruluk noktasi), `WebhookSocketGuard` (baglanti geri cagrisi; TOCTOU yok), `WebhookHttpClient` (`IHttpClientFactory` KULLANILMAZ, K-164), `WebhookSigner`, `WebhookPublisher`, `WebhookDeliveryJobHandler` (Faz 17'nin AYNI kuyrugu), `InMemoryWebhookStore`.
- **Is kuyrugu geri adimli bekleme tasir** (2026-08-03, Faz 21): `IJobStore.ReleaseForRetryAsync(jobId, error, retryAfter, ct)` gecikmeyi `jobs.scheduled_for`'a yazar; `LeaseJob` zaten `scheduled_for <= now` suzer. Isleyici gecikmeyi `JobRetryException { RetryAfter = ... }` ile talep eder. Is basina deneme siniri `JobRecord.MaxAttempts` (NULL = genel ayar). Ikinci bir kuyruk yoktur (K-160).
- **MCP prompts/resources/OAuth `Tracon.Mcp/`de** (2026-08-04, Faz 22): `McpConnection` (kesif+onbellek+abonelik), `McpTransportFactory` (arka plan OAuth denemesi hemen basarisiz, K-171), `McpResourceContextProvider` (Mod A), `McpOAuthAuthorizationCoordinator` (TCS koprusu, K-170/171). Soyutlamalar `Abstractions/Mcp/`de (K-174). Detay: `docs/arsiv/fazlar/22-MCP-DERINLESMESI.md`.
- **🚨 Bir MCP sunucusunun başlıklarını yalnız `McpHeaderBuilder` okur** (2026-09-24, Faz 190, K-868): arka plan bağlantısı (`McpTransportFactory`) ve OAuth akışı (`McpOAuthAuthorizationCoordinator`) aynı yardımcıyı çağırır; `server.Headers` başka yerde yalnız `McpConnection.ComputeFingerprint`'te geçer (`McpHeaderBuilderTests.Only_the_builder_and_the_fingerprint_read_a_servers_headers`). SDK ölçümü (`ModelContextProtocol.Core` 2.2.0, ilspy): `ClientOAuthProvider` Bearer'ı yalnız istekte `Authorization` YOKSA yazar — düz `Authorization` ilk istekte OAuth token'ını ezer, `401` sonrası tekrar Bearer'la gider; `CopyAdditionalHeaders` eklenemeyen başlıkta (içerik başlığı, CR/LF) **değeri** exception mesajına koyar ve bağlantı yolu onu loglar — yardımcı böyle başlığı `CanSend` ile önceden düşürür. Webhook tarafında eş iş `WebhookDeliveryJobHandler.AddCredentialHeaders`'tadır (anahtar kaynaklı başlıklar önce, `MaxExtraHeaders` yalnız düz başlığı keser).
