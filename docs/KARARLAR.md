# KARARLAR.md — Canlı Karar Defteri

> Bu dosya, projede **bilinçli olarak alınmış ve gerekçelendirilmiş kararların** kaydıdır. Amacı: gelecekteki bir oturumun (insan veya AI) daha önce kanıtla reddedilmiş bir işi yeniden önermesini veya kapatılmış bir tartışmayı yeniden açmasını engellemek.
>
> **Kullanım kuralı:** Buradaki bir kalemi öneri olarak gündeme getirmeden önce "yeniden açılma koşulu" sütununa bak. Koşul gerçekleşmediyse önerme. Koşul gerçekleştiyse, bu dosyayı güncelleyerek kalemi yeniden aç.

---

## 1. Kanıt Olmadan Yeniden Açılmayacak İşler

Kaynak: Faz 0 altyapı çalışması (2026-08-01). Ayrıntı: [`00-ALTYAPI.md`](arsiv/fazlar/00-ALTYAPI.md), [`MIMARI.md`](MIMARI.md).

| Karar | Tarih | Gerekçe | Yeniden açılma koşulu |
|---|---|---|---|
| **Arayüz Blazor ile yazılmadı** (WebAssembly veya Server) *(kullanıcı kararı)* | 2026-08-01 | WebAssembly ilk yükleme boyutu (~2 MB+) "lite arayüz" hedefiyle çelişiyor. Blazor Server kalıcı SignalR bağlantısı ister; bir NuGet paketi olarak tüketicinin barındırma modelini kısıtlar. React+Vite çıktısı gömülü statik varlık olarak sıfır tüketici bağımlılığı üretir. | Blazor WASM AOT çıktısı gzip <300 KB'ye inerse yeniden ölçülür |
| **EF Core kullanılmadı** *(kullanıcı kararı)* | 2026-08-01 | Kütüphane, tüketiciye EF Core sürüm kısıtı dayatmamalı; EF Core sürüm çakışması .NET'te en sık bağımlılık sorunlarından biri. Ayrıca EF migration'larını tüketicinin projesinden çalıştırmak gerekir — kütüphane kendi şemasını kendi yönetemez. Ham Npgsql + gömülü SQL bu iki sorunu da ortadan kaldırır. | AgentPrism bir kütüphane olmaktan çıkıp bağımsız bir servise dönüşürse |
| **`netstandard2.0` ve `net472` hedeflenmedi** | 2026-08-01 | AgentPrism'in çalışma yeri modern ASP.NET Core. Bu iki hedef `IAsyncEnumerable`, `System.Text.Json` kaynak üreteçleri ve minimal API yapılarında ciddi ek yük getirir; karşılığında AgentPrism senaryosunda kullanıcı üretmez. | .NET Framework üzerinde somut bir kullanıcı talebi gelirse |
| **Tek paket (monolitik) paketleme yapılmadı** *(kullanıcı kararı)* | 2026-08-01 | PostgreSQL kullanmayan tüketici `Npgsql`'i çekmemeli. Modüler yapıda `AgentPrism.SqlServer` veya `AgentPrism.Anthropic` eklemek breaking change olmaz. | — |
| **Geçişli sabitleme (`CentralPackageTransitivePinningEnabled`) açılmadı** | 2026-08-01 | Ölçüldü: bayrak açıkken `AgentPrism.PostgreSql` üretilen `.nuspec` içinde **13** doğrudan bağımlılık bildiriyordu (`OpenAI`, `OpenTelemetry.Api` gibi hiç referans verilmemişler dahil). Kapalıyken **2**. Uygulamalarda doğru, kütüphanelerde tüketicinin bağımlılık grafiğini kirletir. | Bir CVE nedeniyle geçişli sürüm zorlanması gerekirse — o zaman bilinçli tek bir `PackageReference` eklenir, bayrak yine açılmaz |
| **Arayüzden tool kodu yazma özelliği eklenmedi** | 2026-08-01 | Arayüze erişen herkes sunucuda kod çalıştırabilirdi. Arayüz yalnız kodda kayıtlı tool'lardan seçim yaptırır. Bu sınır güvenlik sınırıdır. | Yeniden açılmaz. MCP tool'ları (Faz 6) ayrı policy + zorunlu onay + denetim izi ile bilinçli istisnadır. |
| **MAF tipleri sarmalanmadı** | 2026-08-01 | `AIAgent`, `AgentSession`, `ChatMessage`, `AIFunction` doğrudan kullanılır. Paralel tip hiyerarşisi her MAF sürümünde bakım borcu üretir ve tüketiciyi MAF ekosisteminden koparır. AgentPrism bir kontrol düzlemidir, soyutlama katmanı değil. | MAF kırıcı bir API değişikliği yapar ve sarmalama tek çare kalırsa |
| **Trim/AOT analyzer'ları kök seviyede açılmadı** | 2026-08-01 | Denendi ve örnek uygulamayı kırdı: `app.MapGet(pattern, delegate)` `IL2026` + `IL3050` üretiyor, `TreatWarningsAsErrors` build'i kırıyor. Minimal API yönlendirmesi doğası gereği reflection kullanır. | ASP.NET Core minimal API tam AOT uyumlu hale gelirse |
| **Katalog MAF'ın `AddAIAgent` kayıtlarını doğrudan okumadı** | 2026-08-02 | Plan bunu öngörüyordu. Ancak `AddAIAgent`, ön sürüm durumundaki `Microsoft.Agents.AI.Hosting` paketindedir ve K-008 gereği `AgentPrism.Core` o pakete bağımlı olamaz. Yerine `IAgentSource` soyutlaması geldi (K-019). | MAF `Hosting` GA olursa bile soyutlama korunur — üç kaynağı (kod, MAF, veritabanı) birleştirmek zaten gerekliydi |
| **`AddToolsFrom<T>()` (attribute taramalı tool kaydı) Faz 1'de yapılmadı** | 2026-08-02 | Yansıma gerektirir ve `AgentPrism.Core` AOT uyumlu işaretlidir. Faz 3'e ertelendi; eklendiğinde `[RequiresUnreferencedCode]` ile işaretlenecek. | **(yeniden açıldı: 2026-08-02, Faz 3'te eklendi)** Bkz. K-033 |
| **Yerleşik OpenAI model listesi kodda tutulmadı** | 2026-08-02 | Ölçüldü: Faz 3'te bilgiye dayanarak yazılan liste (`gpt-5.1`, `gpt-5`, `gpt-4.1`, `gpt-4o`, `o3`, `o4-mini`) gerçek bir hesabın erişebildiği modellerin **hiçbirini** içermiyordu; `gpt-4.1-mini` çağrısı `HTTP 403 model_not_found` döndü. Gerçek liste `gpt-5.4-mini`, `gpt-5.6-luna`, `gpt-5.6-terra` idi. Bir NuGet paketi model listesini güncel tutamaz. Bkz. K-032. | OpenAI kararlı ve makinece okunabilir bir yetenek kataloğu yayınlarsa (`/v1/models` bugün yalnız `id` döner) |
| **Responses API'de sunucu tarafı konuşma durumu kullanılmadı** | 2026-08-02 | Ölçüldü: açıkken `ChatClientAgent` çalışma anında `InvalidOperationException: Only ConversationId or ChatHistoryProvider may be used, but not both` atıyor. `UsePostgreSql()` her agent'a `ChatHistoryProvider` bağlar. Ayrıca sunucu tarafı durum, AgentPrism'in denetim izi, kiracı yalıtımı ve replay vaadini bozardı. Bkz. K-030. | MAF iki mekanizmanın birlikte çalışmasına izin verirse ve sunucu tarafı durumun kiracı sahipliği doğrulanabilirse |
| **`ValidateDataAnnotations()` kullanılmadı** | 2026-08-02 | `IL2026` üretir ve AOT vaadini bozar. Yerine elle yazılmış `AgentPrismOptionsValidator` geldi. | — |
| **Çalıştırma kaydı MAF middleware'i olarak yazılmadı** | 2026-08-01 | MAF middleware zinciri agent'a özgüdür ve `HarnessAgent` kendi iç dekoratörlerini ekler. Dış `DelegatingAIAgent` sarmalayıcısı harness dahil her agent tipinde aynı çalışır. | MAF agent tipinden bağımsız global bir middleware noktası sunarsa |
| **EF Core migration'ları yerine gömülü SQL — uygulandı ve doğrulandı** | 2026-08-02 | Faz 2'de ölçüldü: `AgentPrism.PostgreSql` üretilen nuspec'inde **2** doğrudan bağımlılık taşıyor (`AgentPrism.Core`, `Npgsql`). EF Core ile bu sayı en az 5 olurdu ve tüketiciye EF sürüm kısıtı dayatırdı. | Bkz. bölüm 1, "EF Core kullanılmadı" |
| **`tenant_id` sütunlarına yabancı anahtar konmadı** | 2026-08-02 | `tenants` tablosuna FK, kaydı olmayan bir kiracı için çalışma anında beklenmedik hata üretirdi. Varsayılan kiracı satırı `MigrationHostedService` tarafından açılışta eklenir; yalıtım `ITenantContext` üzerinden sağlanır ve `TenantIsolationTests` ile korunur. **(yeniden değerlendirildi: 2026-08-02, Faz 6)** Kiracı yönetimi (`ITenantStore`, `/api/tenants`) geldi — yeniden açılma koşulu gerçekleşti — ancak karar **korundu**: kiracı kaydı bilerek **isteğe bağlıdır**. `ITenantContext` bir claim'den kayıtsız bir kiracı döndürebilir ve o kiracının çalıştırması FK yüzünden başarısız olmamalıdır. Kayıt yalnızca arayüzdeki kiracı seçiciye ad sağlar. | Kiracı kaydı **zorunlu** hâle gelirse (ör. kota veya faturalandırma kiracı satırı gerektirirse) |
| **`tool_invocations` tablosu Faz 2'de doldurulmadı** | 2026-08-02 | Şema kuruldu ancak yazan yok. `duration_ms` alanı `ToolInvoking`/`ToolInvoked` olay çiftinin korelasyonunu gerektirir; bu, tek yazıcılı `RunEventWriter` tasarımına ek durum sokar. Faz 6'da gözlemlenebilirlik ile birlikte yazılır. | Faz 6 |
| **MAF'ın `MapOpenAIResponses()` / `MapOpenAIConversations()` uçları kullanılmadı** *(kullanıcı kararı)* | 2026-08-02 | Ölçüldü: `AddOpenAIResponses()` depolamayı `IConversationStorage`, `IAgentConversationIndex`, `IResponsesService`, `IResponseExecutor` üzerinden kaydediyor ve dördü de `Microsoft.Agents.AI.Hosting.OpenAI` içinde **internal** (`svcPublic=False`); imzalarındaki model tipleri de öyle. `InternalsVisibleTo` yalnız Microsoft'un test derlemesine açık. Tüketici derleme bu tipleri adlandıramaz, dolayısıyla `TryAdd` sırası ne olursa olsun üzerine yazamaz. O uçları kullanmak kalıcılık, kiracı yalıtımı, denetim izi ve replay vaatlerinin tümünü sessizce kaybettirirdi. Bkz. K-036. | Paket bu dört arayüzü public yaparsa yeniden ölçülür |
| **`/v1/conversations` ucu Faz 4'te yazılmadı** *(kullanıcı kararı)* | 2026-08-02 | MAF'ın Conversations ucu yukarıdaki internal depoyu kullanır; kendimiz yazmak elle üretilmiş OpenAI biçimli DTO'lar gerektirir. Faz 4 kapsamı `/v1/responses` + `/v1/chat/completions` ile sınırlandı. **(yeniden açıldı: 2026-08-02, ölçüm)** Stok Python SDK'sı ile ölçüldü: `conversations.create()`, `conversations.retrieve()` ve `conversations.items.list()` **404** veriyordu — yani OpenAI'in belgelenmiş akışı kırıktı ve çalışan tek yol istemcinin konuşma kimliğini kendisi uydurmasıydı (gerçek OpenAI'de olmayan bir davranış). S1'deki engel burada geçerli değildi: kendi uçlarımızı kendi oturum soyutlamamız üzerine yazmak serbesttir. Uçlar eklendi; bkz. K-043. | — |
| **`/api/stats` maliyet döndürmüyor** | 2026-08-02 | `runs` tablosu **model adı taşımaz**; maliyet model kataloğundaki fiyatla çarpım gerektirir. Sütun eklemek yeni bir migration ister ve maliyet raporlaması Faz 6'nın (gözlemlenebilirlik) kapsamındadır. Token toplamları, durum sayıları ve hata oranı döndürülüyor. | Faz 6'da `runs.model` sütunu eklendiğinde |
| **OpenAPI paketi `AgentPrism.AspNetCore` bağımlılığı yapılmadı** | 2026-08-02 | Uçlar paylaşılan çerçeveden gelen üstveriyi taşır; tüketici `AddOpenApi()` çağırdığında belge kendiliğinden oluşur. Ölçüldü: nuspec **3 doğrudan bağımlılık** taşıyor. Ayrıca `Microsoft.AspNetCore.OpenApi` 10.0.10, CVE'li `Microsoft.OpenApi` 2.0.0 çekiyor (NU1903) — bunu tüketiciye dayatmak kabul edilemezdi. Bkz. K-039. | — |
| **`run_events` gerçekten partition'lanmadı** | 2026-08-02 | `created_at` sütunu partition anahtarı adayı olarak eklendi, ancak birincil anahtar K-014 gereği `(run_id, seq)`. PostgreSQL partition'lı tabloda PK'nın partition anahtarını içermesini zorunlu kılar; bu değişiklik yeni bir migration ister ve hacim ölçülmeden yapılmamalıdır. | Faz 6'da olay hacmi ölçüldüğünde |
| **Arayüzden tool istatistiği ve model sağlık kontrolü Faz 5'te gösterilmedi** *(kullanıcı kararı)* | 2026-08-02 | Faz 5 planı Tools ekranı için "kullanım istatistikleri", Models ekranı için "bağlantı sağlık kontrolü" diyordu. İkisinin de **veri kaynağı yok**: `tool_invocations` tablosu Faz 2'de kuruldu ama yazan yok, `/api/models` yapılandırmadan gelen listeyi döndürür ve sağlayıcıya hiç gitmez (K-032). Çalıştırma olaylarından saymak yalnız sayfalanmış bir alt kümeyi kapsardı — K-041'de bilerek reddedilen hata. Sağlık kontrolü ucu ise her denetimde ücretli bir model çağrısı harcardı. Ekranlar bunun yerine Faz 6'ya işaret eden bir not taşıyor. | Faz 6'da `tool_invocations` doldurulduğunda ve `runs.model` sütunu eklendiğinde |
| **Arayüz i18n altyapısı kurulmadı; dil İngilizce** *(kullanıcı kararı)* | 2026-08-02 | Paket NuGet.org'a uluslararası yayınlanır; ekran görüntüleri, hata mesajları ve topluluk desteği İngilizce üzerinden yürür. Elle yazılmış bir sözlük + hook ~2 KB gzip tutardı ancak her yeni metin iki dilde yazılmayı sürekli bir bakım maliyeti olarak getirirdi. Depo dokümanları Türkçe kalır; bu iki karar birbirinden bağımsızdır. | **(yeniden açıldı: 2026-08-02 — kullanıcı çok dilli arayüz istedi; koşul gerçekleşti.)** İş [Faz 30](arsiv/fazlar/30-ARAYUZ-CILASI.md)'a planlandı: kütüphane alınmaz, `lib/i18n.ts` + tip güvenli sözlükler yazılır. Faz **en sona** konuldu çünkü i18n'in maliyeti kurulum değil bakımdır: araya girecek ~10 yeni ekranın metinleri iki dilde yazılmak zorunda kalırdı  — **kapandı: 2026-08-05, Faz 30 uygulandı; arayüz artık İngilizce ve Türkçe konuşur.** |
| **`MigrationRunner`'a deadlock (SQL Server hata 1205) için yeniden deneme eklenmedi (Faz 63)** | 2026-08-18 | `faz-denetim`'in bağımsız denetçisi tüm test paketlerini AYNI ANDA paralel koşturunca `0017_approval_conditions` migration'ının `ALTER TABLE ... ADD CONSTRAINT` adımında bir SQL Server deadlock'u (1205) ölçtü; `ApplyOneAsync`'in yeniden deneme mantığı yalnız `IsUniqueViolation`'ı (K-389, şema başına migration kilidi + paylaşılan katalog nesnesi çakışması) kapsıyor, deadlock'u kapsamıyor. İzole koşumda (yalnız `AgentPrism.SqlServer.IntegrationTests`, üç ayrı çalıştırma) **495/495** geçti — bu bir üretim senaryosu değil, bu geliştirme makinesinde SQL Server + PostgreSQL + Playwright'ın aynı anda Docker kaynağı paylaşmasından doğan bir test-altyapısı sıkışmasıydı. `0006_sessions_tenant_key.sql` da aynı DROP+ADD CONSTRAINT deseniyle yazılmıştı ve bugüne kadar bu sorunu göstermemişti — deadlock migration'ın İÇERİĞİNE değil, eşzamanlı DDL yüküne bağlı. Kapsam disiplini: `MigrationRunner`'ın yeniden deneme sınıflandırmasını genişletmek üç sağlayıcıyı da etkileyen ayrı bir tasarım kararıdır, Faz 63'ün argüman-koşulu kapsamına girmez. | Deadlock gerçek bir üretim ortamında (izole test değil) tekrarlanırsa, veya `MigrationRunner`'a dokunan bir sonraki faz bunu ele alırsa |

---

## 2. Kalıcı Mimari/Altyapı Kararları

| Karar | Tarih | Gerekçe | Yeniden açılma koşulu |
|---|---|---|---|
| **K-001 — Modüler paket ailesi + meta paket** *(kullanıcı kararı)* | 2026-08-01 | 7 paket: `Abstractions`, `Core`, `PostgreSql`, `OpenAI`, `AspNetCore`, `UI`, `AgentPrism` (meta). Bağımlılık grafiği tek yönlü ve döngüsüz. | — |
| **K-002 — Arayüz React 19 + TypeScript + Vite, assembly'ye gömülü** *(kullanıcı kararı)* | 2026-08-01 | Vite çıktısı `EmbeddedResource` olur, middleware ile sunulur. Tüketici projede sıfır JS bağımlılığı. gzip bütçesi 250 KB, CI kapısı. | — |
| **K-003 — Hibrit agent tanımı: kod + çalışma anı veritabanı** *(kullanıcı kararı)* | 2026-08-01 | `AddAIAgent` ile kodda tanımlananlar katalogda görünür; arayüzden tanımlananlar PostgreSQL'de saklanır ve çalışma anında derlenir. Ad çakışmasında **kod kazanır** — kod derleme zamanında doğrulanmıştır. | — |
| **K-004 — Veri erişimi: Npgsql + elden yazılmış SQL + gömülü migration runner** *(kullanıcı kararı)* | 2026-08-01 | Bkz. bölüm 1, "EF Core kullanılmadı". Migration'lar gömülü `.sql`, `pg_advisory_lock` ile korumalı, checksum doğrulamalı. | — |
| **K-005 — Hedef framework `net8.0;net9.0;net10.0`** *(kullanıcı kararı)* | 2026-08-01 | `Microsoft.Agents.AI` ve `Npgsql 10.0.3` dahil tüm bağımlılıkların üçünü de desteklediği doğrulandı. Tek `net10.0` erişimi gereksiz daraltırdı — üretimdeki projelerin büyük kısmı `net8.0` LTS'te. | — |
| **K-006 — Trim/AOT uyumu katman bazlı** | 2026-08-01 | `Abstractions`, `Core`, `PostgreSql`, `OpenAI` → AOT uyumlu. `AspNetCore`, `UI`, meta → değil. `AgentPrismAotCompatible` özelliği ile. Veremeyeceğimiz bir vaadi vermiyoruz… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-006. | — |
| **K-007 — Geçişli sabitleme kapalı** | 2026-08-01 | Bkz. bölüm 1. | — |
| **K-008 — Ön sürüm MAF bağımlılığı yalnız `AgentPrism.AspNetCore` içinde** | 2026-08-01 | `Microsoft.Agents.AI.Hosting` preview, `.Hosting.OpenAI` alpha. `Abstractions`, `Core`, `PostgreSql`, `OpenAI` yalnız GA paketlere bağlı. Sonuç: MAF GA'ya geçtiğinde tek pakette sürüm güncellemesi yeterli. AgentPrism o ana kadar `1.0.0-preview.N` yayınlanır. | Her iki paket GA olduğunda `1.0.0` yayınlanır |
| **K-009 — Sırlar `dotnet user-secrets` ile** *(kullanıcı kararı)* | 2026-08-01 | `appsettings.json` yalnız boş placeholder ve şema taşır. Bağlantı dizesi ve API anahtarı repoya hiç girmez. | — |
| **K-010 — Arayüz erişimi üç katmanlı** *(kullanıcı kararı)* | 2026-08-01 | 1) loopback varsayılan, 2) opsiyonel bearer token (sabit zamanlı karşılaştırma), 3) ASP.NET Core authorization policy kancası. `/api/meta` kimlik doğrulaması olmadan erişilir — arayüzün kimlik yöntemini öğrenmesi için; hassas veri içermez. | — |
| **K-011 — Klasör düzeni `src/` + `samples/` + `tests/`** *(kullanıcı kararı)* | 2026-08-01 | NuGet paket repolarının standart düzeni. Yeni paket eklemek düzeni bozmaz. | — |
| **K-012 — Tool'lar yalnız kodda tanımlanır** | 2026-08-01 | Bkz. bölüm 1. Güvenlik sınırı. | — |
| **K-013 — Ayrı `agentprism` PostgreSQL şeması** | 2026-08-01 | Tüketicinin `public` şemasına hiç dokunulmaz. Tablo adı çakışması, migration çakışması ve yanlışlıkla veri silme riski ortadan kalkar. | — |
| **K-014 — `run_events` append-only** | 2026-08-01 | Çalıştırma olayları güncellenmez, yalnız eklenir. `(run_id, seq)` birincil anahtar. Canlı akış (SSE) ve geçmişe dönük replay aynı kod yolundan geçer. | — |
| **K-015 — Birincil anahtarlar `uuid` v7** | 2026-08-01 | Zaman sıralı UUID. Rastgele UUID'nin B-tree index parçalanmasını önler, `bigserial`'ın merkezî sıra darboğazını getirmez. | — |
| **K-016 — Public API takibi Faz 7'ye ertelendi** *(yeniden açıldı: 2026-08-16, K-421 — takip Faz 60'ta yayından bağımsız açıldı)* | 2026-08-01 | Analyzer Faz 0'da kuruldu, `EnablePublicApiTracking=false` ile susturuldu. Faz 1–6 boyunca API yüzeyi hızla değişecek; her değişikliği kaydettirmek yayınlanmamış bir pakette hiçbir koruma sağlamaz. | — |
| **K-017 — Sürümleme MinVer ile git etiketinden** | 2026-08-01 | Elle sürüm düzenlemesi yok. Etiket yokken `0.0.0-preview.0`. | — |
| **K-018 — Bellek içi store'lar birinci sınıf implementasyon** | 2026-08-01 | `AddAgentPrism()` tek başına, veritabanı olmadan çalışır. Bu bir test yardımcısı değil, desteklenen bir moddur; sınırları (süreç ömrü, tek düğüm) dokümante edilir ve `/api/meta` aktif store tipini bildirir. | — |
| **K-019 — Agent kaynakları `IAgentSource` ile soyutlandı** | 2026-08-02 | Katalog, önceliğe göre sıralanmış kaynaklardan agent toplar: `CodeAgentSource` (0), MAF barındırma köprüsü (10, Faz 4), `DefinitionStoreAgentSource` (100). Ad çakışmasında öncelikli kaynak kazanır. Böylece `AgentPrism.Core` ön sürüm `Hosting` paketine bağlanmadan MAF agent'larını da katalogda gösterebilir. | — |
| **K-020 — `MAAI001` bastırması tek dosyada toplandı** | 2026-08-02 | MAF'ın `HarnessAgentOptions` üyeleri *"for evaluation purposes only"* işaretli ve `TreatWarningsAsErrors` ile build'i kırıyor. Bastırma yalnız `AgentDefinitionCompiler.CompileHarnessAgent` içinde, gerekçesi koda yazılı. MAF bu API'yi değiştirirse tek nokta güncellenir. | MAF harness API'sini GA yaparsa bastırma kaldırılır |
| **K-021 — Yapılandırma elle bağlanır, `Bind()` kullanılmaz** | 2026-08-02 | `optionsBuilder.Bind()` yansıma kullanır ve `IL2026` + `IL3050` üretir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-021. | Kaynak üreteci `dotnet format` ile uyumlu hale gelirse |
| **K-022 — Çalıştırma olayı sıra numarasını yazıcı üretir, depo değil** | 2026-08-02 | `RunEventWriter` tek yazıcıdır ve `Interlocked.Increment` ile boşluksuz sıra üretir. Depo yalnız yazar. Böylece sıralama deterministiktir; canlı akış (SSE) ve yeniden oynatma aynı sonucu verir. | — |
| **K-023 — Kendi UUIDv7 üretecimiz (`AgentPrismId`)** | 2026-08-02 | `Guid.CreateVersion7()` yalnız .NET 9+ içindedir; paket `net8.0` da hedefler. Depolama anahtarlarının tüm hedeflerde aynı üretilmesi gerekir. RFC 9562 uygulaması ~20 satır ve testlidir. | Paket `net8.0` desteğini bırakırsa BCL'e geçilir |
| **K-024 — `IAgentDecorator` genişleme noktası** | 2026-08-02 | Çalıştırma kaydı katalogun içine gömülmedi; `IAgentDecorator` olarak dışarı açıldı. Tüketici kendi sarmalayıcısını ekleyebilir. K4 kuralının uygulaması. | — |
| **K-025 — `UsePostgreSql()` `Replace` kullanır, `TryAdd` değil** | 2026-08-02 | `AddAgentPrism()` bellek içi depoları `TryAddSingleton` ile kaydeder ve zincirde **önce** çalışır; `UsePostgreSql()` içinde `TryAdd` yazmak sessizce hiçbir şey yapmazdı — uygulama hata vermeden bellek içi depoyla çalışır ve tüm veri süreçle birlikte kaybolurdu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-025. | — |
| **K-026 — Oturum yaşam döngüsü `AgentSessionManager` ile, MAF `AgentSessionStore` ile değil** *(kullanıcı kararı)* | 2026-08-02 | MAF'ın `AgentSessionStore` sınıfı ön sürüm `Microsoft.Agents.AI.Hosting` paketindedir; K-008 gereği `AgentPrism.Core` ve `.PostgreSql` o pakete bağlanamaz. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-026. | MAF `Hosting` GA olsa bile soyutlama korunur — kimlik damgası zaten gerekliydi |
| **K-027 — Opak ve polimorfik JSON yükleri `json` sütununda, `jsonb` değil** | 2026-08-02 | Ölçüldü: PostgreSQL `jsonb` nesne anahtarlarını yeniden sıralar (önce uzunluğa, sonra bayt sırasına göre). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-027. | .NET 9+ `AllowOutOfOrderMetadataProperties` tüm hedeflerde kullanılabilir olursa yeniden ölçülür (paket `net8.0` da hedefler) |
| **K-028 — Sağlayıcı ve depolama ayarları kendi alt bölümlerinde** *(kullanıcı kararı)* | 2026-08-02 | `AgentPrism:PostgreSql:*` ve `AgentPrism:Providers:OpenAI:*`. Kökte düz ayar tutmak (`AgentPrism:ConnectionString`) `SchemaName`, `CommandTimeoutSeconds` gibi eklere yer bırakmıyordu ve ileride `AgentPrism.SqlServer` eklenirse çakışırdı. | — |
| **K-029 — Şema adı SQL metnine gömülür, katı doğrulamadan sonra** | 2026-08-02 | PostgreSQL tanımlayıcıları parametre olarak gönderilemez. `SchemaName` yapılandırmadan gelir, bu yüzden `SqlIdentifier.RequireSchemaName` yalnız küçük harf, rakam ve alt çizgi kabul eder (en çok 63 karakter, `public` yasak). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-029. | — |
| **K-030 — Responses API her zaman sunucu tarafı depolama kapalı çalışır** | 2026-08-02 | Ölçüldü: `GetResponsesClient().AsIChatClient(model)` kullanıldığında OpenAI bir konuşma kimliği döndürüyor ve `ChatClientAgent.UpdateSessionConversationId` şu hatayı atıyor: *"Only ConversationId or ChatHistoryProvider may be used, but not both."* `UsePostgreSql()` derlenen **her** agent'a bir `ChatHistoryProvider` bağlar (Faz 2), dolayısıyla iki mekanizma aynı anda çalışamaz… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-030. | Bkz. bölüm 1, "Responses API'de sunucu tarafı konuşma durumu kullanılmadı" |
| **K-031 — `OPENAI001` bastırması tek dosyada toplandı** | 2026-08-02 | OpenAI kitaplığının Responses üyeleri (`GetResponsesClient`, `AsIChatClient(ResponsesClient, …)`, `OpenAIClient.Endpoint`) ve MAF'ın `AsIChatClientWithStoredOutputDisabled` üyesi *"for evaluation purposes only"* işaretli; `TreatWarningsAsErrors` ile build'i kırıyor. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-031. | OpenAI ve MAF bu API'leri GA yaparsa bastırma kaldırılır |
| **K-032 — Model kataloğu yapılandırmadan gelir, kodda yerleşik liste yoktur** *(kullanıcı kararı)* | 2026-08-02 | Ölçüldü: koda gömülen liste yayınlandığı gün bile yanlıştı — gerçek hesabın erişebildiği üç modelin (`gpt-5.4-mini`, `gpt-5.6-luna`, `gpt-5.6-terra`) hiçbiri listede yoktu ve listedeki `gpt-4.1-mini` `HTTP 403 model_not_found` döndü. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-032. | Bkz. bölüm 1, "Yerleşik OpenAI model listesi kodda tutulmadı" |
| **K-033 — Tool taraması açık işaretleme ister (`[AgentPrismTool]`)** *(kullanıcı kararı)* | 2026-08-02 | `AddToolsFrom` bir tipin **tüm** public metotlarını alsaydı, o sınıfa eklenen her yeni metot sessizce agent'lara açılırdı — K2 güvenlik sınırının kenarından sızıntı. Açık işaretleme bunu kapatır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-033. | — |
| **K-034 — `ModelBinding.ReasoningEffort` derleyicide bağlanır, geçersiz değer reddedilir** *(kullanıcı kararı)* | 2026-08-02 | Alan Faz 1'den beri hiçbir yerde okunmuyordu — dokümanı "destekleyen modellerde kullanılır" diyordu ama derleyici yok sayıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-034. | — |
| **K-035 — Ayar sınıfları `record` olamaz** | 2026-08-02 | `record`'un derleyici tarafından üretilen `ToString` metodu **tüm** özellikleri yazar. `OpenAIProviderOptions.ApiKey` bir sırdır; tek bir `logger.LogDebug("{Options}", options)` çağrısı anahtarı günlüğe ifşa ederdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-035. | — |
| **K-036 — OpenAI uyumlu uçlar AgentPrism tarafından yazılır, MAF'ın `Map*` uçlarıyla değil** *(kullanıcı kararı)* | 2026-08-02 | Bkz. bölüm 1. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-036. | MAF depolama arayüzlerini public yaparsa yeniden ölçülür |
| **K-037 — `ChatHistoryProvider` `AddAgentPrism()` içinde açıkça kaydedilir** | 2026-08-02 | Kayıt olmasaydı MAF her agent için kendi `InMemoryChatHistoryProvider` örneğini kurardı ve o örneğe dışarıdan erişilemezdi; `/api/sessions/{id}` mesaj geçmişi yalnız PostgreSQL açıkken okunabilirdi — yani K1 ("sıfır sürpriz") iki farklı davranış üretirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-037. | MAF geçmiş okumak için ayrı bir public API sunarsa bastırma kaldırılır |
| **K-038 — `/v1/*` uçları OpenAI hata biçimini kullanır, `ProblemDetails` değil** | 2026-08-02 | Stok OpenAI SDK'ları hata gövdesini `{"error":{"message":…,"type":…}}` biçimiyle çözümler. `ProblemDetails` döndürmek istemcide anlamsız bir hata üretirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-038. | — |
| **K-039 — Kütüphane OpenAPI üretimini dayatmaz, yalnız üstveri taşır** | 2026-08-02 | `WithName`, `WithTags`, `WithSummary`, `WithDescription` paylaşılan çerçevededir; `Microsoft.AspNetCore.OpenApi` ayrı bir pakettir ve CVE'li `Microsoft.OpenApi` 2.0.0 çeker (NU1903, GHSA-v5pm-xwqc-g5wc). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-039. | — |
| **K-040 — Enum'lar JSON'da ad olarak yazılır** | 2026-08-02 | `origin: 0`, `status: 1` gibi sayısal değerler kablo sözleşmesi olarak okunaksızdı ve enum sırası değişirse sessizce kırılırdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-040. | — |
| **K-041 — Çalıştırma özeti depoda hesaplanır** | 2026-08-02 | `/api/stats` için `QueryRunsAsync` sonuçlarını bellekte toplamak yalnız **sayfalanmış** bir alt kümeyi kapsar ve yanlış sonuç verirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-041. | — |
| **K-042 — `MapAgentPrism()` yalnız korumalı grubun convention builder'ını döndürür** | 2026-08-02 | Meta ucu ile korumalı uçlar iki ayrı `MapGroup` üzerinde. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-042. | — |
| **K-043 — Konuşma ile oturum aynı şeydir; `POST /v1/conversations` bir kimlik rezervasyonudur** | 2026-08-02 | İkinci bir kimlik uzayı açmak (konuşma ≠ oturum) aynı sohbetin `/v1/conversations/{id}/items` ile `/api/sessions/{id}` üzerinden **farklı** görünmesine yol açardı; ayrıca yeni bir depo soyutlaması ve migration gerektirirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-043. | Bir tüketici agent'tan bağımsız, mesaj taşımayan kalıcı konuşma kaydı talep ederse ayrı bir depo eklenir |
| **K-044 — Çalıştırma kimliğini çağıran üretir (`AgentPrismRunOptions`)** *(kullanıcı kararı)* | 2026-08-02 | Akışlı bir uç, çalıştırma kimliğini **ilk çerçeveden önce** bilmek zorundadır: istemci akan yanıtı çalıştırma kaydıyla ancak o zaman ilişkilendirebilir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-044. | Faz 6 çalıştırma başına ayar (örnekleme oranı, kiracı geçersiz kılma) eklerse aynı tip genişletilir |
| **K-045 — Arayüz yönlendirmesi elle yazıldı, TanStack Router kullanılmadı** *(kullanıcı kararı)* | 2026-08-02 | Uygulamada yedi ekran ve iki dinamik parametre var. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-045. | Ekran sayısı veya rota karmaşıklığı elle bakımı zorlaştırırsa yeniden ölçülür |
| **K-046 — Arayüz kabuğu bearer token katmanından muaftır** *(kullanıcı kararı)* | 2026-08-02 | Tarayıcı bir `<script src>` veya `<link href>` isteğine `Authorization` başlığı **ekleyemez**. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-046. | — |
| **K-047 — Arayüz token'ı `sessionStorage`'da tutulur** *(kullanıcı kararı)* | 2026-08-02 | Token bir sırdır; diskte kalma süresi mümkün olan en kısa olmalıdır. `sessionStorage` sekme kapandığında silinir, `localStorage` kalıcıdır ve bir XSS durumunda sır kalıcı olarak sızar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-047. | — |
| **K-048 — Arayüz varlıkları Brotli sıkıştırılmış gömülür** | 2026-08-02 | Ölçüldü: 315,1 KB ham → **80,9 KB** Brotli. İstemci `br` kabul ediyorsa içerik olduğu gibi gönderilir ve çalışma anında **sıfır** sıkıştırma maliyeti oluşur; kabul etmiyorsa bir kez açılıp bellekte tutulur. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-048. | — |
| **K-049 — `IAgentPrismUiProvider` tek metotlu tutuldu** | 2026-08-02 | Bağımlılık yönü `AgentPrism.UI → AgentPrism.AspNetCore` şeklindedir ve tersine çevrilemez; `MapAgentPrism` arayüz paketini doğrudan çağıramaz, kaydı DI'dan çözer. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-049. | Yeni metot eklemek bu esnekliği harcar; gerçekten kaçınılmazsa yeni bir arayüz tanımlanır |
| **K-050 — Frontend derlemesi dış (outer) MSBuild derlemesinde çalışır** | 2026-08-02 | Ölçüldü: `AgentPrism.UI` üç hedef çerçeve için derlenir ve MSBuild iç derlemeleri **paralel** koşturur. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-050. | — |
| **K-051 — `AgentPrism.UI.csproj` SDK'yı açık `Import` ile yükler** | 2026-08-02 | `Sdk="Microsoft.NET.Sdk"` niteliği kullanıldığında SDK hedefleri projenin **en sonuna** yerleştirilir; `AgentPrism.UI.Frontend.targets` ondan önce yüklenir ve `BeforeTargets="AssignTargetPaths"` hedefi *"the target ... **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-051. | — |
| **K-052 — Frontend birim testleri `npm run build` içinde koşar** | 2026-08-02 | Yalnız bir CI adımı olsaydı `dotnet test` onları koşmaz ve bir geliştirici sessizce kırabilirdi; proje kuralı dört doğrulama kapısının yetkili olmasıdır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-052. | — |
| **K-053 — `arastirmaci` örnek agent'ı Playground'da tool çağrısıyla birlikte bozuk bırakıldı** | 2026-08-02 | Kanıtlandı: Playground'dan `arastirmaci` (Harness + `get_order_status` tool'u) çalıştırılınca model `finishReason: tool_calls` ile bitiyor ama `Microsoft.Agents.AI.Harness` fonksiyonu hiç çağırmıyor; akış `done` olmadan kesiliyor… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-053. | `Microsoft.Agents.AI.Harness` GA olduğunda veya bu senaryo yeniden test edilip düzeldiği doğrulandığında |
| **K-054 — Faz 6 kapsamı daraltıldı: Workflows ertelendi** *(kullanıcı kararı)* | 2026-08-02 | Faz 6 planı altı iş alanı içeriyordu; hepsini tek oturumda yapmak her alanı yüzeysel bırakırdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-054. | İkinci faz planında ele alınır; `docs/arsiv/BEYIN-FIRTINASI.md` F-01 |
| **K-055 — Span'ler `ActivityListener` ile toplanır, exporter ile değil** | 2026-08-02 | AgentPrism bir kütüphanedir ve tüketicinin OpenTelemetry kurulumunu ele geçiremez. Bir exporter kaydetmek, tüketicinin kendi OTLP boru hattıyla yarışırdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-055. | — |
| **K-056 — Örnekleme kararı çalıştırma bittiğinde verilir** | 2026-08-02 | Hatalı çalıştırmaların span'leri hata ayıklamanın en değerli girdisidir, ancak bir çalıştırmanın hata verip vermeyeceği başlangıçta bilinmez. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-056. | Yük testinde bellek darboğazı ölçülürse başlangıç örneklemesine dönülür |
| **K-057 — `ModelContextProtocol.Core`, tam `ModelContextProtocol` paketi değil** | 2026-08-02 | Ölçüldü: tam paket `Microsoft.Extensions.Hosting.Abstractions` ve `Caching.Abstractions` da çeker; bunlar MCP **sunucusu** barındırmak içindir ve AgentPrism yalnızca istemcidir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-057. | — |
| **K-058 — MCP'de yalnızca uzak HTTP aktarımı; stdio yok** | 2026-08-02 | Stdio aktarımı sunucuda bir **süreç başlatır**. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-058. | AgentPrism bir kütüphane olmaktan çıkıp izole edilmiş bir sandbox'ta çalışan bağımsız servise dönüşürse |
| **K-059 — MCP kimlik doğrulama değeri veritabanında saklanmaz** | 2026-08-02 | Sunucu kaydı yalnızca değerin okunacağı **yapılandırma anahtarının adını** taşır (`authorization_configuration_key`); değer çalışma anında `IConfiguration` üzerinden çözülür ve `dotnet user-secrets` veya ortam değişkeninde kalır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-059. | — |
| **K-060 — MCP tool adları `{sunucu}_{tool}`; nokta kullanılmaz** | 2026-08-02 | Önek zorunludur: iki farklı sunucuda aynı adlı tool bulunması olağandır ve öneksiz biri diğerini gizlerdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-060. | — |
| **K-061 — "Bir daha sorma" MAF'ın biçimiyle değil AgentPrism deposunda tutulur** | 2026-08-02 | MAF `CreateAlwaysApproveToolResponse` sunar ancak o kararı kalıcılaştıracak bir yer sunmaz: kural süreç belleğiyle sınırlı kalır, kiracıya bağlanamaz ve arayüzden geri alınamazdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-061. | — |
| **K-062 — Harness'ta shell yoktur; dosya erişimi ve arka plan agent'ları kapalı bırakıldı** | 2026-08-02 | Reflection ile ölçüldü: MAF 1.16.0'ın `HarnessAgentOptions` yüzeyinde **shell erişimi diye bir üye yoktur**; faz 6 planının varsayımı hatalıydı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-062. | Kaynak sınırı, denetim izi ve ayrı bir authorization policy tasarlandığında; arka plan agent'ları için `docs/arsiv/BEYIN-FIRTINASI.md` F-04 |
| **K-063 — `run_events` partition'ı açılmadı (kapandı → K-199)** | 2026-08-02 | Faz 6 planı partition'ı bir risk önlemi olarak listeliyordu. Partition'a geçmek birincil anahtara `created_at` eklemeyi ve tabloyu yeniden kurmayı gerektirir; ölçüm olmadan yapılan böyle bir değişiklik, çözdüğünden fazla risk taşır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-063. | — (kapandı) |
| **K-064 — İkinci faz önceliği: yetenek derinliği** *(kullanıcı kararı)* | 2026-08-02 | Kullanıcıya üç ölçüt soruldu: kullanıcı sayısı (F-03, F-06), kurumsal satın alma (F-20, F-21) veya yetenek derinliği (F-09, F-10, F-27). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-064. | Kullanıcı önceliği değiştirirse; fazlar arası bağımlılık az olduğu için sıra değişimi ucuzdur |
| **K-065 — Ses: hedef gerçek zamanlı konuşma katmanı** *(kullanıcı kararı)* | 2026-08-02 | F-13 iki ayrı işti: (1) tool olarak TTS, (2) gerçek zamanlı konuşma katmanı. Kullanıcı **konuşma katmanını** seçti. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-065. | — |
| **K-066 — Skill'lerde script çalıştırma kabul edildi; K2'nin ikinci bilinçli istisnası** *(kullanıcı kararı)* | 2026-08-02 | K2 ("tool'lar yalnız kodda") arayüze erişen birinin sunucuda kod çalıştırmasını engeller. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-066. | Güvenlik olayı yaşanırsa veya kullanıcı geri çekerse; özellik varsayılan kapalı olduğu için geri alma ucuzdur |
| **K-067 — Alt agent çalıştırması ayrı bir `runs` satırıdır** *(kullanıcı kararı: "olabilir")* | 2026-08-02 | Kullanıcı ayrı satırı olabilir buldu; plan bunu benimsedi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-067. | Uygulama sırasında ağaç sorguları beklenenden pahalı çıkarsa; kullanıcı cevabı kesin değil ("olabilir"), bu yüzden uygulama oturumunda yeniden teyit edilebilir |
| **K-068 — Faz 7 (yayın) sıradan çıkarıldı; zamanı belirsiz** *(kullanıcı kararı)* *(kısmen yeniden açıldı: 2026-08-16, K-421 — public API takip yarısı artık geçersiz)* *(kapatıldı: 2026-08-24, K-602 — sürüm politikası ve yayın provası kapısı Faz 97'de karara bağlandı; `Shipped` dolumu K-603 ile GA'ya ertelendi)* | 2026-08-02 | Kullanıcı yayın zamanını henüz belirlemedi ve özellik geliştirmeye devam etmeyi seçti. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-068. | Kullanıcı yayın kararı verdiğinde |
| **K-069 — `UseOpenAICompatible(ad, ...)` ayrı ad, `UseOpenAI` aşırı yüklemesi değil** *(kullanıcı kararı)* | 2026-08-02 | Faz 8 açık sorusu olarak soruldu; kullanıcı dokümanın önerisini onayladı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-069. | — |
| **K-070 — Model sağlayıcı devre kesici varsayılan olarak açık** *(kullanıcı kararı)* | 2026-08-02 | Faz 8 açık sorusu; kullanıcı "açık" öneriyi onayladı. Bir sağlayıcı sorununda çalıştırmalar hızlı başarısız olur (istek gitmeden `AgentPrismProviderUnavailableException`) — bu üretimde genelde istenen davranıştır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-070. | — |
| **K-071 — Sağlayıcı sağlık denetimi arka planda varsayılan olarak koşmaz** *(kullanıcı kararı)* | 2026-08-02 | Faz 8 açık sorusu; kullanıcı "kapalı" öneriyi onayladı. Koşsaydı boşta duran bir kurulum bile sağlayıcıya düzenli istek atardı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-071. | Bir kullanım senaryosu sürekli taze durum isterse |
| **K-072 — Sağlayıcı adı deseni regex değil elle karakter denetimiyle doğrulanır** | 2026-08-02 | Ölçüldü: `MA0009` (Meziantou regex-DoS analizi) `[GeneratedRegex]` ile kaynak üretilmiş düzenli ifadeleri de işaretliyor; basit bir `^[a-z0-9][a-z0-9-]{0,31}$` deseni için bastırma eklemek yerine `OpenAICompatibleProviderExtensions.IsValidNamePattern` elle karakter döngüsüyle aynı kuralı uygular. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-072. | Desen karmaşıklaşırsa (ör. Unicode desteği gerekirse) yeniden değerlendirilir |
| **K-073 — Sağlık denetimi hata detayında `HttpRequestException.Message` değil `HttpRequestError` kategorisi kullanılır** | 2026-08-02 | Ölçüldü: bağlantı reddi gibi durumlarda `.Message` hedef adresi (host:port) gövdeye gömüyor; Faz 8'in DoD'si "hata detayında API anahtarı veya uç adresi sızmıyor" şartını koyuyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-073. | — |
| **K-074 — Devre kesici `ModelProviderRegistry.CreateChatClient` seviyesinde dekoratör olarak entegre edildi** | 2026-08-02 | Devre kesici hiçbir sağlayıcı paketinin (`AgentPrism.OpenAI` vb.) içine gömülmedi; `ModelProviderRegistry` yapıcısına opsiyonel `ModelProviderCircuitBreaker? **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-074. | — |
| **K-075 — Rol policy'si kayıtlı değilse eski davranışa dönülür; kontrol `MapAgentPrism()` çağrısında bir kez yapılır** | 2026-08-02 | `AgentPrismRolePolicies.Resolve` her rol için `IAuthorizationPolicyProvider.GetPolicyAsync(name)` çağırır ve sonucu `AgentPrismEndpointOptions.RequireRolePolicies` ile birlikte değerlendirir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-075. | Üretim kurulumu `RequireRolePolicies = true` ile bu davranışı açılışta hataya çevirebilir. |
| **K-076 — Denetim izi aktörü `AsyncLocal` köprüsüyle (`AuditActorContext`) okunur, `IHttpContextAccessor` ile değil** | 2026-08-02 | `AgentPrism.Core` ASP.NET Core'a bağımlı değildir (AOT, K-006) ama "kim yaptı" sorusu `HttpContext.User`'a ihtiyaç duyar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-076. | Core, `IHttpContextAccessor`'a (veya ASP.NET Core'a) bağımlı hale gelmesi gerekirse — beklenmiyor. |
| **K-077 — Denetim izi dekoratörleri, genel bir `Decorate<T>` yardımcısı yerine her paketin kendi kaydında sarılır** | 2026-08-02 | `AddAgentPrism()` bellek içi depoları doğrudan `AuditingXStore` ile sarılmış olarak kaydeder (`new AuditingAgentDefinitionStore(new InMemoryAgentDefinitionStore(), ...)`); `UsePostgreSql()` aynı dekoratörleri `PostgresXStore` ile sarar (`ActivatorUtilities.CreateInstance` ile kurularak)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-077. | Üçüncü bir depolama paketi eklenirse aynı desen tekrarlanır. |
| **K-078 — `/api/meta` yanıtına rol bilgisi (`roles`) eklendi** | 2026-08-02 | Faz 9'un arayüz gereksinimi ("Kullanıcının rolü `/api/meta` yanıtından okunur") böyle karşılandı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-078. | — |
| **K-079 — `mcp.refresh` denetim izine uç katmanında yazılır, depo dekoratöründe değil** | 2026-08-02 | Kuralın istisnası: elle tazeleme (`POST /api/mcp-servers/refresh`) bir depo yazması değildir, `IMcpToolRefresher.RefreshAsync` çağrısıdır ve bu arayüz `AgentPrism.Mcp` paketindedir (Core'a bağımlı olamaz, K-057 ile aynı bağımlılık yönü). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-079. | — |
| **K-080 — Denetim kaydında `before`/`after` tam tanım olarak saklanır, yalnız değişen alanlar değil** | 2026-08-02 | Faz 9'un açık sorusu buydu; "tam tanım" seçildi çünkü basit ve `agent_definition_versions`'ın zaten tuttuğu tam geçmişle tutarlıdır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-080. | Hacim Faz 25'in saklama politikasında ele alınacak (aynı K-063'ün `run_events` için verdiği kararla aynı tetikleyici). |
| **K-081 — Sır süzgeci "token" fragmanını, çoğulu (Tokens) hariç tutacak şekilde eşler** | 2026-08-02 | Ölçüldü: `/agentprism` örnek uygulamasında gerçek bir `agent.create` denetim kaydında `model.maxOutputTokens` alanı `"***"` ile gizlenmişti — basit alt dize eşlemesi "token" ile "maxOutputTokens" içindeki "Tokens"ı da eşliyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-081. | Depoya tekil "token" ile biten ama sayım olan yeni bir alan eklenirse yeniden değerlendirilir. |
| **K-082 — Skill sürüm geçmişi tutulmaz** *(kullanıcı kararı)* | 2026-08-02 | Skill düzenlemeleri denetim izine yazılır; ikinci bir sürüm/geri alma mekanizması Faz 10 için bakım maliyeti üretirdi. Skill değişiminden sonra agent cache'i `UpdatedAt` parmak iziyle yenilenir. | Faz 19'un sürüm karşılaştırma yüzeyi skill geçmişini de kapsamak zorunda kalırsa. |
| **K-083 — `AllowedTools` yalnız saklanır, zorlanmaz** *(kullanıcı kararı)* | 2026-08-02 | MAF frontmatter alanı korunur ancak Faz 10'da agent'ın kodla kayıtlı tool listesini değiştirmez. Tool yetkisi K-012 güvenlik sınırında kalır; script davranışı Faz 11 ile birlikte ele alınır. | Faz 11 script güvenlik modeli bu alanı somut bir yetki kuralına dönüştürürse. |
| **K-084 — Bir agent için varsayılan skill sınırı 10'dur** *(kullanıcı kararı)* | 2026-08-02 | Sınırsız skill listesi bağlam penceresini sessizce tüketir. Sınır `AgentPrismSkillOptions.MaxSkillsPerAgent` ile tüketici tarafından değiştirilebilir; HTTP derleyici yolu ve UI aynı varsayılanı uygular. | Bağlam sıkıştırma Faz 13'te ölçülüp daha iyi bir dinamik sınır kanıtlanırsa. |
| **K-085 — Derlenmiş agent cache anahtarında skill parmak izi vardır** | 2026-08-02 | Agent sürümü skill düzenlemesinde değişmez. Tenant, sıralı skill adları, skill sürümü ve `UpdatedAt` ile üretilen SHA-256 parmak izi anahtara girmezse çalışan süreç eski skill içeriğini kullanırdı. | Skill tanımları atomik olarak agent sürüm geçmişine bağlanırsa. |
| **K-086 — Script çalıştırma varsayılan olarak kapalıdır ve `PlatformIsolationAcknowledged` olmadan açılamaz** | 2026-08-02 | K-012'nin ("tool'lar yalnız kodda tanımlanır") ikinci bilinçli istisnasıdır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-086. | İşletim sistemi yalıtımı (seccomp, job object, cgroup) kütüphane içinden taşınabilir biçimde kurulabilirse. |
| **K-087 — Hem dosya tabanlı hem veritabanında saklanan script'ler desteklenir** *(kullanıcı kararı)* | 2026-08-02 | Faz dokümanı yalnız dosya tabanlı kaynağı (A) öneriyordu; kullanıcı "A + B" seçti. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-087. | — |
| **K-088 — Yorumlayıcı beyaz listesi varsayılan olarak boştur** | 2026-08-02 | Boş liste "hiçbir script çalışmaz" demektir. Varsayılan bir yorumlayıcı (ör. `python3`) koymak, özelliği açan tüketiciye farkında olmadığı bir çalıştırma yüzeyi verirdi. Tüketici `Interpreters["py"] = "python3"` yazarak niyetini açıkça bildirir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-088. | — |
| **K-089 — Denetim izine yazılamayan bir script çalıştırılmaz** | 2026-08-02 | Faz 9'un "gözlemlenebilirlik işlevselliği bozmaz" kuralının **bilinçli istisnasıdır**. Diğer yazmalarda denetim hatası yutulur ve loglanır; script çalıştırmada yutulursa sonuç, hiçbir kaydı olmayan bir uzaktan kod çalıştırma olurdu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-089. | — |
| **K-090 — Argüman doğrulaması sığdır; tam JSON Schema doğrulayıcı eklenmedi** | 2026-08-02 | Tam doğrulama yeni bir NuGet bağımlılığı gerektirirdi; AgentPrism bir kütüphanedir ve tüketicinin bağımlılık grafiğini kirletmez. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-090. | Tüketiciler derin şema doğrulaması isterse bir genişleme noktası (delege) eklenir. |
| **K-091 — Argümanlar script sürecine stdin ile geçirilir** | 2026-08-02 | Komut satırı argümanı kullanmak, modelin ürettiği metni kabuk/işlem argümanı ayrıştırmasına maruz bırakırdı. Stdin ham bayt taşır; ayrıştırma yoktur. Ayrıca süreç listesinde (`ps`) argüman içeriği görünmez. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-091. | — |
| **K-092 — İzin kaydı silinmez, `revoked_at` ile iptal edilir** | 2026-08-02 | "Kim ne zaman izin verdi ve ne zaman geri aldı" sorusu denetim izinden bağımsız olarak da cevaplanabilmelidir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-092. | — |
| **K-093 — Alt çalıştırma ayrı bir `runs` satırıdır** *(K-067'nin uygulaması)* | 2026-08-02 | Bir agent başka bir agent'ı çağırdığında alt çalıştırma kök satıra karışmaz. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-093. | — |
| **K-094 — `root_run_id` denormalize edilir** | 2026-08-02 | Bir ağacın tamamını `parent_run_id` üzerinden çekmek özyinelemeli CTE gerektirir ve arayüzün ağaç görünümü bunu her açılışta çalıştırırdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-094. | Ağaç derinliği veya genişliği CTE'yi zorunlu kılan bir sorgu şekli getirirse. |
| **K-095 — `runs.parent_run_id` için yabancı anahtar konmaz** | 2026-08-02 | Alan aynı tabloya işaret eder ve bir silme sırası kısıtı üretirdi: bir ağacın kökünü silmek önce tüm yapraklarını silmeyi gerektirirdi. Faz 25'in toplu temizleme işi bundan zarar görürdü. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-095. | — |
| **K-096 — Bütçe ağaç boyunca tek nesnedir; `AgentRunBudget` bilerek `class`'tır** | 2026-08-02 | `record` olsaydı kopyalanmaya davet ederdi; kopyalanan bütçe her dala kendi sınırını verir ve sınır anlamını yitirirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-096. | — |
| **K-097 — Alt agent yolu `AIContextProviders` üzerinden kurulur; `BackgroundAgentsProvider` için `MAAI001` bastırılır** | 2026-08-02 | Reflection ile doğrulandı: `BackgroundAgentsProvider` bir `AIContextProvider`'dır, harness gerektirmez. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-097. | MAF bu API'yi GA'ya alırsa bastırma kaldırılır. |
| **K-098 — Alt agent gec (lazy) çözülür; derleme anında çözülmez** | 2026-08-02 | `ChildAgentInvoker` alt agent'ı **her çağrıda** `IAgentCatalog` üzerinden çözer. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-098. | — |
| **K-099 — Trace tamponunun sahibi yalnız kök çalıştırmadır** | 2026-08-02 | 🚨 Gerçek bir çağrıda ölçüldü: ağaçtaki her çalıştırma aynı W3C trace kimliğini paylaşır (alt span'ler kök span'in altına yerleşir) ve `RunTraceCollector` tamponu o kimlikle anahtarlar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-099. | — |
| **K-100 — `RunQuery.OnlyRootRuns` varsayılanı `true`** *(kullanıcı kararı)* | 2026-08-02 | Faz 12 açık sorusu; kullanıcı dokümanın önerisini onayladı. Varsayılan `false` olsaydı Runs listesi kullanıcının başlatmadığı satırlarla dolardı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-100. | — |
| **K-101 — Varsayılan ağaç bütçesi: derinlik 3, 200.000 token, 25 alt çalıştırma** *(kullanıcı kararı)* | 2026-08-02 | Faz 12 açık soruları; kullanıcı dokümanın üç önerisini de onayladı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-101. | Faz 21'in kiracı kotası ayrı bir kavramdır ve bununla birleştirilmez. |
| **K-102 — Alt çalıştırmalar kök akışa yalnız özet olay yazar** *(kullanıcı kararı)* | 2026-08-02 | Faz 12 açık sorusu; kullanıcı "özet olay" önerisini onayladı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-102. | — |
| **K-103 — Alt agent onay isteyemez (v1); isteyen alt çalıştırma `Failed` olur** | 2026-08-02 | Faz 6'da ölçüldü: onay gereken çağrıda çalıştırma **biter** ve karar bir sonraki turun girdisidir (sapma S4). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-103. | MAF ağaç ortasında askıya alma/sürdürme mekanizması sunarsa. |
| **K-104 — Sıkıştırma bağımlılığı için yeni paket alınmadı** | 2026-08-02 | Ölçüldü: `dotnet list package --include-transitive` `Microsoft.ML.Tokenizers` 2.0.0'ın `Microsoft.Agents.AI.Abstractions`'ın geçişli bağımlılığı olduğunu gösterdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-104. | MAF ileride `CompactionMessageIndex`'i dışarıdan tokenizer isteyecek şekilde değiştirirse. |
| **K-105 — `ChatHistoryMemoryProvider` (vektör tabanlı bellek) bu fazın kapsamı dışında** *(kullanıcı kararı)* | 2026-08-02 | Faz 13'ün plan taslağı bunu basit "oturum içi bellek" sanıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-105. | 🚨 **GÜNCELLENDİ (Faz 51, K-342):** embedding sağlayıcısı Faz 51'de çözüldü ama `VectorStore` hâlâ çözülmedi — ölçülmüş sebep için K-342'ye bakın. `IVectorSearchStore` bu tipi sarmalamaz. |
| **K-106 — Sıkıştırma varsayılan kapalıdır** *(kullanıcı kararı)* | 2026-08-02 | `CompactionSettings.Strategy` varsayılanı `None`. `HarnessSettings.DisableCompaction`'ın "MAF varsayılanını koru" felsefesiyle tutarlı — konuşma geçmişi bir agent tanımı açıkça istemeden sessizce değişmez (K1). | — |
| **K-107 — Özetlenen mesajlar `conversation_items`'ta saklanır, silinmez** *(kullanıcı kararı)* | 2026-08-02 | Sıkıştırma yalnızca modele gönderilen bağlamı etkiler; kalıcı geçmiş (denetim izi) bozulmaz. Depo zamanla büyür ama K1 (sıfır sürpriz) ve mevcut append-only desenle (K-014) tutarlıdır — "bu mesaj neden yok" sorusu hiçbir zaman sorulmaz. | — |
| **K-108 — Özet modeli çözümleme sırası: agent ayarı → yardımcı model → agent'ın kendi modeli; özet token'ları çalıştırma toplamına dâhildir** | 2026-08-02 | `CompactionSettings.SummarizationModel` boşsa `AgentPrismOptions.UtilityModel` kullanılır, o da boşsa agent'ın kendi modeli — ucuz bir iş için pahalı bir modelle maliyet üretmemek amacıyla… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-108. | — |
| **K-109 — `Pipeline` sırası sabittir: ToolResult → SlidingWindow → Summarization** | 2026-08-02 | Serbest sıra, arayüzde anlaşılması zor bir yapılandırma yüzeyi üretir. Üç alt strateji de aynı `CompactionSettings` alanlarından (aynı tetikleyici, `MinimumPreservedGroups`/`Turns`) kurulur; kullanıcı sırayı değiştiremez. | — |
| **K-110 — `TextSearchProvider` kayıtlı `AgentFileStore`'a bağımlıdır; bu faz `InMemoryAgentFileStore`** | 2026-08-02 | `AgentFileStore` bir soyutlamadır (K-062 ile aynı gerekçe: dosya sistemi değil). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-110. | Kalıcı bir `AgentFileStore` implementasyonu eklendiğinde (Faz 14+). |
| **K-111 — İkili ek içeriği ayrı tabloda, mesajda yalnız küçük bir referans** | 2026-08-02 | `attachments.content` (`bytea`) yükleme anında yazılır; `ChatMessage.Contents`'e eklenen tek şey `UriContent({prefix}/api/attachments/{id})`'dir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-111. | — |
| **K-112 — `attachments.session_id` yabancı anahtar DEĞİLDİR** | 2026-08-02 | 🚨 Denendi ve geri alındı: FK ile kuruldu, ancak gerçek akışta (istemci önce dosyayı yükler, oturum satırı ancak ilk çalıştırmada doğar) yükleme anında `INSERT`, "violates foreign key constraint" ile başarısız oldu — ölçüldü, `AttachmentStoreContract` testleriyle yakalandı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-112. | — |
| **K-113 — Ek türü sihirli bayta göre doğrulanır; istemcinin bildirdiği `Content-Type` yok sayılır** | 2026-08-02 | `AttachmentTypeGuard` (Core) yalnızca ilk baytlardaki imzaya bakar (PNG/JPEG/GIF/WEBP/PDF/WAV/OGG/MP3 + basit bir metin sezgisi); beyaz listede olmayan hiçbir tür (özellikle yürütülebilir içerik) üretilmez. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-113. | — |
| **K-114 — Kalıcı `PostgresAgentFileStore`: agent adı ambient kapsamdan okunur** | 2026-08-02 | `Microsoft.Agents.AI.AgentFileStore` arayüzünde agent kimliği için bir parametre yoktur (tek bir `TryAddSingleton<AgentFileStore>` tüm agent'lar arasında paylaşılır, K-110). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-114. | — |
| **K-115 — `IFormFile` alan minimal API uçları `.DisableAntiforgery()` gerektirir** | 2026-08-02 | 🚨 Ölçüldü: ASP.NET Core, bir uç `IFormFile` parametresi aldığında OTOMATİK olarak anti-forgery gerektiren metadata ekler (tarayıcı form gönderimlerine karşı varsayılan CSRF koruması). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-115. | — |
| **K-116 — `/v1/chat/completions` bu fazda çok modlu girdi kabul etmez** *(kapsam kararı)* | 2026-08-02 | DoD yalnızca `/v1/responses`'ın OpenAI biçimli görsel girdiyi kabul etmesini ister (bölüm 14, doğrulama kapıları). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-116. | İstenirse ayrı bir küçük iş olarak; `AttachmentIngestion` zaten paylaşıma hazır. |
| **K-117 — Kalıcı agent dosya belleği (`agent_files`) aynı migration'da (0006) eklendi** *(kullanıcı kararı)* | 2026-08-02 | Faz 14'ün açık soru 4'ü; kullanıcı dokümanın önerisini onayladı. Maliyeti bir tablo ve bir `AgentFileStore` uygulamasıydı; Faz 13'ün `InMemoryAgentFileStore` ile bıraktığı yarım işi (K-110) kapattı — `FileMemoryProvider`/`TextSearchProvider` kod değişmeden kalıcı belleğe döndü. | — |
| **K-118 — Workflow yürütmesi ayrı pakette (`AgentPrism.Workflows`), `AspNetCore` bu pakete referans vermez** | 2026-08-03 | K-001'in doğrudan uygulaması: `Microsoft.Agents.AI.Workflows` **130 public tip** getirir ve yansımaya dayandığı için AOT vaadini kırar. Workflow kullanmayan tüketici bunu çekmemelidir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-118. | — |
| **K-119 — Beş desenin tamamı Faz 15'te uygulandı** *(kullanıcı kararı)* | 2026-08-03 | Faz 15 dokümanı üç desen (Sequential, Concurrent, Handoff) öneriyor, `GroupChat` ve `Magentic`'i Faz 16'ya bırakıyordu. Kullanıcı beşinin tamamını istedi. Ölçülen maliyet düşük çıktı: `AgentWorkflowBuilder` hepsi için hazır fabrika sunuyor ve derleyicideki fark desen başına 10–20 satır. | — |
| **K-120 — Workflow çalıştırmaları `runs` tablosunda yaşar; `workflow_runs` açılmadı** | 2026-08-03 | Runs ekranı, SSE akışı, kiracı filtreleri, istatistikler ve waterfall zaten `runs` üzerine kurulu; ikinci bir kayıt hattı hepsini ikiye katlardı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-120. | — |
| **K-121 — Kontrol noktası durumu `json` sütununda, `jsonb` DEĞİL** | 2026-08-03 | K-027'nin doğrudan uygulaması, bu kez **ölçülmüş kanıtla**: üç agent'lı bir Sequential workflow'un 7.537 baytlık kontrol noktasında `{"$type":0,...}` ayracı 1260. bayttan itibaren bulunuyor. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-121. | — |
| **K-122 — 🚨 MAF executor kimlikleri agent ÖRNEĞİNDEN türer; sarmalayıcılar önbelleklenir** | 2026-08-03 | Ölçüldü: kimlik `{Name}_{AIAgent.Id}` biçimindedir ve `AIAgent.Id` her örnek için rastgele üretilir — reflection ile doğrulandı, **sanal değildir**, türetilmiş sınıf değiştiremez. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-122. | MAF executor kimliği üretimini dışarıdan verilebilir kılarsa; veya grafı `WorkflowBuilder` + `AIAgentBinding.Id` ile elle kurma maliyeti kabul edilirse (Faz 16 kararı). |
| **K-123 — Kodda tanımlı workflow'lar agent'ları `GetWorkflowAgent` ile bağlar** | 2026-08-03 | 🚨 Ölçüldü ve düzeltildi: örnek uygulama agent'ları `IAgentCatalog.ResolveAsync` ile **doğrudan** alıyordu; çıktı doğruydu ama `runs` ağacı **tek satır** döndü — katalogdan gelen agent MAF tarafından `options = null` ile çağrılır, ağaç bilgisini okuyamaz ve kendi kök satırını açar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-123. | — |
| **K-124 — `Magentic` plan onayı Faz 15'te kapalı (`RequirePlanSignoff(false)`)** | 2026-08-03 | Ölçüldü: varsayılan ayarla MAF ilk super-step sonunda `RequestInfoEvent` yayınlar ve yürütme `PendingRequests` durumunda kalır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-124. | Faz 16 human-in-the-loop akışını getirdiğinde açılır. |
| **K-125 — `GroupChat` yönetici agent kabul etmez; `managerAgentName` yalnız `Magentic` içindir** | 2026-08-03 | Plan taslağı alanı "GroupChat / Magentic" için işaretlemişti. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-125. | — |
| **K-126 — Workflow tanımlarında sürüm GEÇMİŞİ tutulmaz** | 2026-08-03 | `agent_definition_versions` benzeri bir tablo açılmadı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-126. | Bir workflow tanımı talimat metni taşımaya başlarsa. |
| **K-127 — 🚨 Executor kimliği `(workflow, agent)` çiftinden türetilir; MAF'ın özel alanına yazılır** | 2026-08-03 | Ölçüldü: bir grafta kimliği **değişken olan tek şey** agent executor'udur (`{ad}_{AIAgent.Id}`); `OutputMessages`, `Batcher/*`, `HandoffStart`, `HandoffEnd`, `ConcurrentEnd`, `GroupChatHost`, `MagenticOrchestrator` zaten sabittir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-127. | MAF `AIAgent.Id`'yi sanal yaparsa veya executor kimliği üretimini dışarıdan verilebilir kılarsa; o zaman bastırma kaldırılır ve düzgün bir `override` yazılır. |
| **K-128 — Bekleyen insan istekleri için tablo açılmadı; olay yükünde yaşarlar** | 2026-08-03 | İstek bir `RunEventType.WorkflowRequest` olayının `Payload` alanındadır (`requestId`, `portId`, `requestType`, `responseType`, `prompt`, `form`). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-128. | Bir çalıştırmanın bekleyen istek sayısı olay akışını taramayı pahalı yapacak kadar büyürse. |
| **K-129 — `Microsoft.Agents.AI.Workflows.Declarative` alınmadı** *(kullanıcı kararı)* | 2026-08-03 | Faz 15'in "sürüm uyuşmazlığı" endişesi doğrulanmadı: **1.16.0 sürümü vardır**. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-129. | MAF `ChatClientAgent` tabanlı bir sağlayıcıyı desteklerse veya hazır bir `ResponseAgentProvider` yayınlarsa. Alınırsa ayrı bir `AgentPrism.Workflows.Declarative` paketi olarak alınır — +19 paket workflow kullanan herkese gitmemelidir. |
| **K-130 — Yanıtlanmış çalıştırma `AwaitingInput` olarak kalır; yanıt YENİ bir satır açar** | 2026-08-03 | Durumu geriye dönük değiştirmek olay akışının append-only kuralını (K-014) bozardı: "bu çalıştırma ne zaman ve neyle bitti" sorusu kaydın içinde cevaplanabilir kalmalıdır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-130. | — |
| **K-131 — Graf tanımdan değil, DERLENMİŞ workflow'dan çıkarılır** | 2026-08-03 | Hazır desenler kullanıcının yazmadığı düğümler ekler ve çalıştırma olayları (`ExecutorInvoked`) tam olarak o düğümleri adlandırır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-131. | — |
| **K-132 — Workflow grafı elle SVG ile çizilir; mermaid.js alınmadı** *(K-002'nin uygulaması)* | 2026-08-03 | mermaid.js ~100 KB gzip eder; bundle bütçesi 250 KB ve tek bir ekran için bütçenin %40'ı harcanamaz. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-132. | Bütçe belirgin biçimde büyütülürse veya graf düzeni elle çizilemeyecek kadar karmaşıklaşırsa. |
| **K-133 — Graf hatası çalıştırmayı `Failed` yapar** | 2026-08-03 | Ölçüldü: bir executor `Cannot output object of type String` ile patladı, hiçbir çıktı üretilmedi ve çalıştırma yine de `Completed` kaydedildi — listede yeşil görünen ama hiçbir sonucu olmayan bir satır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-133. | — |
| **K-134 — `WorkflowJobHandler` ayrı bir pakete değil, `AgentPrism.Core`'a konur** | 2026-08-03 | `IWorkflowRunner` arayüzü zaten `AgentPrism.Abstractions`'tadır (Core zaten ona bağımlı); somut uygulama yalnız tüketici `AgentPrism.Workflows` paketini ekleyip `UseWorkflows()` çağırırsa DI'a girer. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-134. | Eval (Faz 18) veya başka bir `IJobHandler` gerçekten ayrı bir pakette yaşaması gereken ağır bağımlılık taşırsa, o zaman ayrı paket değerlendirilir. |
| **K-135 — Optional `IJobHandler` bağımlılığı DI'da açık fabrika ile enjekte edilir, kurucu varsayılan değeriyle değil** | 2026-08-03 | Ölçüldü: `WorkflowJobHandler(IWorkflowRunner? runner, ILogger? **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-135. | — |
| **K-136 — Zamanlanmış bir işi belirli bir kiracı olarak çalıştırmak `AmbientTenantScope` (AsyncLocal) ile yapılır** | 2026-08-03 | `SingleTenantContext`/`HttpTenantContext` tekildir (singleton) ve kiracıyı ya sabit varsayılandan ya `HttpContext`'ten okur; ikisinde de "bu iş hangi kiracı için çalışıyor" bilgisi yoktur — bu bilgi yalnızca `JobRecord.TenantId` içinde taşınır… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-136. | Çok kiracılı arka plan çalıştırma ileride HTTP dışı başka bir yoldan (ör. gRPC) da gerekirse aynı mekanizma oradan da kullanılır. |
| **K-137 — İş iptali ayrı bir `cancel_requested` sütunu gerektirmez; `jobs.status` tek gerçek kaynaktır** | 2026-08-03 | `POST .../cancel` durumu CAS ile (yalnız Pending/Leased/Running'den) `Cancelled` yapar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-137. | — |
| **K-138 — `jobs` tablosuna `(schedule_id, scheduled_for)` üzerinde benzersiz kısıt eklendi; faz belgesinin DDL'i eksikti** | 2026-08-03 | `docs/arsiv/fazlar/17-TOPLU-VE-ZAMANLANMIS-CALISTIRMA.md` bölüm 17.5 metni bu kısıtın var olduğunu söylüyordu ("Aynı schedule_id + scheduled_for için ikinci bir jobs satırı oluşmasını benzersiz bir kısıt engeller") ama bölüm 17.2'deki DDL örneğinde kısıt yoktu — metin doğru niyeti anlatıyordu, DDL eksikti. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-138. | — |
| **K-139 — Eval (Faz 18) için yeni bir NuGet paketi eklenmedi** | 2026-08-03 | Ölçüldü: `EvalItem`/`EvalCheck`/`EvalChecks`/`LocalEvaluator`/`FunctionEvaluator` `Microsoft.Agents.AI` ad alanındadır — `AgentPrism.Core` zaten bu pakete **doğrudan** referans veriyor… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-139. | Microsoft bu tipleri `Microsoft.Agents.AI`'dan çıkarırsa |
| **K-140 — AI yargıç (`AIJudgeLoopEvaluator`/`LoopAgent`) Faz 18 kapsamı dışında bırakıldı — (yeniden açıldı: 2026-08-07, Faz 49 ile karşılandı, bkz. K-327)** | 2026-08-03 | Plan zaten bu ayrımı öngörüyordu (bkz. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-140. | Kullanıcı model tabanlı (LLM-judge) puanlama isterse — o zaman maliyet uyarısı ve model seçimi ile ayrı bir faz açılır |
| **K-141 — Eval vaka çalıştırmaları `runs` istatistiklerinden hariç tutulur; `RunKind.Eval` eklendi** *(kullanıcı kararının uygulanması, doc açık soru 4)* | 2026-08-03 | Örnek uygulamada gerçek bir OpenAI çağrısıyla ölçüldü: düzeltmeden **önce** tek bir eval vakası çalıştırıldığında `/api/stats` `totalRuns:1` gösteriyordu — eval bir test çağrısı olmasına rağmen normal trafik gibi sayılıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-141. | — |
| **K-142 — `LocalEvaluator.EvaluateAsync(...).DetailedItems` boş döner; gerçek sonuç `Items[0].Metrics`'tedir** 🚨 | 2026-08-03 | Reflection dökümü `AgentEvaluationResults.DetailedItems`'i (tip: `IReadOnlyList<EvalItemResult>`) listeledi ve plan bunu okuma yolu sandı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-142. | — |
| **K-143 — Eval vaka düzenleyici arayüzde JSON değil, tekrarlanan alan formu (query/expectedOutput/expectedTools/context)** | 2026-08-03 | Doc'un önerdiği "vaka düzenleyici" ifadesi hem JSON metin kutusu hem yapılandırılmış form ile karşılanabilirdi; `skills.tsx`'teki `ResourceEditor`/`ScriptEditor` deseniyle tutarlılık için ikincisi seçildi — kullanıcı tırnak/virgül kaçışıyla uğraşmaz… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-143. | Denetim türleri arayüzden görsel olarak seçilebilir hâle getirilmek istenirse |
| **K-144 — Deney atama anahtarı varsayılan olarak oturum kimliğidir; `Experiment.AssignmentKey` bu fazda rezerve** *(kullanıcı kararı)* | 2026-08-03 | Gerçek anahtar her zaman `request.SessionId ?? **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-144. | Tüketici kendi `AssignmentKey` stratejisini (örn. `"user"`) istediğinde — o zaman alanın okunma mantığı eklenir. |
| **K-145 — `EvalRunTriggerRequest.AgentVersion` eklendi; eval, A/B deneyinden tamamen bağımsız bir mekanizmadır** *(kullanıcı kararı, doc açık soru 2)* | 2026-08-03 | Eval sabit bir sürüme karşı çalışır (`EvalJobHandler` `catalog.ResolveAsync(name, version, ct)` çağırır), bir deney varyantına değil. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-145. | — |
| **K-146 — `agentprism.agent.version` metrik/span etiketi varsayılan açıktır (`IncludeAgentVersionTag = true`); `experiment_id`/`variant` hiçbir zaman etiket olmaz** *(kullanıcı kararı, doc açık soru 3)* | 2026-08-03 | Sürüm numarası zamanla artar ve agent başına onlarca zaman serisi üretir — kabul edilebilir kardinalite, `AgentPrismObservabilityOptions.IncludeAgentVersionTag` ile kapatılabilir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-146. | — |
| **K-147 — A/B deney ataması yalnızca `AgentEndpoints.RunAsync` (deneme ucu) içine gömülüdür** | 2026-08-03 | `IAgentCatalog.ResolveAsync` tüm tüketiciler (`EvalJobHandler`, `AgentBatchJobHandler`, `ChildAgentInvoker`, `WorkflowAgentBinding`, `/v1/*` uçları) arasında paylaşılan tek kapıdır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-147. | Deneyi çağrı ağacının tamamına yaymak açıkça istenirse — o zaman alt-agent ve workflow'ların da deneye girip girmeyeceği ayrıca kararlaştırılır. |
| **K-148 — Sürüm çözümü `IVersionedAgentSource` marker arayüzüyle eklendi; `IAgentSource`'a doğrudan metot eklenmedi** | 2026-08-03 | `IAgentSource`'a `ResolveVersionAsync` eklemek `CodeAgentSource`'u da bu metodu uygulamaya zorlardı — kod kaynağının sürüm kavramı yoktur (K-003). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-148. | Yeni bir `IAgentSource` (örn. MAF barındırma köprüsü) sürüm geçmişi tutmaya başlarsa, o da bu arayüzü uygular. |
| **K-149 — `experiments.variants` tek bir `jsonb` sütununda saklanır; ayrı bir `experiment_variants` tablosu açılmadı** | 2026-08-03 | Bir deneyin kolları her zaman **bütün olarak** okunur ve yazılır, hiçbir zaman tek tek güncellenmez — `EvalSuite.Checks`'in aynı gerekçesi (K-045'in devamı). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-149. | — |
| **K-150 — Maliyette para birimi dönüşümü yapılmaz** *(kullanıcı kararı, doc açık soru 1)* | 2026-08-03 | Kur kaynağı gerektirirdi ve K-032'nin ("AgentPrism fiyat uydurmaz") aynı sorununu doğururdu — yanlış kur, yanlış fiyat gibi sessizce raporu bozar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-150. | Kullanıcı çok para birimli raporlama isterse — ayrı bir faz gerekir |
| **K-151 — Ağaç maliyeti kendi maliyetiyle toplanmaz; iki ayrı alan** *(kullanıcı kararı, doc açık soru 2)* | 2026-08-03 | `RunRecord.Cost` (kendi maliyeti) ve `RunRecord.TreeCost` (agaç toplami, kendi maliyetini de içerir) ayrı alanlardır — Faz 12'nin `Usage`/`TreeUsage` ayrımıyla birebir aynı desen. Kök satırda ikisi ayrı sütun olarak gösterilir, toplanmaz. | — |
| **K-152 — `/api/stats` Eval/Workflow'u hariç tutmaya devam eder (K-141); yeni `/api/stats/timeseries` bilerek hariç TUTMAZ** *(kullanıcı kararı, doc açık soru 3)* | 2026-08-03 | Zaman serisi "gerçekten harcanan" toplam maliyeti göstermelidir — bir eval koşusu da gerçek bir model çağrısı yapar ve gerçek para harcar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-152. | — |
| **K-153 — `POST /api/stats/recalculate-costs` eklendi: Admin + denetim izi** *(kullanıcı kararı, doc açık soru 4)* | 2026-08-03 | Fiyat sonradan tanımlandığında veya düzeltildiğinde geçmiş çalıştırmaları güncel kaynağa göre tazelemenin tek yolu budur — aksi halde geçmiş sonsuza dek ilk (yanlış/eksik) fiyatla donardı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-153. | — |
| **K-154 — Yeniden hesaplama saglayiciyi bilmez; model adiyla alfabetik ilk eşleşen kazanır** | 2026-08-03 | `runs` tablosuna saglayici sütunu eklenmedi (yalnız `model_id` vardır — bkz. K-155'in aksine bilinçli kapsam sınırı). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-154. | `runs`'a bir saglayici sütunu eklenirse (büyük bir şema değişikliği, bu fazda kapsam dışı bırakıldı) |
| **K-155 — `AgentPrism:Pricing` `AgentPrismOptions`'ta (Core), bir saglayici paketinde değil** | 2026-08-03 | Fiyat, hangi saglayici paketleri kurulu olursa olsun uygulanabilir olmalıdır — `OpenAIProviderOptions.Models` gibi pakete özgü değildir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-155. | — |
| **K-156 — Dashboard'un devre kesici uyarısı mevcut `/api/models/health` `Unhealthy` durumunu kullanır; yeni uç eklenmedi** | 2026-08-03 | Devre kesicinin açık olduğu zaten `ModelProviderHealthCache`/`ModelProviderCircuitBreaker` (Faz 8) tarafından `Unhealthy` durumuna yansıtılıyordu. İkinci bir "devre durumu" ucu var olan bilgiyi tekrar eden gereksiz bir yüzey olurdu. | — |
| **K-157 — `RunEventWriter.CompleteAsync`'e eklenen `cost` parametresi ilk yazımda `RunCompletion`'a bağlanmamıştı — canlı sınamada yakalandı** 🚨 | 2026-08-03 | Derleme ve 1068 testin tamamı bu hatayı yakalamadı: birim testleri `RunPricingResolver`'ı izole çağırıyordu, sözleşme testleri `Store.CompleteRunAsync`'i `RunCompletion.Cost` elle doldurarak çağırıyordu, ikisi de `RunRecordingAgent → RunEventWriter → Store` boru hattını uçtan uca hiç çalıştırmıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-157. | — |
| **K-158 — Hız sınırı ve kota AYRI mekanizmalardır; biri bellekte, biri veritabanında** | 2026-08-03 | İki farklı zaman ölçeği iki farklı çözüm ister. Hız sınırı saniye/dakika ölçeğinde ani yükü düzleştirir ve bir istek kadar yaşar — bellekte tutmak doğrudur, kalıcılaştırmak her isteğe bir veritabanı gidişi eklerdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-158. | — |
| **K-159 — Kota yaklaşıktır; eşzamanlılıkta küçük aşım kabul edilir** | 2026-08-03 | Denetim çalıştırma **öncesinde**, tüketim **sonrasında** yazılır. Aynı anda başlayan çalıştırmalar kotayı bir miktar aşabilir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-159. | Kullanıcı kesin kota isterse — çalıştırma öncesi kilit, ayrı bir faz |
| **K-160 — Webhook teslimi Faz 17'nin kuyruğunu kullanır; `IJobStore` geri adımlı beklemeyle genişletildi** *(kullanıcı kararı)* | 2026-08-03 | Faz 21 dokümanı kendi içinde çelişiyordu: karar "ikinci kuyruk yazma" derken `webhook_deliveries` tablosu kendi `next_attempt_at`/`attempt` sütunlarını taşıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-160. | — |
| **K-161 — Webhook yükü yalnızca ÖZET taşır; mesaj içeriği hiçbir zaman girmez** *(kullanıcı kararı, doc açık soru 3)* | 2026-08-03 | Gerekçe iki katmanlıdır: (1) yük dış bir sisteme gider ve hassas veri taşıyamaz; (2) aynı metin `webhook_deliveries.payload` sütununda da saklanır — içerik taşısaydı veritabanı yedeği de hassas veri taşırdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-161. | — |
| **K-162 — Kota aşımında devam eden çalıştırma KESİLMEZ; yalnızca yeni çalıştırma reddedilir** *(kullanıcı kararı, doc açık soru 1)* | 2026-08-03 | Kesmek yarım bir yanıt ve harcanmış token üretir — kullanıcı hem parayı öder hem sonucu alamaz. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-162. | — |
| **K-163 — Webhook imzası zaman damgasını İÇERİR** | 2026-08-03 | İmza `HMAC-SHA256(timestamp + "." + body, secret)` üzerinden hesaplanır. Zaman damgası imzaya dâhil olmasaydı, yakalanan bir istek sonsuza kadar yeniden oynatılabilirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-163. | — |
| **K-164 — SSRF koruması `WebhookHttpClient`'ın İÇİNE gömülüdür; `IHttpClientFactory` kullanılmaz** | 2026-08-03 | İki gerekçe: (1) `Microsoft.Extensions.Http` paketi bağımlılık grafiğine eklenirdi (K-007) — ölçüldü, `AgentPrism.Core` grafiğinde yok; (2) tüketicinin adlandırılmış istemciyi yeniden yapılandırması korumayı sessizce kaldırabilirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-164. | — |
| **K-165 — Hız sınırının varsayılanı KAPALIDIR** *(kullanıcı kararı, doc açık soru 2)* | 2026-08-03 | Bir kütüphane tüketicisinin trafiğini bilmez. Açık gelen bir varsayılan, yükseltme yapan bir kurulumun canlı trafiğini sessizce `429` ile karşılardı — kütüphanenin yapabileceği en kötü şey. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-165. | — |
| **K-166 — `JobRecord.Payload` atanmazsa `/api/jobs` TÜM listeyi 500 ile döndürür** 🚨 | 2026-08-03 | `WebhookPublisher` işi kuyruğa yazarken `Payload` alanını hiç atamamıştı; alan `default(JsonElement)` (ValueKind = `Undefined`) kaldı ve `JsonElementConverter.Write` onu serileştiremeyip `InvalidOperationException` fırlattı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-166. | — |
| **K-167 — `AllowInsecureHttp` loopback ADRESİNİ de açar, yalnız şemayı değil** 🚨 | 2026-08-03 | İlk yazımda `AllowInsecureHttp` yalnızca `http` şemasına izin veriyor, adres denetimi loopback'i "özel ağ" sayıp reddediyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-167. | — |
| **K-168 — MCP OAuth yalnız Mod 1 (Authorization Code); Mod 0 SDK'da yok** 🚨 | 2026-08-04 | Faz 22 planı iki mod varsayıyordu: Mod 0 (istemci kimlik bilgileri, etkileşimsiz) ve Mod 1 (yetkilendirme kodu). Kullanıcı önce yalnız Mod 0'ı seçti. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-168. | SDK ileride client_credentials eklerse |
| **K-169 — MCP OAuth geri dönüş adresi (`OAuthCallbackBaseUri`) sabit bir ayardır, istekten türetilmez** | 2026-08-04 | OAuth sağlayıcıları `redirect_uri`'nin önceden kayıtlı, sabit bir değer olmasını zorunlu kılar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-169. | — |
| **K-170 — MCP OAuth token'ları `(kiracı, sunucu)` başına tek bellek içi önbellekte, iki tüketici arasında paylaşılır** | 2026-08-04 | Etkileşimli akış (`McpOAuthAuthorizationCoordinator`) token'ı alır; arka plan tazeleme (`McpToolCatalog`) onu kullanır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-170. | — |
| **K-171 — Arka plandaki (etkileşimsiz) OAuth denemesi hemen başarısız olur, beklemez** | 2026-08-04 | `McpToolCatalog`'un periyodik tazeleme döngüsünde bekleyen bir yönetici yoktur; `AuthorizationCallbackHandler`'ın gerçek bir yetkilendirme beklemesi süreci sonsuza kadar askıda bırakırdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-171. | — |
| **K-172 — `[JsonPropertyName]` iki büyük harfle başlayan alan adlarında AÇIKÇA verilir** 🚨 | 2026-08-04 | `OAuthEnabled` gibi alanlar ilk yazımda öznitelik taşımıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-172. | — |
| **K-173 — Prompt "aktarma" bu fazda panoya kopyalama olarak kaldı, agent editör entegrasyonu ertelendi** *(kapsam daraltma)* | 2026-08-04 | Tam "aktarma" (agent `Metadata`'sına `mcp.prompt.*` yazma + sunucudaki değişimi rozetle gösterme) `agent-editor.tsx`'in değiştirilmesini gerektirir — ayrı bir ekranın entegrasyonu, tek oturumluk kapsamı önemli ölçüde büyütürdü. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-173. | Agent editörüne dokunan bir sonraki fazda tamamlanabilir |
| **K-174 — Mod A kaynak okuması `AgentDefinitionCompiler`'a `IMcpResourceContextProviderFactory` soyutlamasıyla bağlanır** | 2026-08-04 | `AgentPrism.Core` (`AgentDefinitionCompiler`'ın yaşadığı paket) `AgentPrism.Mcp`'ye bağımlı olamaz (paket yönü kuralı, K2). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-174. | — |
| **K-175 — MCP kaynak toplu okuma önbelleği `ConcurrentDictionary`'e geçirildi** | 2026-08-04 | Mod A (`McpResourceContextProvider`) her agent çalıştırmasında `McpToolCatalog._connections`'ı **kilitsiz** okur; alan daha önce yalnız arka plan tazeleme döngüsü tarafından (tek yazar, `_refreshGate` altında) yazılan düz bir `Dictionary` idi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-175. | — |
| **K-176 — SQL kalıcılık mantığı `AgentPrism.Sql.Shared` altında PAYLAŞILAN KAYNAK olarak yaşar** *(kullanıcı kararı)* | 2026-08-04 | Faz 23 SQL Server'ı eklerken üçüncü bir NuGet paketi (tüketici asla doğrudan kullanmaz) veya kopyala-yapıştır (N yerde düzeltme) yerine PAYLAŞILAN KAYNAK dosyaları seçildi: `src/AgentPrism.Sql.Shared/` csproj taşımaz, `<Compile Include>` ile her sağlayıcı paketine derlenir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-176. | Dördüncü bir sağlayıcı `SqlDialect` yüzeyini şişirirse ayrı paket yeniden değerlendirilir |
| **K-177 — SQL Server upsert'lerinde `MERGE` KULLANILMAZ** | 2026-08-04 | `MERGE` ifadesinin belgelenmiş eşzamanlılık ve doğruluk sorunları vardır (benzersizlik ihlali yarışları, yanlış tetikleyici davranışı). Yerine `UPDATE ... WITH (UPDLOCK, SERIALIZABLE) ... OUTPUT inserted.*` ve ardından `IF @@ROWCOUNT = 0 INSERT ... **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-177. | — |
| **K-178 — Migration numaraları sağlayıcı başına bağımsızdır** | 2026-08-04 | `AgentPrism.SqlServer` kendi gömülü `.sql` setini taşır ve numaralandırma `0001`'den başlar; PostgreSQL'in 0001–0013'ü ile **eşleşmez**. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-178. | — |
| **K-179 — Şema adı kuralı iki sağlayıcıda AYNIDIR** | 2026-08-04 | SQL Server tanımlayıcıları PostgreSQL'den daha geniştir (128 karakter, büyük harf serbest). AgentPrism yine de tek ve dar kuralı uygular: küçük harf veya alt çizgi ile başlar, küçük harf/rakam/alt çizgi içerir, en çok 63 karakterdir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-179. | — |
| **K-180 — SQL Server'da yoğun yazılan tablolarda birincil anahtar NONCLUSTERED, kümelenmiş indeks zaman sütununda** | 2026-08-04 | SQL Server'ın `uniqueidentifier` karşılaştırması bayt sırasına göre **değildir** (son altı bayt önce karşılaştırılır). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-180. | Gerçek yük altında ölçüm yapıldığında kümelenmiş anahtar seçimi yeniden değerlendirilebilir |
| **K-181 — `AgentPrism.SqlServer` AOT uyumlu olarak İŞARETLENMEZ** | 2026-08-04 | Ölçüldü (2026-08-04, `Microsoft.Data.SqlClient` 7.0.2, net10.0): `UseSqlServer()` çağıran bir konsol uygulaması `PublishAot=true` ile `-p:TrimmerSingleWarn=false -p:SuppressTrimAnalysisWarnings=false` altında yayınlandı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-181. | CI'da canlı SQL Server'a karşı AOT sorgu yolu doğrulandığında bayrak açılır |
| **K-182 — Diziler SQL Server'a JSON metni olarak taşınır** | 2026-08-04 | SQL Server'da dizi parametresi yoktur. PostgreSQL'in `text[]`/`uuid[]` parametreleri (webhook `events`, `job_items` toplu ekleme) SQL Server tarafında `nvarchar(max)` JSON dizisi olarak gönderilir ve SQL içinde `OPENJSON` ile açılır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-182. | — |
| **K-183 — İki kalıcılık sağlayıcısı aynı anda kaydedilirse açılışta UYARI loglanır** | 2026-08-04 | `UsePostgreSql()` ve `UseSqlServer()` aynı zincirde çağrılırsa son kayıt kazanır (ikisi de `Replace` kullanır, K-025) ve verinin hangi veritabanına gittiği çağrı sırasına bağlanır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-183. | — |
| **K-184 — SQL Server benzersiz indekste NULL'ları EŞİT sayar; `COALESCE`'li ifade indeksi gerekmez** | 2026-08-04 | PostgreSQL'de NULL hiçbir NULL'a eşit değildir; bu yüzden `tool_approval_rules`, `skill_script_grants` ve `quotas` tablolarında benzersizlik `COALESCE(sütun, '')` ifade indeksiyle kurulmuştu (K-027 dönemi dersleri, `docs/hafiza/postgresql.md`). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-184. | — |
| **K-185 — `AgentPrism` meta paketi `AgentPrism.SqlServer`'ı İÇERMEZ** | 2026-08-04 | Meta paket bugün `AgentPrism.PostgreSql` içerir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-185. | Meta paketin sağlayıcısız bir varyantı (`AgentPrism.Core.All`) istenirse yeniden değerlendirilir |
| **K-186 — SQL Server sözleşme testleri `azure-sql-edge` (arm64) ile doğrulandı; gerçek `mssql/server` hâlâ koşturulamadı** *(kullanıcı kararı)* | 2026-08-05 | Geliştirme makinesinde (Apple Silicon, Docker Desktop) amd64 emülasyonu (Rosetta) kapalı kaldığı için `mcr.microsoft.com/mssql/server` yine başlatılamadı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-186. | Docker Desktop'ta Rosetta emülasyonu açıldığında veya CI Linux amd64'te SQL Server container'ını çalıştırdığında gerçek `mssql/server` ile tekrar koşturulur |
| **K-187 — `SqlServerQueries`'teki tüm `@@ROWCOUNT` referansları `@@` önekini kaybetmişti** | 2026-08-05 | 17 upsert sorgusu ve 1 değişken ataması `IF ROWCOUNT = 0` / `SET @updated = ROWCOUNT` yazıyordu — T-SQL'de geçersiz sözdizimi (`ROWCOUNT` tek başına tanımlı bir sistem değişkeni değildir, `@@ROWCOUNT` olmalıdır). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-187. | — |
| **K-188 — `DbHelpers.ReadSingleAsync` ve `ExecuteScalarAsync` yalnızca İLK sonuç kümesine bakıyordu; SQL Server'ın iki dallı upsert deseni ikinci kümeye yazabiliyor** | 2026-08-05 | K-177'nin `UPDATE ... OUTPUT` + `IF @@ROWCOUNT = 0 INSERT ... **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-188. | — |
| **K-189 — `SqlWebhookStore.ReadSubscription` diziyi `Dialect.ReadTextArray` yerine doğrudan `reader.GetFieldValue<string[]>` ile okuyordu** | 2026-08-05 | K-182 dizilerin SQL Server'da JSON metni olarak taşındığını ve okumanın `SqlDialect.ReadTextArray` üzerinden geçmesi gerektiğini karara bağlamıştı; `SqlWebhookStore` (paylaşılan katman) bunu atlayıp Npgsql'in doğal dizi tipine dayanan `GetFieldValue<string[]>` çağırıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-189. | — |
| **K-190 — SQLite'ta şema yerine tablo öneki; `SqlQueriesBase.Schema` bu değeri taşır** | 2026-08-05 | SQLite'ta şema kavramı yoktur. K-013'ün ("tüketicinin şemasına dokunma") SQLite karşılığı yapılandırılabilir bir tablo önekidir (varsayılan `agentprism_`), doğrudan tablo adının başına eklenir (`{Schema}runs`, nokta yok). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-190. | — |
| **K-191 — SQLite'ta uuid BÜYÜK harfle yazılır; `SqliteDialect.AddUuid` özellikle EZİLMEZ** | 2026-08-05 | Faz 24 dokümanının ilk planı (bölüm 24.2) uuid'in "küçük harf, tireli" TEXT olarak saklanmasını öngörüyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-191. | — |
| **K-192 — SQLite migration kilidi sidecar dosya kilididir, `BEGIN IMMEDIATE` tüm migration süresince açık TUTULMAZ** | 2026-08-05 | SQLite'ta `pg_advisory_lock`/`sp_getapplock` karşılığı yoktur (bölüm 24.3'ün öngördüğü gibi "dosya kilidi düşünülmeli"). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-192. | — |
| **K-193 — SQLite'ta indeks adları VERİTABANI GENELİNDE tektir; migration DDL'indeki her indeks de tablo önekiyle EZİLİR** | 2026-08-05 | PostgreSQL'de indeks adları şema içinde, SQL Server'da tablo içinde kapsamlıdır; SQLite'ta ise TÜM veritabanı için TEK düz ad alanı vardır (tablo, indeks, tetikleyici, görünüm hepsi aynı ad alanını paylaşır). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-193. | — |
| **K-194 — SQLite upsert deseni PostgreSQL ile BİREBİR aynıdır: tek ifadelik `INSERT ... ON CONFLICT ... RETURNING`** | 2026-08-05 | SQL Server'ın iki-dallı `UPDATE ... OUTPUT` + `IF @@ROWCOUNT = 0 INSERT ... OUTPUT` deseni (K-177) SQLite için GEREKMEDİ. Ölçüldü: SQLite 3.35+ (bağlı sürüm 3.49.1) `ON CONFLICT ... DO UPDATE ... **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-194. | — |
| **K-195 — `DbHelpers.ToGuid`/`ToBoolean` eklendi: `ExecuteScalarAsync` sonucunun CLR tipi sağlayıcıya göre değişir** | 2026-08-05 | `(Guid)result!` ve `result is bool b && b` kalıpları PostgreSQL (`uuid`→`Guid`, `boolean`→`bool`) ve SQL Server (`uniqueidentifier`→`Guid`, `CAST(...AS bit)`→`bool`) için dogruydu ama SQLite'ta YANLIŞTI: `id` sütunu TEXT olduğu için skaler sonuç `string` döner (`SqlTraceStore.UpsertTraceAsync` `InvalidCastException`… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-195. | — |
| **K-196 — `AgentPrism.Sqlite` AOT uyumlu olarak İŞARETLENMEZ (ölçülmedi)** | 2026-08-05 | `SQLitePCLRaw` yerel kütüphane taşır ve bu genellikle yayınlama (publish) davranışını etkiler; K-181'in SQL Server için izlediği "ölçülmeden vaat verilmez" ilkesi burada da uygulanır. AOT/trim ölçümü Faz 24'ün DoD'sinde açık kalan bir kalemdir. | AOT/publish altında SQLite sorgu yolu ölçüldüğünde bayrak açılır |
| **K-197 — `SQLitePCLRaw.*` paketleri 2.1.12'ye sabitlendi (K-007 deseni)** | 2026-08-05 | `Microsoft.Data.Sqlite` 10.0.10'un çektiği `SQLitePCLRaw.lib.e_sqlite3` 2.1.11'de bilinen yüksek önem dereceli bir açık var (NU1903, GHSA-2m69-gcr7-jv3q)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-197. | Microsoft.Data.Sqlite'ın varsayılan sürümü acığı düzelttiğinde sabitleme kaldırılabilir |
| **K-198 — Saklama SQL'i tek tabloyla üretilir, saglayıcı başına kopyalanmaz** | 2026-08-05 | 11 saklama hedefi × 3 sağlayıcı, her sorgu her alt sınıfta elle yazılırsa 99 neredeyse özdeş SQL metni gerektirirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-198. | Bir hedefin SQL'i şablonla ifade edilemeyecek kadar özelleşirse (üçten fazla ilişkili tabloya bakan bir koşul gibi) o hedef için bağımsız bir `SqlQueriesBase` özelliğine dönülür |
| **K-199 — `run_events` partition'ı açılmadı (K-063 ölçümle kapandı)** | 2026-08-05 | Ölçüldü (PostgreSQL 18, bu makine): 100.000 satırlık `run_events` kümesinde parti parti silme (`ctid` alt sorgusu, parti 5.000) 19 partide 125 ms'de 90.000 satır sildi — saniyede ~720.000 satır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-199. | `run_events` gerçek üretim yükünde (Faz 7'nin kendi ortamı) ölçülen silme veya yazma hızı bu sonuçla çelişirse |
| **K-200 — Parti silme her sağlayıcıda farklı teknik kullanır** | 2026-08-05 | PostgreSQL `DELETE ... LIMIT` tanımaz; `ctid` alt sorgusu kullanılır (`DELETE FROM t WHERE ctid IN (SELECT ctid FROM t WHERE kosul LIMIT n)`). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-200. | — |
| **K-201 — `MaxRows` var ama uygulanmıyor (ertelendi → kapandı K-258)** | 2026-08-05 | Faz 25'in DoD'si yalnız yaş bazlı (`MaxAgeDays`) silmeyi zorunlu kılıyordu; hacim bazlı kırpma (bir hedefi en fazla N satıra indirmek) ayrı bir sorgu deseni (satır sayısına göre "en eski N hariç" seçimi) ve ayrı bir ölçüm ister. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-201. | — (kapandı) |
| **K-202 — Saklama zamanlaması Faz 17'nin kuyruğunu yeniden kullanır** | 2026-08-05 | `JobKind.Retention` eklenip `RetentionJobHandler` (`IJobHandler`) kaydedildikten sonra Faz 17'nin var olan zamanlama altyapısı (`job_schedules` tablosu, `JobWorkerBackgroundService`, `/api/job-schedules` uçları) hiçbir yeni kod olmadan günlük saklama koşusunu karşılıyor: bir yönetici `kind=Retention, targetName="*" (veya belirli hedef), cron="0 3 * * *"` ile bir zamanlama satırı ekler… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-202. | — |
| **K-203 — `sessions`/`conversations` ayrı hedeftir** | 2026-08-05 | 25.1'in saklama tablosu ikisini tek satırda gruplamıştı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-203. | — |
| **K-204 — Anthropic ve Google için RESMİ SDK'lar kullanıldı, topluluk paketleri değil** *(kullanıcı kararı)* | 2026-08-05 | Faz 26 planı topluluk paketlerini (`Anthropic.SDK` 5.10.0, `Google_GenerativeAI` 3.6.7) varsayıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-204. | Bir SDK'nın bakımı durur veya lisansı değişirse ham HTTP istemcisine geçiş yolu açıktır (bağımlılık tek pakette izole) |
| **K-205 — `Google.GenAI`'ın geçişli ağırlığı bilerek kabul edildi ve tek pakette izole edildi** *(kullanıcı kararı)* | 2026-08-05 | Ölçüldü: `Google.GenAI` 1.16.0, `Google.Apis.Auth` üzerinden `Newtonsoft.Json` 13.0.3, `System.Management` 7.0.2, `System.CodeDom` 7.0.0 ve `Google.Apis`/`.Core` paketlerini çeker — toplam **11 geçişli bağımlılık** (karşılaştırma: `Anthropic` 4, hepsi Microsoft/System). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-205. | `Google.GenAI` kimlik doğrulama bağımlılığını hafifletirse veya bir tüketici somut bir çakışma bildirirse |
| **K-206 — İçerik filtresi tespiti `AgentPrism.Core`'da ortak dekoratördür, sağlayıcı paketlerinde değil** *(kullanıcı kararı)* | 2026-08-05 | Faz 26 dokümanı "güvenlik filtresi boş yanıtı hata olarak kaydedilir" diyordu ama nerede yapılacağını söylemiyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-206. | — |
| **K-207 — Paket adı `AgentPrism.Google`, sağlayıcı adı `google`** *(kullanıcı kararı)* | 2026-08-05 | Faz dokümanı `.UseGemini(...)` örneği veriyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-207. | Vertex AI ayrı bir kimlik/uç modeli gerektirdiği ortaya çıkarsa ikinci bir sağlayıcı adı (`google-vertex`) aynı paket içinde kaydedilir — paket adı değişmez |
| **K-208 — `ModelBinding.ProviderSettings` sözleşmeye eklendi; bilinmeyen anahtar derleme hatasıdır** *(kullanıcı kararı)* | 2026-08-05 | Anthropic'in prompt caching'i ve Gemini'nin güvenlik eşikleri `ModelBinding`'in sabit alanlarına sığmaz; onları gövdeye eklemek `AgentPrism.Abstractions`'a bir satıcının kavramını sızdırırdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-208. | Sağlayıcı sayısı arttıkça anahtar listeleri büyürse ayarlar sağlayıcı başına tipli bir kayda taşınabilir; sözlük biçimi geriye uyumluluk için kalır |
| **K-209 — `AgentPrism` meta paketi Anthropic ve Google sağlayıcılarını İÇERMEZ** | 2026-08-05 | K-185'in (SQL Server) doğrudan devamı. Meta paket bugün `AgentPrism.OpenAI` içerir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-209. | Meta paketin sağlayıcısız bir varyantı istenirse (K-185 ile aynı koşul) yeniden değerlendirilir |
| **K-210 — Sağlayıcı adı `azure-openai`; kimlik fabrikası tüketiciden gelir, `Azure.Identity` alınmadı** *(kullanıcı kararı)* | 2026-08-05 | Ad `azure` değil `azure-openai` — Foundry (K-212)/Inference sonradan eklenirse tek ada kilitlenmiş olmasın diye. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-210. | Tüketici somut bir kimlik senaryosunda tıkanırsa hazır `AzureOpenAIClient` alan kurucu zaten kaçış kapısıdır; `Azure.Identity` bağımlılığı yine alınmaz |
| **K-211 — `azure-openai` hiçbir `ProviderSettings` anahtarı sunmaz; `AzureChatExtensions` çalışma anında kırıktır** | 2026-08-05 | 🚨 Ölçüldü: `Azure.AI.OpenAI` 2.1.0'ın istek-tarafı uzantı metotları (`AddDataSource` vb.), AgentPrism'in merkezî sürüm yönetiminin zorladığı `OpenAI` 2.12.0'a karşı çalışma anında `MissingMethodException` atıyor — derleme sıfır uyarı verse de. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-211. | `Azure.AI.OpenAI` OpenAI 2.12+ ile uyumlu bir sürüme geldiğinde ölçüm tekrarlanır; `SupportedSettings` boş liste olduğu için genişleme kırıcı değildir |
| **K-212 — Azure AI Foundry ertelendi; gerekçe sürüm uyumu değil, 37 geçişli paket ve doğrulanamazlık** *(kullanıcı kararı)* | 2026-08-05 | Ölçüldü: MAF 1.16.0 ile UYUŞUYOR (Faz 27 planının koşulu gerçekleşmedi) ama üç ayrı gerekçeyle ertelendi: 37 geçişli paket ağırlığı, gerçek bir Azure aboneliği olmadan doğrulanamazlık, ve uzak agent'ın AgentPrism'in tool onayı/kalıcılık/kiracı yalıtımı garantilerini geçersiz kılması. | Foundry geçişli ağırlığını düşürürse, ya da bir tüketici gerçek bir Azure aboneliğiyle doğrulama sağlayıp ödünleşmeyi kabul ederse — ayrı paket koşuluyla |
| **K-213 — Azure'ın Responses yüzeyi desteklenmiyor** | 2026-08-05 | Ölçüldü (2026-08-05): `AzureOpenAIClient.GetResponsesClient()` Azure'a özgü bir istemci **döndürmüyor**; dönen tip `OpenAI.OpenAIClient+TopLevelResponsesClient`, yani OpenAI'ın taban sınıfı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-213. | `Azure.AI.OpenAI` Azure'a özgü bir `ResponsesClient` sunduğunda ve gerçek bir abonelikle doğrulanabildiğinde `azure-openai-responses` adıyla ikinci bir sağlayıcı eklenir |
| **K-214 — `KARARLAR-INDEKS.md`'den tarih sütunu kaldırıldı** | 2026-08-05 | İndeks Faz 25'te 24 KB'yi aşmış, bütçe 1 KB büyütülmüş ve `scripts/dokuman-bakim.py` içine "bir sonraki aşımda yapısal çözüm" notu yazılmıştı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-214. | İndeks yeniden 25 KB'yi aştığında — bu kez bütçe büyütülmez, bölünme uygulanır |
| **K-215 — Ses sözleşmeleri `AgentPrism.Abstractions`'ta yaşar; ElevenLabs bir uygulamadır** | 2026-08-05 | `ISpeechSynthesizer`, `ISpeechTranscriber`, `IVoiceHealthCheck`, `IVoicePricingReader` ve taşıdıkları tipler `AgentPrism.Voice`'ta **değil** `Abstractions`'ta durur. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-215. | — |
| **K-216 — `AgentPrism.Voice` hiçbir NuGet paketi almaz; ham `HttpClient` kullanılır** | 2026-08-05 | `ElevenLabs-DotNet` (3.7.2) mevcuttu ve alınmadı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-216. | Sağlayıcı yüzeyi üç uçtan belirgin biçimde büyürse (ör. ses klonlama, sözlük yönetimi) SDK yeniden değerlendirilir |
| **K-217 — `AgentRunScope.SessionId` eklendi; oturumsuz yazılan ek saklama tarafından silinir** | 2026-08-05 | 🚨 Bir tool `AgentSession`'a erişemez; oturum kimliği `AgentSession.StateBag`'dedir. `AgentRunScope` bu alanı taşımadığı için `speak` tool'u eki `session_id = NULL` ile yazardı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-217. | — |
| **K-218 — Tool bağımlılıkları KURULUM anında alınır; `AIFunctionArguments.Services` MAF boru hattında boştur** | 2026-08-05 | 🚨 Fazın en pahalı bulgusu: `AIFunctionArguments.Services` DI'ı ÇÖZMÜYOR (gerçek sağlayıcı `Microsoft.Extensions.AI.EmptyServiceProvider`, bir prob programı yanıltmıştı). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-218. | MAF `functionInvocationServices`'i `AsAIAgent` üzerinden akıtırsa ölçüm tekrarlanır. 🚨 `ToolMethodScanner`'ın örnek-metot yolu Faz 52'de onarıldı (K-347, 2026-08-08): `[AgentPrismTool]` işaretli örnek metotlar artık ne yansıma yolunda ne derleme anı üretecinde çalışır — MAF köprüyü açana kadar bilinçli bir kısıtlama. |
| **K-219 — `tool_invocations` beş ölçüm sütunu taşır; ses maliyeti token maliyetiyle toplanmaz** | 2026-08-05 | Faz 20'nin maliyet modeli **token** varsayar ve `runs` tablosuna yazar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-219. | — |
| **K-220 — `POST /api/voice/speak` operatör eylemidir ve `tool_invocations`'a yazmaz** | 2026-08-05 | Arayüzdeki "Speak" düğmesi bir uç ister; ama `tool_invocations.run_id` zorunlu bir yabancı anahtardır ve çalıştırmasız bir ölçüm satırı yazılamaz. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-220. | `run_id` nullable yapılırsa veya operatör eylemleri için ayrı bir ölçüm tablosu açılırsa |
| **K-221 — Ses API anahtarı düz `ApiKey`'dir; K-059 yalnız veritabanı içindir** | 2026-08-05 | Fazın ilk taslağı `ApiKeyConfigurationKey` (anahtarın **adı**) öneriyor ve K-059'a dayanıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-221. | Ses sağlayıcısı arayüzden düzenlenebilir hâle gelirse K-059 aynen uygulanır |
| **K-222 — Konuşma katmanı Seçenek A ile ve `AgentPrism.Core`'da** | 2026-08-05 | **Seçenek A (boru hattı):** ses, sağlayıcının gerçek zamanlı API'sine vekillenmez; `AIAgent.RunStreamingAsync` çağrılır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-222. | Ölçülen gecikme kabul edilemez bulunursa Seçenek B **ek bir mod** olarak eklenir; kaybedilenler açıkça yazılır |
| **K-223 — `MapAgentPrism` `UseWebSockets()`'i koşullu olarak kendisi kurar** | 2026-08-05 | Kestrel `IHttpWebSocketFeature` sağlamaz; onu `WebSocketMiddleware` kurar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-223. | WebSocket kullanan ikinci bir yetenek eklenirse aynı koşula bağlanır; ara yazılımı koşulsuz kurmak yeteneği kullanmayan tüketicinin boru hattına dokunmak olur |
| **K-224 — WebSocket bearer token'ı alt protokolde taşınır, sorgu dizesinde kabul edilmez** | 2026-08-05 | Tarayıcı bir WebSocket el sıkışmasına `Authorization` başlığı **ekleyemez** — K-046'nın arayüz kabuğu için tespit ettiği kısıtın aynısı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-224. | — |
| **K-225 — `PersistAudio` yalnız agent'ın ürettiği sesi saklar; kullanıcının sesi hiç saklanmaz** *(kullanıcı kararı: saklama varsayılan kapalı)* | 2026-08-05 | Ses **biyometrik veridir**. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-225. | Kullanıcının sesini saklama talebi gelirse önce EBML imzası eklenir ve beyaz listenin video WebM'i de kabul edeceği açıkça kabul edilir |
| **K-226 — Artımlı (geçici) transkript yok; çözüm tek atımlıdır** *(kullanıcı kararı)* | 2026-08-05 | İstemci VAD'i sessizliği görüp `commit` gönderir; sunucu biriken sesi tek bir `TranscribeAsync` çağrısıyla çözer. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-226. | Gerçek bir abonelikle sözleşme doğrulanabilirse `IStreamingSpeechTranscriber` **ayrı bir arayüz** olarak eklenir; `ISpeechTranscriber`'a üye eklemek tüketici uygulamalarını kırar |
| **K-227 — Ses dakikası bir kota birimi değildir** *(kullanıcı kararı)* | 2026-08-05 | Faz 28'in devir notu `QuotaEnforcer`'ın token ve çalıştırma saydığını, ses dakikasının bir birim olmadığını yazmıştı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-227. | Ses dakikasının kendisi bir maliyet kalemi olarak kota altına alınmak istenirse Faz 21'in şemasına yeni bir birim eklenir |
| **K-228 — i18n kütüphanesi alınmadı; `lib/i18n.tsx` elle yazıldı** | 2026-08-05 | `react-i18next` + `i18next` 15–25 KB gzip'tir ve çoğul kuralı tabloları, ad alanları, gecikmeli yükleme gibi iki yakın dilin ihtiyaç duymadığı yetenekler taşır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-228. | Üçüncü bir dil çoğul kuralı bakımından İngilizce/Türkçe'den ayrılırsa `Intl.PluralRules`'a geçilir (tarayıcıda vardır, birkaç satır); ad alanı veya gecikmeli yükleme gerekirse yeniden ölçülür |
| **K-229 — `t` fonksiyonu modül düzeyindedir ve kimliği hiç değişmez** | 2026-08-05 | `useT()` bağlamı okur (bileşen dil değişiminde yeniden çizilir) ama **modül düzeyindeki** `translate` fonksiyonunu döndürür. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-229. | — |
| **K-230 — Dil tercihi `localStorage`'da; token `sessionStorage`'da kalır (K-047)** | 2026-08-05 | Dil bir sır değildir. `sessionStorage` her yeni sekmede tercihi unuturdu ve bu bir "tercih" olmaktan çıkardı. K-047'nin gerekçesi (XSS'te sır kalıcı sızmasın) dile uygulanmaz. Sıra: `localStorage` → `navigator.languages` → `en`. | — |
| **K-231 — Varsayılan dil tarayıcıdan gelir** *(kullanıcı kararı)* | 2026-08-05 | Saklanmış tercih yokken `navigator.languages` içindeki ilk desteklenen birincil etiket seçilir (`tr-TR` → `tr`). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-231. | — |
| **K-232 — Sunucu yanıtları çevrilmez; API sözleşmesi tek dillidir** | 2026-08-05 | `ProblemDetails` metinleri, doğrulama mesajları ve sağlayıcı hataları İngilizce kalır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-232. | Bir tüketici `Accept-Language`'a göre çevrilmiş `ProblemDetails` isterse sunucu tarafı bir kaynak katmanı gerekir; bu, tüm uç noktaların sözleşmesini değiştirir |
| **K-233 — Rozet metni küçük harf, süzgeç/başlık metni büyük harf: iki ayrı anahtar kümesi** | 2026-08-05 | `runs.status.*` bir rozetin içidir ve konsolun rozet dili küçük harftir (`code`, `db`, `harness`, `approval`, `ok`, `done`). Aynı durum bir `<option>` etiketinde veya sütun başlığında büyük harf ister. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-233. | — |
| **K-234 — Dil başına ses eşlemesi istemcide tutulur; protokol zaten taşıyordu** | 2026-08-05 | 🚨 Türkçe bir yanıtı İngilizce bir sesle seslendirmek sonucu anlaşılmaz kılar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-234. | Sağlayıcı sesin dilini bildiren bir alan eklerse eşleme kendiliğinden türetilebilir ve ayar kaldırılabilir |
| **K-235 — Konuşma çözümlemesine dil kodu gönderilmez** | 2026-08-05 | Seslendirme dile bağlandı (K-234) ama **çözümleme** (speech-to-text) bağlanmadı: `VoiceConversationDriver` `TranscribeAsync`'e `options: null` geçer ve sağlayıcı dili sezer. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-235. | Sezme yanlış dil üretirse `VoiceClientMessage`'a `language` eklenir — tip `internal` olduğu için bu kırıcı bir değişiklik değildir |
| **K-236 — `--ap-subtle` ve `--ap-muted` WCAG AA'ya göre düzeltildi** | 2026-08-05 | Ölçüldü: eski `--ap-subtle` açık temada `#8e8e9d` → panel üzerinde **3.23:1**, koyu temada `#6e6e80` → **3.79:1**. İkisi de 11–12 px'te, yani WCAG için normal boy metinde kullanılıyor ve 4.5:1 eşiğinin altındaydı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-236. | — |
| **K-237 — Klavye kısayolu metin alanında tetiklenmez; `Ctrl+Enter` yereldir** | 2026-08-05 | Bir metin alanına yazılan tuş o metin alanına aittir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-237. | — |
| **K-238 — Komut paleti istemci tarafında arar** | 2026-08-05 | Katalog zaten tarayıcıdadır (agent'lar, workflow'lar, son 50 çalıştırma) ve ~40 satırlık alt dizi puanlaması yeterlidir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-238. | Katalog 200 kaydı aşarsa sunucu tarafı arama eklenir |
| **K-239 — `run_scores`: `author` NULL'ı sağlayıcı bazlı işlenir** | 2026-08-06 | K-184'ün "NULL benzersizliği SQL Server'da TERS çalışır" dersi üçüncü kez: SQL Server'da NULL'lar birbirine EŞİT sayıldığı için benzersizlik indeksi `WHERE author IS NOT NULL` ile FİLTRELENDİ (migration `0005_run_scores.sql`); PostgreSQL/SQLite'ta düz sütun yeterliydi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-239. | — |
| **K-240 — `ScoredRuns`/`PositiveRate` iki yolla hesaplanır** | 2026-08-06 | `IRunScoreStore` bilinçli olarak `IRunStore`'dan ayrı tutuldu (açık soru 1) ama istatistik özeti `IRunStore.GetStatisticsAsync`'in bir parçası olmak zorunda (`/api/stats` tek çağrıdır). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-240. | — |
| **K-241 — `InMemoryRunStore`'a isteğe bağlı `IRunScoreStore` eklendi** | 2026-08-06 | K-240'ın çözümü bir kurucu bağımlılığı gerektiriyordu ama sınıf `public`'tir ve ~20 test dosyası `new InMemoryRunStore()` ile parametresiz çağırıyordu — hepsini güncellemek gereksiz bir kırılma olurdu. Çözüm: `IRunScoreStore? **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-241. | — |
| **K-242 — `FeedbackControl` ikili puan gösterir, yıldız YAZILMADI** | 2026-08-06 | `RunScoreKind.Stars` depo ve HTTP katmanında tam desteklenir (sözleşme testleri ikisini de kapsar) ama arayüz bileşeni yalnız `Binary`'i sunar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-242. | Yıldız puanı arayüzde istenirse `RunFeedbackRequest.kind='Stars'` ile aynı `FeedbackControl` deseni genişletilir |
| **K-243 — Her çalıştırma (kök VE alt) kendi `CancellationTokenSource`'unu üretir; defter ağaç cascade'ini kendi mantığıyla uygular, akan `CancellationToken`'ın doğal yayılımına GÜVENMEZ** | 2026-08-06 | `RunRecordingAgent.RunCoreAsync`/`RunCoreStreamingAsync` gelen `cancellationToken`'dan `CancellationTokenSource.CreateLinkedTokenSource` ile KENDİ kaynağını kurar ve deftere onu kaydeder. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-243. | — |
| **K-244 — `IRunCancellationRegistry` varsayılan AÇIK kaydedilir; ayrı bir `Use...()` çağrısı yok** | 2026-08-06 | Faz 32 açık soru 4'ün önerisi (A) benimsendi: defter yalnız bellekte bir `ConcurrentDictionary` tutar, hiçbir isteği reddetmez, hiçbir yan etki üretmez — K-165'in "yeni davranış varsayılan kapalı gelir" kuralı gözlemlenebilir bir davranış değişikliğini hedefler, bu defter bir davranış değiştirmez… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-244. | Dağıtık bir defter isteyen bir kurulum arayüzü kendi uygulamasıyla `TryAddSingleton`'dan önce değiştirebilir |
| **K-245 — `WorkflowRunner` aynı deftere kendi kök kaydını yazar; `ExecuteAsync`'in zaten kurduğu `timeout`+istek `CancellationTokenSource` birleşimi (`linked`) yeniden kullanılır** | 2026-08-06 | Faz 32 açık soru 3'ün önerisi (A) benimsendi: `WorkflowRunner.ExecuteAsync` içindeki `linked = CreateLinkedTokenSource(cancellationToken, timeout.Token)` zaten vardı (Faz 15); yeni bir kaynak kurmak yerine bu ikisi birleştirilmiş kaynak doğrudan `registry.Register(execution.RunId, execution.RunId, tenantId, linked)` ile kaydedilir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-245. | — |
| **K-246 — İptal isteği `run.cancel` eylemiyle denetim izine yazılır** | 2026-08-06 | Faz 32 açık soru 2'nin önerisi (A) benimsendi: iptal bir operatör eylemidir, "kim durdurdu" sorusu Faz 9'un denetim izi altyapısıyla (`AuditRecorder.WriteAsync`) aynı yoldan cevaplanır. `entity` alanı `run:{runId}` biçimindedir; `before`/`after` boş bırakılır (çalıştırma satırı zaten `GET /api/runs/{id}` ile okunabilir). | — |
| **K-247 — K-183'ün isareti `AgentPrism.Sql.Shared`'daki internal `SqlPersistenceRegistration`'dan `AgentPrism.Abstractions`'daki public `SqlPersistenceRegistrationMarker`'a taşındı** | 2026-08-06 | Faz 33 teşhis toplayıcısı, kaç SQL sağlayıcısının kayıtlı olduğunu Core katmanından okumak zorundaydı ama Core, Sql.Shared'ı referans edemez (K-176: Sql.Shared kendi derlemesi değildir, kaynağı her sağlayıcı paketine ayrı ayrı derlenir)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-247. | — |
| **K-248 — `MigrationRunner` `ISqlPersistenceDiagnostics`'i doğrudan uygular; ayrı bir adaptör sınıfı yok** | 2026-08-06 | `MigrationRunner` zaten `SqlStoreContext.ProviderName`'e erişiyordu ve `GetSnapshotAsync` migration keşif/okuma mantığının çoğunu `ApplyAsync` ile paylaşıyor (aynı `MigrationDescriptor.Discover`, aynı `ReadAppliedAsync`). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-248. | — |
| **K-249 — `UseOpenAICompatible()` hiçbir `ConfigurationDiagnostic` bildirmez; `UseOpenAI()`'nin sabit `AgentPrism:Providers:OpenAI` bölümü yalnız KENDİSİ için geçerlidir** | 2026-08-06 | İlk taslak `OpenAIModelProvider.BuildConfigurationDiagnostic`'in `OpenAIProviderOptions.SectionName` sabitini HER iki kayıt yolunda da kullanmasıydı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-249. | UseOpenAICompatible bir gün sabit bir bölüm yoluna (`AgentPrism:Providers:OpenAICompatible:{ad}:ApiKey`, IConfiguration asırı yüklemesiyle) bağlanırsa o yol acıkca geçirilebilir |
| **K-250 — `AgentPrismDiagnosticsReport` genel bir Healthy/Degraded/Unhealthy alanı TASIMAZ; üç durumlu karar yalnız `AgentPrism.AspNetCore.AgentPrismHealthCheck` içindedir** | 2026-08-06 | `Microsoft.Extensions.Diagnostics.HealthChecks.HealthStatus` paylaşılan çerçeveden (`Microsoft.AspNetCore.App` FrameworkReference) gelir; `AgentPrism.Core` bu referansa sahip değildir ve olmamalıdır (K-006: AspNetCore'a özgü tipler Core'a sızmaz)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-250. | — |
| **K-251 — `AddAgentPrismHealthChecks()` `AddAgentPrism()`'in önceden çağrıldığını KAYIT ANINDA denetlemez** | 2026-08-06 | İlk taslak `MapAgentPrism`'in `IAgentCatalog` kontrolünü taklit ediyordu ama `MapAgentPrism` bu kontrolü `app.Build()` SONRASI, tamamlanmış bir `IServiceProvider` üzerinde yapar — `AddAgentPrismHealthChecks()` ise `IServiceCollection` kurulum aşamasında çalışır ve `services.AddAgentPrism()` ile `services.AddHealthChecks().AddAgentPrismHealthChecks()` çağrılarının hangi sırada geleceği tüketiciye bağlıdır (DI kaydı sıradan bağımsızdır)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-251. | — |
| **K-252 — Döngü denetimi `AgentCallGraph.ValidateDetailed`'e taşındı, yeni dosya yok** | 2026-08-06 | Faz 34 planı statik döngü denetiminin hiç var olmadığını varsayıyordu (`grep -rni "cycle" src/AgentPrism.Core/Compilation/ src/AgentPrism.Core/Agents/` boş döndü) ama arama yanlış dizinlerdeydi — gerçek denetim `src/AgentPrism.Core/Graph/AgentCallGraph.cs`'te önceki bir fazdan beri vardı ve `POST`/`PUT /api/agents` kaydetme anında zaten kullanılıyordu… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-252. | Döngü denetiminin mantığı değişirse (yeni hata sınıfı, derinlik tahmini) tek dosyadan güncellenir |
| **K-253 — `AgentPrismValidationOptions.McpTimeout` Core'a eklendi, AspNetCore'a değil** | 2026-08-06 | Faz 34 planının açık soru 2'si zaman aşımını `AgentPrismEndpointOptions`'ta öneriyordu; ama `AgentDefinitionValidator` `AgentPrism.Core`'da yaşar ve `AgentPrism.Core` paket yönü gereği `AgentPrism.AspNetCore`'a bağımlı olamaz… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-253. | Doğrulama ucuna HTTP'ye özgü bir ayar (istek başına zaman aşımı gibi) gerekirse o ayar `AgentPrismEndpointOptions`'a eklenip `ValidateAsync`'e parametre geçirilir |
| **K-254 — Kota ölçerine `quota.metric` eklendi** *(kullanıcı kararı)* | 2026-08-06 | Faz 35 planı yalnız `tenant.id`/`quota.scope`/`quota.period` öngörüyordu, ama bir `QuotaDefinition` aynı anda `MaxRuns`+`MaxTokens`+`MaxCost` taşıyabilir (`QuotaEnforcer.EnumerateLimits`) — üçü farklı birimdir ve etiketsiz aynı seride toplanamaz. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-254. | — |
| **K-255 — Kota ölçeri `usage`+`limit` için AYRI iki `ObservableGauge`'dur** *(kullanıcı kararı)* | 2026-08-06 | Planın kendi gerekçesiyle aynı: yalnız oran (`used/limit`) yayınca sınırın SONRADAN değiştiği pano üzerinde görünmez kalır. `agentprism.quota.limit` eklendi; ikisi aynı etiket kümesini (`tenant.id`, `quota.scope`, `quota.period`, `quota.metric`) paylaşır. | — |
| **K-256 — `QuotaUsageObserver` senkron kapılı önbellektir, zamanlayıcı değil** | 2026-08-06 | Plan "önbellek arka planda tazelenir" diyordu; `PeriodicTimer`/`BackgroundService` deseni (`JobWorkerBackgroundService` ile aynı) düşünüldü ama `ObservableGauge` geri çağırması yoklama sıklığından bağımsız çalışır ve testte `TimeProvider`'ı gerçekten ilerletebilmek gerekir — `System.Threading.Lock`/`PeriodicTimer(TimeSpan, TimeProvider)` net8.0'da yoktur (proje üç çerçeveyi de hedefler) ve `ManualTimeProvider` `CreateTimer`'ı override etmez… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-256. | Gerçek arka plan tazelemesi (yoklama sıklığından tamamen bağımsız) istenirse `BackgroundService`'e geçilir — bugünkü davranışın bilinen bedeli: onbellek bayatken İLK yoklama bloklanır |
| **K-257 — Kota ölçeri yalnız KAYITLI kiracıları tarar** | 2026-08-06 | `TenantDescriptor` belgesi kiracı kaydının ZORUNLU olmadığını söyler (`runs.tenant_id` bir yabancı anahtarla bağlanmaz) ve `IQuotaStore`'da "tüm kiracıları listele" sorgusu yoktur — yalnız `ListAsync(tenantId)` (tek kiracı kapsamlı). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-257. | Çok kiracılı bir kurulumda gösterge panosu tamlığı gerekiyorsa `IQuotaStore`'a bir "tüm kiracı kimliklerini listele" üyesi eklenir |
| **K-258 — `MaxRows` sıra, silme adımından çıkarılarak uygulandı (K-201 kapandı)** | 2026-08-06 | Plan (36.1) sırayı silme sorgusundan çıkarıp önce eşiği bulmayı öneriyordu; bu birebir uygulandı. `IRetentionStore.FindRowLimitCutoffAsync(target, maxRows)` en yeniden sayarak N… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-258. | — |
| **K-259 — Saklama korelasyonları BARE hedef adı değil, TAM NİTELENDİRİLMİŞ ad kullanır (Faz 25 hatası düzeltildi)** | 2026-08-06 | `RetentionTargetRegistry`'nin `eval_case_results`/`workflow_checkpoints`/`attachments` hedefleri, kendi `EXISTS`/`NOT EXISTS` alt sorgularında dış tabloya BARE adla (`eval_case_results.eval_run_id` gibi) atıfta bulunuyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-259. | — |
| **K-260 — `MaxRows` kiracı genelinde uygulanır, kiracı başına DEĞİL (Faz 36 planının Açık Soru 3'ünden sapma)** | 2026-08-06 | Plan "A: politikanın kiracı kapsamında" öneriyordu ve sorgunun `tenant_id` filtresi taşımasını bekliyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-260. | Çok kiracılı bir kurulumda kiracı-izole saklama gerekiyorsa hem 3 eski hem 1 yeni şablona `tenant_id` eklenir — bu, mevcut Faz 25 davranışını da değiştiren AYRI bir faz olmalıdır |
| **K-261 — `eval_case_results`/`workflow_checkpoints` için `MaxRows` eşiği İLİŞKİLİ tablo üzerinden hesaplanır (Açık Soru 2 çözüldü)** | 2026-08-06 | `id` (eval_case_results'ın sıra sütunu) bir UUID'dir, zaman damgası DEĞİLDİR — `FindRowLimitCutoffAsync`'in `DateTimeOffset?` dönüş tipine dönüştürülemez… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-261. | — |
| **K-262 — Şablonda `IncludeSymbols=false` zorunlu** | 2026-08-06 | Ölçüldü: `src/Directory.Build.props` her paket için `IncludeSymbols=true` + `snupkg` atar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-262. | Şablon paketi gerçek derlenen bir yardımcı derleme kazanırsa (bugün yok) yeniden değerlendirilir |
| **K-263 — Şablonda `TargetFrameworks` boşaltılır** | 2026-08-06 | `src/Directory.Build.props` `TargetFrameworks=net8.0;net9.0;net10.0` atar; projede yalnız `TargetFramework` (tekil) `net10.0` yazmak `dotnet build`i tek çıktıya indirger ama `dotnet pack`in çapraz-hedefleme orkestrasyonu (`_GetFrameworksWithSuppressedDependencies`) çoğul değeri okumaya devam eder ve… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-263. | — |
| **K-264 — Şablon içeriği `<None Pack>` ile paketlenir** | 2026-08-06 | Resmi NuGet deseni (`learn.microsoft.com/nuget/reference/msbuild-targets#packagepath`). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-264. | — |
| **K-265 — Şablon paket sürümü varsayılanı kayan `*-*`** | 2026-08-06 | AgentPrism nuget.org'da yayınlanmamıştır (Faz 7 beklemede, K-068) ve K-008 gereği sürekli `1.0.0-preview.N` olarak kalacaktır (MAF GA olana kadar). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-265. | AgentPrism `1.0.0` GA olduğunda `*-*` artık stabil sürümü eşlemez (floating söz dizimi ön-sürüm eki gerektirir); o noktada yeniden değerlendirilir |
| **K-266 — Üretilen `OrderTools.cs` `using AgentPrism;` taşır** | 2026-08-06 | C#'ın kapsayan ad alanı arama kuralı (`namespace A.B` içinden `namespace A`'daki üyelere niteliksiz erişim) yalnız ad alanı GERÇEKTEN "AgentPrism" ile başladığı sürece çalışır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-266. | — |
| **K-267 — `ModelBinding.ResponseFormat` yetenek denetimi yalnız `Json`/`JsonSchema` kiplerinde çalışır** | 2026-08-06 | `ModelDescriptor.SupportsStructuredOutput` bayrağının anlamı "model JSON semasina uyan cikti uretebiliyor mu"dur; bu yalnızca `Json` (sağlayıcının JSON-modu API'si) ve `JsonSchema` (şema dayatma) kiplerinde anlamlıdır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-267. | Bir sağlayıcı `Text` kipini de reddeden bir kısıt getirirse (ölçülmedi, bugün bilinmiyor) |
| **K-268 — `TemplateFixture` sablon testleri icin tam cozum yerine `AgentPrism.src.slnf` (yalniz `src/` paketlerini listeleyen bir cozum filtresi) paketler** | 2026-08-06 | Olculdu: `dotnet pack AgentPrism.slnx` cozumdeki 13 test projesini (Postgres/SqlServer/Sqlite container'li entegrasyon + Playwright E2E dahil) de restore+build ediyordu — Pack hedefi onlarda no-op olsa da Build ONA BAGIMLI oldugu icin calisiyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-268. | Sablon paketleri artarsa (bugun 16) filtre dosyasi guncellenir |
| **K-269 — `AgentPrism.Testing.FakeModelProvider` modele ozel, BIR KEZ tuketilen bir yanit kuyrugu tutar; mesaj gecmisi taranarak "hangi tool zaten cagrildi" cikarilmaz** | 2026-08-06 | Bes ayri dosyaya kopyalanmis 523 satir sahte saglayici kodu (`EchoModelProvider` ×2 — tek fark ad alani, `diff` ile dogrulandi; `ScriptedModelProvider`; `RoutingModelProvider`; `Fakes/FakeModelProvider`) birlestirildi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-269. | Bir tuketici senaryosu tool-tamamlanma-durumuna gore DINAMIK dallanma isterse (bugun karsilasilmadi) bir "custom responder" kancasi eklenir |
| **K-270 — `AgentPrism.Testing` yalniz `net10.0` hedefler (cogul `TargetFrameworks` ozelligiyle ezilerek)** | 2026-08-06 | `Microsoft.AspNetCore.TestHost` merkezi surumu (10.0.10) yalniz `net10.0` destekler (`NU1202`, net8.0/net9.0'da basarisiz) — barindirma paketi surumu framework surumuyle birebir eslenir, tek bir surum coklu TFM'i kapsayamaz… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-270. | `Microsoft.AspNetCore.TestHost` cok-TFM'li bir surum yayinlarsa (bugun yok) yeniden degerlendirilir |
| **K-271 — `tests/AgentPrism.Core.UnitTests/Fakes/FakeModelProvider.cs` SILINMEDI (plandan sapma)** | 2026-08-06 | Faz 39 plani bu dosyayi da "kaldirilan kopyalar" listesine yaziyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-271. | `AgentPrism.Testing`'e genel amacli bir "custom responder" kancasi (K-269'un acik notu) eklenirse bu 27 cagri yeniden degerlendirilebilir |
| **K-272 — Uç etiketleri TEK `.WithTags("AgentPrism", "<Alan>")` çağrısıyla verilir** | 2026-08-07 | Ölçüldü (Faz 40): `RouteHandlerBuilder` üzerinde art arda çağrılan iki `.WithTags(...)` **birikmez** — ikincisi birincinin ürettiği `ITagsMetadata`'yı tamamen ezer (`OpenApiDocumentTests.Belge_ozet_ve_etiket_ustverisini_tasir` bunu `tags[0]` "AgentPrism" yerine "Meta" dönerek yakaladı)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-272. | Framework bu metadata'yı birleştirmeye başlarsa (bugün yok) |
| **K-273 — SSE/ikili yanıtlar `Produces<T>` ile tiple bildirilir; `responseType: null` içerik tipini tamamen düşürür** | 2026-08-07 | Ölçüldü (Faz 40, izole repro): `.Produces(200, contentType: "text/event-stream")` (responseType verilmeden) belgede `"200": {"description": "OK"}` üretir — `content` alanı **hiç oluşmaz**, verilen `contentType` sessizce atılır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-273. | — |
| **K-274 — Aynı statü koduna birden fazla `.Produces` çağrısı yapılmaz; çoklu içerik tipi TEK çağrıya `additionalContentTypes` ile yazılır** | 2026-08-07 | Ölçüldü (Faz 40): `/v1/responses` ve `/v1/chat/completions` hem JSON hem SSE döndürür (govdedeki `stream` bayrağına göre). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-274. | ASP.NET Core statü başına birden fazla tip/içerik-tipi eşlemesini destekleyecek bir API sunarsa |
| **K-275 — On bir ham `Task<IResult>` ucunun tamamı yol B (`.Produces`/`.ProducesProblem` üstverisi) ile belgelendi; hiçbiri yol A'ya (`Results<...>` imza değişikliği) taşınmadı** | 2026-08-07 | `docs/arsiv/fazlar/40-OPENAPI-YAYINI.md` bölüm 40.2'nin kuralı: dönüş kümesi sonlu ve tipliyse A, akışlı/ikiliyse B. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-275. | Bir uç gerçekten sonlu/tipli JSON'a geçerse (örn. `/v1/responses` için MAF'ın `WriteResponse`'u `JsonElement` yerine somut bir tip döndürmeye başlarsa) o uç tek başına A'ya taşınabilir |
| **K-276 — `docs/openapi/agentprism.json` üretim kaynağı `AgentPrism.AspNetCore.FunctionalTests`'tir, `samples/AgentPrism.Api` DEĞİL** | 2026-08-07 | Ölçüldü (Faz 40, F-76): `samples/AgentPrism.Api` SqlServer + Sqlite sağlayıcılarını BİRLİKTE referanslıyor (K-185 örneği için bilerek); ikisi `AgentPrism.Sql.Shared`'ı linked-source olarak derlediği için (K-185) `MigrationRunner` gibi tipler İKİ derlemede aynı tam nitelikli adla XML doküman üretir… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-276. | F-76 çözülürse (bkz. `docs/ADAYLAR.md`) `samples/AgentPrism.Api`'den üretim yeniden değerlendirilebilir |
| **K-277 — Bellek içi depolar `ITenantContext` alır; kiracı süzgeci artık isteğe bağlı değildir** | 2026-08-07 | Ölçüldü (Faz 41): `InMemoryAgentDefinitionStore` kiracı kavramını **hiç** taşımıyordu; `InMemorySessionStore.GetAsync/DeleteAsync`, `InMemoryRunStore.GetRunAsync/ReadEventsAsync/ListToolInvocationsAsync` ve `InMemoryTraceStore.GetTraceByRunAsync` kiracıyı okumuyordu… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-277. | — |
| **K-278 — `sessions` birincil anahtarı `(tenant_id, id)`; oturum kimliği kiracı içinde benzersizdir** | 2026-08-07 | 🚨 **Güvenlik düzeltmesi.** Ölçüldü (Faz 41, `Ayni_ad_iki_kiracida_bagimsiz_yasar` sözleşme testi kırmızıydı): `sessions.id` çağıran tarafından verilen bir metindir (`AgentSession` kimliği, `/v1/responses` konuşma kimliği) ve tek başına birincil anahtar olduğu için kimlik **bütün kiracılar arasında** benzersizdi… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-278. | — |
| **K-279 — Saklama veri düzlemi kiracıya kilitlidir; `IRetentionStore`'un dört metodu `tenantId` alır** *(kullanıcı kararı)* | 2026-08-07 | 🚨 **Güvenlik düzeltmesi.** Ölçüldü (Faz 41): `RetentionTargetRegistry` hiçbir hedefte kiracı süzgeci taşımıyordu, ama saklama politikası kiracı başınadır (`retention_policies.tenant_id`, Faz 25 kullanıcı kararı). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-279. | Kurulum geneli tek çağrılık temizlik gerekirse `tenantId: null` geçen bir yönetim ucu eklenebilir |
| **K-280 — Çalıştırmanın alt yazmaları ambient kiracıyla süzülmez; `[TenantAgnostic]` ile gerekçesi yazılır** | 2026-08-07 | Ölçüldü (Faz 41): `IRunStore.AppendEventAsync`, `CompleteRunAsync`, `UpdateRunCostAsync` ve `RecordToolInvocationAsync`'e ambient kiracı süzgeci eklendi ve **geri alındı**. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-280. | Kayıtlara açık kiracı alanı eklenirse (aday listesinde) süzgeç geri gelir |
| **K-281 — `TenantAgnosticAttribute` `AgentPrism.Sql.Shared` içinde ve `internal`'dir** | 2026-08-07 | Faz 41 planı özniteliği test projesinde tanımlamayı öneriyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-281. | — |
| **K-282 — Kiracı yalıtımı iki depo örneğiyle değil, değiştirilebilir tek bir kiracı bağlamıyla sınanır** | 2026-08-07 | Faz 41 planı iki kiracı için iki depo örneği kurmayı öngörüyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-282. | — |
| **K-283 — Görünmeyen bir oturum YOK sayılır; "başkasının oturumu" reddi kaldırıldı** | 2026-08-07 | Ölçüldü (Faz 41): `VoiceConversationEndpoint.OwnsSessionAsync` `record is null || record.TenantId == tenantId` kuralıyla çalışıyordu ve **kiracı körü bir depo** varsayıyordu — başka bir kiracının oturumunu okuyup "başkasının" diye reddediyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-283. | — |
| **K-284 — Tek yürütücü seçimi bir kira TABLOSUYLA yapılır, oturum kilidiyle değil** | 2026-08-07 | SQLite'ın `pg_try_advisory_lock`/`sp_getapplock` karşılığı yok; bir tablo üç sağlayıcıda da (PostgreSQL, SQL Server, SQLite) aynı davranışı verir ve bağlantı havuzundan bağımsızdır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-284. | — |
| **K-285 — `SingletonGuard` `public`tir; "internal yardımcı" planı uygulanamadı** *(yeniden açıldı: 2026-08-24, K-601 — engel Faz 88'de kalktı, Faz 96'da `internal` yapıldı)* | 2026-08-07 | Faz 42 planı `SingletonGuard`'ı `AgentPrism.Core` içinde `internal` bir yardımcı olarak tasarlamıştı ve `McpDiscoveryService` (`AgentPrism.Mcp`) ile `ModelProviderHealthBackgroundService`'in (`AgentPrism.Core`) AYNI yardımcıyı kullanmasını istiyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-285. | `AgentPrism.Mcp` `AgentPrism.Core`'a birleştirilirse (olası değil) `internal`'e geri alınabilir |
| **K-286 — Kira süresi varsayılanı 60 sn, yenileme aralığı `LeaseDuration/3` (en az 1 sn taban); gerçek devralma ölçüldü** | 2026-08-07 | İki gerçek `samples/AgentPrism.Api` süreci, aynı SQLite dosyası, `LeaseDuration=12sn` (yenileme aralığı 4 sn) ile devralma **~17,6 sn** sürdü — ölçüm: kira sahibi (B) öldürüldüğünde kira zaten neredeyse sona ermek üzereydi, devralan (A) kendi 4 sn'lik yenileme turunu bekledi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-286. | Üretimde ölçülen gerçek devralma süreleri 90 sn'yi anlamlı ölçüde aşarsa (ör. yavaş bir SQL sağlayıcısında) varsayılan yeniden gözden geçirilir |
| **K-287 — Saat kayması: `expires_at` uygulama saatiyle hesaplanır, veritabanı saatiyle değil** | 2026-08-07 | Faz 42 planının Açık Soru 5'inde B seçeneği (uygulama saati) bilinçli seçildi: `SqlSingletonLeaseStore` `DateTimeOffset.UtcNow`'ı C# tarafında hesaplar — `SqlJobStore.LeaseAsync`/`RenewLeaseAsync`'in (Faz 17) zaten yaptığıyla birebir aynı desen… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-287. | Gerçek bir saat kayması olayı yaşanırsa `expires_at`'i veritabanı saatine taşımak (üç diyalektte üç ayrı ifade) yeniden değerlendirilir |
| **K-288 — `/api/agents/{name}/run` `Idempotency-Key` varsa akışsız (JSON) çalışır; plandan sapma (kullanıcı kararı)** | 2026-08-07 | Faz 43 planı bu ucun hem akışsız hem akışlı çalışabileceğini varsayıyordu, ama kodda uç KOŞULSUZ SSE dönüyordu (`AgentRunStream` her zaman akıtıyordu) — fazın kendi motivasyon örneği tam olarak bu uçtu (`QuotaGate`'in ikinci kez tüketmesi). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-288. | — |
| **K-289 — `AgentPrismIdempotencyOptions` `AgentPrism.Core`'dadır, plandaki gibi `AgentPrism.AspNetCore`'da değil** | 2026-08-07 | Plan seçenekler sınıfını AspNetCore paketine koymuştu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-289. | — |
| **K-290 — `idempotency_keys` için ayrı bir `Retention: TimeSpan` alanı yerine standart `RetentionTargets`/`AgentPrismRetentionOptions` üçlüsü kullanıldı** | 2026-08-07 | Planın taslak `AgentPrismIdempotencyOptions.Retention` alanı, Faz 25'in devir notunun zorunlu kıldığı üçlüye (`RetentionTargets` + `RetentionTargetRegistry` + `AgentPrismRetentionOptions`) PARALEL ikinci bir saklama mekanizması olurdu — aynı kavram için iki farklı yapılandırma yüzeyi… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-290. | — |
| **K-291 — Idempotency desteği varsayılan AÇIKTIR (`Enabled = true`); K1'in "varsayılan kapalı" kuralının bilinçli bir yorumu** | 2026-08-07 | Hız sınırı (K-165) ve kota gibi özelliklerin aksine bu özellik yalnız istemci `Idempotency-Key` başlığı GÖNDERDİĞİNDE devreye girer — başlıksız istek hiçbir ek sorgu/maliyet ödemez, davranışı değişmez. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-291. | — |
| **K-292 — Ham govde, filtreden ÖNCE `MapAgentPrism` içine eklenen koşullu bir ara yazılımla tamponlanır** | 2026-08-07 | `IdempotencyFilter` parmak izini HAM baytlardan hesaplamalıdır (43.3). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-292. | — |
| **K-293 — `RunError` sınıf/parmak izini doğrudan taşır; ayrı bir arama tablosu açılmadı** | 2026-08-07 | `error_class`/`error_fingerprint` K-151'in deseniyle (türetilmiş bilgi ham bilginin yanına) `runs` tablosuna, `RunError` record'una `Class`/`Fingerprint` alanı olarak eklendi — `RunCost`/`RunTreeCost` ayrımıyla aynı biçim… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-293. | — |
| **K-294 — Sınıflandırma TEK bir noktada, `RunRecordingAgent.CompleteAsync` içinde, `error` `null` değilse çalışır** | 2026-08-07 | `RunCoreAsync`/`RunCoreStreamingAsync`'in üç ayrı hata yakalama noktası (model hatası, güvenlik filtresi, alt çalıştırma onay reddi) hepsi `CompleteAsync`'e bir `RunError` geçirir; sınıflandırma orada, depoya yazımdan hemen önce, tek satırda yapılır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-294. | — |
| **K-295 — `ByErrorClass` ayrı bir depo metodu değil, `GetStatisticsAsync`'in genişletilmiş sonucu; `/api/stats/errors` o sonucun dar bir dilimi** | 2026-08-07 | Plan bir `RunErrorStatisticsQuery`/ayrı uç önerebilirdi, ama `ByAgent`/`ByModel`/`ByVersion` zaten AYNI `GetStatisticsAsync` çağrısının kırılımlarıdır (K-141'in deseni) — hata kırılımı da doğal olarak oraya katıldı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-295. | Sınıf başına kümeleme ayrı bir filtre (ör. yalnız belirli bir `agentName`'in kümeleri) isterse `RunStatisticsQuery` genişletilir |
| **K-296 — Hata sınıflandırıcının SDK istisna adları örnek uygulamada gerçek bir OpenAI hatasıyla ölçüldü; `ClientResultException` eksikti** | 2026-08-07 | Plan taslağı yalnız `HttpRequestException`/`SocketException`/`IOException`'ı `provider_error` kalıbına koymuştu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-296. | Yeni bir birinci sınıf sağlayıcı eklendiğinde o SDK'nın istisna adı da ölçülüp gerekirse listeye eklenir |
| **K-297 — `quota_exceeded` sınıfı otomatik sınıflandırıcı için YAPISAL olarak ulaşılamazdır; taksonomide kalır ama örnek uygulamada uçtan uca gösterilemedi** | 2026-08-07 | `QuotaGate` (Faz 21) bir çalıştırma `RunRecordingAgent`'a hiç ulaşmadan `429`'u HTTP katmanında döndürür (K-162: "devam eden bir çalıştırma kota aşılınca kesilmez" kuralının doğal sonucu) — kota aşımı hiçbir zaman bir `RunError` üretmez… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-297. | Kota aşımı gelecekte bir çalıştırmanın ORTASINDA (K-162'nin gevşetilmesiyle) uygulanırsa bu sınıf gerçek bir yolla erişilebilir hale gelir |
| **K-298 — Parmak izi normalleştirmesi tırnak içi metni SİLMEZ (Açık Soru 3 → C)** | 2026-08-07 | Doç 44.4'ün açık sorusu ölçüm istiyordu; ölçüm şu gözlemle karara bağlandı: bir tool hatası mesajı çoğunlukla `Tool '<ad>' calisirken hata olustu` biçimindedir ve `<ad>` iki farklı arızayı ayırt eden TEK bilgidir — silinirse `refund_order` ile `track_shipment` hataları aynı kümede toplanır ve "hangi tool bozuk" sorusu kümeden cevaplanamaz hale gelir… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-298. | Gerçek üretim mesajlarında tırnak içi metnin (ör. rastgele sipariş numarası tırnak içinde geçiyorsa) küme patlamasına yol açtığı ölçülürse yeniden değerlendirilir |
| **K-299 — Sınıf başına en sık üç küme, pencere fonksiyonlarıyla (`ROW_NUMBER()`/`COUNT() OVER`) tek geçişte hesaplanır — bu desenin kod tabanındaki İLK kullanımı** | 2026-08-07 | Alternatif (kiracı başına N+1 sorgu veya uygulama tarafında sıralama) ya sağlayıcı başına ayrı bir kod yolu ya da tüm başarısız satırların belleğe çekilmesini gerektirirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-299. | Dördüncü bir SQL sağlayıcısı eklenirse pencere fonksiyonu desteği önce doğrulanmalıdır |
| **K-300 — Terfi sorgusu `run_events`'teki `RunStarted.Text`'ten okunur; plan taslağının "run_events zaten kullanıcı girdisini taşır" iddiası yanlıştı ve düzeltildi (kullanıcı kararı)** | 2026-08-07 | Faz 45 planı ilk turda oturum tabanlı okuma (`ISessionStore`/`ChatHistoryProvider`) ile uygulandı — çekirdek kayıt hattına dokunmamak için bilinçli seçildi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-300. | — |
| **K-301 — Çok turluluk, "bu oturumda DAHA ÖNCE başlamış başka bir çalıştırma var mı" sorusuyla belirlenir; tam konuşma geçmişi okunmaz** | 2026-08-07 | K-300 ile sorgu artık oturumdan değil `run_events`'ten geldiği için tam transkript okumaya (ve onun `ISessionStore`/`IAgentCatalog`/`ChatHistoryProvider` bağımlılığına) gerek kalmadı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-301. | Çok turlu terfi (F-71/F-54'ün konusu) `EvalCase` sözleşmesini genişletirse bu kontrol yeniden değerlendirilir |
| **K-302 — `AddCaseAsync`'in `seq`/`source_run_id` eşzamanlılığı `ON CONFLICT`/`MERGE` değil, düz `INSERT` + `SqlDialect.IsUniqueViolation` yakalama + yeniden deneme ile çözülür** | 2026-08-07 | `seq`, `MAX(seq)+1` alt sorgusuyla depoda atomik hesaplanır; iki eş zamanlı terfi aynı değeri hesaplayabilir ve bu GERÇEK bir `eval_cases_suite_seq_uq` ihlaline düşer (Postgres/SQLite'ın `ON CONFLICT DO NOTHING`'i SQL Server'da karşılığı olmadığı için kullanılmadı — K-177'nin "MERGE kullanılmaz" deseniyle tutarlı)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-302. | — |
| **K-303 — "Olumsuz puan" otomatik terfi tetikleyicisi olarak `Binary` için `Value == 0`, `Stars` için `Value <= 2` (5 üzerinden) tanımlandı** | 2026-08-07 | `RunScoreKind`in iki türü var ama `RunStatistics.PositiveRate` (Faz 31, K-239) yalnız `Binary` puanları sayar — `Stars` için "olumsuz" eşiği hiç tanımlanmamıştı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-303. | Arayüzde "olumsuz" göstergesi (ör. dashboard rozetleri) eklenirse bu eşik oraya da taşınmalı — şu an yalnız terfi mantığında yaşıyor |
| **K-304 — Kuyruğa alınan (`Prefer: respond-async`) bir çalıştırmanın `runs` satırı, işçinin gerçek yürütme satırıyla AYNI birincil anahtarı paylaşır; `IRunStore.StartRunAsync` bu yüzden bir UPSERT'tir** | 2026-08-07 | Faz 46 planı "runs satırı kuyruğa alma anında yazılır" derken mekanizmayı açık bırakmıştı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-304. | — |
| **K-305 — Kuyruğa alınan bir çalıştırmada `JobRecord.Id` ile `RunRecord.Id` bilinçli olarak AYNI değeri taşır** | 2026-08-07 | `GET /api/runs/{runId}` ucunun iş kuyruğu farkındalığı olmadan (yalnız `IRunStore` okuyarak) doğru cevap verebilmesinin tek yolu budur — K-304 ile birlikte, `runs` satırı zaten `Queued` olarak mevcuttur. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-305. | — |
| **K-306 — `AgentPrismAsyncRunOptions.MaxAttempts` varsayılanı `1`'dir (kullanıcı kararı)** | 2026-08-07 | "Dayanıklı" kelimesi "iş asla kaybolmaz" değil "iş sessizce kaybolmaz" demektir. Varsayılan olarak kuyruğa alınan bir çalıştırma yeniden denenmez; sessizce iki kez koşan yan etkili bir tool, hiç koşmayandan kötüdür. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-306. | — |
| **K-307 — `AgentPrismAsyncRunOptions.Enabled` varsayılanı `true`'dur (kullanıcı kararı)** | 2026-08-07 | Faz 43'ün `AgentPrismIdempotencyOptions.Enabled` kararıyla aynı K1 okuması: `Prefer: respond-async` başlığı TAŞIMAYAN bir istek için hiçbir şey değişmez, sorgu bile atılmaz. Kapalı gelseydi başlığı gönderen bir istemci arka planda koşulduğunu sanıp korunmazdı; asıl sessiz sürpriz bu olurdu. | — |
| **K-308 — Çalıştırmanın girdisi AYRI bir `run_inputs` tablosunda ve `json` sütununda saklanır; `runs`'a sütun EKLENMEZ** | 2026-08-07 | K-027'nin dördüncü uygulaması. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-308. | — |
| **K-309 — Varsayılan tool modu `ReplayTools`; kayıtlı sonucu olmayan bir çağrı yeniden oynatmayı DURDURUR ve `422` döner** | 2026-08-07 | Sadık karşılaştırmanın amacı budur ve yan etki üretmez. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-309. | — |
| **K-310 — `LiveTools` `Admin` rolü ister ve onay gerektiren bir tool taşıyan agent bu modda çalıştırılamaz (`409`)** | 2026-08-07 | Gerçek yan etki üreten tek moddur; rol farkı bunu görünür kılar ve uç `Operator` ile bağlı olduğu için fark çalışma anında zorlanır. Onay yasağının gerekçesi K-103'ün aynısıdır: yeniden oynatma arka planda başlayabilir ve onay isteği o anda cevaplayacak bir istemci bulamaz. Denetim `ToolDescriptor.RequiresApproval` üzerinden yapılır; kiracıdaki otomatik onay kuralları hesaba KATILMAZ (kullanıcı kararı) — kural sonradan silinirse aynı istek sessizce 409'a dönerdi. | — |
| **K-311 — Dallanma öğeleri KOPYALAR; işaretçi zinciri reddedildi** | 2026-08-07 | İşaretçi zinciri seçilseydi her geçmiş okuması özyinelemeli olurdu ve `SqlChatHistoryProvider` — her agent turunda çalışan en sıcak okuma yolu — üç diyalektte özyinelemeli CTE'ye dönerdi; dallanma kullanmayan tüketici de bedel öderdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-311. | — |
| **K-312 — `conversations.parent_conversation_id` yabancı anahtar TAŞIMAZ** | 2026-08-07 | Plan `ON DELETE SET NULL` öngörüyordu. Ölçüldü: SQL Server kendine referans veren bir yabancı anahtarda `SET NULL` kabul etmez (hata 1785, "may cause cycles or multiple cascade paths"). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-312. | — |
| **K-313 — Konusma dallandırma yalnız SQL sağlayıcısı açıkken çalışır; bellek içi kurulumda uç `501` döner (kullanıcı kararı)** | 2026-08-07 | Ölçüldü: SQL kayıtlı değilken sohbet geçmişi MAF'ın `InMemoryChatHistoryProvider` nesnesinde, oturum durumunun OPAK bloğunda yaşar (K3: MAF sarmalanmaz) ve belirli bir sıra numarasına kadar kopyalanamaz… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-313. | Bellek içi bir AgentPrism `ChatHistoryProvider` yazılırsa. |
| **K-314 — Kod kaynaklı agent'lar model bindirmesi ve `NoTools`/`ReplayTools` ile oynatılamaz; `400` döner (kullanıcı kararı)** | 2026-08-07 | Kod agent'ının `AgentDefinition` karşılığı yoktur (`CodeAgentSource` doğrudan bir fabrika çağırır) ve bindirme için yeniden derlenecek bir tanım yoktur. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-314. | Kod agent'ları için bir tanım anlık görüntüsü tutulursa. |
| **K-315 — Yeniden oynatma OTURUMSUZDUR** | 2026-08-07 | Kaynak çalıştırma bir oturuma bağlıysa girdisi yalnız O TURUN mesajlarıdır; geçmiş `ChatHistoryProvider` tarafından enjekte edilir ve kayıtlı girdinin parçası değildir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-315. | Konuşma anlık görüntüsü alan bir tasarım ölçülürse. |
| **K-316 — Yeniden oynatma yalnız SENKRON ve akışsızdır; kuyruğa alma bu fazın kapsamı dışındadır** | 2026-08-07 | Plandaki Açık Soru 6 "ikisi de" öneriyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-316. | F-8x olarak aday listesine yazıldı. |
| **K-317 — `azure-sql-edge` artık güvenilir bir yerel doğrulama ikamesidir; hazır-olma denetimi `sqlcmd` yerine ADO.NET ile yazılmalıdır (kullanıcı kararı)** | 2026-08-07 | K-186'nın (Faz 23) tek seferlik başarısı sonrası Faz 25'te "artık HER ZAMAN çalışmayabilir" diye kayda geçmişti (`docs/hafiza/sql-saglayicilari.md`); kök sebep hiç teşhis edilmemişti. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-317. | Docker Desktop güncellenir/Rosetta gerçekten çalışırsa veya CI Linux amd64'te koşarsa gerçek `mssql/server` ile tekrarlanır ve bu karar arşive taşınır. |
| **K-318 — SQL Server migration'larında `ALTER TABLE ADD` ile eklenen sütunu AYNI toplu işlemde `CREATE INDEX`'te kullanmak "Invalid column name" verir; dört migration dosyası etkiliydi** | 2026-08-07 | `MigrationRunner.ApplyOneAsync` her migration dosyasını GO'suz TEK bir toplu işlem olarak gönderir (`docs/hafiza/sql-saglayicilari.md`da CREATE SCHEMA için zaten bilinen bir kısıt — aynı kök sebep). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-318. | Yeni bir migration `ALTER TABLE ADD` sonrası aynı sütunu içeren bir `CREATE INDEX`/`CREATE UNIQUE INDEX` yazarsa aynı `EXEC(N'...')` sarmalaması UYGULANMALIDIR. |
| **K-319 — `EvalCaseResult.Scores` ayarlanmamışken (varsayılan `JsonValueKind.Undefined`) SQL Server'a `"[]"` yazılır, `"null"` DEĞİL — `ISJSON` kısıtı bare `null`'ı reddeder** | 2026-08-07 | `SqlEvalStore`'un paylaşılan `RawJson` yardımcı metodu `Undefined` için jenerik olarak `"null"` metnini üretiyordu; bu, PostgreSQL/SQLite'ın `json`/`jsonb` sütunlarında sorunsuzdur (ikisi de bare `null`'ı geçerli kabul eder) ama `eval_case_results.scores` sütununun `CHECK (ISJSON(scores) = 1)` kısıtı bunu REDDEDER. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-319. | `checks`/`payload` gibi başka bir `RawJson` tüketen alan da bir `ISJSON` kısıtı taşıyan bir sütuna yazılırsa aynı kontrol yapılmalıdır — jenerik yardımcı hâlâ `Undefined` → `"null"` döner. |
| **K-320 — Model çağrı boru hattının TAMAMINI `ModelProviderRegistry` kurar; `IModelProvider` HAM istemci döndürür (kullanıcı kararı)** | 2026-08-07 | Faz 48'in guard'ı tool sonuçlarını görmek zorundaydı ve ölçüm planın konum kararını çürüttü: `grep -rn "UseFunctionInvocation" src/` dört sağlayıcı fabrikasının (OpenAI:78, Anthropic:129, Google:122, Azure:112) ve `AgentPrism.Testing/FakeModelProvider:221`'in tool çağrı döngüsünü **kendi içinde** ku… Ölçümün tamamı: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md). | MAF `FunctionInvokingChatClient`'ı agent düzeyine taşırsa veya bir sağlayıcı kendi tool döngüsünü zorunlu kılarsa sıra yeniden değerlendirilir. |
| **K-321 — İçerik guard'ı `IChatClient` katmanındadır ve tool çağrı döngüsünün İÇİNDEDİR** | 2026-08-07 | Aday listesi guard'ın zincire bir `IAgentDecorator` olarak takılmasını öneriyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-321. | Tool argümanı denetimi (F-61) farklı bir sınırdadır ve bu kararı değiştirmez. |
| **K-322 — Devre kesici içerik engellemesini ardışık hata SAYMAZ** | 2026-08-07 | Guard K-320 ile döngünün içine, devre kesici ise dışında kaldı; bir `AgentPrismContentBlockedException` `CircuitBreakingChatClient`'ın `catch (Exception)` bloğuna ulaşıyor ve `RecordFailure` çağırıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-322. | — |
| **K-323 — Yeni bir genişleme noktasının "varsayılan kapalı" kapısı KAYITTIR, bir `Enabled` bayrağı değildir (kullanıcı kararı)** | 2026-08-07 | Faz 48'in planı iki şeyi birlikte istiyordu: yerleşik guard `TryAddEnumerable` ile HER ZAMAN kaydedilsin, ve hiç guard kayıtlı değilken maliyet TAM SIFIR olsun. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-323. | Bir genişleme noktası hem her istekte çalışıyor hem davranışı değiştiriyorsa kapısı kayıt olmalıdır; yalnız belirli bir istek şekli için devreye giriyorsa açık gelebilir. |
| **K-324 — `422` yalnızca AKIŞSIZ çalıştırma dalında dönebilir (kullanıcı kararı)** | 2026-08-07 | Faz 48'in planı "girişte engelleme `422` döner" diyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-324. | Akışlı uç bir gün `runId`'yi başlıkta dönerse SSE'yi geciktirmek ucuzlaşır ve karar yeniden açılabilir. |
| **K-325 — Engellenen veya maskelenen içerik HİÇBİR yere yazılmaz** | 2026-08-07 | Engellenen içerik tanımı gereği hassastır; onu denetim izine veya olay yüküne yazmak sorunu KALICI hale getirir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-325. | Kayıt redaksiyonu ayrı bir aday kalemidir ve Faz 45'in eval terfisi ile Faz 47'nin yeniden oynatmasıyla çatışır; o çatışma önce karara bağlanmalıdır. |
| **K-326 — `RunErrorClass.ContentBlocked` `ContentFiltered`'dan AYRIDIR** | 2026-08-07 | Faz 44'ün taksonomisinde `ContentFiltered = 5`'in tanımı "model yanıtı SAĞLAYICININ güvenlik/içerik filtresiyle kesildi"dir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-326. | — |
| **K-327 — `AIJudgeLoopEvaluator` KULLANILMADI; `IRunJudge` sıfırdan yazıldı (Faz 49)** | 2026-08-07 | Ölçüldü (MAF 1.16.0, reflection dökümü): `LoopEvaluation` bir PUAN döndürmez, yalnız `ShouldReinvoke` (bool) ve `Feedback` (metin) taşır; `LoopContext`'in kurucusu canlı bir `AIAgent` **ve** `AgentSession` ister — bitmiş bir çalıştırmayı puanlamak için sentetik bir agent/oturum kurmak gerekirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-327. | MAF `LoopEvaluation`'a bir puan alanı eklerse veya bitmiş bir çalıştırmayı puanlayan ayrı bir tip yayınlarsa yeniden değerlendirilir. |
| **K-328 — Yargıç maliyeti `RunKind.Eval` dışlamasıyla ayrılır; yeni bir sütun açılmadı (Faz 49)** | 2026-08-07 | Yargıcın KENDİ çalıştırması `agent_name = "judge:{ad}"` ile bir `runs` satırı olarak, `RunKind.Eval` ile kaydedilir (`ModelRunJudge`, ephemeral bir `ChatClientAgent`'ı doğrudan `RunRecordingAgent` ile sarar — `IAgentCatalog`'a hiç girmez). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-328. | — |
| **K-329 — İki kapılı varsayılan: `OnlineEvaluationOptions.Enabled = false` VE `SampleRate = 0.0` (Faz 49)** | 2026-08-07 | Faz 48 ile aynı düz K1 okuması: yargıç her puanlamada gerçek para harcayan bir model çağrısı yapar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-329. | — |
| **K-330 — Yargıcın `IChatClient`'ı guard boru hattından GEÇER; engelleme özel olarak ele alınmadı (Faz 49, D1)** | 2026-08-07 | Üç seçenek vardı: (a) yargıç istemcisini guard'sız kurmak, (b) engellemeyi sessizce puanlama başarısızlığı saymak, (c) davranışı belgelemek. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-330. | Guard maskelemesinin puan kalitesini ölçülebilir şekilde bozduğu görülürse yargıç için guard'sız bir kurulum yolu (a) eklenebilir. |
| **K-331 — `RunScore.Author` yargıç puanlarında `judge:{ad}` ile BİLEREK DOLU yazılır (Faz 49)** | 2026-08-07 | K-239'un tersine NULL semantiği ("author NULL iken benzersizlik hiç uygulanmaz, her çağrı yeni satır açar") insan geri bildirimi için kasıtlıydı (kimliksiz kurulumda her tıklama ayrı satırdır). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-331. | — |
| **K-332 — Cevrimiçi değerlendirme pencere özeti BELLEK İÇİDİR; yeni bir SQL sorgu yüzeyi açılmadı (Faz 49)** | 2026-08-07 | `GET /api/evaluation/online`'ın "son 1 saatteki ortalama puan" sorusu `run_scores`'u zaman aralığına göre tarayan yeni bir agregasyon sorgusu (3 diyalekt) gerektirirdi… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-332. | Çok örnekli bir dağıtımda pencerenin örnekler arasında tutarlı olması istenirse kalıcı bir sayaç deposuna (veya gerçek bir SQL agregasyonuna) geçilir. |
| **K-333 — `OnlineEvalJobHandler` DI'da hem `IJobHandler` hem KENDİ somut tipiyle kayıtlıdır (Faz 49)** | 2026-08-07 | `POST /api/runs/{id}/judge` ucu, iş kuyruğu isleyicisiyle AYNI kodu (`JudgeRunAsync`) paylaşmak için `OnlineEvalJobHandler`'ı somut tipiyle ister. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-333. | Aynı desen (HTTP ucu + iş kuyruğu isleyicisinin ortak mantığı) tekrar gerekirse bu iki-kayıt deseni doğrudan uygulanır. |
| **K-334 — K-057 güncellendi: AgentPrism artık MCP istemcisi VE sunucusudur (Faz 50)** | 2026-08-08 | Eski metin "AgentPrism yalnızca istemcidir" diyordu; bu artık doğru değil. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-334. | K-057'nin bağımlılık yönü (istemci `.Core`'da, sunucu yalnız `.AspNetCore`'da) bozulursa yeniden açılır. |
| **K-335 — A2A sunucu maliyeti yeniden ölçüldü: "+2 paket" değil "+4 paket"; iki paket plan taslağında hiç yoktu (Faz 50)** | 2026-08-08 | Plan (50.1) yalnız `Microsoft.Agents.AI.Hosting.A2A` + `A2A`'yı ölçmüştü ("artımlı maliyet iki pakettir"). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-335. | — |
| **K-336 — A2A her disa acik agent icin AYRI bir alt yol ve AYRI bir agent karti kullanir; tekil kart varsayimi terk edildi (Faz 50)** | 2026-08-08 | Plan taslağının doğrulama örneği `/agentprism/a2a/.well-known/agent-card.json` (tekil, agent adı yok) varsayıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-336. | A2A SDK'sı ileride çoklu-agent tek kart desteği eklerse yeniden değerlendirilir. |
| **K-337 — MCP/A2A dış çağrısı `ChildAgentInvoker`'ı KULLANMAZ; `ExternalAgentProxy`/`CatalogToolCallHandler` her zaman YENİ bir kök çalıştırma açar (Faz 50)** | 2026-08-08 | `ChildAgentInvoker` bir agent'ın BAŞKA bir agent'ı çağırmasını modeller ve `AgentPrismRunContext.Current` üzerinden ambient bir ÜST kapsam (derinlik, bütçe, kiracı) bekler — dış çağıran bir agent değildir, böyle bir üst kapsamı yoktur ve `ChildAgentInvoker.Refuse()` "calistirma kapsami yok" ile HER ZAMAN reddederdi… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-337. | — |
| **K-338 — MCP/A2A dış yüzeyleri erişim ayarlarını `IApplicationBuilder.Properties` üzerinden `MapAgentPrism`'den DEVRALIR; `MapAgentPrism` önce çağrılmalıdır (Faz 50)** | 2026-08-08 | `MapAgentPrismMcpServer`/`MapAgentPrismA2A` ayrı, opsiyonel `IEndpointRouteBuilder` uzantılarıdır (K1'in "tek giriş noktası" ruhunu korumak için `MapAgentPrism`'in kendisine gömülmediler — MCP/A2A herkesin ihtiyaç duymadığı ek paketler taşır). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-338. | — |
| **K-339 — `ChildRunApproval` public yapıldı: üçüncü tüketici MCP/A2A dış çağrı katmanıdır (Faz 50)** | 2026-08-08 | K-103'ün "onay isteyen bir alt çalıştırma `Failed` kapanır ve çağırana anlaşılır bir hata döner" sınırı artık İKİ değil ÜÇ yerde uygulanıyor: `ChildAgentInvoker` (Core), `RunRecordingAgent` (Core), ve şimdi `CatalogToolCallHandler`/`ExternalAgentProxy` (AspNetCore). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-339. | — |
| **K-340 — Dışa açılan bir agent'ın `AgentRunBudget.MaxDepth` değeri, kaç seviye TORUN çağrısına izin verildiğidir; "0" = hiç, "1" = bir seviye (Faz 50)** | 2026-08-08 | Plan taslağının DoD'si "`MaxDepth = 1` iken dışarıdan çağrılan agent alt agent çağıramaz" diyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-340. | — |
| **K-341 — Vektör gömüsü metin biçiminde (`::vector` cast) yazılır, hiçbir vektör paketi alınmaz (Faz 51)** | 2026-08-08 | Üç aday ölçüldü: `Microsoft.SemanticKernel.Connectors.PgVector` 1.74.0-preview `Npgsql 8.0.7`'ye karşı derlenmiş (bizde 10.0.3); `Pgvector` 0.3.2 `Npgsql 8.0.5`'e karşı; `Microsoft.Extensions.VectorData.Abstractions` 10.8.0 tek başına işe yaramaz (K-343). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-341. | `Npgsql` sürüm kayması deseni bu paketlerde giderilirse veya ikili biçim ölçülebilir bir kazanç gösterirse. |
| **K-342 — K-105 güncellenir: `ChatHistoryMemoryProvider` yine bağlanmadı, sebep artık ölçülmüş (Faz 51)** | 2026-08-08 | K-105'in yeniden açılma koşulu ("`VectorStore` implementasyonu ve embedding sağlayıcısı seçildiğinde") kısmen karşılandı — embedding sağlayıcısı çözüldü (tüketici kaydeder) ama `VectorStore` çözülmedi: ölçüldü (`Microsoft.Extensions.VectorData.Abstractions` 10.8.0), `VectorStoreCollection<TKey,TRecord>.GetAsync`… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-342. | Microsoft bu tipin AOT-güvenli bir filtre yüzeyi sunarsa, ya da tüketici ifade ağacı çevirmenin maliyetini kabul ederse. |
| **K-343 — Anlamsal arama yalnız PostgreSQL'de uygulanır (Faz 51)** *(kullanıcı kararı, 2026-08-06)* | 2026-08-08 | SQL Server'ın yerel `VECTOR` tipi ve SQLite'ın `sqlite-vec` uzantısı bu fazda ÖLÇÜLMEDİ (SQLite için: `SQLitePCLRaw` yerel kütüphanemiz bunu taşımıyor). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-343. | SQL Server `VECTOR` tipi veya SQLite `sqlite-vec` ölçülüp bir tüketici/katkı bu sağlayıcılardan birine somut bir `IVectorSearchStore` eklerse. |
| **K-344 — Sql.Shared'in cross-provider katmanı `IVectorSearchStore` için kullanılmaz (Faz 51)** | 2026-08-08 | K-176'nın "paylaşılan katman, sağlayıcıdan bağımsız SQL üretir" kuralı 20 ÇOK-SAĞLAYICILI depo içindir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-344. | SQL Server veya SQLite için somut bir uygulama eklenirse, o zaman ortak bir arayüz zaten `IVectorSearchStore`'un kendisidir — yeni bir soyutlama katmanına gerek yoktur. |
| **K-345 — `document_embeddings.metadata` sütunu `jsonb`'dir, `json` değil (Faz 51)** | 2026-08-08 | K-027'nin "`$type` ilk özellik olmalı" kısıtı burada GEÇERLİ DEĞİLDİR: bu alan polimorfik `ChatMessage` taşımaz, düz bir `string → string` sözlüktür; `jsonb`'nin anahtar yeniden sıralaması zararsızdır ve indekslenebilirlik kazançtır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-345. | — |
| **K-346 — Migration şablonlama genelleştirildi: `SqlStoreContext.MigrationTemplateValues` (Faz 51)** | 2026-08-08 | `{dimension}` yer tutucusu `{schema}` ile AYNI mekanizmadan geçemezdi (`SqlQueriesBase.ApplySchema` yalnız şemayı bilir) ama "vektöre özel" bir çözüm de yanlış katmana ait olurdu (`MigrationRunner` üç sağlayıcıda ortaktır, K-176). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-346. | — |
| **K-347 — K-218 kapatıldı: `ToolMethodScanner` örnek metotları TARAMA ANINDA reddeder (Faz 52)** | 2026-08-08 | Eski kod `arguments.Services is { } services ? ... **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-347. | MAF `functionInvocationServices`'i `AsAIAgent` üzerinden akıtırsa (K-218'in kendi yeniden açılma koşulu) örnek metot desteği eklenebilir. |
| **K-348 — Kaynak üreteci ayrı bir NuGet paketi değildir; `AgentPrism.Core` nupkg'sinde `analyzers/dotnet/cs/` altında taşınır (Faz 52)** | 2026-08-08 | Üç seçenek tartıldı (52.4): ayrı paket tüketiciye ikinci bir `PackageReference` yükler ve unutulursa sessizce hiçbir şey üretilmez; `AgentPrism.Abstractions` içine koymak `netstandard2.0` (üreteç) ile `net8/9/10` (Abstractions) hedef çakışması yaratır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-348. | — |
| **K-349 — Kaynak üretecinin Roslyn sürümü `Microsoft.CodeAnalysis.CSharp` `4.8.0`'dır (Faz 52)** | 2026-08-08 | Plan taslağı `4.14.0` öneriyordu ama "uygulama anında ölçülmelidir" diye işaretlemişti. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-349. | AgentPrism'in minimum desteklenen .NET 8 SDK yama sürümü K-005 ile birlikte yükseltilirse ölçüm tekrarlanmalıdır. |
| **K-350 — `AddGeneratedTools()` `IAgentPrismBuilder`'a EKLENMEDİ; derlemeye özel üretilmiş bir uzantı metodudur (Faz 52)** | 2026-08-08 | Planın "Planlanan Public API" taslağı bunu arayüz üyesi gösteriyordu ama aynı planın 52.3 bölümü ve Açık Soru 7'nin cevabı ("internal statik sınıf + uzantı metodu") zaten çelişiyordu; teknik olarak da imkânsızdı — `AgentPrism.Core` derlenirken tüketicinin `AgentPrism.Generated` ad alanındaki sınıfı henüz YOKTUR, Core ileri referans veremez. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-350. | — |
| **K-351 — Parametre tipi beyaz listesi `record`/`class` (composite) tipleri KAPSAMAZ; `APG0003` ile reddedilir (Faz 52)** | 2026-08-08 | Açık Soru 5 "composite dahil beyaz liste" önermişti. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-351. | Composite tip desteği istenirse, ikinci bir kaynak üreteci (`JsonSerializerContext` üretimi) ile TEK derleme geçişinde çok-üreteç etkileşiminin gerçekten çalıştığı ÖLÇÜLMELİDİR — varsayılmamalıdır. |
| **K-352 — F-76 (OpenAPI 500) YALNIZ `ProjectReference` tüketicisini etkiler; düzeltme kütüphanede değil, örnek uygulamanın derlemesindedir (2026-08-08 denetimi)** | 2026-08-08 | Rapor "`AddOpenApi()` kullanan HER çok-sağlayıcılı tüketici için `/openapi/v1.json` kırık" diyordu; **kapsam ölçülüp daraltıldı.** Kök neden doğrulandı: Ölçümün tamamı: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md). | K-185 kaldırılır (Sql.Shared gerçek bir paket olur) veya `dotnet/aspnetcore` üreteci çoklu derlemede aynı doküman kimliğini tolere ederse — o zaman hedef ve testi kaldırılır. |
| **K-353 — `.UseMcp()` yapılandırmayı AÇIKÇA alır; AgentPrism kendiliğinden `IConfiguration` okumaz (2026-08-08 denetimi)** | 2026-08-08 | Ölçülmüş kusur: `AgentPrismMcpOptions.SectionName` (`"AgentPrism:Mcp"`) tanımlıydı ama **hiçbir kod `Bind` çağırmıyordu** — `services.AddOptions<AgentPrismMcpOptions>()` yalnız kod-taraflı `configure` delegesini kabul ediyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-353. | — |
| **K-354 — SQL'e dokunan arka plan servisleri `SchemaReadyGate`'i bekler; hosted service kayıt sırası ZORLANMAZ (2026-08-08 denetimi)** | 2026-08-08 | Faz 42'de ölçülen kusur: `MigrationHostedService.StartAsync` migration'ları TAM bekler ama `BackgroundService` taban sınıfının `StartAsync`'i `ExecuteAsync`'i **beklemeden döner**. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-354. | — |
| **K-355 — Çalıştırmanın alt yazmaları BEKLENEN kiracıyı taşır; ambient kiracı KULLANILMAZ ve alan HTTP sözleşmesine girmez (2026-08-08 denetimi)** | 2026-08-08 | K-280'in kapattığı boşluk: `IRunStore.AppendEventAsync`, `CompleteRunAsync`, `UpdateRunCostAsync`, `RecordToolInvocationAsync` hiçbir kiracı süzgeci taşımıyordu. Ölçümün tamamı: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md). | — |
| **K-356 — API anahtarı ozeti SHA-256'dır; Argon2 DEĞİL (Faz 53, Açık Soru 2)** | 2026-08-08 | Anahtar 32 baytlık RASTGELE bir değerdir, kullanıcı parolası değildir; sözlük saldırısı söz konusu değildir ve yavaş bir hash (Argon2) yalnızca her isteğe gecikme ekler, güvenlik kazandırmaz. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-356. | Anahtar üretim kaynağı kullanıcı girdisine (düşük entropili) bağlı hale gelirse yeniden değerlendirilir. |
| **K-357 — API anahtarı doğrulaması önbelleklenmez; her istekte `key_hash` ile SQL aranır (Faz 53, Açık Soru 3, seçenek A)** | 2026-08-08 | Önbellek (seçenek B) iptalin ne kadar gecikeceği sorusunu açardı ve ölçülmeden eklenen bir karmaşıklıktı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-357. | Ölçülen p99 gecikme hot path'i gözle görülür yavaşlatırsa (ör. MCP/A2A yüksek QPS) kısa ömürlü bir bellek önbelleği eklenir — iptalin gecikme süresi o zaman açıkça kararlaştırılır. |
| **K-358 — `last_used_at` her istekte değil, en az bir dakikada bir yazılır (Faz 53, Açık Soru 4, seçenek B)** | 2026-08-08 | Her istekte yazmak sıcak okuma yoluna gereksiz bir `UPDATE` ekler ve okuma ağırlıklı bir uca yazma maliyeti bindirir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-358. | Denetim ihtiyacı saniye hassasiyeti isterse eşik yapılandırılabilir hale getirilir. |
| **K-359 — `Authorization` başlığı sunulduğunda, statik `AuthToken` tanımsız olsa bile doğrulanmalıdır; eşleşmezse 401 (Faz 53, bilinçli davranış değişikliği)** | 2026-08-08 | Eski davranış: `AuthToken` ayarlanmamışsa filtre hiçbir bearer denetimi yapmıyordu — başlık ne taşırsa taşısın istek geçiyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-359. | — |
| **K-360 — API anahtarı kapsam (`scope`) denetimi yalnız agents/runs/external-invoke uçlarına uygulandı; tam taksonomi ERTELENDİ (Faz 53)** | 2026-08-08 | `ApiKeyScope` beş değer tanımlar ama HTTP yüzeyi 100'ün üzerinde uç taşır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-360. | Kalan uçlar (webhook, kota, retention, skill, workflow yönetimi vb.) için scope taksonomisi genişletilmek istenirse ayrı bir aday kalemi (`ApiKeyScope`'a yeni değerler + ilgili `Endpoints.cs` dosyalarına `RequireApiKeyScope` eklenmesi) olarak ele alınır. |
| **K-361 — `docs/MIMARI.md` sıcak yol bütçesi 42.000 → 44.000 bayt (Faz 53)** | 2026-08-08 | Faz 53 "Güvenlik Modeli" bölümüne gerçek, kalıcı bir mimari katman ekledi (API anahtarı doğrulama akışı + kiracı öncelik sırası); bu narrative/geçmiş anlatı değil, **bugünkü** mimarinin bir parçasıdır ve `docs/arsiv/`'e taşınamaz (protokolün "bugünkü mimari değiştiyse ilgili bölümü düzelt" kuralı önce denendi — bearer token katmanı ve kiracı çözümleme diyagramı yerinde güncellendi, eklenmedi)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-361. | Bir sonraki fazda dosya yine sınıra yaklaşırsa, bu kez gerçekten narrative olan eski bir alt bölüm (ör. tarihsel gerekçe içeren bir not) `docs/arsiv/FAZ-GECMISI.md`'ye taşınarak çözülür — bütçe ikinci kez büyütülmez. |
| **K-362 — Heartbeat toplu yazılır: `IRunStore.TouchHeartbeatAsync` tek `Guid` değil `IReadOnlyCollection<Guid>` alır (Faz 54, Açık Soru 1, seçenek B)** | 2026-08-09 | Plan taslağı imzası tek `runId` alıyordu ama aday listesinin kendi önerisi ("tek bir `UPDATE ... **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-362. | — |
| **K-363 — `RunErrorClass.Infrastructure` yeni değer olarak eklendi (Faz 54, Açık Soru 3, seçenek A'nın düzeltilmiş hâli)** | 2026-08-09 | Plan "Infrastructure" sınıfının zaten var olduğunu varsayıyordu; `RunErrorClass` enum'ı okununca böyle bir değer YOKTU. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-363. | — |
| **K-364 — Oksuz hata parmak izi SABİT bir dize (`"orphaned"`); `ErrorFingerprint.Compute` ÇAĞRILMAZ (Faz 54)** | 2026-08-09 | `ErrorFingerprint` `AgentPrism.Core`'da `internal`'dır; `SqlRunStore` (`AgentPrism.Sql.Shared`, ayrı derleme) ona erişemez (K-176'nın "paylaşılan dosya sağlayıcıya özgü ad alanına referans veremez" kuralının derleme-sınırı kardeşi). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-364. | ErrorFingerprint bir gün paylaşılan (Abstractions) bir yardımcıya taşınırsa yeniden değerlendirilir. |
| **K-365 — Heartbeat/oksuz-kapama sorguları dizi parametresi (`WHERE id IN (@array)`) KULLANMAZ; tekil `UPDATE` döngüsü ve alt-sorgulu tek `UPDATE` tercih edildi (Faz 54)** | 2026-08-09 | `AddUuidArray` SQL Server/SQLite'ta `System.Text.Json` ile diziyi KÜÇÜK harfli JSON'a serileştirir; `runs.id` ise `DbHelpers.Add` ile BÜYÜK harfli yazılır (K-191). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-365. | — |
| **K-366 — `ClaimOrphanedRunsAsync` kapattığı çalıştırmanın `RunFailed` olayını da KENDİSİ yazar; ayrı bir yazma turu yok (Faz 54)** | 2026-08-09 | `RunEventWriter` o çalıştırmayı yazan süreçte artık YOKTUR (süreç çökmüştür) — olayı biri yazmak zorundadır ve sıra numarası mevcut en büyük değerin bir fazlası olmalıdır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-366. | — |
| **K-367 — `MapAgentPrismMcpServer`/`MapAgentPrismA2A` onay-yüzeyi denetimi, `Map*()` sırasında senkron çalışan bir kontrolden istek-bazlı `IEndpointFilter`e taşındı (kullanıcı kararı)** | 2026-08-09 | Eski kod `Map*()` içinde `catalog.ListAsync().AsTask().GetAwaiter().GetResult()` ile SENKRON okuma yapıyordu; tamamen boş bir veritabanında (migration'lar henüz koşmamışken) bu çağrı tabloyu bulamayıp çöküyordu — `Map*()` `IHostedService`lerden ÖNCE, `MigrationHostedService` şemayı kurmadan önce çalışır… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-367. | — |
| **K-368 — `RunStatus.AwaitingApproval` eklendi; onay kararından sonra AYNI `RunId` devam ETMEZ, `AwaitingInput` emsaliyle birebir aynı şekilde YENİ bir çalıştırma açılır (Faz 55, kullanıcı kararı)** | 2026-08-09 | Askıya alınmış bir çalıştırmanın satırını değiştirmek K-014'ün (olay/çalıştırma geçmişi ekleme-yalnız) ihlalidir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-368. | — |
| **K-369 — `pending_approvals.run_id`, `runs(id)`e `ON DELETE CASCADE` ile bağlı; sözleşme testleri gerçek bir `runs` satırı önceden kuran bir `PrepareRunAsync` kancası kazandı (Faz 55)** | 2026-08-09 | Yabancı anahtar olmadan rastgele bir `RunId` ile `pending_approvals` satırı yazmak veritabanı düzeyinde referans bütünlüğünü bozardı — bekleyen onay her zaman gerçek bir çalıştırmanın izdüşümüdür, çalıştırma silinirse (ör. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-369. | — |
| **K-370 — `ApprovalEndpoints.DecideAsync`, `audit` kaydını mutasyondan ÖNCE ve `AuditRecorder.WriteAsync` (hataları yutan sarmalayıcı) DEĞİL doğrudan `IAuditLog.WriteAsync` ile yazar (Faz 55)** | 2026-08-09 | K-089'un "audit kaydı mutasyonu kilitler" deseni burada da geçerlidir: bir onay/red kararı geri alınamaz bir eylemdir (yeni bir çalıştırma açar), bu yüzden audit yazımı BAŞARISIZ olursa karar hiç uygulanmamalıdır… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-370. | — |
| **K-371 — `IPendingApprovalStore.ExpireAsync`, planın taslak imzası `ValueTask<int>` yerine `ValueTask<IReadOnlyList<PendingApproval>>` döner (Faz 55, plandan sapma)** | 2026-08-09 | `docs/arsiv/fazlar/55-ASENKRON-ONAY-KUTUSU.md`'nin ilk taslağı yalnız süresi dolan kayıt SAYISINI döndürüyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-371. | — |
| **K-372 — Senkron/MCP/A2A çalıştırma yolu `pending_approvals`'a HİÇ yazmaz; yalnız kuyruktan koşan (`Prefer: respond-async`) çalıştırmalar yazar (Faz 55, plandan sapma — planın Açık Soru 1 önerisi "B: her ikisi" idi, uygulanan "A: yalnız kuyruk")** | 2026-08-09 | Plan, operatörün "bekleyen her şeyi" tek ekranda görmesi gerekçesiyle B'yi önerdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-372. | Playground'un da bekleyen onayları operatör ekranında görmesi istenirse, senkron yoldaki `approvals` alanının kendisi (oturum durumu) okunarak ayrı bir salt-okunur izdüşüm eklenebilir — `pending_approvals` tablosuna yazmadan. |
| **K-373 — Kanarya kuralı yalnızca İKİ kollu deneylerde tanımlanabilir; planın "kalan kollar kontrol sayılır" (çoğul) ifadesi UYGULANMADI (Faz 56, plandan sapma)** | 2026-08-09 | N>2 kollu bir deneyde kanarya ağırlığı arttıkça geri kalan ağırlığın birden fazla kontrol koluna ORANTILI dağıtılması gerekirdi; bu, `ExperimentAssignmentResolver`'ın kova aralıklarını her ramp adımında TÜM kontrol kolları için yeniden hesaplamayı gerektirir ve "var olan oturumlar kolunu değiştirmez" garantisini (56.4) yalnız kanarya-kontrol sınırı için değil, kontrol kolları ARASINDAKİ sınır için de kanıtlamayı ister — bu ikinci kanıt genel halde YOKTUR (bir kontrol kolunun payı küçülürken sınırın hangi tarafında hangi oturumların kaldığı deterministik değildir)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-373. | N kollu kanarya gerçekten istenirse, orantılı kontrol dağıtımının oturum kararlılığını nasıl koruduğu AYRI bir kanıtla (test) gösterilmeden bu kısıt kaldırılmaz. |
| **K-374 — `ExperimentAssignmentResolver.SelectVariant`, kanarya kuralı tanımlıyken kanarya kolunu HER ZAMAN `[0, ağırlık)` aralığına yerleştirir — bu, `Experiment.Variants`'ın FİZİKSEL sırasından bağımsızdır (Faz 56)** | 2026-08-09 | İlk uygulama kova aralıklarını `Variants` listesindeki sıraya göre hesaplıyordu (mevcut, Faz 19'dan kalan davranış); bir deney `[control, canary]` sırasıyla oluşturulup ramp ile ağırlık artınca kanarya kolunun aralığı `[95,100)`'den `[75,100)`'e KAYIYOR, control-atanmış bazı oturumlar sohbetin ORTASINDA kanaryaya GEÇİYORDU — tam olarak 56.4'ün yasakladığı davranış. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-374. | — |
| **K-375 — `CanaryPolicy`'ye ayrı bir `RampRequiresSampleSize` alanı AÇILMADI; kademeli artırma da `MinSampleSize`'ı AYNEN kullanır (Faz 56, plandan sapma)** | 2026-08-09 | Planın 56.4 bölümü düz yazıda `RampRequiresSampleSize` adlı üçüncü bir eşik alanından bahsediyordu ama "Planlanan Public API" kod örneği bu alanı HİÇ içermiyordu — plan kendi içinde tutarsızdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-375. | — |
| **K-376 — `CanaryPolicy`'ye Faz 49'unkine benzer ayrı bir `EvaluationWindow` eklenmedi; kanarya kararı `ExperimentVariantResult`'ın TÜM-ZAMANLI (deney başından beri biriken) sonuçlarına dayanır (Faz 56, plandan sapma, Açık Soru 5)** | 2026-08-09 | Planın Açık Soru 5'i "B: ayrı ayar, varsayılanı A'dan (Faz 49) alır" öneriyordu, ama bunu uygulamak `ExperimentResultsQuery`'ye bir zaman aralığı parametresi eklemeyi ve üç SQL sağlayıcısında da `SelectExperimentResults`'ı zaman filtreli hale getirmeyi gerektirirdi — "Planlanan Dosya Listesi" bu değişikliği HİÇ öngörmüyordu… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-376. | Bir kanarya kolu çok uzun süre (haftalar) canlı kalıp veri birikimi anlamlılığını yitirirse, o zaman ayrı bir pencere değerlendirilir — bugün ölçülen bir ihtiyaç değildir. |
| **K-377 — `ExperimentVariantResult`e `AverageScore` eklendi; `SelectExperimentResults` sorgusu `run_scores`'a (önce çalıştırma başına ortalama, sonra kol başına o ortalamaların ortalaması) genişletildi (Faz 56)** | 2026-08-09 | `CanaryPolicy.MinScore` kararı (DoD ve `CanaryEvaluatorTests` açıkça ister) kanarya kolunun ortalama puanına ihtiyaç duyar ama `run_scores`'u okuyan hiçbir deney sorgusu yoktu… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-377. | — |
| **K-378 — Kademeli artırma adımı denetim izine YAZILMAZ; yalnız otomatik GERİ ALMA `IAuditLog.WriteAsync` ile mutasyondan ÖNCE (K-089 emsali) yazılır (Faz 56)** | 2026-08-09 | 56.2'nin akış şeması yalnız "GERİ AL" dalından sonra `audit_log'a YAZ` kutusu taşır; "ağırlığı bir adım artır" dalında böyle bir kutu YOKTUR. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-378. | — |
| **K-379 — `IExperimentStore.SetCanaryPolicyAsync`, `SaveAsync`'in Draft-yalnız kısıtından MUAFTIR; kanarya kuralı deney Running iken de tanımlanabilir veya kaldırılabilir (Faz 56)** | 2026-08-09 | Bir kanarya kuralı, deney ZATEN trafik alırken de eklenmek istenebilir (örn. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-379. | — |
| **K-380 — `CompiledAgentCache`'in anahtarına `TenantId` eklendi; iki farklı kiracının aynı ad+sürüm+bağımlılık parmak izinde bir tanımı olması artık BİRİNCİ kiracının derlenmiş agent'ını İKİNCİ kiracıya sızdırmaz (manuel kabul testi hazırlığı, kullanıcı talebiyle bulundu)** | 2026-08-10 | Onbellek anahtarı `(Name, Version, DependencyFingerprint)` idi — kiracı boyutu yoktu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-380. | — |
| **K-381 — Paylaşılan `AgentFileStore` (dosya belleği/metin araması) her kiracı için `TenantPrefixingAgentFileStore` ile sarmalanır; kiracı sınırı MAF'ın kendi deposuna dokunmadan bir vekil (proxy) katmanında uygulanır (manuel kabul testi hazırlığı)** | 2026-08-10 | `AgentDefinitionCompiler`'ın `TextSearchProvider` arama callback'i (`Func<string, CancellationToken, ...>`) hangi kiracının aradığını bilmez ve derleme anında KOK "/" dizininden yinelemeli arama kurar; depo (`InMemoryAgentFileStore`, `TryAddSingleton`) tüm kiracılar arasında PAYLAŞILIR. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-381. | Kiracı-İÇİ ajan/oturum yalıtımı istenirse F-105 (ambient/`AsyncLocal` tabanlı bir "güncel oturum" kapsamı) ele alınmalı — `AsyncLocal` bu kod tabanında dört kez yanlış açılmıştı (Faz 6/11/12/15), beşinci deneme dikkatli tasarım ister. |
| **K-382 — `AgentPrismEndpointFilter`'a `CheckTenancyWhitelist` eklendi; `AllowedTenants` doluyken listede olmayan bir aday artık istek endpoint'e ulaşmadan 403 ile reddedilir, varsayılan kiracıya SESSİZCE düşmez (manuel kabul testi hazırlığı)** | 2026-08-10 | `HttpTenantContext.TenantId`'nin `??` zinciri (`... ?? Resolve() ?? **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-382. | — |
| **K-383 — `AgentPrism.Core.csproj`'un üreteç-paketleme hedefi `@(Analyzer)` yerine Generators projesinin `GetTargetPath` çıktısını MSBuild görevi ile okur; `dotnet pack --no-build` artık `analyzers/dotnet/cs/AgentPrism.Generators.dll`'i İÇERİR (manuel kabul testi hazırlığı — daha önce Kritik olarak not düşülmüştü)** | 2026-08-10 | `@(Analyzer)` item grubu yalnız `ResolveReferences` gecişiyle doldurulur ve bu geçiş BUILD'e bağlıdır; `--no-build` onu hiç çalıştırmaz — ölçüldü: `dotnet pack --no-build` sonrası `analyzers/dotnet/cs/` klasörü BOŞ çıkıyordu (`artifacts/package/release/`'deki son dört `AgentPrism.Core` paketinin DÖRDÜNDE de eksikti). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-383. | — |
| **K-384 — SSE akış uçlarındaki (`AgentEndpoints`, `OpenAIResponsesEndpoints`, `OpenAIChatCompletionsEndpoints`) dar istisna filtresi kaldırıldı; `OperationCanceledException` dışındaki HER istisna artık bir `error` çerçevesine dönüşür (K-296'nın tamamlanması, manuel kabul testi hazırlığı)** | 2026-08-10 | K-296 yalnız KAYDEDİLEN `RunError.Class` alanını düzeltmişti; SSE akışının KENDİSİ hâlâ `catch (Exception ex) when (ex is AgentPrismException or InvalidOperationException or HttpRequestException)` ile sınırlıydı — gerçek sağlayıcı SDK istisnaları (OpenAI'nin `ClientResultException`'ı, Anthropic'in `AnthropicApiException`'ı, ikisi de bu üç tipe uymaz) baglantıyı `error` çerçevesi ÜRETMEDEN kapatıyordu; istemci (playground dahil) bunu SESSİZ BAŞARI sanıyordu — `GET /api/runs/{id}` gerçek durumun `Failed` olduğunu gösterse bile. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-384. | — |
| **K-385 — K-302 güncellendi: `AddCaseAsync`'in yeniden deneme döngüsüne jitter eklendi, üst sınır 5 → 10'a çıkarıldı** | 2026-08-12 | `EvalStoreContract.AddCaseAsync_es_zamanli_terfiler_farkli_seq_uretir` (8 eş zamanlı çağrı) PostgreSQL'de art arda iki koşuda da `5 denemede sira numarasi atanamadi` ile GERÇEKTEN ve tekrarlanabilir şekilde başarısız oldu — hipotetik değil, ölçülen bir yakınsama kırılganlığıydı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-385. | Aynı test 10 denemeyle de tekrarlanabilir şekilde başarısız olursa (daha yüksek eş zamanlılık veya daha yavaş donanım), jitter aralığı büyütülür veya K-302'nin ON CONFLICT/MERGE'den kaçınma kararı yeniden değerlendirilir. |
| **K-386 — K-317 kapandı: Docker Desktop 4.29.0 → 4.86.0 güncellemesi gerçek `mssql/server`'daki Rosetta hatasını çözdü; SQL Server sözleşme testleri bu makinede artık `azure-sql-edge` ikamesi OLMADAN, gerçek imajla koşuyor** | 2026-08-12 | K-317'nin yeniden açılma koşulu ("Docker Desktop güncellenir/Rosetta gerçekten çalışırsa ... **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-386. | Bu makine Docker Desktop'ı tekrar çok eski bir sürüme düşürürse veya başka bir Apple Silicon makinede aynı hata görülürse: önce Docker Desktop sürümünü host macOS sürümüyle karşılaştır; K-317'nin `azure-sql-edge` + özel `IWaitUntil` tekniği hâlâ geçerli bir geçici ikamedir. |
| **K-387 — `SqlServerTestContext.DisposeAsync` artık test şemasını GERÇEKTEN bırakır (dokümantasyon iddia ediyordu, kod yapmıyordu); deadlock'a çarparsa 5 denemeli backoff ile yeniden dener** | 2026-08-12 | `DisposeAsync`'in XML belgesi "SQL Server'da her test kendi semasını birakir" diyordu ama gövde yalnız `DataSource.DisposeAsync()` çağırıyordu — şema HİÇ silinmiyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-387. | — |
| **K-388 — `MigrationRunner.ApplyOneAsync` migration SQL'ini ve `INSERT INTO __migrations` kaydını TEK round-trip'te birleştirir; `CreateSchema`+`CreateMigrationsTable` birleşimi ve `SET XACT_ABORT ON` ile tam tek-round-trip BİLEREK yapılmadı** | 2026-08-12 | K-387 sonrası SqlServer paketi Postgres'ten hâlâ ~2,8 kat yavaştı (355,5 sn'e karşı 128,6 sn, tam çözüm koşusunda). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-388. | `EXEC(N'...')` ile ertelenmiş `CreateSchema`+`CreateMigrationsTable` birleşimi veya `XACT_ABORT`'lu tam tek-round-trip birileri gerçekten ölçülebilir bir ihtiyaç görürse (ör. round-trip sayısı yine baskın maliyet olur) ayrı bir karar olarak ele alınır. |
| **K-389 — Migration kilidi VERİTABANI genelinden SEMAYA/ONEĞE kapsandı: `SqlServerDialect`, `PostgresDialect`, `SqliteDialect` artık `sp_getapplock`/`pg_advisory_lock`/kilit dosyası anahtarını sema (veya SQLite'ta tablo öneki) adından türetir** | 2026-08-12 | K-387/K-388 sonrası bile SqlServer paketi hâlâ en yavaş kalemdi (261,8 sn izole koşum) — kök neden kilit anahtarının VERİTABANI genelinde sabit olması, farklı semalardaki migrasyonları TEK kuyrukta sıraya sokmasıydı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-389. | — |
| **K-390 — Sözleşme test izolasyonu "her test kendi şeması" modelinden "her test SINIFI kendi şeması + her test verisini `ResetDataAsync` ile sıfırlar" modeline geçti (K-389 ile birlikte)** | 2026-08-12 | K-389 tek başına serileştirmeyi kaldırdı ama işin HACMİNİ azaltmadı: SqlServer'da 479 test hâlâ 479 ayrı şema + tam migrasyon seti demekti. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-390. | Gelecekte bir migration `NO ACTION` bir FK eklerse SQL Server'ın hesaplanan silme sırası hâlâ doğru kalır (topolojik sıralama, sabit liste değil) — ama bu varsayım ihlal edilirse `ResetDataAsync` testleri (her sağlayıcıda birer `seed → reset → tüm sayımlar sıfır` testi) kırılır ve erken yakalar. |
| **K-391 — PostgreSQL: `CREATE EXTENSION IF NOT EXISTS vector` içeren `0024_vector.sql`, K-389 sonrası eş zamanlı ilk-kez migrasyonlarda benzersizlik ihlaline (`pg_extension_name_index`, SQLSTATE 23505) düşebilir — `MigrationRunner.ApplyOneAsync` bunu jitter'lı yeniden deneme ile karşılar, test fixture'ı ise uzantıyı bir kez ÖNCEDEN kurarak yarışı tamamen önler** | 2026-08-12 | K-390'ın 29 sınıf fixture'i eş zamanlı ilk-kez migrasyon çalıştırınca `pg_extension` (semaya değil VERİTABANI GENELİNE ait) üzerinde yarış oluştu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-391. | Uygulama başlangıcında (test dışı) iki bağımsız AgentPrism kurulumu aynı veritabanına GERÇEKTEN eş zamanlı ilk kez bağlanırsa `ApplyOneAsync`'in 8 denemesi teorik olarak yetersiz kalabilir (ölçülmedi, yalnız testin yapay 29-yönlü yükünde gözlemlendi) — böyle bir rapor gelirse jitter aralığı büyütülür. |
| **K-392 — `InvariantGlobalization` hem örnek uygulamadan (`samples/AgentPrism.Api`) hem paket şablonundan (`AgentPrism.Starter`) KALDIRILDI; SQL Server desteğiyle bağdaşmıyor** | 2026-08-13 | Manuel kabul testi (MT-SQL-024 ve §2/§3'ün SQL Server yarısı) `UseSqlServer()` yapılandırıldığında uygulamanın HİÇ açılmadığını ölçtü: `SqlConnection.Open` koşulsuz `System.NotSupportedException: Ölçümün tamamı: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md). | Bir tüketici ICU taşımayan bir taban imajda (ör. `alpine` + `icu-libs` kurulmamış) çalıştırmak isterse: `InvariantGlobalization` KENDİ projesinde açılabilir — ama o zaman `AgentPrism.SqlServer` kullanılamaz. Bu ikilem paket dokümanına yazılmalı; şablonun `--UseSqlServer` seçeneğine koşullu olarak geri eklemek DÜŞÜNÜLDÜ ve reddedildi (tüketici sonradan SqlServer eklerse sessizce tuzağa düşerdi). |
| **K-393 — `A2AApprovalGuardFilter`/`McpApprovalGuardFilter`: şema hazır değilken (`AutoApplyMigrations=false`) katalog sorgusu HEMEN uygulamayı durdurmak yerine sınırsız sayıda, üstel gecikmeli (5 sn'de tavanlanan) yeniden dener; deneme SAYISI değil yalnız uygulama kapanışı sınırlar (HATA-S1-002, manuel kabul testi S1)** | 2026-08-13 | `MT-SQL-004`/`014` ölçtü: `AutoApplyMigrations=false` ve şema henüz kurulmamışken uygulama `Now listening` yazıp HEMEN ardından kendini kapatıyordu — `A2AApprovalGuardFilter`/`McpApprovalGuardFilter` (uc bağlama anında BAŞLAYAN arka plan `Task`'lar) `SchemaReadyGate.MarkReady()`'nin (bu bayrak kapal… Ölçümün tamamı: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md). | Şema gerçekten hiç hazırlanmazsa (kalıcı yanlış yapılandırma) A2A/MCP istekleri süresiz beklemeye devam eder — bir operatör bunu "neden yanıt vermiyor" olarak bildirirse sınırlı deneme sayısı + açık bir hata durumu yeniden değerlendirilir. |
| **K-394 — Workflow çalıştırmaları artık kota muhasebesinden geçiyor: `WorkflowEndpoints.RunAsync` agent'larla AYNI `QuotaGate.CheckAsync` 429 kapısından geçer, `WorkflowRunner.CompleteAsync` workflow'un TAMAMINI (Depth 0, `TreeUsage`/`TreeCost` toplamından) TEK bir "run" olarak `QuotaEnforcer.RecordAsync`'e yazar (HATA-S1-006, Kritik, manuel kabul testi S1)** | 2026-08-13 | `MT-RET-033` ölçtü: `ozetle-ve-cevir` workflow'u iki kez art arda çalıştırıldı, `quota_usage.runs` **hiç artmadı** (`FARK=0`, deterministik) — `WorkflowEndpoints.RunAsync` `RunRecordingAgent`'a hiç uğramıyordu (kendi `IWorkflowHost` motoruyla çalışıyor), dolayısıyla ne 429 kapısı ne muhasebe hiç dev… Ölçümün tamamı: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md). | Sürdürme (`resume`/`respond`) uçlarının da kendi 429 ön-denetiminden geçmesi istenirse (bugün yalnız muhasebe alıyorlar, kapı almıyorlar) ayrı bir karar olarak ele alınmalı — K-162'nin "devam eden çalıştırma kesilmez" ilkesiyle çelişebilir. |
| **K-395 — `AgentPrismEndpointFilter`: `requireBearerToken:false` gruplarına (eşlenmemiş `api/*` yolları, gerçek zamanlı ses ucu) AgentPrism'in kendi statik `AuthToken`'ıyla eşleşen bir başlık artık REDDEDİLMİYOR — başlık YOKMUŞ gibi nötr davranılıyor (HATA-S1-014, manuel kabul testi S1)** | 2026-08-13 | `MT-MM-031`/`063` ölçtü: `requireBearerToken:false` kurulan bir grupta (kurucu `_authToken`'ı bilerek `null` yapar) geçerli statik bearer token'lı bir istek `401` alıyordu — `IApiKeyStore`'da aranıp bulunamadığı için (statik token bir API anahtarı DEĞİLDİR) genel `Unauthorized()`'a düşüyordu; doğru token, yanlış token ve HİÇ token arasında davranış farkı YOKTU, teşhisi yanıltıyordu ("bu uç yok" yerine "kimlik doğrulanamadı" görünüyordu)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-395. | — |
| **K-396 — `AgentDefinitionCompiler.SearchFileStoreAsync`: `TextSearchProvider`'ın doğal dil sorgusu artık `AgentFileStore.SearchAsync`'e ham regex olarak DEĞİL, boşluğa göre ayrılmış ≥3 karakterlik tokenlerin kaçışlanıp "VEYA" ile birleştirildiği bir desen olarak geçiyor (HATA-S1-009, manuel kabul testi S1)** | 2026-08-13 | `MT-MEM-013`/`014` `EnableTextSearch: true` bir agent'ın dosya belleğinde HARFİYEN mevcut bir dizgiyi ("FILE-7841") iki doğal dil sorgusuyla da BULAMADIĞINI ölçtü. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-396. | Bu bir sezgiseldir (uzunluk eşiği, dilden bağımsız stopword listesi yok) — kod seviyesinde daha iyi doğrulanmış bir arama (embedding tabanlı benzerlik, gerçek bir tokenizer) gerekirse yeniden değerlendirilir; `MT-MEM-013`/`014` bu düzeltmeyle yeniden koşulmalı. |
| **K-397 — `ApiKeyScope`'a `KnowledgeRead`/`KnowledgeAdmin` eklendi; `KnowledgeEndpoints`'in tüm uçları artık `RequireApiKeyScope` çağırıyor (HATA-S1-011, Yüksek, manuel kabul testi S1)** | 2026-08-13 | `MT-MEM-031` ölçtü: `scopes:["RunsRead"]` ile üretilen salt-okunur bir API anahtarı `DELETE /api/knowledge/{collection}/documents/{sourceId}`'i `204` ile çalıştırdı — `KnowledgeEndpoints`'in HİÇBİR ucu `RequireApiKeyScope` çağırmıyordu (kontrol grubu `AgentEndpoints`'te `403` veriyordu, yani kapsam… Ölçümün tamamı: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md). | Diğer dört yüzey (`WorkflowEndpoints`, `SchedulingEndpoints`, eval-experiment, `GovernanceEndpoints`) için AYNI kapsam denetimi eksikliği ayrı test case'leriyle doğrulanıp belgelenirse, aynı desenle (yeni tür ekle, mevcut kapsamı genişletme) kapatılmalı — K-53.3'ün "kapsam ∩ rol" ilkesi geçerli kalır. |
| **K-398 — `RunRecordingAgent.RunCoreStreamingAsync`: terminal durum artık tüketicinin ERKEN `DisposeAsync()`'i (dogal bitiş değil, istisna da değil) durumunda da yazılıyor — `Canceled`, o ana kadar biriken kısmi `usage` ile (HATA-S1-015, Yüksek, manuel kabul testi S1)** | 2026-08-13 | `MT-MM-078` ölçtü: gerçek zamanlı bir ses turunda kullanıcı `cancel` gönderdiğinde WebSocket katmanı doğru davranıyordu ama altındaki `runs` satırı KALICI OLARAK `Running`'de asılı kalıyordu (`completedAt`/`usage`/`cost` hep `null`) — gerçek OpenAI+ElevenLabs maliyeti oluşup hiç kaydedilmiyordu (ses… Ölçümün tamamı: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md). | — |
| **K-399 — `AgentPrismRetentionOptions`'a `RunInputs`/`VoiceSessions`/`RunScores` (varsayılan sırasıyla 30/30/180 gün) ve `DocumentEmbeddings` (varsayılan KAPALI, `MaxAgeDays=null`) eklendi — dört hedef daha önce `ForTarget`'ta `_ => null` dalına düşüp config varsayılanını SESSİZCE yok sayıyordu (HATA-S1-005, Düşük, manuel kabul testi S1)** | 2026-08-13 | `MT-RET-015` ölçtü: `Enabled=true` ve `RunInputs:MaxAgeDays=60` verilmiş hâlde `preview?target=run_inputs` `{"maxAgeDays":null,"enabled":false}` döndürüyordu — `AgentPrismRetentionOptions.ForTarget` 16 hedeften yalnız 12'si için ayar nesnesi döndürüyordu, kalan dördü (`run_inputs`, `voice_sessions`,… Ölçümün tamamı: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md). | `DocumentEmbeddings` için 30/60/90 gün gibi somut bir varsayılan istenirse (bugün yalnız açık bir veritabanı politikasıyla silinebiliyor) ayrı bir karar olarak değerlendirilmeli — bu seçim temkinli bir varsayılandır, ölçülmüş bir kullanıcı ihtiyacına dayanmaz. |
| **K-400 — Skill script çalıştırma iki ayrı kök nedenle TAMAMEN çalışmıyordu: (1) resolver'sız `JsonSerializerOptions`, (2) MAF'ın nullable-ama-required arguman semasını `null` ile reddetmesi (HATA-K-002, Kritik, manuel kabul testi Ortak Kuyruk)** | 2026-08-14 | `MT-SKILL-058` ölçtü: `UseSkillScripts()` + `AllowStoredScripts=true` açıkken, saklı script taşıyan HERHANGİ bir agent'a gönderilen HER istek, model hiç çağrılmadan `InvalidOperationException: JsonSerializerOptions instance must specify a TypeInfoResolver setting before being marked as read-only` ile çöküyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-400. | MAF'ın nullable-required şema/bağlama tutarsızlığı (kök neden 2) yalnız `AgentPrism.Core`'un KENDİ delege imzasıyla dolaylı olarak aşıldı — MAF'ın kendisi güncellenirse (örn. `null`'ı nullable parametreler için doğru kabul ederse) bu varsayılan-değer numarası gereksiz kalabilir, kaldırılabilir ama kaldırmak ZORUNLU değildir (geriye dönük hâlâ doğru çalışır). |
| **K-401 — `WorkflowRunner.ToRunError` artık `TargetInvocationException`/tek-elemanlı `AggregateException` sarmalayıcılarını soyar; gerçek neden `RunError.Message`'a yazılır (HATA-K-003, Kritik, manuel kabul testi Ortak Kuyruk)** | 2026-08-14 | `MT-WF-071`/`073` ölçtü: bir Magentic + `requirePlanApproval` iş akışında `maxIterations` plan+onay-sonrası-devam+katılımcı döngüsü için yetersiz kalınca (case'in kendi `maxIterations: Ölçümün tamamı: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md). | Raporun kendi açık sorusu ("(a) yalnız `maxIterations` ayarlama sorumluluğu tüketiciye mi ait, (b) AgentPrism'in round-limit'e ulaşıldığında akışı sonlandırma mantığında bir kusur mu") kısmen yanıtlandı: (b)'nin "opak hata" yarısı bu kararla kapandı; MAF'ın round-limit-sonrası fazla çağrısını TAMAMEN önleyip planı zarif bir `Completed`'e çevirmek (raporun "zarif durdurma" beklentisi) ayrı, daha kapsamlı bir tasarım kararı gerektirir — bu davranış MAF'ın kendi orkestrasyon durum makinesinin bir parçasıdır, AgentPrism'in bugünkü çalıştırıcısı bunu önceden kestiremiyor; ihtiyaç ölçülürse `docs/ADAYLAR.md`'ye yeni-yetenek adayı olarak yazılmalı. |
| **K-402 — `UseWorkflows()` artık `AgentPrismWorkflowOptions`'ı `AgentPrism:Workflows` bölümünden `IConfiguration`'a BAĞLIYOR (HATA-K-004, Kritik, manuel kabul testi Ortak Kuyruk)** | 2026-08-14 | `MT-WF-091`/`092`/`093` ölçtü: `Enabled=false`, `MaxSuperSteps=2`, `EnableCheckpointing=false` — üçü de ayrı ayrı `dotnet user-secrets` ile denendi, üçü de HİÇBİR ETKİ göstermedi; çalıştırmalar ayar hiç verilmemiş gibi normal tamamlandı (gerçek model çağrıları, gerçek ücretle). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-402. | — |
| **K-403 — `WorkflowRunner.RunStreamingAsync` gerçek bir `async IAsyncEnumerable` yineleyicisi yapıldı; `sessionId` doğrulaması artık SSE `event: error` üretiyor, düz `HTTP 500`'e düşmüyor (HATA-K-005, Kritik, manuel kabul testi Ortak Kuyruk)** | 2026-08-14 | `MT-WF-095`/`096` ölçtü: geçersiz `sessionId` (128 karakter sınırı aşımı veya izin verilmeyen karakter) gönderildiğinde istemci hiçbir SSE çerçevesi (ne `event: run` ne `event: error`) almıyordu — düz, teşhis bilgisi taşımayan bir `HTTP 500` (`{"title":"An error occurred while processing your request."}`) geliyordu; sunucu logunda doğru mesaj görülüyordu ama istemciye hiç ulaşmıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-403. | — |
| **K-404 — `POST/PUT /api/agents` artık `AgentDefinitionValidator.ValidateAsync`'i SAVE ZAMANINDA çağırıyor; bilinmeyen skill/tool/callable-agent adı `400` ile reddediliyor (HATA-K-001, Yüksek, manuel kabul testi Ortak Kuyruk)** | 2026-08-14 | `MT-SKILL-020` ölçtü: `skillNames` alanında var olmayan bir skill adı taşıyan bir agent'ı `POST /api/agents` ile kaydetmek `HTTP: 201 Created` ile BAŞARILI oluyordu — hiçbir doğrulama hatası yoktu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-404. | — |
| **K-405 — `WorkflowEndpoints` artık `ApiKeyScope.WorkflowsRead`/`WorkflowsAdmin`/`RunsRead`/`RunsWrite` uyguluyor (HATA-K-006, Yüksek, manuel kabul testi Ortak Kuyruk)** | 2026-08-14 | `MT-WF-100` ölçtü: `WorkflowEndpoints.Map` (`src/AgentPrism.AspNetCore/Endpoints/WorkflowEndpoints.cs`) HİÇBİR ucunda `.RequireApiKeyScope(...)` çağırmıyordu — `AgentPrismEndpointFilter.CheckScope` bir uçta kapsam metadata'sı YOKSA denetimi KOŞULSUZ geçiriyor (`if (requirement is null || record.Scopes.Contains(requirement.Scope)) return null;`). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-405. | Frontend `SCOPES` dizisinin backend `ApiKeyScope` enum'uyla senkron tutulması (Knowledge + Workflows dahil) ayrı bir iş kalemi; ihtiyaç ölçülürse `docs/ADAYLAR.md`'ye yazılmalı. |
| **K-406 — `BindRunRecording` artık `RecordRunInput`'ı config'ten okuyor (HATA-K-007, Yüksek, manuel kabul testi Ortak Kuyruk — aynı kök neden HATA-S2-002/HATA-S4-015'te de bağımsız bulunmuştu)** | 2026-08-14 | `MT-EVAL-092` ölçtü: `AgentPrism:RunRecording:RecordRunInput=false` `dotnet user-secrets`'a yazılıp uygulama yeniden başlatıldığında yeni bir çalıştırmanın girdisi YİNE tam olarak kaydediliyordu — `GET /api/runs/{id}/input` beklenen `404` yerine `200` + tam girdi (`messages: [...]`) döndürdü. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-406. | — |
| **K-407 — `EvalEndpoints`/`ExperimentEndpoints`/`RunEndpoints` artık Eval/Experiment/`RunsRead`/`RunsWrite` kapsamlarını uyguluyor (HATA-K-008, Yüksek, manuel kabul testi Ortak Kuyruk)** | 2026-08-14 | `MT-EVAL-100`/`101` ölçtü: bu koşum sırasında `ApiKeyScope` enum'ı `KnowledgeRead/Admin` ve `WorkflowsRead/Admin` dahil edilmiş olsa bile Eval/Experiment için HİÇBİR kapsam değeri taşımıyordu — `EvalEndpoints`/`ExperimentEndpoints` bu yüzden `.RequireApiKeyScope(...)` hiç ÇAĞIRAMAZDI (çağrılacak bir kapsam yoktu). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-407. | `SchedulingEndpoints` (`MT-JOB-090`) ve `GovernanceEndpoints` yüzeyleri bu düzeltmenin kapsamı DIŞINDA bırakıldı — bu Ortak Kuyruk koşumunun konfirme ettiği HATA-K-NNN listesine dahil değillerdi; aynı kalıbın devamı ölçülürse ayrı bir karar/düzeltme gerektirir. Frontend `api-key-panel.tsx`'teki `SCOPES` dizisi bu dört değeri de (WorkflowsRead/Admin ve KnowledgeRead/Admin gibi) İÇERMİYOR — K-405'te not edilen aynı, ayrı boşluk. |
| **K-408 — Kaynak dili sınırı: pakete giren veya çalışma anında çalışan her şey İngilizce'dir; geliştirme aparatı (`docs/`, `.agents/skills/`, `scripts/`) Türkçe kalır** (kullanıcı kararı) | 2026-08-16 | Kullanıcı isteğiyle (Faz 57) AgentPrism'in kodu — 8.837 satır XML doküman dahil — Türkçe'den İngilizce'ye çevrildi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-408. | — |
| **K-409 — Migration `.sql` yorumları İngilizce'ye çevrildi; 61 dosyanın tamamının checksum'ı değişti — var olan bir veritabanına karşı çalıştırmadan önce şema düşürülüp yeniden kurulmalıdır** (kullanıcı kararı) | 2026-08-16 | `MigrationDescriptor.ComputeChecksum` dosyanın ham metninin TAMAMININ SHA-256'sını alır; yorumlar hash'e dahildir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-409. | Bir tüketicide uygulanmış migration'lar varsa (Faz 7 sonrası) |
| **K-410 — `SourceLanguageTests` eklendi: taban çizgisi güdümlü, kelime sınırlı Türkçe sözlük + aksan taraması; `ProblemDetailsLanguageTests`in genelleştirilmiş kardeşi** | 2026-08-16 | Faz 57'nin iş hacmi (~17.700 satır, ~1.030 dosya) tek oturumda bitmeyeceği için cırcır kapısı İLK yazıldı — kapı olmadan ilk oturumun temizlediği dosyaya ikinci oturum Türkçe geri yazardı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-410. | Üçüncü bir dil eklenirse sözlük ve taban çizgisi mimarisi yeniden değerlendirilir |
| **K-411 — Senkronizasyon kopyası (`<ad> 2.<uzantı>`) taraması `faz-tamamlama` skill'ine KAPI olarak eklendi; kapsam `src tests samples`** | 2026-08-16 | Faz 57 kapanışı iki kopya test dosyasını COMMIT etti (`64882c6`) ve `main` DERLENMEZ hâlde kaldı: `GovernanceEndpointTests 2.cs` + `RunScoreStatisticsTests 2.cs`, ikisi de kendi ad alanında ikinci bir tip bildirdiği için `error CS0101`. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-411. | Kopya üreten senkronizasyon istemcisi kaldırılırsa veya `.gitignore` deseni bu sınıfı yakalarsa |
| **K-412 — Doküman dizin bütçesi `docs/arsiv/` ve `docs/manuel-test/kosumlar/` HARİÇ ölçülür** *(kullanıcı kararı)* | 2026-08-16 | Faz 58'in planı `docs/` için 4 MB üst sınır öneriyordu; gerekçesi "58.1 ~600 KB siler" idi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-412. | Arşiv dizini sıcak yolda okunmaya başlarsa (o zaman arşiv olmaktan çıkar) |
| **K-413 — `docs/YOL-HARITASI.md` ÜRETİLEN dosyadır; kaynak her fazın kendi `> **Durum:**` satırıdır** | 2026-08-16 | Faz 58'in DoD'si "README yol haritası Faz 0–59'u tek tek listeliyor" istiyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-413. | Faz dokümanlarının `Durum:` satır biçimi değişirse üretici güncellenir |
| **K-414 — Manuel test koşum kaydı ile spesifikasyon AYRI dosyalarda yaşar; ikinci koşum `kosumlar/<tarih>/` kardeşi açar, üzerine yazmaz** | 2026-08-16 | `docs/manuel-test/` 2,7 MB ile `docs/`'un yarısıydı ve her case dosyası iki farklı ömürlü şeyi aynı yerde tutuyordu: SPEC (`Ön koşul`/`Adımlar`/`Beklenen sonuç` — yeniden koşulabilir, kalıcı) ve KOŞUM KAYDI (`Gerçek sonuç`/`Durum` — 2026-08-13'e ait, donmuş). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-414. | Üçüncü bir koşum yapılırsa desen aynen tekrarlanır; `kosumlar/` altına yeni tarih dizini açılır |
| **K-415 — Ürün dokümantasyonu ayrı bir Astro Starlight sitesindedir (`docs-site/`), İngilizce'dir ve `farukatasoy.github.io/AgentPrism` adresinde yayınlanır** *(kullanıcı kararı)* | 2026-08-16 | Kullanıcıya dönük doküman bugüne kadar `README.md` + 18 paket README + `docs/MIMARI.md` ile ~2.500 satırdı; geri kalan ~99.500 satır Türkçe geliştirme günlüğüydü ve dış okuyucu için okunamazdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-415. | Kullanıcı özel alan adı isterse `site`/`base` ve üretilen bağlantılar birlikte değişir |
| **K-416 — API referansı DocFX'in `outputFormat: markdown` çıktısından üretilir ve Starlight içine gömülür; DocFX'in kendi HTML sitesi kullanılmadı** | 2026-08-16 | Faz 59.0 spike'ı ölçtü: DocFX 2.78.5'in **assembly + XML modu** 17 pakette MSBuild'siz çalışıyor (588 public tip, 0 hata, ~7 sn) — plandaki .NET 10 SDK riski böylece ortadan kalktı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-416. | DocFX markdown çıktısı xref'leri çözmeye başlarsa çözücünün 3. adımı kaldırılır |
| **K-417 — HTTP API sayfaları OpenAPI belgesinden ÜRETİLİR; Scalar gömülmedi** | 2026-08-16 | Plan Scalar öngörüyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-417. | Bir "try it" konsolu gerçekten istenirse Scalar ayrı bir rotaya, arama dışına konur |
| **K-418 — 🚨 `AddOpenApi()` ÇIPLAK çağrılmalıdır; yapılandırma `Configure<OpenApiOptions>("v1", ...)` ile AYRI kaydedilir — aksi hâlde ŞEMA XML DOKÜMANI SESSİZCE DÜŞER** | 2026-08-16 | Bu fazda `AddOpenApi(ProductOpenApiDocument.Configure)` yazıldı ve belge **226 şemanın 166'sının** property `description` alanlarını kaybetti (1080 property'nin 819'u belgeliydi → o alanlar boşaldı). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-418. | .NET'in OpenAPI XML desteği interceptor olmaktan çıkarsa yeniden ölçülür |
| **K-419 — Doküman sitesinin ekran görüntüleri E2E koşumundan üretilir ve commit edilir; elle alınan görsel kabul edilmez** | 2026-08-16 | Elle alınan görsel arayüz değişince sessizce bayatlar ve kimse fark etmez. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-419. | Ekran sayısı çok artarsa görsel bütçesi (bugün 2,6 MB) yeniden ölçülür |
| **K-420 — Yayınlanan `description` metinleri iç doküman referansı taşımaz** | 2026-08-16 | 68 açıklamanın 6'sı `docs/arsiv/fazlar/43-IDEMPOTENCY-KEY.md`, `docs/KARARLAR.md K-152` gibi Türkçe geliştirme günlüğüne işaret ediyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-420. | — |
| **K-421 — Public API takibi (`EnablePublicApiTracking`) Faz 60'ta, yayın kararından (Faz 7, K-068) bağımsız olarak açıldı** *(kullanıcı kararı)* *(yeniden açılma notu güncellendi: 2026-08-24, K-603 — dolum zamanı Faz 7'den `1.0.0` GA'ya taşındı)* | 2026-08-16 | K-016'nın "Faz 7'ye ertelendi" gerekçesi geçerliliğini yitirdi: API yüzeyi artık hızla değişmiyor, 59 faz boyunca kayıtsız birikmişti (ölçüldü: 5.052 `RS0016` — sonra kod büyümesiyle 6.792'ye çıktı). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-421. | `1.0.0` GA'da `Unshipped.txt` → `Shipped.txt` tek seferlik taşınır (K-603) — bu K-421'i geçersiz kılmaz, yalnız dosya rolünü değiştirir |
| **K-422 — RS0026'nın 10 kalemi üç ayrı stratejiyle çözüldü: birleştirme yalnız `ResolveAsync` içindir, geri kalanı ayrıştırma veya statik fabrikadır** | 2026-08-16 | Roslyn'in RS0027 kuralı ("opsiyonel parametre taşıyan aşırı yükleme, aynı ada sahip aşırı yüklemeler arasında EN ÇOK parametreye sahip olmalı") ölçülerek keşfedildi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-422. | — |
| **K-423 — RS0041 (oblivious reference type) `AgentPrism.Abstractions.csproj`'da tek satırlık `NoWarn` ile bastırıldı; kök neden ölçüldü** | 2026-08-16 | 51 örneğin TAMAMI tek kaynağa izlendi: `WebhookEventPayloadJsonContext` (System.Text.Json kaynak üreteci çıktısı, `obj/.../System.Text.Json.SourceGeneration/*.g.cs`) — 17 üye × 3 TFM (net8/9/10). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-423. | System.Text.Json'ın kaynak üreteci nullable ek açıklaması üretmeye başlarsa yeniden ölçülür |
| **K-424 — `AgentPrism.Generators` ve `AgentPrism.Templates` public API takibinden yeni bir `AgentPrismPublicApiTrackingEnabled` MSBuild özelliğiyle hariç tutuldu** | 2026-08-16 | Faz 60 planı bu iki paketi "dosya eklenmez" diyerek hariç tutmayı öngörüyordu, ama `src/Directory.Build.props`'taki `Microsoft.CodeAnalysis.PublicApiAnalyzers` referansı KOŞULSUZDU — dosya eklenmese bile analyzer ikisinde de çalışıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-424. | — |
| **K-425 — Aday yeteneği üretimi `aday-kesfi` skill'iyle yazılı bir protokole bağlandı; oturum elemeli diyalog olarak koşar ve her bulgu üç kanala ayrışır** *(kullanıcı kararı)* | 2026-08-16 | Zincirin başı yazılı değildi: `faz-planlama` "seçilmiş bir adayı" plana çevirir, ama adayın kim tarafından ve hangi disiplinle üretildiği hiçbir yerde durmuyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-425. | Aday listesi zorunlu tetikleyici olmadan iki kez tükenirse tetikleyici yeniden tartışılır |
| **K-426 — Keşif turu kaydı `docs/kesif/<tarih>-<konu>.md` altında yaşar ve doküman dizin bütçesinden HARİÇ tutulur** *(kullanıcı kararı)* | 2026-08-16 | K-412 deseninin aynısı: `docs/arsiv/` ve `docs/manuel-test/kosumlar/` gibi bu da bir **koşumun kaydıdır**, spec değildir; hiçbir oturum baştan sona okumaz, yalnız grep'lenir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-426. | `docs/kesif/` spec gibi kullanılmaya (bir oturumda baştan sona okunmaya) başlanırsa bütçeye alınır |
| **K-427 — `docs/` kökü YALNIZ canlı dokümanı taşır; kapanmış her kayıt `docs/arsiv/`'e taşınır** *(kullanıcı kararı)* | 2026-08-17 | Kök 12 dosyaya çıkmıştı ve altısı ölüydü: iki dalga yol haritası (`IKINCI-FAZ-YOL-HARITASI.md`, `UCUNCU-FAZ-YOL-HARITASI.md` — anlattıkları Faz 8–56'nın tamamı bitmişti), kendini "✅ KAPATILDI" ilan eden `DEGERLENDIRME-RAPORU-2026-08.md`, ve iki SOĞUK üretilen karar indeksi (`KARARLAR-INDEKS-ARSIV.md`, `KARARLAR-INDEKS-REDDEDILEN.md` — hiçbir oturum başında okunmaz, bütçeye girmezler). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-427. | Kökte yeni bir dosya açmadan önce "bu canlı mı" sorusu sorulur; cevap hayırsa doğrudan `arsiv/`'e yazılır |
| **K-428 — Aday listesi tur numarası taşımaz: `UCUNCU-FAZ-ADAYLARI.md` → `ADAYLAR.md`** *(kullanıcı kararı)* | 2026-08-17 | Dosya üçüncü turun listesi olarak doğmuştu ama dördüncü dalga (Faz 53–56) ve sonrası da ona yazıldı; bugün TEK aday listesidir ve adı yanlış yönlendiriyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-428. | Beşinci bir tur açılırsa dosya adı DEĞİŞMEZ; tur bilgisi dosyanın içindeki bölüm başlığında durur |
| **K-429 — Manuel kabul testi PROTOKOLÜ `manuel-test-kosumu` skill'ine taşındı; `docs/manuel-test/` yalnız spec + koşum kaydı taşır** *(kullanıcı kararı)* | 2026-08-17 | `KOSUM-PLANI.md` (26 KB) ve `KAPANIS-PLANI.md` (138 KB) iki farklı ömürlü şeyi aynı dosyada tutuyordu; ÖLÇÜLDÜ: 164 KB'ın **21 KB'ı** turdan bağımsız protokol (değişmez kurallar, şerit izolasyonu, oturum yordamı, sonuç kaydı biçimi, aile aile kapanış, bitti tanımı), **142 KB'ı (%87)** 2026-08-13 turunun kaydıdır (şerit dağılımı, A–W kusur aileleri, durum tabloları, commit hash'leri). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-429. | Üçüncü koşum turu skill'i okur, arşivi değil; skill tur adı ya da tarih taşımaz |
| **K-430 — `00-INDEKS.md` §7 `Koşum` sütunu koşum kaydından ÖLÇÜLÜR, elle işaretlenmez** | 2026-08-17 | Kapanış planının §11 adım 3'ü ("`00-INDEKS.md` §7 `Koşum` sütunu 25 satır için güncellenir") YAPILMAMIŞTI: koşum 1094 case ile bitmişken tablo hâlâ çoğu satırda `☐` gösteriyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-430. | — |
| **K-431 — Örnek uygulama rol politikalarını `AgentPrism:Demo:Roles:Enabled` ile kaydeder ve o anda `RequireRolePolicies`'i AÇAR; şema gösterim amaçlı bir başlık okuyucusudur** | 2026-08-18 | F-104 ölçüldü ve doğrulandı: `grep -rn "AgentPrismPolicies\.\|AddPolicy" samples/` **boş dönüyordu** — `RoleEndpointConventionBuilderExtensions.RequireRole` politika adını `null` çözünce hiçbir yetkilendirme eklemez (bilinçli, K1: Ölçümün tamamı: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md). | Örnek uygulamaya gerçek bir kimlik sağlayıcısı (OIDC) eklenirse gösterim şeması kaldırılır |
| **K-432 — Workflow çalıştırması iptal istendiğinde `Completed` yerine `Canceled` yazılır; zorlama süper-adım sınırında VE pompa çıkışında yapılır** | 2026-08-18 | F-107 (`MT-RES-005`/`HATA-S2-010`) süreç içinde yeniden üretildi: `CancelingAgent` adım ortasında `Cancel()` çağırıyor, MAF'ın `AgentWorkflowBuilder.BuildSequential` grafiği token'ı honor **etmiyor** ve — kritik ölçüm — **istisna da atmıyor**: `WatchStreamAsync` akışı **sessizce** bitiriyor, `MoveNextAsync` `false` dönüyor. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-432. | MAF grafiği token'ı gerçekten honor etmeye başlarsa çift zorlama sadeleştirilebilir |
| **K-433 — Pompa, yanıtlanmış bir istekten sonra akışı yeniden açmadan ÖNCE MAF'ın kendi çalıştırma durumunu sorar** | 2026-08-18 | F-106 (`HATA-K-003`, K-401): Magentic orkestrasyonu round-limit'e ulaşınca kendi `WorkflowOutputEvent`'ini üretip **sonlanıyor**; pompa "istek yanıtlandı" diye akışı yeniden açtığı için MAF "orkestrasyon zaten tamamlandı" istisnası atıyor ve gerçekten sonuç üretmiş bir çalıştırma `RunFailed` olarak kaydediliyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-433. | Gerçek modelli manuel koşum davranışı doğrularsa F-106 kapatılır; hâlâ görülürse kalem yeniden açılır |
| **K-434 — Dosya belleği/metin araması alt ağacı kiracı VE agent adıyla öneklenir; oturumlar arası paylaşım BİLEREK korunur** | 2026-08-18 | F-105: K-381 kiracı sınırını kapatmıştı, kiracı **içi** sınırı açık bırakmıştı — aynı kiracının `EnableFileMemory` açık bir agent'ının yazdığı dosyayı, `EnableTextSearch` açık **başka** bir agent buluyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-434. | Aynı agent'ın oturumlarının birbirinden yalıtılması istenirse ayrı bir kalem olarak ve ambient tasarımı ölçülerek açılır |
| **K-435 — `IToolRegistry`/`AgentPrismToolRegistration` `AITool`'a değil `AIFunctionDeclaration`'a genişledi** | 2026-08-18 | F-108 planı `AIFunction -> AITool` öngörüyordu (istemci tool'unun `AIFunction` olmadığı için). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-435. | MAF `AITool` tabanına `JsonSchema` taşırsa daraltma gereksizleşir |
| **K-436 — İstemci tool'u bildirimi `AIFunctionFactory.Create(...).AsDeclarationOnly()` değil `AIFunctionFactory.CreateDeclaration(...)` ile kurulur** | 2026-08-18 | Planın Açık Soru 2'si iki seçenek arasında kararsızdı; ikisi de ölçülmemişti. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-436. | `CreateDeclaration` MAF'tan kaldırılır veya davranışı değişirse `Create(...).AsDeclarationOnly()`'ye dönülür |
| **K-437 — İstemciden gelen tool sonucu modele `ChatRole.Tool` altında gönderilir, `ChatRole.User` değil** | 2026-08-18 | Onay yanıtları (`ToolApprovalResponseContent`) bu depoda `ChatRole.User` altında gönderiliyor (`ToolApprovalResolver`, `ApprovalResumeJobHandler`) — ama bu farklı bir içerik tipi… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-437. | Başka bir sağlayıcıda `ChatRole.Tool` reddedilirse (ör. bir uyumlu-olmayan endpoint) sağlayıcı bazlı istisna değerlendirilir |
| **K-438 — CORS, `services.AddCors()` OLMADAN `CorsService`/`CorsMiddleware`'in elle kurulmasıyla uygulanır** | 2026-08-18 | F-108 planı `AllowedOrigins`'i `MapAgentPrism()`'in `configure` callback'ine koyuyordu — ama `MapAgentPrism()` `app.Build()`'den SONRA çalışır ve o noktada DI kabı mühürlüdür; `AddCors()` orada çağrılamaz. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-438. | ASP.NET Core `UseCors`'un `Build()` sonrası, DI'sız bir aşırı yüklemesi eklerse elle kurulum kaldırılabilir |
| **K-439 — `toolResults` eşleştirmesi bilerek İKİ KEZ yapılır: akış başlamadan önce doğrulama, sonra gerçek mesaj kurulumu** | 2026-08-18 | K-324 aynı fiziksel kısıtı belgeliyordu (SSE başlıkları akış başlamadan ÖNCE gönderilir) ama guard bloğu için. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-439. | `AgentRunStream`'e `AgentSession`/eşleşme sonucunu taşıyan bir alan eklenirse (attachments'in yaptığı gibi) tekrar kaldırılabilir |
| **K-440 — `ClientToolResult.Result`/`ErrorMessage` 65.536 karakterle sınırlanır** | 2026-08-18 | Plan 61.4 "boyut sınırı" istiyordu ama bir sayı vermiyordu; `request.Message` için de bu depoda hiç sınır yok (emsal yok). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-440. | Gerçek kullanımda sınır dar/geniş çıkarsa `ClientToolResultResolver.MaxResultLength` tek bir sabit noktadan değişir |
| **K-441 — Gömülebilir bileşen yalnız akışsız (`Idempotency-Key`) istek kullanır; SSE ayrıştırıcı taşımaz** | 2026-08-18 (kullanıcı onayı örtük — bütçe kısıtı açık) | 30 KB gzip bütçesi konsolun kendi `sse.ts`'ini widget'a taşımayı göze alamaz. Akışsız yol zaten tam bir tur tamamlıyor (kanıt: `samples/AgentPrism.Api` üzerinde gerçek bir OpenAI çağrısıyla uçtan uca doğrulandı — bkz. fazın DoD bölümü) ve widget'ın "yaz, gönder, yanıtı gör" senaryosu için akış deneyimi zorunlu değil. Ölçülen sonuç: widget 2,7 KB gzip (bütçenin ~%9'u), kalan pay büyük. | Widget'a token-token akan yanıt istenirse ayrı bir bütçe/karar gerekir — bugünkü 30 KB'ta sığmayabilir |
| **K-442 — Gömülebilir bileşen `wwwroot/embed/` altına ayrı bir Vite girişiyle derlenir ve VAR OLAN genel varlık sunum mekanizmasıyla, sıfır yeni C# `endpoint` koduyla sunulur** | 2026-08-18 | `EmbeddedUiAssetCatalog`/`EmbeddedUiProvider`/`UiEndpoints` zaten `wwwroot/**/*`'i içerik türüne göre genel olarak embed edip sunuyor (uzantıdan `Content-Type` çözer, alt dizin farketmez)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-442. | Widget birden fazla dosyaya (ayrı CSS gibi) bölünürse hash'siz-tek-dosya varsayımı yeniden değerlendirilir |
| **K-443 — `FallbackChatClient` devre kesicinin DIŞINDA, `ContentFilterDetectingChatClient`'ın İÇİNDE durur (K-320'nin yerleştirme kuralının Faz 62'ye uygulanışı)** | 2026-08-18 | Yedeğe geçmek için önce birincilin devresinin açık olduğunu görmek gerekir (devre kesici dışı zorunlu). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-443. | K-320'nin kendi yeniden açılma koşulu (MAF tool döngüsünü agent seviyesine taşırsa) bu yerleşimi de etkiler |
| **K-444 — `ModelFallback` yalnız `Provider`+`Model` taşır; birincil bağlamanın diğer alanları (sıcaklık, `ProviderSettings`, ...) yedeğe DEVRETMEZ** | 2026-08-18 | Tam `ModelBinding` taşımak özyinelemeli bir tip (`Fallbacks` içinde `Fallbacks`) üretirdi ve doğrulaması zorlaşırdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-444. | Kullanıcı yedek bağlamaya kendi ayarını (ör. sıcaklık) taşımak isterse `ModelFallback` genişletilir — bugün darlık BİLİNÇLİ |
| **K-445 — Sağlayıcı başına giden eşzamanlılık sınırı DOLDUĞUNDA isteği REDDETMEZ, kendi `CancellationToken`'ıyla sınırlı olarak BEKLER** | 2026-08-18 | Sınırın amacı 429 fırtınasını üretmemek; anında `429` dönmek tam da önlenmek istenen sonucu üretirdi (kısa bir trafik patlamasını gerçek bir arızaya çevirir). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-445. | Bir semaforun ömrü boyunca sınır sayısı SABİTTİR (`SemaphoreSlim` yeniden boyutlanamaz); çalışma anında sınırı DEĞİŞTİRMEK gerekirse ayrı bir tasarım gerekir |
| **K-446 — Ön uçuş reddi `400 Bad Request` döner, `413` DEĞİL** | 2026-08-18 | `413 Payload Too Large` gövde BOYUTU içindir; burada sorun gövdenin baytı değil, modelin bağlam penceresidir — istem küçük olsa bile (ör. uzun bir sistem talimatı) reddedilebilir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-446. | — |
| **K-447 — `ModelFallbackUsed` olayı HEM `run_events`'e HEM kök span'in `agentprism.model.id` etiketine yazılır** | 2026-08-18 | Gece nöbetçisi ya `run` kaydına ya izleme aracına bakar; ikisi de gerçek modeli göstermeli. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-447. | — |
| **K-448 — Ön uçuş token sayımı için `Microsoft.ML.Tokenizers` + `Microsoft.ML.Tokenizers.Data.O200kBase` AÇIK `PackageReference` ile eklendi (Faz 62 Açık Soru 1, seçenek A); `Microsoft.Bcl.Memory` CVE zorlamasıyla sabitlendi** | 2026-08-18 | Ölçüldü: `TiktokenTokenizer.CreateForModel(...)` veri paketi olmadan `InvalidOperationException` fırlatıyor ("Microsoft.ML.Tokenizers.Data.O200kBase.dll could not be loaded"); K-104'ün "veri paketi gerekmedi" bulgusu yalnız MAF'ın SIKIŞTIRMA için tokenizer'ı KENDİ İÇİNDE çözdüğü yola özgüydü — burada ÇAĞIRAN AgentPrism'in kendisidir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-448. | Anthropic/Google resmî bir çevrimdışı tokenizer paketi yayınlarsa sağlayıcıya özgü sayım yeniden değerlendirilir |
| **K-449 — Yedek zincirinde retryable OLMAYAN bir hata (401/403, iptal) HANGİ HALKADA olursa olsun ANINDA ve SARMALANMADAN fırlatılır; zincir yalnız TÜM halkalar retryable hatayla tükendiğinde `AgentPrismProviderUnavailableException` ile "ilk hata" özetine sarılır** | 2026-08-18 | İlk tasarım "son halkada asla yakalama" kuralıyla `ChainExhausted`'ı ölü koda düşürüyordu (yakalama koşulu `index < _fallbacks.Count` idi — son halkada hep yanlış, o yüzden istisna hep ÇIPLAK kaçıyordu). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-449. | — |
| **K-450 — `FallbackRetryClassifier.IsRetryable` istisnanın TAMAMINI (`InnerException` zinciri + her `AggregateException` kolu) gezer; yalnız en dıştaki istisnaya bakmaz** | 2026-08-18 | `faz-denetim`'in bağımsız denetçisi ölçtü: gerçek `OpenAI` 2.12.0 istemcisini dinlemeyen bir porta bağlamak `AggregateException` ("Retry failed after 4 tries...") fırlatıyor; Ölçümün tamamı: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md). | — |
| **K-451 — Argüman-koşulu onay kuralı `POST /api/approvals/rules` YENİ bir uçtur; Faz 63 planı bunu yanlışlıkla "mevcut uç" sanıyordu (Faz 63, plandan sapma)** | 2026-08-18 | Plan kanıt tablosu `GET`/`DELETE /api/approvals/rules`'un var olduğunu doğru tespit etmişti ama `POST`'un da var olduğunu ÖLÇMEDEN varsaydı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-451. | — |
| **K-452 — `POST /api/approvals/rules` gövdesi `ArgumentsHash` alanı TAŞIMAZ; yalnız `toolName`/`agentName`/`argumentConditions`** | 2026-08-18 | `ArgumentsHash` bir parmak izidir — yalnız `ToolApprovalResolver`'ın "bu argümanları hatırla" akışında, gerçek bir çağrının argümanlarından hesaplanır; bir admin'in elle bir hash yazması anlamsızdır ve K2'nin ("arayüzden ifade yazılamaz") ruhuna aykırıdır… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-452. | — |
| **K-453 — Plandaki `ToolApprovalDecision` adı `ToolApprovalPolicyDecision` olarak gerçekleşti (Faz 63, plandan sapma)** | 2026-08-18 | `AgentPrism.AspNetCore.Contracts.GovernanceContracts.cs` içinde Faz 6'dan beri `ToolApprovalDecision` adında BAŞKA bir public tip zaten vardı (arayüzden gelen onay/red kararının HTTP sözleşmesi — `RequestId`/`Approved`/`Reason`/`Remember`). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-453. | — |
| **K-454 — `POST /api/approvals/rules`'ta "aynı kapsam + aynı koşul" çakışması, depoyu değiştirmeden ÜRETİLEN id ile DÖNEN id'yi karşılaştırarak tespit edilir** | 2026-08-18 | `IToolApprovalRuleStore.AddAsync` bilerek idempotent bir upsert'tir (aynı kapsam ikinci kez eklenince var olan satır sessizce döner) — bu davranış `ToolApprovalResolver.RememberAsync`'in agent onay akışı için ZORUNLUDUR (kullanıcı "bir daha sorma"ya iki kez tıklarsa hata almamalı)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-454. | — |
| **K-455 — `conditions_hash` kanonikleştirmesi sayısal normalizasyon YAPMAZ (`100` ile `100.0` farklı hash üretebilir); sıralama + `JsonElement.GetRawText()` yeterli sayıldı** | 2026-08-18 | `faz-denetim`'in bağımsız denetçisi bunu 🟢 (aday listesi) olarak işaretledi: bugün TEK bir yazma yolu var (`POST /api/approvals/rules`, `System.Text.Json` ile serileştirilmiş sayı), dolayısıyla aynı sayısal değer her zaman aynı ham metni üretir — pratikte çakışma riski yok. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-455. | İkinci bir yazma yolu (ör. içe aktarma ucu) farklı bir sayısal biçim üretirse yeniden ölçülür |
| **K-456 — Denetim izi (`audit_log`) veri konusu silmesinin kapsamı dışındadır; silme yalnız İÇERİK verisinde (oturum, çalıştırma, konuşma, ek, puan, ses, çalıştırma girdisi) uygulanır (Faz 64, kullanıcı kararı)** | 2026-08-18 | `audit_log` "kim ne yaptı" bilgisidir, kişinin içeriği değil — silinirse hesap verebilirlik kaybolur. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-456. | — |
| **K-457 — `DataSubjectScope`'a plandaki taslağın öngörmediği üçüncü bir alan (`ConversationIds`) eklendi (Faz 64, plandan sapma)** | 2026-08-18 | Plan "şema bunları zaten oturuma bağlar" varsayıyordu; ölçüldü: `conversations` tablosunun `session_id` sütunu YOK (yalnız `tenant_id`/`agent_name` taşır, Faz 4'ün Conversations API'si `sessions`'tan bağımsız bir kavramdır). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-457. | — |
| **K-458 — `DocumentEmbeddings` (bilgi tabanı gömüleri) veri konusu silme/dışa aktarım kapsamının DIŞINDA bırakıldı (Faz 64, plandan sapma)** | 2026-08-18 | Plan "gömüler oturuma bağlıdır" varsayıyordu; ölçüldü: `document_embeddings` şeması (`0024_vector.sql`) yalnız `tenant_id`/`collection`/`source_id` taşır — session/run/conversation'a bağlayan HİÇBİR sütun yok. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-458. | Bir gömü üretim akışı doğrudan bir data subject'e bağlanacak şekilde tasarlanırsa yeniden değerlendirilir |
| **K-459 — Denetim zinciri "tenant'ın son yazılan satırı" sorgusu `ORDER BY created_at, id` YERİNE ayrı bir sıra sütunuyla (`chain_seq`: PostgreSQL `GENERATED ... AS IDENTITY`, SQL Server `SEQUENCE` + `DEFAULT NEXT VALUE FOR`, SQLite yerleşik `rowid`) bulunur** | 2026-08-18 | Gerçek PostgreSQL'e karşı ölçüldü: uuid v7 (`id`) alt bitleri RASTGELEdir, ekleme sırasını yansıtmaz; 20 eşzamanlı yazıcı testi (`AuditLogContract.Concurrent_writes_for_one_tenant_produce_a_single_valid_chain`) bu yüzden tekrarlanan `23505` ile YAKALANMADAN kilitlendi — bir yazıcının rastgele büyük `id`'si "son" olarak SONSUZA KADAR raporlanabiliyor, yeni satırlar eklense bile… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-459. | — |
| **K-460 — `audit_log.before`/`after` PostgreSQL'de `jsonb`'den `json`'a değiştirildi (Faz 64, K-027'nin beşinci uygulaması)** | 2026-08-18 | Gerçek PostgreSQL'e karşı ölçüldü: `jsonb` yazılan metni bayt-bayt korumuyor (anahtar sırasını ve boşluğu değiştiriyor); hash zinciri yazma anında hesaplanan hash'in okuma anında AYNEN yeniden üretilebilmesini gerektirir — `jsonb` ile her `before`/`after` taşıyan kayıt sahtekarlıkla (`Broken`) yanlış raporlanıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-460. | — |
| **K-461 — Denetim zinciri yazımında eşzamanlılık, oturum/advisory kilit YERİNE benzersiz dizin + yeniden deneme + rastgele gecikme (jitter) ile çözülür** | 2026-08-18 | K-284'ün "oturum kilidinden kaçının, bağlantı havuzuyla ilişkilendirilmez" ilkesi burada da uygulandı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-461. | — |
| **K-462 — Veri konusu önizleme/silme AYNI SQL işlemi (transaction) üzerinden yürür: önizleme her zaman geri alınır (`ROLLBACK`), gerçek silme yalnız çağıranın denetim yazımı (`beforeCommitAsync`) başarılı olursa `COMMIT` edilir (Faz 64, K-370 emsali)** | 2026-08-18 | Aynı `DELETE` sorgu kümesinin hem sayım hem gerçek silme için kullanılması, iki ayrı COUNT/DELETE sorgu setinin zamanla birbirinden sapması riskini ortadan kaldırır (tek doğruluk kaynağı)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-462. | — |
| **K-463 — `SessionConversationResolver` (Core) sıradan bir `AgentSession`'ın dahili sohbet geçmişini veri konusu kapsamına ekler; `DataSubjectScope.SessionIds` tek başına bunun için YETERSİZDİR (Faz 64, bağımsız denetim 🔴 #1)** | 2026-08-18 | `faz-denetim`'in bağımsız denetçisi ölçtü: `SqlChatHistoryProvider`'ın yazdığı `conversation_id`, `conversations`/`conversation_items` tablolarına hiçbir `session_id` sütunu olmadığı için SQL'den erişilemez bir şekilde, oturumun kendi `state` alanı (`ChatHistoryState`, bir state bag girdisi) içine gömülüdür — bir tüketicinin `IDataSubjectResolver`'ı bunu asla bilemez… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-463. | — |
| **K-464 — `DataSubjectTargetRegistry`'deki her `ArrayContains` çağrısı sütunu TAM NİTELİKLİ (`{table}.column`) verir, bare ad değil (Faz 64, bağımsız denetim sonrası bulunan gerçek kusur)** | 2026-08-18 | Gerçek SQLite'a karşı ölçüldü: `json_each()` çıktısının kendi sabit sütun kümesi (`key, value, type, atom, id, parent, fullkey, path`) bulunur; `EXISTS (SELECT 1 FROM json_each(@dizi) WHERE value = id)` içindeki bare `id`, dış tablonun `id`'sini DEĞİL, `json_each`'in KENDİ `id` sütununu bağlar (iç kapsam dış kapsamı gölgeler) — eşleşme SESSİZCE hiçbir zaman gerçekleşmez, hata yoktur… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-464. | — |
| **K-465 — `SqliteDialect.AddUuidArray` Guid'leri BÜYÜK harf metin olarak yazar (`System.Text.Json`'ın varsayılan küçük harf biçimi DEĞİL) (Faz 64, bağımsız denetim sonrası bulunan gerçek kusur)** | 2026-08-18 | Bu dialect'teki TÜM DİĞER Guid bağlamaları (tekil `AddUuid`, `DbHelpers.Add`) zaten büyük harf yazıyordu (K-191, "aynı mantıksal kimlik iki farklı metinle saklanmasın" dersinin doğrudan uygulaması); yalnız dizi (array) yolu bu kuralı KAÇIRMIŞTI çünkü ayrı bir kod yolundan (`System.Text.Json`'ın Guid dönüştürücüsü, HER ZAMAN küçük harf) geçiyordu… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-465. | — |
| **K-466 — Kiracı kimlik bilgisi/egress çözümlemesi ASYNC bir ikinci yol olarak eklendi; mevcut SENKRON `AgentDefinitionCompiler.Compile`/`ModelProviderRegistry.CreateChatClient`/`CompiledAgentCache.GetOrAdd` üçlüsü DEĞİŞTİRİLMEDİ (Faz 65)** | 2026-08-19 | Plan `IModelProviderRegistry.CreateChatClient`'in imzasının sabit kaldığını varsayıyordu; uygulama anında ölçüldü (`faz-uygulama` Adım 1): kiracı `store`'ları (`ITenantProviderBindingStore`, `ITenantEgressPolicyStore`) zaten kurulu async desene (`ValueTask`) uyuyor, ama `AgentDefinitionCompiler.Compile` TAMAMEN senkron… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-466. | Yayından sonra sync üçlü kullanımdan kaldırılabilir; bugün `PublicAPI.Shipped.txt` boş olduğu için bu bedava bir seçenektir |
| **K-467 — Kiracı egress politikası kontrolü, kimlik bilgisi çözümlemesinden ÖNCE ve TEK bir noktada (`ModelProviderRegistry.CreateChatClientAsync`) çalışır — bu nokta hem gerçek `run` derlemesini hem `AgentDefinitionValidator`'ın ön-uçuş kontrolünü kapsar (Faz 65)** | 2026-08-19 | `AgentDefinitionCompiler.Compile`'in `_models.CreateChatClient(binding)`'i TEK çağırdığı nokta olduğu kod okumasıyla doğrulandı (`AgentDefinitionCompiler.cs:327`) — bu hem gerçek `run`'ın derleme adımını (`CompiledAgentCache` üzerinden) hem `AgentDefinitionValidator.CheckModel`'in doğrudan çağrısını besliyor. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-467. | — |
| **K-468 — Dört sağlayıcı paketinin (`OpenAI`/`Anthropic`/`Google`/`Azure`) kiracı-kimlik-bilgisi başına istemci önbelleği TEK bir paylaşılan `AgentPrism.Core.ProviderCredentialClientCache<TFactory>` sınıfıyla yapılır; dört ayrı kopya YAZILMADI (Faz 65)** | 2026-08-19 | Ölçüldü: dört sağlayıcının SDK istemcisi de (`OpenAIClient`/`AnthropicClient`/`Client`/`AzureOpenAIClient`) kurulum anında bir kez inşa edilip paylaşılıyor — bir kiracı kimlik bilgisi bu paylaşılan örneği kullanamaz, yeni bir istemci gerekir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-468. | — |
| **K-469 — Kiracı sağlayıcı bağlama/egress uçları (`/api/tenants/{tenantId}/providers`, `/api/tenants/{tenantId}/egress`) kiracıyı AMBIYANS `ITenantContext`'ten değil, ROTA parametresinden alır — `ApiKeyEndpoints`'in aksine, `GovernanceEndpoints`'in `PUT /api/tenants/{slug}` deseniyle AYNI (Faz 65)** | 2026-08-19 | Bu uçlar platform yöneticisinin HERHANGİ bir kiracının bağlamasını yönetmesi içindir, yalnız isteği yapanın KENDİ kiracısını değil — `ApiKeyEndpoints`'in `tenants.TenantId` (ambiyans) deseni burada YANLIŞ olurdu: bir yönetici başka bir kiracı adına anahtar tanımlayamazdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-469. | — |
| **K-470 — `FallbackChatClient` bir fallback'i tetiklediğinde kiracı/egress'ten HABERSİZ senkron `CreateChatClient` metot grubunu değil, `ModelProviderRegistry.CreateChatClientAsync`'i çağırır (Faz 65, bağımsız denetim 🔴 #1)** | 2026-08-19 | `faz-denetim`'in bağımsız denetçisi ölçtü: `_buildClient` alanı `Func<ModelBinding, IChatClient>` idi ve `BuildPipeline`'daki `resolveFallback` parametresi eklenmeden önce `FallbackChatClient` her zaman senkron `CreateChatClient`'ı (kiracı credential'ı ve egress kontrolü YOK, her zaman global) çağırıyordu — birincil sağlayıcı devre dışı kalıp bir fallback tetiklendiğinde kiracının kendi kimlik bilgisi asla kullanılmıyordu ve egress politikası fallback sağlayıcısına hiç uygulanmıyordu… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-470. | — |
| **K-471 — `CompiledAgentCache`, kiracı credential'ı gömülü bir `AIAgent`'ı credential'dan HABERSİZ bir anahtarla asla saklamaz; bağlama varken üç kaynak (`DefinitionStoreAgentSource`, `CodeAgentSource`) önbelleği TAMAMEN atlar (Faz 65, bağımsız denetim 🔴 #2)** | 2026-08-19 | `faz-denetim`'in bağımsız denetçisi ölçtü: önbellek anahtarı (`tenantId`, `name`, `version`, bağımlılık parmak izi) kiracının provider bağlaması ROTATE edilse veya SİLİNSE bile değişmiyordu — önbellekteki `AIAgent`'ın içine gömülü eski kimlik bilgisi sonsuza dek kullanılmaya devam ederdi, sessizce. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-471. | — |
| **K-472 — `InboundTriggerDispatcher`, "tetikleyici yok" ile "imza yanlış"ı TEK bir `InboundTriggerOutcome.Unauthorized`'a (HTTP `401`) birleştirir; ayrı bir `NotFound`/`404` yolu YOKTUR (Faz 66, bağımsız denetim 🔴 #1)** | 2026-08-19 | Plan taslağı (66.2, 66'nın DoD'si) "aynı gövde ve kod" istiyordu ama plan taslak kodu ile manuel case tablosu (case 7: "bilinmeyen kiracı → 404") ÇELİŞİYORDU — ilk uygulama bu çelişkiyi `NotFound=404` ile `Unauthorized=401`'i AYRI tutarak "çözdü", ki bu tam olarak enumeration açığının kendisiydi: rastgele imzayla gönderilen bir istek `404` alırsa ad YOK, `401` alırsa ad VAR — kiracı/tetikleyici adı numaralandırılabilirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-472. | Trigger adı numaralandırmasının pratikte önemsiz olduğu (örn. tetikleyiciler yalnız içeride bilinen adlar taşıyorsa) kanıtlanırsa, admin arayüzünün kullanılabilirliği için ayrı kodlar yeniden değerlendirilebilir |
| **K-473 — `InboundTriggerDispatcher.ValidateAsync`, imza doğrulamasını hız sınırından ÖNCE çalıştırır (Faz 66, bağımsız denetim 🔴 #2)** | 2026-08-19 | İlk sıralama (hız sınırı → imza) `faz-denetim`'in bağımsız denetçisi tarafından somut bir DoS olarak ölçüldü: tetikleyici adını bilen/tahmin eden kimliksiz bir saldırgan, dakikada `MaxRequestsPerMinute` kadar RASTGELE imzalı istek göndererek gerçek gönderenin (örn. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-473. | — |
| **K-474 — `InboundTriggerDispatcher.EnqueueAsync`, hedef agent/workflow'un GERÇEKTEN var olup olmadığını kabul anında DOĞRULAMAZ; kontrolü işleyiciye (`AgentRunJobHandler`/`WorkflowJobHandler`) bırakır (Faz 66, bağımsız denetim ile onaylandı)** | 2026-08-19 | `AgentEndpoints.RunQueuedAsync`'in aksine (o, `catalog.ResolveAsync` ile `404` döner) tetikleyici kabul anında hedefi çözmez. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-474. | Bir tetikleyici tanımı KAYDEDİLİRKEN (PUT anında) hedefin var olup olmadığını doğrulamak ayrı bir aday kalemi olabilir — bugünkü kapsamda ele alınmadı |
| **K-475 — Migration ledger'ın `set_name`/`(set_name, id)` şema yükseltmesi numaralı bir migration DOSYASI değil, `SqlDialect.UpgradeMigrationsTableAsync` bootstrap adımıdır (Faz 67, plandan sapma)** | 2026-08-19 | Plan (67.2, açık soru 2) yükseltmenin normal bir çekirdek migration dosyasıyla gelmesini öngörüyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-475. | — |
| **K-476 — `IVectorSearchStore`, `EnableKnowledge = false` iken KAYITSIZ bırakılmaz; fabrikası `null` döner (Faz 67)** | 2026-08-19 | `UsePostgreSql(Action<Options> configure)` çağrıldığı anda `configure` henüz ÇALIŞMAMIŞTIR (Options deseni tembel bağlanır) — DI kayıt zamanında "`EnableKnowledge` açık mı" sorusu YANITLANAMAZ, kayıt kararı yalnız ÇÖZÜMLEME zamanında verilebilir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-476. | — |
| **K-477 — `SqlPersistenceDiagnosticsSnapshot.PendingMigrations`'a yeni alan eklenmedi; çekirdek-dışı bir setin bekleyen adı `"{set}:{ad}"` önekiyle yazılır (Faz 67, açık soru 2'nin kapanışı)** | 2026-08-19 | Plan "`/api/diagnostics` bekleyen migration listesini set adıyla birlikte raporlar, alan adı kapanışta belirlenir" diyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-477. | Bir tüketici setleri makine tarafından AYRIŞTIRMAK isterse (önek metninden değil), yapılandırılmış bir alan yeniden değerlendirilir |
| **K-478 — Çalıştırma kimliği `IRunAttributionContext`'ten gelir; istek GÖVDESİNDEN asla alınmaz (Faz 68)** | 2026-08-19 | Ölçüldü: faz öncesi `grep -rn "UserId" src --include="*.cs" \| wc -l` → **0**. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-478. | — |
| **K-479 — Etiketler ayrı tabloya değil `runs.labels` JSON sütununa yazılır (Faz 68, açık soru 1, kullanıcı kararı)** | 2026-08-19 | K-027'nin `jsonb` yasağı POLİMORFİK yükler içindir (`$type` ilk özellik olmalı); etiket haritası düz `string→string`'dir, ayraç taşımaz, sıralama zararsızdır (K-345 ile aynı gerekçe). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-479. | Etiket süzgeci SQL Server/SQLite'ta ölçülebilir bir darboğaz olursa ayrı tablo yeniden değerlendirilir |
| **K-480 — Sınır aşımı KIRPILMAZ, REDDEDİLİR; gürültülü sınır HTTP'de (`400`), sessiz düşürme kayıt yolundadır (Faz 68)** | 2026-08-19 | Sınırlar: 8 etiket, anahtar 64, değer 256, kullanıcı kimliği 200 karakter (sonuncusu SQL Server'ın `nvarchar(max)` INDEKSLENEMEZ kısıtından). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-480. | — |
| **K-481 — Kullanıcı kimliği ve etiketler METRİK etiketi OLMAZ; yalnız sorgu boyutudur (Faz 68)** | 2026-08-19 | Serbest bir etiket kümesini `Meter` boyutuna çevirmek zaman serisi kardinalitesini ÜST SINIRSIZ büyütür ve zarar TÜKETİCİNİN metrik arka ucunda oluşur. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-481. | — |
| **K-482 — Token kırılımı toplamların İÇİNDE sayılır; bildirilmeyen sayaç `null` kalır, `0` OLMAZ (Faz 68)** | 2026-08-19 | Sözleşme MEAI'den devralındı: `UsageDetails` "cached input tokens should be counted as part of `InputTokenCount`" der; `SUM(input) + SUM(cached)` aynı token'ı iki kez sayar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-482. | — |
| **K-483 — Cache fiyatı ÇIKARMALI hesaplanır ve HER maliyet toplamının ÜÇÜNCÜ terimidir; tanımsız oran `Unknown`'a DÜŞÜRMEZ (Faz 68, açık soru 2/3, kullanıcı kararı)** | 2026-08-19 | `tam_fiyatlı_girdi = InputTokens − CachedInputTokens`; toplayıcı bir hesap cache token'ını İKİ KEZ faturalardı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-483. | Bir sağlayıcı akıl yürütmeyi AYRI faturalarsa fiyat şeması yeniden ölçülür |
| **K-484 — `UsageDetails`'in ses sayaçları için `MEAI001` bastırması TEK dosyada (`UsageBreakdown`) toplandı (Faz 68)** | 2026-08-19 | Ölçüldü: MEAI **10.8.3**'te `CachedInputTokenCount`/`ReasoningTokenCount` işaretsizdir ama `InputAudioTokenCount`/`OutputAudioTokenCount` `[Experimental]`'dır (`TreatWarningsAsErrors` → HATA). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-484. | MEAI ses sayaçlarını kararlı hâle getirirse bastırma kaldırılır |
| **K-485 — `/api/stats`'a `groupBy` EKLENMEDİ; `byUser`/`byLabel` her zaman döner (Faz 68, plandan sapma)** | 2026-08-19 | Plan `?groupBy=user\|label` öngörüyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-485. | — |
| **K-486 — Attribution UPSERT'te `COALESCE` ile KORUNUR, üzerine yazılmaz (Faz 68)** | 2026-08-19 | Kuyruğa alınmış bir `run` (Faz 46) `StartRunAsync`'i İKİ KEZ çağırır: yer tutucu HTTP isteği İÇİNDE yazılır (kullanıcı BİLİNİR), sonra arka plan işçisi yeniden yazar (HTTP'ye bağlı bir bağlamın okuyacağı istek YOKTUR → `null`). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-486. | — |
| **K-487 — Tool sarmalama sırası Authorizing (dış) → Timeout → ApprovalRequired (iç) → gerçek fonksiyon; MCP yolunda AYNI mantık TEKRARLANIR (Faz 69)** | 2026-08-19 | İzin, onay veya süre beklemesinden ÖNCE gelir: yapamayacağı bir çağrıyı bir insana onaylatmak veya süresini beklemek yanlıştır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-487. | Üçüncü bir sarmalama noktası (yeni bir tool kaynağı) eklenirse aynı üç katman oraya da elle taşınmalı |
| **K-488 — Yetki reddi İSTİSNA fırlatmaz, normal sonuç döner; kanca hata fırlatırsa fail-closed (Faz 69)** | 2026-08-19 | Ret bir hata değil bir SINIRDIR: `run`'ı düşürmek modelin izinli bir alternatifi denemesini engeller. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-488. | — |
| **K-489 — `RunErrorClass.ToolTimeout` yeni değer (13); `AgentPrismToolTimeoutException` stabil kimliği `tool_timeout` (Faz 69)** | 2026-08-19 | Zaman aşımı sarmalayıcının kendi `Task.WhenAny` yarışı `OperationCanceledException`'a benzer bir görünüm üretebilirdi; `Canceled` sınıfıyla birleştirmek her tool zaman aşımını "kullanıcı iptal etti" arkasına gizlerdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-489. | — |
| **K-490 — MAF `FunctionInvokingChatClient`'ın onay kısa devresi `AITool.GetService<T>()` pipeline'ı üzerinden çalışır; `ApprovalRequiredAIFunction.InvokeCoreAsync`'i DOĞRUDAN çağırmak defer ETMEZ, gerçek gövdeyi çalıştırır (ÖLÇÜLDÜ, Faz 69)** | 2026-08-19 | `TimeoutAIFunction`/`AuthorizingAIFunction`, `ApprovalRequiredAIFunction`'ı sararken onun kısa devre davranışını KIRMAMASI gerekiyordu; standalone bir birim testi (`ApprovalRequiredAIFunction`'ı sarmalayıcı katmanların İÇİNDE gerçekten `.InvokeAsync` ile çağırmak) 30 saniyelik gerçek gövdeyi çalıştırdı — kısa devre `InvokeCoreAsync`'in içinde DEĞİL… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-490. | MAF `ApprovalRequiredAIFunction`'ın kısa devre mekanizmasını değiştirirse (örn. `GetService` yerine somut tip kontrolüne dönerse) yeniden ölçülmeli |
| **K-491 — Tool yürütme varsayılan timeout'u 30 saniye; ÖLÇÜLMEDİ, ilk gerçek koşumdan sonra gözden geçirilecek (Faz 69, F-114, açık soru 5)** | 2026-08-19 | Skill script varsayılanıyla (`AgentPrismSkillScriptOptions.Timeout`) aynı değer seçildi — tutarlı bir varsayılan, ama gerçek üretim trafiğine karşı kalibre edilmedi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-491. | Gerçek bir model anahtarıyla `samples/AgentPrism.Api` üzerinden ilk uçtan uca koşum yapıldığında (bu fazda sandbox kısıtı nedeniyle yapılamadı) |
| **K-492 — `RunEventType.ReasoningDelta` değeri 23, plandaki 22 DEĞİL (Faz 70, F-115)** | 2026-08-19 | Plan dokümanı "yirmi iki değer, 0–21" diyordu ve bunu ÖLÇÜLDÜ etiketiyle kanıt tablosuna yazmıştı, ama `RunEventType.cs` okununca `ModelFallbackUsed = 22` Faz 62'den beri ZATEN doluydu — plan Faz 62'den ÖNCEKİ bir kod okumasına dayanıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-492. | — |
| **K-493 — `IRunEventSink` fan-out'u depo başarısından TAM bağımsız; `RunEventWriter.CompleteAsync` artık `IsDisabled` iken erken dönmez (Faz 70, F-115, Açık Sorular 1/2/4)** | 2026-08-19 | Dört karar birlikte: (1) hedef sıcak yolda çalışır, "hızlı ol" sözleşmesiyle — paket içinde kuyruk AÇILMADI, K1'i zorlardı ve tüketicinin bilinçli kararı olmalı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-493. | — |
| **K-494 — Fonksiyon düğümü yalnız `Sequential`'da desteklenir; `WorkflowDefinition.Nodes` `AgentNames` ile karşılıklı dışlanır (Faz 71, F-116)** | 2026-08-19 | `AgentWorkflowBuilder`'ın `Concurrent`/`Handoff`/`GroupChat`/`Magentic` için hazır kalıp kurucuları yalnız `AIAgent` kabul eder (**ölçüldü**, reflection ile: `BuildConcurrent`, `CreateHandoffBuilderWith`, `CreateGroupChatBuilderWith`, `CreateMagenticBuilderWith` — hiçbiri `Executor` almıyor); bunlar… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-494. | Diğer dört kalıp için elle graf kurmak ayrı ve çok daha büyük bir faz olarak ölçülürse |
| **K-495 — Karışık zincirde agent düğümü `AIAgentBinding` değil `WorkflowAgentStepExecutor` (bir `FunctionExecutor` alt sınıfı) olarak bağlanır (Faz 71, F-116)** | 2026-08-19 | **Ölçüldü:** elle kurulmuş bir grafta, giriş düğümü OLMAYAN bir `AIAgentBinding`, düz `AddEdge` ile bağlandığında sarılı agent'ı hiç çağırmıyor — mesajı alıyor ama sessizce hiçbir şey yapmadan tamamlanıyor (ne `runs` satırı, ne hata, ne çıktı). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-495. | MAF elle kurulmuş graflarda ardışık agent-host protokolünü genel bir API olarak sunarsa (`TurnToken` röle mekanizması public olursa) |
| **K-496 — Fonksiyon adı kaydetme anında da doğrulanır (agent adının aksine, yalnız derleme anında); kayıt süreç ömrü boyunca sabittir (Faz 71, F-116)** | 2026-08-19 | `WorkflowDefinitionValidator`'ın kendi belgesi agent varlığının SAVE anında değil COMPILE anında denetlendiğini söyler, çünkü katalog kayıt ile ilk çalıştırma arasında değişebilir — erken denetim yanlış bir garanti verirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-496. | — |
| **K-497 — Kod düğümünün kendi zaman aşımı bu fazda ele alınmadı; Faz 69'un tool timeout sözleşmesi tek aday olarak bırakıldı (Faz 71, F-116, Açık Soru 2, ÇÖZÜLMEDİ)** | 2026-08-19 | Plan bunu bilerek kapsam dışı bıraktı ("Kapsam dışı" tablosu) ve tek öneri yazdı: Faz 69'un `TimeoutAIFunction` desenini yeniden kullanmak — iki ayrı timeout modeli öğretmenin maliyeti aynıydı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-497. | İhtiyaç ölçülürse `docs/ADAYLAR.md`'ye yeni-yetenek adayı olarak eklenmeli |
| **K-498 — Kontrol noktasından devam sözleşmesi ÖLÇÜLDÜ: EN SON kontrol noktasından sürdürme kod düğümünü YENİDEN ÇAĞIRMAZ, DAHA ERKEN bir kontrol noktasından sürdürme ÇAĞIRIR — `AddWorkflowFunction` işleyicisi bu yüzden İDEMPOTENT olmak ZORUNDADIR (Faz 71, F-116, planın "en riskli hata modu")** | 2026-08-19 | İki bağımsız unit test ile ÖLÇÜLDÜ: `Resuming_an_ALREADY_COMPLETED_mixed_chain_does_NOT_re_run_its_function_node` (açık `checkpointId` verilmeden `/resume` — varsayılan, EN SON kontrol noktasını alır — çağrı sayacı `1`'de kalır: kontrol noktası zaten bitmiş grafı yansıtır, yapılacak bir şey kalmamıştır) ve `Resuming_from_an_EARLIER_checkpoint_RE_RUNS_the_function_node_that_follows_it` (fonksiyonun KENDİ süper-adımından ÖNCEKİ bir `checkpointId` açıkça verilirse — gerçek bir çökme kurtarmasının alacağı biçim — o süper-adım YENİDEN OYNANIR, çağrı sayacı `2`'ye çıkar)… Tam gerekçe: [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-498. | — |
| **K-499 — Talimat sözlüğü için migration YAZILMADI (Faz 72, F-117); plan yanlıştı** | 2026-08-19 | Plan "üç migration seti gerekli" diyordu, **ölçüldü ve yanlış çıktı**: `agent_definitions.definition` zaten tüm içeriği tek `jsonb` blob'unda taşıyor (`AgentDefinitionPayload`, K-027'nin polimorfik-olmayan istisnası) ve üç `AgentPrismJsonContext`'in ikisi zaten `Dictionary<string, string>` kayıtlı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-499. | — |
| **K-500 — Kültür sürümün İÇİNDEDİR; dil başına ayrı sürüm hattı açılmadı (Faz 72, F-117, plan Açık Soru 1, Seçenek A, kullanıcı kararı)** | 2026-08-19 | Bir sürüm tüm dilleri taşır; bir dili düzeltmek yeni sürüm açar ve tüm dilleri etkiler. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-500. | Deney/kanarya sonuçlarının dil başına ayrışması ölçülü bir ihtiyaç olursa |
| **K-501 — `SpeechAlignment` KARAKTER bazlıdır, kelime bazlı DEĞİL (Faz 72, F-118)** | 2026-08-19 | ElevenLabs'ın gerçek OpenAPI şeması (`api.elevenlabs.io/openapi.json`, doğrulandı) `CharacterAlignmentResponseModel` döner: `characters`/`character_start_times_seconds`/`character_end_times_seconds` paralel dizileri — kelime granülerliği hiç yok. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-501. | — |
| **K-502 — Akışlı sentez + `IncludeTimestamps` kombinasyonu istisna ile REDDEDİLİR, sessizce yok sayılmaz (Faz 72, F-118, K1)** | 2026-08-19 | `ISpeechSynthesizer.SynthesizeStreamingAsync` yalnız ham ses baytı döner (`IAsyncEnumerable<ReadOnlyMemory<byte>>`); hizalama verisini taşıyacak bir kanal yok. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-502. | Akışlı yolda hizalama gerçek bir ihtiyaç olarak ölçülürse — ayrı bir dönüş tipi gerekir (mevcut arayüz kırılmadan eklenemez) |
| **K-503 — Alt agent çağrıları ebeveynin `culture`'ını MİRAS ALMAZ (Faz 72, F-117, denetimde bulundu)** | 2026-08-19 | `CallableAgentResolver`/`ChildAgentInvoker` zinciri her zaman `culture: null` ile çözümler; kültür yalnız KÖK agent'ın derlenmesinde kullanılır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-503. | Çok dilli bir alt-agent zinciri gerçek bir ihtiyaç olarak ölçülürse |
| **K-504 — `speak` tool şeması `includeTimestamps` ALMAZ; yalnız `POST /api/voice/speak` (operatör HTTP yolu) destekler (Faz 72, F-118, plan sapması)** | 2026-08-19 | Plan dosya listesi `Tools/SpeakTool.cs`'i "değişir" diye işaretlemişti, dokunulmadı: tool sonucu modele düz metin özet döner (`attachmentId`/`type`/`size`/`duration`), hizalama verisini taşıyacak bir alan yok ve planın HTTP uç tablosu zaten yalnız operatör ucunu adlandırıyordu. | Bir model çağrısının hizalamayı doğrudan okuması gerçek bir ihtiyaç olarak ölçülürse |
| **K-505 — Yetenek haritası `capabilities.md`'den ÜRETİLİR, commit edilir ve `dotnet pack` onu okur; Node zinciri `dotnet build`'e bağlanmaz (Faz 73, F-120, plan Açık Soru 4)** | 2026-08-19 | K-413'ün deseni tüketiciye dönük bir dosyaya uygulandı: harita, `llms.txt` ve `llms-full.txt` tek elle yazılan kaynaktan üretilir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-505. | — |
| **K-506 — `AgentPrism.Usage` tanıları `Warning`'dir; `Info` `dotnet build` çıktısına DÜŞMEZ (Faz 73, F-120, kullanıcı kararı, ölçümle)** | 2026-08-19 | Gerçek bir tüketici projesinde ölçüldü: `Info` seviyeli analyzer tanısı `dotnet build` çıktısına **hiçbir ayrıntı seviyesinde** düşmüyor (`-v:normal`, `-t:Rebuild` denendi); `.editorconfig` ile `warning`e çıkarılınca görünüyor. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-506. | Roslyn/MSBuild `Info` tanılarını varsayılan çıktıya taşırsa yeniden ölçülür |
| **K-507 — Harita sürüm işareti İÇERİK REVİZYONUDUR, paket sürümü değil (Faz 73, F-120, plan sapması)** | 2026-08-19 | Sürüm MinVer ile git yüksekliğinden türer ve her COMMIT'te değişir; plandaki `sürüm: 1.4.0` işareti tüketicinin `AGENTS.md`'sini her commit'te bayat gösterirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-507. | — |
| **K-508 — `APG0302` sarmalayıcıyı değil, DEKORATÖRSÜZ VE FABRİKASIZ sarmalayıcıyı bildirir (Faz 73, F-120, plan Açık Soru 3)** | 2026-08-19 | Planın tanımı uygulamada AgentPrism'in KENDİSİNİ yakaladı: `dotnet pack`, Core'un `RunRecordingAgent`/`ReplayMismatchGuard` sınıflarında hata verdi (Core kendi analyzer'ını kendi üzerinde koşturur). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-508. | — |
| **K-509 — `CapabilityCoverageTests` kapsamı TÜM kayıt giriş noktalarıdır, yalnız `Use*`/`Map*` değil (Faz 73, F-120, kullanıcı kararı)** | 2026-08-19 | Plandaki filtre iki yönden yetersiz çıktı: `AddToolApprovalPolicy`/`AddWorkflowFunction` kapının dışındaydı, `Configure`/`Services` ise "taban çizgisi boş doğar" maddesini tutmuyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-509. | — |
| **K-510 — Yerel referans dosyası PROJE başına yazılır, depo köküne değil (Faz 74, F-121, denetim bulgusu 3)** | 2026-08-20 | Plan dosyayı `AGENTS.md`'nin yanına, git köküne koyuyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-510. | Tüketici tek bir birleşik dosya talep ederse yeniden açılır; o zaman birleştirmeyi build'in DIŞINDA yapan bir adım gerekir |
| **K-511 — `docs/openapi/agentprism.json` `AgentPrism.AspNetCore` paketine KAYNAĞINDAN girer; K-039 yeniden açılmaz (Faz 74, F-121)** | 2026-08-20 | K-039 `Microsoft.AspNetCore.OpenApi` BAĞIMLILIĞINI reddetmişti (CVE'li geçişli `Microsoft.OpenApi` 2.0.0, NU1903). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-511. | Belge paketin %10'unu aşarsa veya isteğe bağlı uçlar belgeye girerse yeniden ölçülür |
| **K-512 — `CapabilityExampleTests` DÖRT iddia taşır; yabancı üye listesi yalnız Microsoft üyelerini içerir (Faz 74, F-121)** | 2026-08-20 | Plan iki iddia öngörüyordu, dört oldu: örnek var mı · 39 adın 39'u okundu mu · örnekteki her `Add*`/`Use*`/`Map*` adı public API'de var mı · örnek KENDİ üyesini çağırıyor mu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-512. | — |
| **K-513 — Bir `<example>`'ın doğruluğunu yalnız DERLEME kanıtlar; metin denetimi yapısal olarak yetersizdir (Faz 74, F-121, denetim bulgusu 4)** | 2026-08-20 | Denetim iki hatalı örnek buldu ve ikisi de K-512'nin metin kapısından geçmişti: `options.DefaultTimeout` (`CS1061`) ve `o.ExposedAgents = [...]` (`CS0200`). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-513. | F-125 planlandığında |
| **K-514 — Sevk edilen dokümantasyon KENDİ KENDİNE YETER: pakete giren bir metin yalnız tüketicinin elindeki şeylere gönderme yapar (Faz 75, F-126)** *(kullanıcı kararı)* | 2026-08-20 | K-408 dil sınırını çizdi; bu karar aynı sınıra hedef kitle boyutunu ekler. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-514. | — |
| **K-515 — Sevk edilen dokümanı denetleyen cırcır, satırı değil BLOĞU okur (Faz 75)** | 2026-08-20 | İlk uygulama `SourceLanguageTests` gibi satır satır tarıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-515. | — |
| **K-516 — Site üreteçleri artık ONARMAZ, HATA VERİR (Faz 75, F-126)** | 2026-08-20 | `build-api-reference.mjs` (70 satır) ve `build-http-api.mjs` (26 satır) toplam ~110 satırlık bir metin onarım zinciri taşıyordu ve tek işi kaynağın kirliliğini gizlemekti. Zincir sessizce bozuyordu: `"Request to promote a run to a case,."`, `"… . **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-516. | — |
| **K-517 — `<see cref>` paketlenen OpenAPI belgesinde TAM İMZA olarak render edilir; sözleşme tiplerinde `<c>ÜyeAdı</c>` yazılır (Faz 75)** | 2026-08-20 | Ölçüldü: `docs/openapi/agentprism.json` içinde **26 yerde** `string? ClientToolResult.ErrorMessage`, `int? ModelBinding.MaxOutputTokens` gibi metinler vardı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-517. | — |
| **K-518 — Kök `README.md` İngilizce'dir (Faz 75)** *(kullanıcı kararı)* | 2026-08-20 | K-408 kök README'yi kapsam dışı bırakmıştı çünkü pakete girmez. Kullanıcı kararı kapsamı genişletti: okuru tüketici olan her yüzey İngilizce'dir, ve GitHub'ın karşılama sayfası paket ailesinin ilk izlenimidir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-518. | — |
| **K-519 — Doküman sitesinin rengi KAPALI bir token kümesidir ve kontrast derleme anında hesaplanır (Faz 76)** | 2026-08-20 | Ölçüm fazın teşhisini düzeltti: site "stok tema" değildi, `site.css` zaten 163 satır ve 21 sınıflık bir ürün katmanı taşıyordu — kimlik vardı, uygulanmamıştı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-519. | Starlight kendi renk mimarisini değiştirirse bağlama listesi yeniden ölçülür |
| **K-520 — Figürler (ekran görüntüsü ve diyagram) temadan BAĞIMSIZDIR; iki temada da açık plaka üzerinde durur (Faz 76)** | 2026-08-20 | İki bağımsız kısıt aynı yöne çıktı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-520. | Mermaid `themeVariables`'ı CSS değişkeninden çözmeye başlarsa, ya da ekran görüntüleri koyu temaya geçerse yeniden ölçülür |
| **K-521 — Kenar çubuğu `src/sidebar.mjs`'te tek kaynaktır; üç tüketici onu okur (Faz 76)** | 2026-08-20 | Bölüm başına `og:image` bir eşleme ister ve o eşleme kenar çubuğu bölümleriyle senkron kalmalıdır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-521. | Starlight `routeMiddleware` veya `logo` sözleşmesini değiştirirse |
| **K-522 — Tüketici doküman standardı faz dokümanından ayrıştırılıp `tuketici-dokuman-senkronu` skill'ine taşındı (kullanıcı kararı)** | 2026-08-20 | Faz 73–76 gerçek bir tüketici doküman makinesi kurdu: dokuz makine iddiası (`check-content.mjs`), sevk edilen metnin kendi kendine yetmesi kuralı, sayfa sözleşmesi, diyagram kuralı, erişilebilirlik ve ağırlık kapıları, ve tüketicinin diskinde üretilen yerel referans yüzeyi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-522. | Standardın gözle denetlenen kalemleri makine kapısına dönüşürse (F-129) veya skill zinciri yeniden düzenlenirse |
| **K-523 — Kapanmış faz dokümanları `docs/arsiv/fazlar/` altında yaşar; `docs/**.md` bütçesi büyütülmez (kullanıcı kararı)** | 2026-08-20 | Faz 76 kapanışında `docs/**.md` sayacı 4.925.858/5.000.000 idi (%1 boş) ve son yedi fazın ortalaması ~41 KB/faz (Faz 75 tek başına +105 KB) — bir sonraki faz sınırı kesin aşacaktı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-523. | Arşiv dizini de okunur hâle gelirse (bir skill onu açılış kümesine alırsa) hariç tutma varsayımı çürür ve sayaç yeniden ölçülür |
| **K-524 — `MIMARI.md` §7 "Güvenlik Modeli" kendi dosyasına ayrıldı** | 2026-08-20 | Bölüm 17.049 B'a ulaşmıştı — dosyanın **%42'si** ve sonraki en büyük bölümün (§6, 8.171 B) iki katı; her güvenlik fazı (48, 64, …) yalnız ona ekliyordu, yani birikimli büyüyen tek bölüm oydu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-524. | Güvenlik dosyası da 21.000'i zorlarsa bölünme ekseni kiracı/rol ile script sandbox arasındadır |
| **K-525 — Kiracı verisi tutan cache anahtarı TİPLİ olur; elle birleştirilen string anahtar yasaktır** | 2026-08-20 | Güvenlik taraması üç yeni vaka buldu: `WorkflowAgentCache` kiracıyı anahtara hiç katmıyordu (K-380'in birebir tekrarı; kiracı A'nın agent `Description`'ı B'nin modeline gidiyordu — düşen testle kanıtlandı), `ModelProviderCircuitBreaker` yalnız sağlayıcı adıyla anahtarlıydı (BYOK'lu bir kiracının 5 başarısız `run`'ı tüm kurulumu `BreakDuration` boyunca kesiyordu), `McpToolCatalog.TryGetConnection` ise yazma yolunun kullandığı ayırıcıyı **taşımıyordu**. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-525. | 68 `ConcurrentDictionary` noktası tarandı; `QuotaEnforcer` ve `CompiledAgentCache` zaten doğruydu. `RunTraceCollector._buffers` `traceId` ile anahtarlı ve `traceId` `traceparent`'tan devralınıyor — B02-11 olarak PLAUSIBLE kaldı |
| **K-526 — Denetim yükü `AuditPayload` ile kurulur; geri alınamaz eylem `AuditRecorder` kullanamaz** | 2026-08-20 | İki kusur aynı sonuca çıkıyordu: mutasyon uygulanmış, denetim kaydı yok. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-526. | Fazla kayıt güvenli yön: kaydedilip uygulanamayan bir grant gürültüdür, uygulanıp kaydedilmeyen bir grant değildir |
| **K-527 — Yürütülebilir yüzeyi genişleten ayar yapılandırmadan okunmaz** | 2026-08-20 | `Interpreters`, `SkillRoots` ve `AllowStoredScripts` yapılandırmaya bağlıydı ve bağlama kodda verilenin **üzerine ekliyordu** — `AgentPrism__Skills__Scripts__Interpreters__sh=/bin/sh` kodun hiç tanımadığı bir yorumlayıcıyı allow-list'e sokuyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-527. | Aynı oturumda `SkillScriptNaming` eklendi: script ADI da arayüzden gelen bir yol bileşeniydi (`Path.Combine` mutlak ikinci parçayı aynen döndürür, `..` çözmez) |
| **K-528 — `external:invoke` kapsamı istek anında zorlanır; anahtarın var olması yetmez** | 2026-08-20 | `ExternalSurfaceGuard` açılışta yalnız "sistemde böyle bir anahtar var mı" diye soruyordu; `CheckScope` ise imzası gereği bir `ApiKeyRecord` istiyordu ve hiçbir kimlik bilgisi sunulmadığında **hiç çalışmıyordu**. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-528. | Aynı sınıfın ikinci vakası ses WebSocket'iydi: sunulan alt protokol değeri `AuthToken` tanımsızken **hiç** doğrulanmıyordu (K-359 sınıfı). Artık statik token **veya** API anahtarı deposuna karşı doğrulanıyor; ters yön (anahtar sunan kullanıcının 401 alması) da düzeldi |
| **K-529 — Giden ağ hedefi TEK bir muhafızdan geçer; üç yüzey de kendi kopyasını taşımaz** | 2026-08-20 | AgentPrism üç yerden dışarı istek atar (webhook teslimi · MCP bağlantısı · model sağlayıcı çağrısı) ve SSRF koruması yalnız birincideydi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-529. | — |
| **K-530 — `AgentPrism:Egress:AllowPrivateNetworkTargets` varsayılanı KAPALI; üç yüzeyde de özel ağ reddedilir (kullanıcı kararı)** | 2026-08-20 | Webhook zaten reddediyordu; MCP ve sağlayıcı yolu hiç denetlenmiyordu. Tek zihinsel model, tek ayar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-530. | Ölçüm iç ağda MCP'nin baskın kurulum olduğunu gösterirse |
| **K-531 — Sağlayıcı istemcisine muhafız YALNIZ kiracı `Endpoint` override'ı varken takılır (kullanıcı kararı)** | 2026-08-20 | Kurulum zamanı global `Endpoint` operatörün kendi kararıdır ve **koda yazılır**; kiracıdan gelen override ise dış girdidir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-531. | Kiracı override'ı olmayan bir SSRF yolu bulunursa |
| **K-532 — Sağlayıcı SDK'larının muhafız kancası ÖLÇÜLDÜ; plan tahmini üçte ikisinde yanlıştı** | 2026-08-20 | Faz planı dört SDK için "`HttpClient` kabul ediyor" diyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-532. | SDK sürümü kancayı değiştirirse yeniden ölçülür |
| **K-533 — `secret` çözen HER yapılandırma anahtarı bir önek allow-list'ine bağlıdır** | 2026-08-20 | Desen iki yerde vardı (`AgentPrism:TriggerSecrets:`, `AgentPrism:ProviderKeys:`) ve iki yerde yoktu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-533. | — |
| **K-534 — MCP önek ayarı `Abstractions`'ta yaşar (`AgentPrismMcpSecurityOptions`), `AgentPrism.Mcp`'de değil** | 2026-08-20 | Kural iki yerde zorlanır: kaydı yazan uç (`AgentPrism.AspNetCore`) ve anahtarı çözen taşıma (`AgentPrism.Mcp`). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-534. | — |
| **K-535 — Webhook ek başlıkları AgentPrism'in kendi başlık adlarını taşıyamaz** | 2026-08-20 | `HttpHeaders.TryAddWithoutValidation` **ekler**, değiştirmez: `X-AgentPrism-Signature` adlı bir abonelik başlığı ikinci bir imza değeri üretiyordu ve alıcı hangisini doğrulayacağını bilemiyordu — doğrulama gürültüsüzce bozuluyordu (B05-5). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-535. | — |
| **K-536 — Giden ağ muhafızı ORTAM PROXY'sini kullanmaz (`UseProxy = false`)** | 2026-08-20 | Faz 77 bağımsız denetiminin 🟡 bulgusu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-536. | Proxy zorunlu bir kurulum ölçülürse; çözüm açık bir proxy ayarıdır, miras alma değil |
| **K-537 — `llms.txt` yetenek haritasından AYRI bütçelenir: 10 240 B ve 20 480 B (Faz 78, F-135, ölçümle)** | 2026-08-21 | İki yapıtın ekonomisi farklıdır: harita her tüketici oturumunun **başında diskten** okunur, `llms.txt` ağdan ve **bilerek** getirilir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-537. | `llms.txt` 20 480'i aşarsa indeks kırpılmaz — hangi sayfaların indekslendiği yeniden düşünülür |
| **K-538 — `APG0402` yalnız yerel referans dosyası GERÇEKTEN yazılırken öter (Faz 78, F-135, denetim 🔴 #1, kullanıcı kararı)** | 2026-08-21 | Ölçüldü: `AdditionalFiles` opt-in'e **bağlı değildir** (bilerek — bir kez açıp kapatan tüketicinin dosyası hâlâ bayatlayabilir), bu yüzden ilk hâlde `APG0402` hiçbir özellik açmamış tüketicide de ötüyordu ve önerdiği satır **hiç yazılmayan** bir dosyayı adlandırıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-538. | Tüketiciler "paketi kurunca haber ver" derse yeniden açılır; o zaman mesaj iki adımı birden adlandırmalıdır |
| **K-539 — Karar defterinin §2 tablosu YAPISAL olarak denetlenir: yinelenen numara · tabloyu kesen boş satır · sıra dışı numara** | 2026-08-21 | Faz 77 (2026-08-20) ve Faz 78 (2026-08-21) aynı tabandan yazıldı; ikisi de son numarayı `K-534` gördü ve **ikisi de** `K-535` ile `K-536`'yı aldı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-539. | Kararlar tek dosyada tutulmaktan vazgeçilirse; o zaman kapı yeni yapıya taşınır, kaldırılmaz |
| **K-540 — Migration çakışmasının İKİ şekli vardır ve ikisi de geçicidir: unique ihlali VE deadlock** | 2026-08-21 | Ölçüldü: dört tam test koşumunda dördüncüsü `SqlServerRetentionStoreContractTests`'in beş case'ini düşürdü — `Migration '0017_approval_conditions' could not be applied: ... **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-540. | Bir çağıran deadlock'ta yeniden denemeyi İSTEMEZSE (tekrarı güvenli olmayan bir yazım); o zaman `IsDeadlock` kalır, yeniden deneme kararı çağırana taşınır |
| **K-541 — Terminal `AwaitingApproval` durumu, ona bağlı kayıtlar GÖRÜNÜR olduktan sonra yayınlanır; sıra `AgentPrismRunOptions.BeforePendingApprovalIsPublished` kancasıyla zorlanır** | 2026-08-21 | F-133'ün kök sebebi ölçüldü ve kaydın "kırılgan test" teşhisi YANLIŞTI: pencere bir yarış değil, **sabit bir sıraydı** ve onay isteyen HER kuyruk çalıştırmasında açıktı. `RunRecordingAgent` çalıştırmayı `agent.RunAsync`'in İÇİNDE kapatıyor, `AgentRunJobHandler` ise onay satırını çağrı DÖNDÜKTEN sonra yazıyordu; arada `GET /api/approvals/pending` kendini "bekliyorum" ilan etmiş bir `run` için boş dizi veriyordu. İki değişmez birlikte sağlanmalıdır ve sıraları sabittir: **önce oturum** (karar bir `ApprovalResume` işi kuyruklar ve o iş oturumu okur), **sonra onay satırı** (tüketici durumu yokladıktan sonra listeyi okur), **en sonda durum**. Tamamlamayı handler'a devretmek REDDEDİLDİ: `RunRecordingAgent.CompleteAsync` yalnız durum yazmaz — bitmemiş tool'ları boşaltır, compaction usage'ını birleştirir, fallback atfını çözer, maliyeti ve metriği yazar; devretmek bunların hepsini kaybettirirdi. Bunun yerine durum yazılmadan hemen önce koşan bir kanca kondu. Kanca **public**tir: `AgentPrismRunOptions` zaten `Depth`, `RootRunId`, `ReplayOfRunId` gibi kontrol düzlemi düğmeleri taşır ve kendi kuyruğunu işleyen tüketici aynı sıralama sorununu yaşar; tip `Unshipped` olduğu için bugün bedelsizdir (👤 kullanıcı kararı). Kanca akışlı yolda da koşar (onay içerikleri tek bir assistant mesajında toplanır) ki genişleme noktası yola göre sessizce farklı davranmasın. K-372 yeniden AÇILMADI: o karar hangi YOLUN yazdığına dairdir, sıraya değil. | MAF `AIAgent` sözleşmesi çalıştırmayı kapatmadan önce bir uzatma noktası verirse; o zaman kanca oraya taşınır |
| **K-542 — Ürün dokümantasyonu `agentprism.doayen.web.tr` alt alan adında kendi sunucumuzda yayınlanır; `base` KALICI olarak `/` oldu ve adres tek dosyada (`docs-site/site.config.mjs`) bildirilir** *(kullanıcı kararı)* | 2026-08-21 | K-415'in geri açılma koşulu ('kullanıcı özel alan adı isterse') gerçekleşti, ama sebebi alan adı tercihi değil: repo ÖZELDİR ve özel repo'da GitHub Pages ücretli plan ister, bu yüzden site **hiç yayınlanmadı** — `farukatasoy.github.io/AgentPrism` bugün 404 verir. Bu adres 118 yerde yazılıydı ve bir kısmı SEVK EDİLEN yüzeydeydi (19 paket README'si, iki analyzer'ın `HelpLinkUri` değeri, `dotnet pack`'in okuduğu `AgentPrism.AgentMap.md`): NuGet'e basılacak her paket 404 veren bir adres gösteriyordu. **Kök alınmadı** — ölçüldü: `doayen.web.tr` kökünde canlı bir ASP.NET Core uygulaması var (Kestrel, `:5001`, Let's Encrypt'li ters proxy); alt alan adı o uygulamaya dokunmaz ve proxy sıralaması gerektirmez. **`base` kalıcı olarak `/`**: alt yol sitenin değil BARINDIRICININ özelliğiydi (Pages proje sitesi depo adını yola sokar). El yazısı 39 sayfadaki 224 bağlantı önekten arındırıldı; bedeli açıktır — site bir gün alt yola dönerse o 224 bağlantı da güncellenmelidir, ama bu SESSİZ bir kırılma değildir, `check-links.mjs` üretilen `dist/` üzerinde 130 320 bağlantıyı yürür ve yayından ÖNCE kızarır. Alternatif (remark eklentisi) REDDEDİLDİ: frontmatter `link:` alanlarını ve MDX'teki ham `<a href>` özniteliklerini görmez, yani kapsamı eksik bir soyutlama olurdu. Adres yedi ayrı kopyada yaşıyordu ve hiçbir kapı uyuştuklarını doğrulamıyordu — biri sondaki eğik çizgi olmadan yazılmıştı; hepsi `site.config.mjs`'ten türetildi ve `check-content.mjs` 11. kontrolü eklendi: türetebilecek bir dosyada adresi HARFİYEN yazmak da, `formerHosts` listesindeki eski bir barındırıcıya işaret etmek de kapıyı kızartır (üçü de ölçümle doğrulandı). C# tarafı ayrı dilde olduğu için ikinci bir bildirim zorunluydu (`DocumentationLinks`); `DiagnosticIntegrityTests` `site.config.mjs`'i OKUR ve ikisinin ayrılmasını engeller — `ShippedDocumentationSelfContainmentTests`'in `internal-history.pattern` için kullandığı desen. **Yayın yerel script'tir** (`scripts/site-deploy.sh`: derleme → dört kapı → rsync): özel repo'nun Actions dakikası sınırlıdır ve GitHub'a SSH anahtarı konmaz. **Barındırma Traefik'tir, host üzerinde nginx/Caddy DEĞİL** — ölçüldü: :80 ve :443'ü Docker dinliyor, sunucuda Traefik v2.11 konteyneri var ve yönlendirme Docker label'ıyla yapılıyor. Site de bir konteynerdir (`nginx:1.27-alpine`, statik dizini salt-okunur bağlar) ve mevcut servisin konvansiyonunu kopyalar: `reverse-proxy` dış ağı, `websecure` entrypoint'i, `le` sertifika çözücüsü. Yapılandırma repo'dadır (`docs-site/deploy/`) ve elle sunucuda tutulmaz; `SITE_HOST` `.env` ile geçer, böylece adres orada da tekrarlanmaz — kapı o dizini de tarar. Script `docker compose up -d` koşar, yani konteyner repo'daki dosyadan sapamaz. CI'nin `pages` işi `site` işine dönüştü — YAYINLAMAZ, yalnız derler ve kapılarını koşar. Aynı sebebin ikinci sonucu: repo özel olduğu için Starlight'ın `editLink`'i ve başlıktaki GitHub ikonu KALDIRILDI — ölçüldü, `github.com/farukatasoy/AgentPrism` anonim okuyucuya **404** veriyor ve 'Edit page' bağlantısı el yazısı 39 sayfanın hepsinde duruyordu; her ziyaretçinin yalnız başarısız olabileceği bir bağlantı, bağlantısızlıktan kötüdür (👤 kullanıcı kararı). Aynı sebeple 10 paket README'sindeki "Repository and full documentation" satırı siteye yönlendirildi — o satır NuGet'te duruyordu ve vaadinin iki yarısı da açılamıyordu. Tekrarı `check-content.mjs` engeller: `repositoryIsPublic` `false` olduğu sürece repo adresi sevk edilen metinde YASAKTIR; repo açılınca tek bayrak çevrilir. **Taşıma geçicidir**; geri dönüş `site.config.mjs`'te tek satırdır. | Kullanıcı ücretli plana geçer veya başka bir barındırıcıya taşınırsa: `site` değişir, çıkan barındırıcı `formerHosts`'a eklenir; alt yola dönülürse el yazısı bağlantılar da güncellenir. Repo herkese AÇILIRSA `editLink` ve GitHub `social` blokları geri eklenir |
| **K-543 — Bağımlılık sürüklenmesini Dependabot fark eder; beş sabit gerekçeli `ignore` listesindedir** *(kullanıcı kararı)* | 2026-08-21 | 2026-08-21 keşif turu ölçtü: `.github/` yalnız `workflows/` taşıyordu ve sürümlerin bayatladığını fark eden **hiçbir mekanizma yoktu**. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-543. | Bir `ignore` kaleminin gerekçesi ortadan kalkarsa (ör. xunit.v3 4.x ile TrxReport 2.x birlikte doğrulanırsa) o satır kaldırılır; gruplama bir sürüm hattı kısıtını artık korumuyorsa veya Dependabot CPM desteğini bırakırsa yeniden açılır |
| **K-544 — MEAI 10.9.0, `Microsoft.Extensions.*` tabanını 10.0.11'e ZORLAR; "yalnız MEAI'yi yükselt" diye bir seçenek yoktur** | 2026-08-21 | Ölçüldü: `Microsoft.Extensions.AI` 10.9.0 `Microsoft.Extensions.Logging.Abstractions` ve `.DependencyInjection.Abstractions` için **`>= 10.0.11`** ister. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-544. | MEAI taban gereksinimini düşürürse (olası değil) veya `Microsoft.Extensions.*` hattı MEAI'den bağımsız sabitlenmek istenirse |
| **K-545 — Geçici çakışma yeniden denemesi migration'ın BOOTSTRAP deyimlerini de kapsar; "bu yalnızca kurulum" muafiyeti yoktur** | 2026-08-21 | K-540 doğru sınıfı buldu ama **dar taradı**: yeniden deneme yalnız `ApplyOneAsync` içindeydi, oysa `ApplyPendingAsync` ondan önce dört deyim koşuyor — `CreateSchema`, `CreateMigrationsTable`, `Dialect.UpgradeMigrationsTableAsync` ve defter okuması — ve hiçbiri korunmuyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-545. | Bir çağıran deadlock'ta yeniden denemeyi istemezse (tekrarı güvenli olmayan bir yazım), K-540'ın koşulu burada da geçerlidir: `IsTransientConflict` kalır, karar çağırana taşınır |
| **K-546 — `AgentPrismToolAttribute.cs`'in `OrderTools` örneği artık `static` DEĞİL; tool metodu yine `static`** | 2026-08-21 | Ölçüldü (Faz 79, F-125 uygulanırken): `IAgentPrismBuilder.cs`'in AYRI bir örneği `.AddToolsFrom<OrderTools>()` çağırıyor ve C# statik bir sınıfı generic tip argümanı olarak kabul etmiyor (`CS0718`) — iki örneği birlikte kopyalayan bir tüketici gerçekten derleyemezdi, bunu yeni derleme kapısı (`ExampleCompilationTests`) yakaladı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-546. | `AddToolsFrom<T>()` bir gün `Type` parametresi yerine gerçek bir generic kısıtlama YAZARSA (bugün öyle değil, yalnız `typeof(T)` kullanıyor) yeniden değerlendirilir |
| **K-547 — `Azure.Identity` yalnızca test projesine (`AgentPrism.Generators.UnitTests`) `PackageReference` olarak eklendi; `AgentPrism.Azure`'un bağımlılık grafiği DEĞİŞMEDİ** | 2026-08-21 | Faz 79. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-547. | Bir tüketicinin dikte ettiği bir yönetilen kimlik senaryosu `AgentPrism.Azure`'un kendisine gerçek bir `Azure.Identity` bağımlılığı eklemeyi gerektirirse |
| **K-548 — `SITE_KURALLARI` kural başına eşleşir; `--site-denetle` `git` hatasında artık çıkış kodu 1 verir (Faz 80, kullanıcı kararı)** | 2026-08-21 | Eskiden kapı herhangi bir docs-site sayfasının değişmesini tetiklenen TÜM kuralların karşılığı sayıyordu — `Workflows/` değiştirip yalnız `packages.md`'yi güncellemek ✅ dönüyordu (ölçüldü). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-548. | Bir kural birden fazla hedefin TÜMÜNÜN değişmesini isterse (bugün hiçbir kural bunu istemiyor) OR semantiği yeniden değerlendirilir |
| **K-549 — `kirik_baglantilar()` site-mutlak (`/...`) bağlantıları slug haritasıyla çözer; üretilen `api/`, `http-api/` ve `openapi/` hem kaynak hem hedef olarak hariç (Faz 80)** | 2026-08-21 | Eskiden `/`, şema (`http:`) ve `#` önekli her bağlantı denetim dışıydı — site-mutlak bağlantılar hiç doğrulanmıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-549. | `site` işi de `build`'e taşınırsa (bugün ayrı, yalnız `main` push'ta) `api`/`http-api`/`openapi` hariç tutması kaldırılabilir |
| **K-550 — Doküman bakım testleri `scripts/dokuman_bakim_test.py`dır (ALT ÇİZGİ); planın önerdiği `dokuman-bakim_test.py` (TİRE) DEĞİL (Faz 80, plandan sapma)** | 2026-08-21 | Ölçüldü: `unittest discover`'ın `VALID_MODULE_NAME` deseni (`[_a-z]\w*\.py$`) tire taşıyan dosya adlarını SESSİZCE atlar — `python3 -m unittest discover -s scripts -p "*_test.py"` tire taşıyan bir dosyayla "Ran 0 tests" döner, hiçbir hata vermez. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-550. | — |
| **K-551 — Yanıt önbelleği anahtarı `record struct CacheKeyScope` (taban anahtar, kiracı, sağlayıcı, sıralanmış tool adları) üzerinden hesaplanır; MEAI'nin varsayılan `GetCacheKey`'ine güvenilmez (Faz 81, F-45, ölçümle)** | 2026-08-22 | Ölçüldü: `DistributedCachingChatClient.GetCacheKey` mesajları, `ModelId`, `Temperature`, `ToolMode` ve `ChatOptions.Instructions`'ı ayırır ama **`ChatOptions.Tools`'u ayırmaz** ve kiracıyı hiç taşımaz. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-551. | MEAI kendi `GetCacheKey`'ine tool kümesini ve bir "ek kapsam" girdisini eklerse, bu sarmalayıcı gereksiz hâle gelir |
| **K-552 — Yanıt önbelleği halkası tool çağrı döngüsünün İÇİNDE, telemetrinin VE içerik güvencesinin DIŞINDA durur (Faz 81, F-45, kullanıcı kararı)** | 2026-08-22 | Karar kullanıcıya soruldu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-552. | K-320'nin kendi yeniden açılma koşulu (MAF tool döngüsünü agent seviyesine taşırsa) bu yerleşimi de etkiler |
| **K-553 — `AllowConcurrentToolCalls`, `ModelProviderRegistry`'nin kurduğu `FunctionInvokingChatClient` örneğine bağlanır; MEAI 1.18.0'ın getirdiği `ChatClientAgentOptions.AllowConcurrentInvocation`'a DEĞİL (Faz 81, F-134)** | 2026-08-22 | Plan yazıldıktan SONRA ölçülen bir sürüm yükseltmesi (K-543/K-544 turu, MAF 1.16→1.18) bayrağın ikinci bir olası evini açtı: `ChatClientAgentOptions.AllowConcurrentInvocation`. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-553. | MAF `ChatClientAgentOptions.AllowConcurrentInvocation`'ı, AgentPrism'in ÖNCEDEN sardığı bir istemciyi de etkileyecek şekilde belgelerse yeniden değerlendirilir |
| **K-554 — `AgentPrismResponseCachingChatClient` `internal sealed`dır; planın taslağı `public` diyordu (Faz 81, plandan sapma)** | 2026-08-22 | Ölçüldü: bu sınıfla AYNI kategorideki (`ModelProviderRegistry.BuildPipeline`'ın kurduğu, tüketicinin asla doğrudan örneklemediği saf halka) her sınıf — `FallbackChatClient`, `ContentFilterDetectingChatClient`, `CircuitBreakingChatClient`, `ProviderConcurrencyLimitingChatClient` — `internal sealed`dır; yalnız DI'a kayıtlı, tüketiciye görünmesi gereken servisler (`ModelProviderCircuitBreaker`, `ProviderConcurrencyLimiter`, `ModelProviderHealthCache`) `public`tir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-554. | Bir tüketici bu halkayı DOĞRUDAN test etmek veya değiştirmek isterse (bugün hiçbir emsal sınıf bunu istemiyor) |
| **K-555 — Yanıt önbelleğinin okuma/yazma hatası `run`'ı DURDURMAZ; loglanır ve isabet/ıska sessizce devam eder (Faz 81, plan metninde açık değildi, repo-geneli ilkeden türetildi)** | 2026-08-22 | Fazın kendi risk tablosu şunu talep ediyordu: "yazma hatası `run`'ı durdurmaz, loglanır — gözlemlenebilirlik işlevselliği bozmaz kuralının önbellek karşılığı" — ama planın taslak imzaları (`WriteCacheAsync`/`WriteCacheStreamingAsync`'in doğrudan `_storage.SetAsync` çağırması) bunu UYGULAMIYORDU. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-555. | — |
| **K-556 — `POST /api/agents/validate`'de eksik `IDistributedCache` `invalid_setting`/`model.providerSettings` olarak görünür; planın beklediği `compilation_error` DEĞİL (Faz 81, plandan sapma, ölçümle)** | 2026-08-22 | Plan 81.4 akış şemasında hatanın `compilation_error` olarak `POST /api/agents/validate`'e çıkacağını varsayıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-556. | — |
| **K-557 — 🚨 Önbellek isabeti hem `ChatResponse.Usage`'ı hem her mesajın `UsageContent`'ini SIYIRIR; aksi hâlde bir `run` kaydı hiç faturalanmamış token'ı ikinci kez sayar (Faz 81, gerçek `samples/AgentPrism.Api` koşumunda ölçülen kusur)** | 2026-08-22 | Otomatik testler (sahte sağlayıcı varsayılan olarak `UsageContent` üretmez) bunu YAKALAMADI; yalnız `samples/AgentPrism.Api` üzerinden gerçek bir OpenAI çağrısıyla ortaya çıktı — `faz-tamamlama`'nın "gerçek run" adımı tam olarak bunun için var. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-557. | Bir tüketici isabet eden çağrının orijinal kullanım bilgisini GÖRMEK isterse (ör. denetim amaçlı) — bugün hiçbir emsal bunu istemiyor, o zaman ayrı bir alan (`CachedUsage` gibi) eklenir, mevcut `Usage` sıyrılmaya devam eder |
| **K-558 — `SqlConversationBranchStore` içerik korumasına dokunmaz: dallanma `conversation_items.item`'i BAYT BAYT kopyalar, zarf DOKUNULMADAN kalır (Faz 82, plandan sapma)** | 2026-08-22 | Planın 82.2 yazma yeri tablosu bu dosyayı bir dokunulacak yer sayıyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-558. | Bir gün dallanma satırları farklı bir anahtarla yeniden şifrelenmek istenirse (bugün ölçülmüş bir ihtiyaç değil) |
| **K-559 — `IContentProtector` varsayılanı `NullContentProtector`, `AddAgentPrism()` içinde `TryAddSingleton` ile kaydedilir; `AddContentProtection(...)` `Replace` ile değiştirir (Faz 82, `IAuditLog`/`InMemoryAuditLog` deseninin aynısı)** | 2026-08-22 | K1'in kapısı kaydın kendisidir, bir `Enabled` bayrağı değil — `AddContentGuard`'ın "kayıt olmadan hiçbir guard eklenmez" ilkesiyle aynı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-559. | — |
| **K-560 — `SqlStoreContext.ContentProtector` `IContentProtector?` (nullable); `NullContentProtector.Instance`'a varsayılan DEĞERİ YOKTUR (Faz 82)** | 2026-08-22 | `NullContentProtector` `AgentPrism.Core`'da `internal`dır ve Core'un `InternalsVisibleTo`'su yalnız KENDİ test projelerini kapsar (`docs/hafiza`'nın "linked-source bir tipin internal işareti cross-assembly güvenilmez" maddesinin ikizi, ama ters yönde: burada tip Core'da yaşıyor, tüketen kod Sql.Shared'da — üç sağlayıcı derlemesinde AYRI derlenir ve hiçbiri Core'un internal üyelerini göremez). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-560. | `NullContentProtector` başka bir nedenle `public` olmak zorunda kalırsa (bugün yok), o zaman varsayılan değer eklenir ve nullable kaldırılır |
| **K-561 — `AgentPrismContentProtectionOptions.Keys` bir kid'i anahtarın KENDİSİNE değil, yapılandırma anahtarının ADINA eşler (K-059 örüntüsünün tekrarı, Faz 82)** | 2026-08-22 | Bu ayar veritabanına hiç yazılmaz ve bir HTTP ucundan da ayarlanmaz — K-059'un asıl gerekçesi (bir kaydın veritabanına sızması) burada BİREBİR geçerli değildir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-561. | — |
| **K-562 — Zarf biçimi metin sütunları için JSON (`$apEnc`/`kid`/`n`/`c`), ikili sütun için sabit başlıklı biçim (`APEB` + sürüm + kid uzunluğu + kid + nonce + şifreli+etiket) — tek yerde tanımlı (Faz 82)** | 2026-08-22 | 82.1'in ölçtüğü zorunluluk: altı sütun JSON geçerliliği ister (PostgreSQL `json`/`jsonb` tipiyle, SQL Server/SQLite `CHECK`'le) ve ham şifreli metin bunu ihlal eder. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-562. | — |
| **K-563 — `AgentPrismContentProtectionOptionsValidator` yalnız YAPISAL doğrulama yapar (`ActiveKeyId` set ve `Keys`'te var); anahtarın gerçek değerini `IConfiguration` üzerinden ÇÖZMEZ (Faz 82)** | 2026-08-22 | Anahtar çözümü BİLEREK tembeldir (82.4): bir `kid`, o `kid`'i taşıyan son satır silinene kadar yapılandırmada kalmalıdır ve bu, değerin HER ZAMAN başlangıçta doğrulanabilir olacağını garanti etmez (örn. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-563. | Anahtar çözümünün de başlangıçta doğrulanması istenirse (bugün ölçülmüş bir talep yok); o zaman validator'a `IConfiguration` eklenir |
| **K-564 — `AgentPrism.Client` NSwag ile üretilir; üretilen kod repoya commit edilir ve davranışsal bir kapıyla (`ClientCoverageTests`) izlenir, byte-diff kapısı DEĞİL (Faz 83, kullanıcı kararı)** | 2026-08-22 | Üç yol tartıldı: Kiota tüketicinin bağımlılık grafiğine `Microsoft.Kiota.*` paketleri ekler (K-007 gerekçesi ister, Faz 27 emsali — 37 paket ertelendi, K-212); elle yazılmış dar yüzey 160 operasyonun onda birini kapsar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-564. | — |
| **K-565 — `AgentPrism.Client` `AgentPrism.Abstractions`'ı REFERANSLAMAZ; DTO'lar OpenAPI belgesinden üretilir, sunucu tipleriyle aynı şekle sahip ama FARKLI CLR tipidir (Faz 83)** | 2026-08-22 | Abstractions'ı yeniden kullanmak kod üretecine karşı savaşmak demektir (üreteç kendi tiplerini üretir, elle yazılmış tiplerle eşlemek ekstra bir katman ister) ve istemciye `IRunStore` gibi SUNUCU TARAFI depo arayüzlerini de taşırdı — bir HTTP tüketicisinin hiç görmemesi gereken soyutlamalar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-565. | — |
| **K-566 — `AgentPrism.Client` ve `AgentPrism.Cli` public API takibinin (`PublicApiAnalyzers`) DIŞINDADIR (Faz 83, Açık Soru 2 karar A)** | 2026-08-22 | Yüzey (160 operasyon, ~250 DTO) `docs/openapi/agentprism.json`'dan TÜRETİLİR ve belgenin kendi kapısı (sunucu tarafında `OpenApiSnapshotTests`, istemci tarafında `ClientCoverageTests`) zaten var — ikinci kez izlemek her `dotnet nswag run` sonrası binlerce satırlık, gözden geçirilmeyen bir `PublicAPI.Unshipped.txt` diff'i üretirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-566. | — |
| **K-567 — `AgentPrism.Client`'ın AOT sözünden VAZGEÇİLDİ: `AgentPrismAotCompatible=false` (Faz 83, ölçülen risk gerçekleşti)** | 2026-08-22 | `AgentPrismClientJsonContext`'in `TypeInfoResolver`'a bağlanması ÇALIŞMA ANINDA reflection'sızdır (doğrulandı — bkz. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-567. | NSwag'ın gelecek bir sürümü `JsonTypeInfo<T>` tabanlı çağrı üretirse (bugün deneysel `SystemTextJson` modunda yok) yeniden ölçülür |
| **K-568 — `IMigrationApplier` arayüzü `AgentPrism.Abstractions`'a eklendi: linked-source `MigrationRunner` tipinin üç sağlayıcı derlemesinde belirsiz olması sorununu çözer (Faz 83)** | 2026-08-22 | `MigrationRunner` `AgentPrism.Sql.Shared`'dan (K-176) üç SQL sağlayıcı paketine AYRI derlenir; `agentprism migrate` CLI komutu `--provider` argümanına göre ÜÇÜNÜ DE aynı derlemede referanslar ve `MigrationRunner`'a unqualified bir referans `CS0433` verir (aynı ad, üç farklı derleme kimliği). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-568. | — |
| **K-569 — `AddAgentPrismClient` `IServiceCollection` döner, `IHttpClientBuilder` DEĞİL; `IHttpClientFactory` kullanılmaz (Faz 83, plandan sapma)** | 2026-08-22 | Planın taslak imzası `IHttpClientBuilder` dönüyordu — ama bu tip `Microsoft.Extensions.Http` paketinden gelir ve §83.7'nin ölçtüğü "sıfır paket" iddiasını bozardı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-569. | — |
| **K-570 — Üretim öncesi OpenAPI belgesinin bir kopyasında her şema `additionalProperties: false` ile kapatılır (Faz 83, ölçülen kusur)** | 2026-08-22 | Hiçbir şema `additionalProperties` anahtarı taşımıyordu; NJsonSchema'nın C# üreteci bunu "keyfi ek JSON alanı kabul edilir" sayıp HER üretilen DTO'ya bir `[JsonExtensionData]` yakalama özelliği (`AdditionalProperties`) ekliyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-570. | — |
| **K-571 — Enum'lar için `[JsonConverter(typeof(JsonStringEnumConverter<T>))]` TİP DÜZEYİNDE her `enum` bildirimine eklenir; `JsonSourceGenerationOptions.Converters` listesi KULLANILMAZ (Faz 83, ölçülen STJ davranışı)** | 2026-08-22 | İlk tasarım 67 enum dönüştürücüsünü `AgentPrismClientJsonContext`'in `[JsonSourceGenerationOptions(Converters = [...])]` listesine TEK YERDEN kaydediyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-571. | Bir gelecek STJ/NSwag sürümü `[JsonSourceGenerationOptions].Converters`'ı property-erişilebilir tipler için de onurlandırırsa yeniden ölçülür |
| **K-572 — `agentprism health` `/api/models/health`'i okur; `MapAgentPrism` içinde jenerik bir `/health` ucu YOKTUR (Faz 83)** | 2026-08-22 | `AgentPrismHealthCheckExtensions`'ın kendi XML dokümanı açık: AgentPrism `MapHealthChecks`'i KENDİSİ ÇAĞIRMAZ — tüketici kendi `/health` yolunu seçer, bu yol `MapAgentPrism`'in öneki ALTINDA değildir ve OpenAPI belgesinde YER ALMAZ (üretilen istemci onu hiç göremez). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-572. | AgentPrism ileride `MapAgentPrism` altına kendi genel sağlık ucunu eklerse (bugün planlanmıyor) `health` komutu ona geçer |
| **K-573 — OpenAPI belgesi VARSAYILAN KAPALI uçları da tarif eder; `GET /api/diagnostics` belgeye girdi ama davranışı değişmedi (Faz 84, §84.3)** | 2026-08-22 | `/api/diagnostics` zaten vardı (Faz 33'ten beri, `AgentPrismEndpointOptions.EnableDiagnosticsEndpoint` ile açılır) ama belgede yoktu — `OpenApiSnapshotTests`'in test host'u ucu hiç açmadan çalışıyordu, `AddOpenApi`/Minimal API'nin OpenAPI üreteci de o yüzden onu hiç görmüyordu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-573. | Belgeye girmesi İSTENMEYEN bir iç/deneysel uç eklenirse (bugün yok), belge üretim test host'unun `configureEndpoints`'i o ucu bilerek AÇMAZ — varsayılan davranış zaten budur |
| **K-574 — `packages/` kök dizini dil sınırı kapısının (`SourceLanguageTests`) kapsamına girdi; `PackagedReadmePattern` regex'i `src/`'nin yanına `packages/`'ı da aldı (Faz 84, §84.7, denetim 🟡 bulgusu)** | 2026-08-22 | `ScanRoots`'a `"packages"` eklemek tek başına YETERSİZDİ: `PackagedReadmePattern = ^src/[^/]+/README\.md$` yalnız `src/<Paket>/README.md`'yi tanıyordu, `packages/agentprism-client/README.md`'yi DEĞİL — bir npm README'si tıpkı bir NuGet README'si gibi paketin kendisiyle birlikte sevk edilir ve registry'de görünen metindir, aynı "kapsam dışı Markdown" istisnasını hak eder. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-574. | Üçüncü bir kök dizin (`src`/`packages` dışında) paket README'si sevk etmeye başlarsa regex yine genişletilir |
| **K-575 — `src/AgentPrism.UI/frontend/package.json`, `@agentprism/client`'a `file:../../../packages/agentprism-client` ile bağlanır; npm registry'den kurulmayı BEKLEMEZ (Faz 84, §84.8)** | 2026-08-22 | Konsol paketin npm'e yayınlanmasını beklerse (yayın yalnız bir `v*` git tag'inde olur) her geliştirme derlemesi ya npm'den ESKI bir sürüm çeker ya da paket hiç yoksa kırılır — repo içi tek bir çalışan ağaç istenir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-575. | — |
| **K-576 — npm yayın işi (`ci.yml`) NuGet'in `--skip-duplicate`'ine karşılık `npm view` ile elle idempotency kontrolü yapar; ikisi de AYNI `v*` git tag'inden türer, ayrı bir npm sürüm şeması YOKTUR (Faz 84, §84.11)** | 2026-08-22 | npm CLI'nin `dotnet nuget push --skip-duplicate`'in doğrudan karşılığı yoktur — `npm publish` var olan bir sürüme tekrar denendiğinde HATA verir (`403`/`EPUBLISHCONFLICT`), CI'yi kırar. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-576. | — |
| **K-577 — Konsol göçü TAM göçtür; üretilen istemcinin üzerine adlandırılmış bir cephe (facade) katmanı EKLENMEDİ (Faz 84, planın kendi kaçış merdiveni kullanılmadı)** | 2026-08-22 | Plan bu riski açıkça öngörmüştü ("göç tek fazda taşınamayacak kadar genişlerse `api.ts` üretilen istemcinin üzerinde adlandırılmış ince cephe olarak kalır"). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-577. | Gelecekte üretilen istemci OpenAPI belgesiyle uyumsuz büyük bir kırılma yaparsa (bugün ölçülmüş bir risk yok) cephe seçeneği yeniden değerlendirilir |
| **K-578 — OpenAPI üretecinin iki sistemik kusuru (eksik `required`, `number\|string` karışımı) VE paylaşılan-şema nullable sızıntısı frontend'de `Fix<T,K>` tek yardımcı tipiyle düzeltilir; sunucu şeması DEĞİŞTİRİLMEZ (Faz 84, §84.6, Açık Soru 4 karar A)** | 2026-08-22 | Ölçüldü: ASP.NET Core'un OpenAPI üreteci (a) C# tarafında varsayılan değerli bir özelliği (`ToolNames { get; init; } = [];`) `required` İŞARETLEMEZ — halbuki `System.Text.Json` bir YANIT nesnesinde bu alanı HER ZAMAN yazar (heuristik istek/yanıt körüdür); (b) belgedeki HER `int`/`long` alanını (254 şema, 72 doğrudan) `["integer","string"]` yapar — repoda `JsonNumberHandling` hiç kullanılmıyor (`grep -rn JsonNumberHandling src` boş), yani bu alan HİÇBİR ZAMAN tel üzerinde string olarak gelmez; (c) aynı şema adı (ör. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-578. |string" yarısı üretecin kendi davranışı değişmedikçe kalıcıdır |
| **K-579 — `Microsoft.AspNetCore.Mvc.Testing` yalnız GERÇEK giriş noktalı örnek uygulamaları test eden projelerde kullanılır; kütüphane testleri `TestHost` kalır (Faz 85)** | 2026-08-22 | Faz 4'ün K-notu `Mvc.Testing` DEĞİL demişti: `WebApplicationFactory<T>` bir giriş noktası derlemesi ister ve `AgentPrism.*` bir kütüphanedir — bu gerekçe hâlâ geçerli. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-579. | Kütüphanenin kendi test projelerinden biri gerçek bir giriş noktasına ihtiyaç duyarsa (bugün yok) aynı gerekçeyle genişletilebilir; tersine, `Mvc.Testing` bir kütüphane test projesine sızarsa bu karar yeniden açılır |
| **K-580 — SQL-tabanlı `jsonb`/`json` yükü taşıyan her ARA tip (`AgentDefinitionPayload` gibi), kaynak tipe (`AgentDefinition`) yeni alan eklendiğinde ELLE senkronize edilmelidir; derleyici bunu zorlamaz (Faz 86, ölçülen kusur)** | 2026-08-22 | `AgentDefinition.Parameters`/`SharedInstructionsName` eklendi, `AgentContracts.cs` ve bellek-içi depo doğru güncellendi, ama `AgentPrism.Sql.Shared/Internal/AgentDefinitionPayload.cs` — jsonb'ye yazılan gerçek tip — unutuldu. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-580. | Yeni bir alan `AgentDefinition`'a eklenirken bu kontrol listesine `AgentDefinitionPayload.cs`'i de ekleyecek bir kalıcı hatırlatma (`docs/hafiza/`) yoksa tekrar yaşanır |
| **K-581 — `CompiledAgentCache`'i atlayan bir çağıran, `IAgentCatalog.ResolveAsync`'in UYGULADIĞI `IAgentDecorator` zincirini de ELLE uygulamak zorundadır; yeni `AgentDecoratorPipeline.Apply` bunu tek bir yerde toplar (Faz 86, ölçülen kusur)** | 2026-08-22 | `AgentDefinitionCompiler.CompileParameterizedAsync` doğrudan çağrıldığında (parametreli `/run` ve eval vakası) `CompositeAgentCatalog.ResolveAsync`'in normalde uyguladığı dekoratör zinciri (`RunRecordingAgentDecorator`, `OpenTelemetryAgentDecorator`, `ToolApprovalAgentDecorator`) HİÇ ÇALIŞMIYORDU — kod derlendi, 632 fonksiyonel test geçti (hepsi bellek-içi/test host'ta koşuyordu ve kayıt davranışını doğrudan test etmiyordu), ve kusur yalnız `samples/AgentPrism.Api`'ye karşı gerçek bir `run` sonrası `GET /api/runs?agentName=parametreli`'nin BOŞ dönmesiyle ortaya çıktı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-581. | Gelecekte üçüncü bir "katalog dışı compile" çağıranı eklenirse (bugün yok) o da bu yardımcıyı çağırmak zorundadır — derleyici bunu zorlamaz, yalnız kod incelemesi/bu not yakalar |
| **K-582 — `AgentPrismOptions.MaxParameterValueLength` tek, üst-düzey bir sınırdır; parametre başına ayrı bir sınır YOKTUR (Faz 86, Açık Soru 2 kapatıldı, öneri A)** | 2026-08-22 | `MaxInstructionsLength`in (`SkillEndpoints.cs`) izlediği aynı desen: tek bir `int` (varsayılan 4096 bayt UTF-8), `AgentPrismOptionsValidator`'da pozitiflik kontrolüyle. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-582. | — |
| **K-583 — Kesilen bir işi sürdürmek üçüncü, ayrı bir işlemdir (`JobKind.RunContinuation`, `runs.continued_from_run_id`); `Replay` (K-315) ve `ApprovalResume` (K-368) ile KARIŞTIRILMAZ (Faz 87, kullanıcı kararı)** | 2026-08-23 | Üç işlem yüzeysel olarak benzer görünür (hepsi yeni bir `RunId` açar) ama tetikleyicisi ve oturum ilişkisi farklıdır: Replay kullanıcı ister ve OTURUMSUZDUR (K-315); `ApprovalResume` kullanıcının onay kararıyla AYNI oturumda devam eder; `RunContinuation` KİMSE istemeden, orphaned-run uzlaştırmasının kendisi tetikler ve AYNI oturumda devam eder. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-583. | — |
| **K-584 — `Destructive`/`External` etkili bir tool taşıyan koşu varsayılan olarak devam ETMEZ; gevşetme bir AYARLA değil, tool'un kendi `SafeToRepeat` bildirimiyle yapılır (Faz 87, kullanıcı kararı)** | 2026-08-23 | `ADAYLAR.md`'nin karşı görüşü doğrulandı: tek bir kurulum ayarı (`AllowDestructiveContinuation` gibi) kapsamı TOPTAN açardı ve üretimde veri bozardı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-584. | Üretimde `SafeToRepeat` yanlış bildirilmiş bir tool bulunursa (yanlış pozitif idempotency iddiası) tekil vaka olarak ele alınır; genel mekanizma değişmez |
| **K-585 — Devam koşusunun eşleşmeme politikası `RecordedToolPlayback`'in (internal) kurucusuna bir `ToolPlaybackMismatchPolicy` parametresi olarak eklendi; `ReplayToolMode`'a dördüncü bir üye veya ikinci bir sarmalayıcı tipi AÇILMADI (Faz 87, Açık Soru 1, öneri A)** | 2026-08-23 | `ReplayToolMode` (`NoTools`/`ReplayTools`/`LiveTools`) bir REPLAY isteğinin sözleşmesidir ve devam bir replay isteği değildir — dördüncü bir üye eklemek iki farklı kavramı aynı enum'da karıştırırdı. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-585. | — |
| **K-586 — Skill veya çağrılabilir alt-agent kullanan bir agent DEVAM ETTİRİLEMEZ; bu Faz 87'nin planında YOKTU, uygulama sırasında ölçülüp keşfedilen bir sınırdır** | 2026-08-23 | `RecordedToolPlayback`'in sarmaladığı yalnız STANDART tool-çağırma boru hattıdır. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-586. | Skill/alt-agent replay'i (`RecordedToolPlayback`'in `AIContextProvider` boru hattına genişletilmesi) ayrı bir aday olarak `docs/ADAYLAR.md`'ye yazılabilir |
| **K-587 — Workflow düğüm retry'ı yalnız `AddWorkflowFunction` ile kaydedilen fonksiyon düğümlerini kapsar; retry döngüsü düğümün KENDİ çağrısının içinde kalır ve `MaxSuperSteps` sayacını ETKİLEMEZ — ÖLÇÜLDÜ (Faz 87, Açık Soru 2 kapatıldı, öneri B)** | 2026-08-23 | Kapsam daraltması ölçümle geldi: `Concurrent`/`Handoff`/`GroupChat`/`Magentic` desenleri MAF'ın KENDİ `AgentWorkflowBuilder` metotlarıyla kurulur ve AgentPrism'in sarmalama noktası YOKTUR — yalnız `Sequential` fonksiyon düğümleri ve karma-zincirdeki agent adımları AgentPrism'in KENDİ derleyicisinin kurduğu bir delege üzerinden geçer. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-587. | Diğer dört desene retry eklemek MAF'ın kendi builder'ının ürettiği graf üzerinde ayrı bir araştırma gerektirir |
| **K-588 — MEAI görsel üretim yüzeyi deneysel kaldığı sürece `MEAI001` bastırması yalnız görsel kayıt ve çağrı sınırlarında DAR tutulur (Faz 88, plan sapması)** | 2026-08-23 | Plan, `Microsoft.Extensions.AI` 10.9.0'ın görsel yüzeyini GA sanıyordu; gerçek paket `IImageGenerator` ve ilgili tiplerde `MEAI001` yayıyor. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-588. | MEAI görsel üretim API'si stabil olduğunda veya imzayı değiştirdiğinde |
| **K-589 — Birden çok image provider kaydı, `AgentPrismImageOptions.Provider` adına göre KEYED çözülür; isimsiz kayıt yalnız özel consumer fallback'idir (Faz 88, denetim öncesi tasarım bulgusu)** | 2026-08-23 | Tek isimsiz `IImageGenerator` kaydı, OpenAI/Azure/Google aynı hostta bulunurken son DI kaydını sessizce seçer; fiyat sağlayıcısı ile gerçek çağrı sağlayıcısı ayrışabilir. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-589. | DI'nin keyed kayıt modeli kaldırılırsa veya consumer gerçek bir çoklu-özel-generator senaryosu getirirse |
| **K-590 — Görsel ek deposu yalnız doğrulanabilir baytı kalıcılaştırır; `HostedFileContent` fail-closed reddedilir (Faz 88, plan sapması)** | 2026-08-23 | `IAttachmentStore` byte dizisi, medya tipi ve dosya adı saklar; sağlayıcı hosted-file kimliği için dayanıklı bir alan taşımaz. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-590. | `IAttachmentStore` dayanıklı hosted-file referansını modellediğinde |
| **K-591 — Google image adapter, `WIDTHxHEIGHT` isteklerini tahminden çevirmek yerine REDDEDER (Faz 88, plan sapması)** | 2026-08-23 | MEAI `ImageSize` genişlik/yükseklik ister; Google'ın image API'si bu sayıyı aynı anlama taşıyan bir değer değil, ayrı aspect-ratio ve size-tier seçenekleri bekler. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-591. | Google API, genişlik/yükseklik için bire bir sözleşme eklerse veya AgentPrism ayrı, doğrulanmış bir eşleme sözleşmesi tasarlarsa |
| **K-592 — Tool çıktısı boyut sınırı UTF-8 bayt biriminde ölçülür; token veya karakter DEĞİL (Faz 89, kullanıcı kararı)** | 2026-08-23 | `McpResourceTrimming`/`MaxInstructionsLength`/`MaxResourceBytesTotal` kurulum ayarlarının HEPSİ zaten bu birimdedir — üçüncü bir birim tutarsızlık üretirdi. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-592. | — |
| **K-593 — Kırpılan tool çıktısı `{"truncated","omittedBytes","content"}` alanlı bir JSON zarfına sarılır; zarf `Utf8JsonWriter` + `JavaScriptEncoder.UnsafeRelaxedJsonEscaping` ile üretilir (Faz 89, kullanıcı kararı + plan sapması)** | 2026-08-23 | Zarf, bayt kırpmanın bir JSON'u ortasından kesme riskini yapısal olarak kapatır — kırpılmış metin artık bir JSON string DEĞERİDİR. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-593. | — |
| **K-594 — `TruncatingAIFunction` yalnız `string` ve `JsonElement` sonuçları ölçer/kırpar; başka bir ham CLR nesnesi HİÇ dokunulmadan geçer (Faz 89, bağımsız denetim bulgusu, plan Açık Soru 1 kapatıldı)** | 2026-08-23 | Plan taslağı ölçümü `result?.ToString()` ile yapıyordu (`ToolInvocationTracker`'ın emsaliyle). **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-594. | Kod üreticisi ileride tip-güvenli bir serileştirme yolu kazanırsa (ör. kaynak üretilen bir `JsonSerializerContext`) kapsam genişletilebilir |
| **K-595 — `TruncatingAIFunction`, zarfın asla sınırı AŞMAYACAĞINI kurucuda GARANTİ eder: `maxOutputBytes`, `MinimumEnvelopeBytes`'ın (bugün 57 B, `omittedBytes` için `int.MaxValue` worst-case ile hesaplanır) altındaysa `ArgumentOutOfRangeException` atar (Faz 89, bağımsız denetim bulgusu)** | 2026-08-23 | Denetim, gerçek derlenmiş DLL'i reflection ile çağırarak KANITLADI: eski kod `budget <= 0` olduğunda zarfın gerçekten sığıp sığmadığını KONTROL ETMEDEN dönüyordu — `maxOutputBytes=10` iken zarf 51 bayt (sınırın 5,1 katı) üretti. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-595. | — |
| **K-596 — `McpResourceTrimming` `AgentPrism.Mcp`'den `AgentPrism.Core`'a `TextTrimming` adıyla TAŞINDI (kopyalanmadı) ve `public` yapıldı (Faz 89, plan)** | 2026-08-23 | Fonksiyon artık MCP'ye özgü değildir — `TruncatingAIFunction` de aynı bayt-kırpma mantığını ister. **Tam gerekçe:** [`arsiv/KARARLAR-GECMISI.md`](arsiv/KARARLAR-GECMISI.md) — K-596. | — |
| **K-597 — Arşivlemek TAŞIMAK değil DAMITMAKTIR: kapanan fazın dokümanı tam metniyle değil, sabit boyutlu bir kayıtla arşivlenir (Faz 90) (kullanıcı kararı)** | 2026-08-23 | Ölçüldü 2026-08-23: `docs/` 144.132 satır, `src/` 139.425 satır C#. Kapanmış 91 faz dokümanının **%62'si plandı** ve kapanışta ölüyordu — `Planlanan Public API` (4.114 satır) `Gerçekleşen` muadili tarafından geçersiz kılınıyor, `Bu Faza Başlarken` (2.088) bir oturum talimatı, `NN.x` iş kalemleri (12.285) planın gövdesi ve sonucu koddadır. `faz-damit` bunları düşürür; `Plandan Sapmalar`, `Bu Fazda Verilen Kararlar`, `Denetim Bulguları`, `Sonraki Faza Devir Notu` ve DoD **aynen** kalır. Sonuç: 3.040.663 → 1.200.025 B (−%61), medyan kayıt 12.584 B, faz başına arşiv büyümesi ~41 KB → ~9 KB. | Damıtılmış bir kayıttan kalıcı bilgi kaybı ölçülürse — o zaman düşen bölüm kümesi daraltılır, damıtma tümüyle geri alınmaz. |
| **K-598 — Tam metin git geçmişinde yaşar ve çözülebilirliği HER denetimde kanıtlanır; `git log --follow` sözleşme DEĞİLDİR (Faz 90)** | 2026-08-23 | Damıtma içerik silmez: her kayıt `git show <sha>:<yol>` komutunu taşır ve `tam_metin_denetle()` bu SHA'yı her `--denetle` koşumunda çözer. Git geçmişine güvenmek ancak bir kapı onu her koşumda doğruluyorsa meşrudur — `filter-branch`, agresif `gc` veya sığ klon SHA'yı geçersizleyebilir. `fetch-depth: 0` MinVer yüzünden zaten zorunludur (`ci.yml:35`) ve beklenmedik bir sigortadır. Ölçüldü: `git log -1 --format=%H -- <yol>` 91/91 faz dokümanı için tam metni aynı yolda verdi (00–59 taşıması dahil). İkincil çıpa `docs/damitma-oncesi-2026-08` etiketidir; eğik çizgili ad bilinçlidir — MinVer'in SemVer ayrıştırıcısına takılmaz, sürüm ölçüldü ve `0.0.0-preview.0.310` değişmedi. | Depo geçmişi yeniden yazılırsa (`filter-branch`, `gc --prune`) — kapı kırmızıya döner ve etiket/SHA sözleşmesi yeniden kurulur. |
| **K-599 — `HARIC` muafiyeti KORUNUR; muaf tutulan her ağaç KENDİ bütçesini alır (Faz 90) (kullanıcı kararı)** | 2026-08-23 | Kusur muafiyet değil, muafiyetin **sınırsız** olmasıydı: ölçüldü, `docs/`un %62'si (89.569 satır) hiçbir tavana tabi değildi ve `--denetle` bunu "denetim dışı 5.324.346 B — sınırı etkilemez" diye yazıyordu. `HARIC`i kaldırmak arşivlemenin sayacı düşürmesini engeller ve tek çıkışı **silmek** yapar; `AGENTS.md` bunu yasaklar. Bunun yerine `DIZIN_BUTCESI` anahtarı üçlüye çıkarıldı ve `docs/arsiv` (3.020.000), `kosumlar` (620.000), `kesif` (260.000) kendi sınırlarını aldı; `docs/**` 5.000.000 → 3.030.000'e **düşürüldü**. Damıtma bunu ödenebilir kıldı. 🚨 `_dizin_boyutu` ve `_commit_boyutu` HARIC'i koşulsuz düşüyordu — `docs/arsiv` kendini düşürüp **0** ölçüyordu; üçlü anahtar olmasa üç yeni bütçe sessizce anlamsız olurdu. | Yeni bir ağaç `HARIC`e eklenirse — testi kırar: her muaf ağacın `DIZIN_BUTCESI`'nde karşılığı olmalıdır. |
| **K-600 — `KARARLAR.md` satırı 450 bayta indirilir; kesilen gerekçe `KARARLAR-GECMISI.md`'ye ÖNCE taşınır, satır SONRA kısaltılır (Faz 90)** | 2026-08-23 | Dosya 456 KB'ye ulaşıp bütçesinin %4'üne düşmüştü; medyan satır 625 B, en uzunu 4.052 B. İşaretçi formatı zaten vardı (376 satırda) ama bayt tavanı ve kapı yoktu. Sıra bağlayıcıdır: önce kes-sonra taşı bir kesintide kalıcı kayıp bırakır. ASLA kesilmez: başlık, tarih, yeniden açılma koşulu, `(kullanıcı kararı)` ve `yeniden açıldı` — `_kararlar_kalemleri()` 👤/🔁 işaretlerini satırın TAMAMINDA arar, kesilirse üretilen indeks **sessizce** yanlış olur (3 satır bu yüzden atlandı ve raporlandı). Sonuç: 482 satır, 134.230 B taşındı, 455.921 → 321.691 B; 596 kalem, 👤 113, 🔁 3 değişmedi. Taşınan metnin `docs/`e göre yazılmış bağlantıları yeniden yazılmazsa 292 kırık bağlantı doğuyordu — ölçüldü ve düzeltildi. | — |
| **K-601 — Public yüzey erişilebilirlik ölçütüyle daraltıldı: yaprak olup başka public imzada geçmeyen 96 tip `internal` yapıldı (Faz 96)** *(yeniden açar: K-285)* | 2026-08-24 | Ad araması (keşifin önerdiği ölçüt) üç yanlış pozitif üretti: uzatma metodu sınıfı, öznitelik tipi, `<see cref>` yorumu — bunun yerine "başka bir public üyenin imzasında geçmeyen tip yapraktır" ölçütü kullanıldı. `SingletonGuard` bu 96'nın içindeydi; K-285'in engeli (Mcp, Core'un `internal`'ını göremiyordu) Faz 88'de `InternalsVisibleTo("AgentPrism.Mcp")` eklenince zaten kalkmıştı. Üç yeni `InternalsVisibleTo` satırı (Core → PostgreSql/SqlServer/Sqlite, `Auditing*` store'ları ve `InMemoryRunStore` için) dışında paket sınırı değişmedi. `PublicSurfaceBaselineTests` yüzeyin yeniden büyümesini kilitler. | Bir aday yanlışlıkla tüketiciyi kırarsa (ölçülmedi — yüzey hiç sevk edilmedi) `internal` geri alınır |
| **K-602 — Tek sürüm hattı: paketlenen 19 projenin hepsi `1.0.0-preview.N` olarak çıkar (Faz 97) (kullanıcı kararı)** *(K-068'i kapatır)* | 2026-08-24 | Ayrık hat (18 stabil + 1 preview, yalnız `AgentPrism.AspNetCore` ön sürümde kalır) değerlendirildi ve reddedildi: `Abstractions` ve `Core`'u anında dondurur ve yüzey kararlarını acil hâle getirir. Preview hattında yüzey küçültme kırıcı değişiklik sayılmaz. Ölçüldü (2026-08-24): `dotnet pack` 19 paket üretiyor; ön sürüm üçüncü taraf bağımlılığı yalnız `AgentPrism.AspNetCore` beyan ediyor (K-008 tutuyor). Hat, MAF'ın `Hosting` paketleri GA olana kadar sürer. | MAF `Hosting` paketleri (`Microsoft.Agents.AI.Hosting*`, `A2A.AspNetCore`) GA olduğunda — o noktada `1.0.0` GA değerlendirilir |
| **K-603 — `PublicAPI.Shipped.txt` preview hattı boyunca boş kalır; 618 tipin `Unshipped` → `Shipped` dolumu `1.0.0` GA'ya ertelendi (Faz 97) (kullanıcı kararı)** *(K-421'in yeniden açılma notunu değiştirir)* | 2026-08-24 | `Shipped` bir uyumluluk sözüdür ve preview hattı o sözü vermez. Dolum, sonraki her yüzey küçültmesinde `RS0017` sürtünmesi ve bilinçli bir "kırıcı değişiklik" kaydı üretirdi — verilmemiş bir sözün bedeli. K-421'in bugünkü değeri (faz başına `Unshipped` diff'i) aynen sürer; yalnız dolum zamanı değişti. Bu karar Faz 7'nin özgün DoD satırını (`tüm PublicAPI.Shipped.txt dolu`) ve K-421'in "Faz 7 geldiğinde tek seferlik taşınır" notunu bilinçli olarak geçersiz kılar. | `1.0.0` GA'da — dolum tek seferde yapılır |
| **K-604 — Yayın işleri (`publish`, `npm-publish`) yayın provası kapısına (`release-dryrun`) bağlandı; kapı her push'ta (PR dahil) koşar (Faz 97)** | 2026-08-24 | Ölçüldü (2026-08-24): `ci.yml`'de `publish: needs: pack` ve `npm-publish: needs: build` idi — paketlerin içeriğini yayından önce doğrulayan hiçbir iş yoktu, ilk `v*` etiketi doğrudan nuget.org ve npm.org'a yazardı. `release-dryrun` işi (`python3 scripts/kapi.py yayin --kuru`) ağa hiçbir şey yazmadan kendi `dotnet pack` çıktısını beş sözleşmeye karşı doğrular: paket kimlik kümesi, tek sürüm hattı, `icon.png`, metaveri (`README`, lisans, `repository`+`commit`, `.snupkg`, XML doküman) ve K-008 ön sürüm sınırı. | — |
| **K-605 — Yeni paket `AgentPrism.Testing.Contracts.Xunit`: `IRunStore` ve 32 diğer store sözleşmesi xunit.v3 test taban sınıfı olarak sevk edilir; ad alanı `AgentPrism.Testing.Contracts.Storage` (Faz 98) (kullanıcı kararı — paket adı ve ad alanı)** | 2026-08-24 | `tests/Shared/Contracts/` altında 8.636 satırlık, sevk edilmeyen bir test ağacındaydı; üçüncü taraf bir `IRunStore` implementasyonu bu sözleşmeye erişemiyordu. Bağımlılığı yalnız `AgentPrism.Abstractions` (+ `Microsoft.Agents.AI` GA, `AgentFileStoreContract` için) — `AgentPrism.Core` **inmez**, ölçüldü (`dotnet list package --include-transitive`). Paket adı framework'ü açıkça taşır: `xunit.v3` değil `xunit.v3.extensibility.core` alır (düz `xunit.v3`'ün `buildTransitive` özellikleri `<OutputType>Exe</OutputType>` dayatır ve kütüphane projesini kırar) — bir NUnit/MSTest tüketicisi bu paketi kullanamaz, ad bunu söylüyor. | Üçüncü taraf geri bildirimi ad alanını veya paket adını yanlış bulursa — GA öncesi ucuz, `PublicAPI.Shipped.txt` boş |
| **K-606 — `ApiKeyGenerator`/`GeneratedApiKey` `AgentPrism.Core`'dan `AgentPrism.Abstractions`'a taşındı (Faz 98)** | 2026-08-24 | `AgentPrism.Testing.Contracts.Xunit`'in `ApiKeyStoreContract`'ı, `IApiKeyStore.FindByHashAsync`'in AgentPrism'in GERÇEK hash formülüyle bulunabildiğini kanıtlamak için `ApiKeyGenerator.ComputeHash`'e ihtiyaç duyar — bu iddia bağımsız bir hash algoritmasıyla ters çevrilemez (üretim kimlik doğrulaması AYNI formülü kullanır). Tip sıfır bağımlılık taşır (yalnız `System.Security.Cryptography`/`System.Text`), taşıma `AgentPrism` ad alanını korur (çağrı yeri değişikliği sıfır) ve her iki paketin `PublicAPI.Shipped.txt`'i bugün boş (K-603) — taşıma maliyeti düşük. | — |
| **K-607 — `IRunStore.AppendEventAsync` yinelenen `Sequence`'i REDDEDER (dört implementasyonda: bellek içi + üç SQL sağlayıcı) (Faz 98) (kullanıcı kararı)** | 2026-08-24 | Açık Soru 1'in A seçeneği: `RunEventWriter` `Sequence`'i tekil üretir; yinelenen bir değer bir **çağıran hatasıdır**. Sessizce yutmak (B seçeneği) olay akışında görünmez bir boşluk bırakır ve "append-only" sözleşmesini bulanıklaştırır. SQL tarafında `run_events(run_id, seq)` birincil anahtar ihlali `SqlDialect.IsUniqueViolation` ile yakalanıp `AgentPrismException`'a çevrilir (daha önce ham sürücü istisnası sızıyordu); bellek içi store aynı kuralı `AppendEventAsync` içinde elle uygular. Sözleşme testiyle dört sağlayıcıda da doğrulandı. | `IRunStore`'un genel yeniden deneme sözleşmesi değişirse |
| **K-608 — `StartRunAsync`'in dönüş değeri dört sağlayıcıda da COALESCE uygulanmış kaydı taşır (Faz 98)** | 2026-08-24 | Bellek içi store zaten böyle davranıyordu; üç SQL sağlayıcısı ham `record`'u (COALESCE öncesi `UserId`/`Labels`) döndürüyordu. PostgreSQL/SQLite `INSERT … ON CONFLICT … RETURNING user_id, labels`, SQL Server iki dallı upsert'in HER İKİ dalına da `OUTPUT inserted.user_id, inserted.labels` eklendi (tek dala eklemek sessiz bir sağlayıcı farkı üretirdi — Riskler tablosunda önceden işaretliydi). `DbHelpers.ReadSingleAsync` bu iki sonuç kümesini zaten doğru sırayla okuyordu (Faz 65'in SQL Server upsert deseni). Sözleşme testi dört sağlayıcıda da geri dönen `RunRecord.UserId`/`Labels`'ı doğrular. | — |
| **K-609 — AgentPrism, `IModelProvider.CreateChatClient`'ın döndürdüğü `IChatClient`'ı HİÇBİR ZAMAN dispose etmez; ömür sağlayıcınındır (Faz 99) (kullanıcı kararı)** | 2026-08-25 | Çalışma anı probuyla ÖLÇÜLDÜ: derlenmiş agent `Microsoft.Agents.AI.ChatClientAgent`'tır ve ne `IDisposable` ne `IAsyncDisposable` uygular; bir `run` sonrası ham istemcinin dispose sayısı **0**; `CompiledAgentCache.Evict` yalnız `TryRemove` yapar. (Boru hattının tepesi elle dispose edilirse zincir ham istemciye iner — ama eden yoktur.) Alternatif (AgentPrism'in dispose etmesi) REDDEDİLDİ: istemciler credential başına cache'lenip paylaşılır, dispose etmek hâlâ o istemciyi kullanan başka bir derlenmiş agent'ın altından nesneyi çekerdi. Sözleşme `IModelProvider` XML dokümanına ve `guides/model-providers` sayfasına yazıldı; `ModelProviderContract` dispose edilmeyen bir istemciden sonra sağlayıcının çalışmaya devam ettiğini doğrular. | MAF `ChatClientAgent`'a dispose semantiği eklerse |
| **K-610 — `ContractCoverage`'ın her çağrısı bir sözleşme AİLESİ adı alır; aile adı almayan aşırı yükleme YOKTUR (Faz 99)** | 2026-08-25 | Faz 98'in `ContractTypes()`'ı derlemedeki adı `Contract` ile biten HER public abstract tipi döndürüyordu. Sağlayıcı ailesi eklenince dört depolama kapsam testi (bellek içi + üç SQL) birden kırıldı: kendilerine ait olmayan sözleşmeleri türetmedikleri için. Kapsamlı bir aşırı yükleme EKLEYİP kapsamsızı bırakmak tuzağı bırakmak olurdu — bir depolama tüketicisi, AgentPrism sağlayıcı sözleşmesi yayınladı diye kırılabilirdi. Tip `…Contracts.Storage`'dan nötr `…Contracts` ad alanına taşındı (iki aileye birden hizmet ediyor). Aile adları `StorageContracts`/`ProviderContracts` sabitleridir (yazım hatası derleme hatası); hiçbir sözleşme tipiyle eşleşmeyen bir kapsam `ArgumentException` verir — sessizce hiçbir şeyi kontrol etmeyen yeşil bir kapı üretmez. Her iki paketin `PublicAPI.Shipped.txt`'i boş olduğu için taşıma maliyeti sıfırdır (K-603). | Üçüncü bir sözleşme ailesi eklenirse desen aynen tekrarlanır |
| **K-611 — Sözleşme suite'inde isteğe bağlı davranış ATLANAN senaryo değil, AYRI bir opt-in sınıftır (Faz 99)** | 2026-08-25 | Plan BYOK ve sağlayıcı ayarlarını tek sınıfta koşullu atlama ile tasarlamıştı. ÖLÇÜLDÜ: `Assert.Skip` `xunit.v3.assert` paketindedir ve o paket sözleşme paketinin çözülmüş grafiğinde YOKTUR (eklemek yeni bir paket bağımlılığı olurdu); xunit v3'ün `[Fact(SkipUnless = …)]` alternatifi ise **public static** bir özellik ister ve bizim koşulumuz örnek düzeyinde sanal bir üyeye bağlıdır. Sonuç: `ModelProviderCredentialContract` ve `ModelProviderSettingsContract` ayrı sınıflardır; türetmek niyet beyanıdır. Yan fayda, atlamadan daha iyidir: sessizce geçen senaryo kalmaz ve `ContractCoverage` muafiyeti hangi davranışın sunulmadığını BELGELER. | Sözleşme paketi bir gün `xunit.v3.assert` alırsa yeniden değerlendirilebilir |
| **K-612 — Uygulanmış migration baytları değiştirilebilir checksum manifestine değil Git kaynak commit'lerine sabitlenir (Faz 99, F-151)** | 2026-08-25 | Migration ledger'ı yalnız daha önce uygulanmış veritabanında hatayı yakalar; commit `9c32242` iki PostgreSQL migration yorumunu değiştirince mevcut kurulumlar açılmadı. `kapi.py tarama`, her migration'ın baytını `scripts/applied-migrations.json` içindeki sabit Git kaynak commit'inden okur; F-151 dosyaları uygulanmış ilk commit'lerine, diğerleri Faz 99 taban commit'ine bağlanır. Manifestte checksum değiştirmek artık dosya değişikliğini onaylayamaz. | Git geçmişi yeniden yazılırsa veya bir migration seti yeni kalıcı kaynak modeli gerektirirse |
| **K-613 — Yargıç model istemcisi ayrı `CreateSetupChatClientAsync` üyesiyle kurulur (Faz 100)** | 2026-08-25 | Planın `CreateChatClientAsync(binding, ModelCredentialSource.Setup, token)` overload'ı ölçüldü ve `RS0027` ile reddedildi: mevcut iki parametreli overload'ın optional `CancellationToken`'ı olduğundan, daha kısa overload artık en çok parametreli olamaz. O optional parametreyi kaldırmak preview olsa da mevcut tüketiciyi kaynak düzeyinde kırar. Ayrı isim, setup credential ile BYOK ayrımını açık tutar; her iki yol tenant egress policy'sini uygular. | Mevcut async overload kaldırılabilecek bir ana sürümde veya analyzer kuralı değişirse |
| **K-614 — `IToolRegistry` public kalır; tanınmayan implementation `IVerifiedToolRegistry` marker'ı taşımadığı için startup'ta reddedilir; `AllowUnverifiedToolRegistry` bilerek yapan için opt-out'tur (Faz 102)** | 2026-08-25 | Interface'i internal yapmak ÖLÇÜMLE elendi: `AgentDefinitionCompiler` ve `AgentPrismDiagnosticsCollector`'ın public ctor'ları onu parametre olarak alıyor — internal yapmak bu iki gerçek tüketici yüzeyini de internal yapmayı gerektirirdi. Bunun yerine `ToolRegistry` (ve `McpToolRegistry`) `IVerifiedToolRegistry` marker interface'ini uygular; `ToolRegistrationValidationService` startup'ta çözülen `IToolRegistry`'nin marker'ı taşıyıp taşımadığına bakar. Taşımıyorsa ve `AgentPrismToolOptions.AllowUnverifiedToolRegistry` (varsayılan `false`) kapalıysa `AgentPrismException` ile durur — çünkü `Replace` dört wrapper'ı (`Authorizing`/`Timeout`/`ApprovalRequired`/`Truncating`) sessizce kaybettirir. Bayrak açıkken tek bir `LogWarning` dört wrapper'ı adıyla sayar. | Interface bir gün yalnız internal tüketicilerce kullanılırsa (iki ctor'un public'liği kalkarsa) internal'a taşınabilir |
| **K-615 — Generator kendi ürettiği `JsonSerializerContext`'i kullanmaz; complex tool sonucu için tool sahibi kendi `[JsonSerializable]` context'ini `AgentPrismToolAttribute.JsonSerializerContext`'e verir, aksi hâlde derleme `APG0008` ile durur (Faz 102)** | 2026-08-25 | Plan taslağı generator'ın kendi `JsonSerializerContext`'ini emit etmesini öngörüyordu. ÖLÇÜLDÜ: Roslyn kaynak üreteçleri birbirlerinin ürettiği kaynağı AYNI derleme geçişinde göremez; gerçek tüketici build'i bu yaklaşımı `CS0534` ile kırdı. Yeni tasarım AOT-safe kalır (context yine derleme zamanı üretilir, reflection yok) ve rehber + sample bunu çalışır biçimde kanıtlar. Bu, `FunctionResultContent.Result`'ın kalıcı yazılan biçimini de değiştirir: tip adı yerine gerçek serialize JSON — content guard bypass'ı ve `MaxOutputBytes` ihlali aynı düzeltmeyle kapanır. | Roslyn çok-üreteç geçişini aynı derlemede desteklerse yeniden değerlendirilebilir |
| **K-616 — `CustomToolContract` yalnız implementer'ın TEK BAŞINA sağlayabileceği isim/metadata/eşzamanlı çağrı sözleşmesini taşır; tenant/timeout/envelope/guard davranışı registry-DI sınırını geçtiği için pakete Core bağımlılığı EKLENMEDEN Core fonksiyonel testlerinde ve sample run'ında kanıtlanır (Faz 102)** | 2026-08-25 | `IToolRegistry` bu fazda AgentPrism'in kendi güvenlik sınırı olarak doğrulandı (K-614); üçüncü tarafın onu implement etmesi tasarlanan kullanım değil. Sözleşme suite'i bu yüzden `IToolRegistry`'yi test etmez — `AgentPrismToolRegistration` + tool gövdesi semantiğini test eder. Authorization/approval registry seviyesi davranışlardır ve `tests/AgentPrism.Core.UnitTests/Tools/` içindeki mevcut fonksiyonel testlerde kalır; sözleşme paketine Core bağımlılığı eklemek K-605'in kurduğu bağımlılık sınırını (yalnız `AgentPrism.Abstractions`) bozardı. | Üçüncü taraf geri bildirimi tenant/timeout davranışının da sözleşme paketinde test edilebilir olmasını isterse — bağımlılık maliyeti yeniden ölçülür |
| **K-617 — Content guard'ın normalize edemediği tool sonucu için fail-closed yolu, guard PATTERN eşleşmesinden TAMAMEN bağımsız, koşulsuz bir değiştirme olarak yeniden yazıldı (Faz 102, bağımsız denetim bulgusu — kapanmadan kapatılan 🔴)** | 2026-08-25 | ÖLÇÜLDÜ (red→green, `git stash` ile eski koda karşı test): `ContentGuardMessageMasker.ReadText` normalize edilemeyen bir `FunctionResultContent` için sabit bir yer tutucu metin (`"[Tool result could not be inspected]"`) döndürüyordu, ama bu metin guard'ın DESEN eşleşmesine sokuluyordu — eşleşme olmadığı (neredeyse hep) durumda `InspectAsync` `null` dönüyor ve orijinal ham içerik hiç dokunulmadan modele/kalıcı kayda gidiyordu. Bu, `ContentGuardPipeline`'ın kendi XML sözüyle ("content that cannot be inspected is not let through") ve planın Açık Soru 1/seçenek A kararıyla ("koşulsuz değiştir") doğrudan çelişiyordu. Aynı buggy desen `ContentGuardingChatClient`'ın iki çıktı-maskeleme metodunda da tekrarlanmıştı (şu an ulaşılabilir değil — model çıktısı `FunctionResultContent` taşımıyor — ama aynı kod yolu). Düzeltme üç çağrı sitesini tek paylaşılan `ContentGuardMessageMasker.RewriteContentAsync`'e topladı; normalize edilemeyen sonuç artık koşulsuz değiştiriliyor ve `ContentGuardPipeline.RecordUninspectableToolResultAsync` bir `ContentMasked` olayı yazıyor (yalnız gerçek karar yolunda, `PreviewAsync`'te değil). | Guard pattern-matching mimarisi kökten değişirse (örn. semantic/embedding tabanlı guard) bu koşulsuz-değiştirme kuralı yeniden değerlendirilir |
| **K-618 — BYOK iki ayrı public interface'e bölündü: `IModelProvider.CreateChatClient(binding)` yalnız setup credential, yeni `ITenantCredentialModelProvider.CreateChatClient(binding, credential)` yalnız tenant credential alır; capability yok ise registry provider'ı hiç çağırmadan `provider_credential_unsupported` ile fail-closed olur (Faz 103) (kullanıcı kararı)** | 2026-08-25 | Eski imza `CreateChatClient(ModelBinding, ModelProviderCredential? = null)` mandatory/optional çelişkisi üretiyordu: base interface nullable credential alıyor ("her provider destekler" izlenimi) ama `ModelProviderCredentialContract` bunu opt-in davranış diye belgeliyordu; registry credential'ı capability kontrolü olmadan doğrudan `CreateChatClient`'a veriyordu — desteklemeyen bir provider'a tenant key'i sessizce yok sayarak setup key ile çağrılıyordu. İki interface'e bölmek capability'yi tip sisteminde ifade eder: `is ITenantCredentialModelProvider` kontrolü derleme zamanı doğrulanabilir, nullable parametreye güvenmez. Dört built-in provider + `FakeModelProvider` + BYOK sample yeni interface'i uygular; `ModelProviderCredentialContract.Provider_declares_the_tenant_credential_capability` bunu contract seviyesinde zorunlu kılar. | Üçüncü taraf geri bildirimi tek-interface + capability flag tercih ederse (ölçülmedi — yayın öncesi karar) |
| **K-619 — Provider construction hatası normalizasyonunda AgentPrism'in KENDİ validation hatası, internal ve tek başına inşa edilemez (`InternalsVisibleTo` ile korunan) bir işaretçi tipiyle "foreign" sayılmaktan çıkarıldı (Faz 103)** | 2026-08-25 | ÖLÇÜLDÜ: `ModelProviderRegistry.BuildPipeline`'ın construction try/catch'i `ModelProviderSettings.Validate`'in attığı düz `AgentPrismException`'ı (ör. "unrecognized provider setting") "bilinmeyen tip, foreign say" kuralıyla generic `upstream_error`'a maskeliyordu — iki mevcut fonksiyonel test (`MultiProviderTests.Unrecognized_provider_setting_...`, `Azure_provider_states_...`) bunu yakaladı. Base `AgentPrismException`'ı doğrudan güvenilir saymak (geniş allowlist) reddedildi: tip public ve constructible, üçüncü taraf bir provider `new AgentPrismException("secret: ...")` ile maskelemeyi bilerek atlatabilirdi (plan 103.2.2'nin yasakladığı tam senaryo). Çözüm: `AgentPrism.Abstractions`'da internal `ProviderSettingsValidationException` (yalnız `AgentPrism.Core`'a `InternalsVisibleTo` ile görünür, başka hiçbir derleme onu inşa edemez) — `ModelProviderSettings.Validate` bunu atar, `ProviderFailureNormalizer.IsKnownSafe` yalnız bu tipi ekler. Yeni PUBLIC exception eklenmedi (plan yasağı korundu). | — |
| **K-620 — `AgentPrismJudgeException` kaldırıldı; stable judge error code'ları (`judge_failed`/`judge_timeout`/`judge_contract`) internal sabitlere taşındı; `IAgentPrismBuilder.AddRunJudge` `AddAgentSource` deseniyle üç overload (generic/instance/factory) olarak eklendi (Faz 103) (kullanıcı kararı)** | 2026-08-25 | Repo genelinde ölçüldü: `AgentPrismJudgeException`'ın sıfır constructor çağrısı vardı; runtime yalnız onun sabit alanlarını kullanıyordu — "belki sonra" diye tutulan kullanılmayan public yüzey. `AddRunJudge` öncesi custom judge guide'ı ham `services.AddSingleton<IRunJudge, T>()` kullanmak zorundaydı; `AddAgentSource`'un üç overload'lu deseni (generic `TryAddEnumerable`, instance/factory `AddSingleton`, tekrar registration idempotent) burada da uygulandı — `AgentPrismBuilderRunJudgeTests` singleton/duplicate davranışını `AddAgentSource`'unkiyle aynı ölçer. `PublicAPI.Shipped.txt` K-603 gereği boş olduğundan kaldırma maliyeti sıfırdır. | — |
| **K-621 — `JudgeTimeout` cooperative cancellation değil GERÇEK wait cutoff'tur; token'ı yok sayan judge gövdesi timeout'ta öldürülmez, arkada tamamlanır ve geç sonuç sessizce atılır (skor/summary/metric yazmaz, unobserved exception üretmez) (Faz 103) (kullanıcı kararı)** | 2026-08-25 | Eskiden `JudgeTimeout` yalnız linked cancellation token'ı iptal ediyordu — token'ı okumayan bir judge (network'süz deterministic olsa bile yanlış yazılmış) handler'ı süresiz tutuyordu. `OnlineEvalJobHandler.JudgeOneAsync` artık body'yi `Task.WhenAny(body, Task.Delay(timeout))` ile bekler; timeout kazanırsa `judge_timeout` retryable failure döner ve body arkada devam etmesine izin verilir. Geç tamamlanan `Task` ayrı bir gözlemci ile tüketilir (başarı sessizce yutulur, hata uygun seviyede loglanır) — `TaskScheduler.UnobservedTaskException` üretilmez. Host/caller cancellation (`OperationCanceledException`) timeout'tan ayrı kalır ve `judge_timeout`'a çevrilmez. Deterministic test `TaskCompletionSource` gate'i ile late success VE late fault'u ayrı vakalarda kanıtlar; `Task.Delay(5s)` gibi wall-clock flaky test kullanılmadı. | F-152'nin per-judge durable checkpoint'i bu timeout modeline yeni bir katman eklerse |
| **K-622 — `kapi.py yayin` beş extension sample'ını (+ Native AOT smoke) izole `NUGET_PACKAGES` cache'i ve tek exact packed version ile doğrulayan `release_extension_samples.py`'a bağlandı; AOT publish restore+publish TEK komutta birleştirildi (Faz 103)** | 2026-08-25 | `release_extension_samples.py` bu fazın planıyla birlikte yazılmıştı ama `kapi.py`'nin `yayin` komutuna HİÇ bağlanmamıştı — script hiç koşmuyordu. Bağlarken ölçülen üç gerçek hata düzeltildi: (1) repo merkezi `ArtifactsPath` kullanır (`artifacts/obj/<Proje>/project.assets.json`), script yanlışlıkla proje yanındaki `obj/`'a bakıyordu; (2) Native AOT publish somut bir RID ister, `--use-current-runtime` izole cache'e runtime pack'i güvenilir çekmiyordu — `dotnet --info`'dan RID okunup `-r` ile açıkça verildi; (3) AOT projesi için AYRI `dotnet restore` + `dotnet publish --no-restore`, ILCompiler'ın native paketini izole cache'e eklemiyordu (`PrivateSdkAssemblies` hatası) — gerçek bir tüketicinin yaptığı gibi restore+publish tek komutta birleştirildi. `python3 scripts/kapi.py yayin --kuru --surum <sürüm>` artık uçtan uca yeşildir: 20 paket + npm dry-run + beş sample + izole-cache Native AOT smoke. | — |
| **K-623 — Kiracı yalıtımı UYGULAMA KATMANINDA tek hat kalır; veritabanı RLS'i (Row Level Security) eklenmez (Faz 104)** *(kullanıcı kararı)* | 2026-08-25 | Ölçüldü: `grep -rn "ROW LEVEL SECURITY" src/` **0** sonuç veriyor ve yalıtım tümüyle `ITenantContext` + sorgu katmanındadır. Üç gerekçe: (1) Yalıtımın zaten bir KAPISI var — `TenantCoverageTests` (Faz 41) paylaşılan store katmanının her public metodunu ya kiracı sözleşmesiyle test edilmiş ya `[TenantAgnostic]` ile gerekçeli muaf olmaya zorlar, bayat girdi de hatadır; RLS ikinci bir hat kurar ama bu kapıyı güçlendirmez. (2) SQLite'ta RLS **yoktur**; PostgreSQL ve SQL Server'a eklemek üç sağlayıcının davranışını ayrıştırır ve `RunStoreContract`'ın "davranış sözleşmesi sağlayıcıdan bağımsızdır" iddiasını kırar. (3) Doğrulama zemini eksik: gerçek `mssql/server` yerelde hâlâ koşturulamıyor (K-186, K-317), SQL Server security policy'lerini yalnız `azure-sql-edge` üzerinde kanıtlamak bir güvenlik sınırı için yeterli kanıt değildir. Karar bir SAVUNMA DERİNLİĞİ reddi değil, bir sıralama kararıdır: RLS bağlantı başına kiracı bağlama disiplini ister (`SET LOCAL`), bu da bugünkü havuzlanmış bağlantı modelini değiştirir. Beyan tarafı bu fazda kapatıldı: `MIMARI-GUVENLIK.md` §Çok kiracılılık ve `concepts/governance.md` yalıtımın hangi katmanda durduğunu açıkça söyler. | Üç sağlayıcının hepsinde gerçek container üzerinde doğrulanabilir bir zemin oluştuğunda (K-317'nin koşuluyla aynı), veya bir tüketici veritabanı seviyesinde yalıtım talep ettiğinde RLS savunma derinliği olarak yeniden değerlendirilir |
| **K-624 — `Production` ortamında kalıcı olmayan store BAŞLANGIÇTA UYARIR; hata fırlatılmaz ve susturma seçeneği eklenmez (Faz 104)** *(kullanıcı kararı)* | 2026-08-25 | Kalıcı olmayan store desteklenen bir moddur, bozuk kurulumun yedeği değil — arayüz metni (`settings.inMemoryNotice`) bunu zaten böyle söyler ve o aynı zamanda tüketicinin hiçbir kalıcılık paketi eklemeden aldığı VARSAYILANDIR. `ToolRegistrationValidationService`'in "fırlat + opt-out" deseni burada uygulanmadı: orada bozulan şey bir güvenlik sarmalayıcısıdır, burada ise bilinçli olabilecek bir kurulum tercihidir; fırlatmak meşru bir demo/test kurulumunu ilk çalıştırmada düşürür ve K1 (sıfır sürpriz) ihlalidir. Susturma seçeneği de eklenmedi — tek bir kalkış satırı için public yüzey büyütmek Faz 96'nın küçültme yönüne terstir. `AgentPrismDiagnosticsReport`'a alan EKLENMEDİ: tip public ve alanları `required`, yayından sonra yeni `required` alan eklemek kırıcıdır ve rapor store'un adını (`PersistenceProvider`) zaten taşır — eksik olan yalnız "bu Production" yargısıydı, onu da log satırı veriyor. Yargının kendisi `StorePersistence` içinde TEK kaynaktır; `/api/meta` ve uyarı aynı metodu okur (K-483'ün elle tekrarlanan ifade kusur sınıfı). | Tüketici geri bildirimi log satırının kaçırıldığını gösterirse teşhis raporuna alan eklemek yeniden değerlendirilir — bu, `required` olmayan bir alan olarak yapılmalıdır |
| **K-625 — Üç SQL sağlayıcısına `Options.DataSource` alanı eklendi; AgentPrism kendi kurduğu `NpgsqlDataSource`/`SqlServerDataSource`/`SqliteDataSource`'u artık PUBLIC bir DI servisi olarak kaydetmez; `DataSource` ve `ConnectionString` birlikte verilirse başlangıç hatası (sessiz öncelik yok) (Faz 110)** | 2026-08-26 | Ölçüldü: `NpgsqlDataSource` bugüne kadar `TryAddSingleton` ile public kaydediliyordu — iki yönlü sıra yarışı (tüketicinin kendi kaydı AgentPrism'e sızabilir, ya da tersi) ve `ConnectionString`'in doğrulayıcısı dış kayıt kazandığında da koşulsuz zorunlu kalıyordu. Kayıt kaldırıldı; data source artık yalnız `SqlStoreContext` üzerinden taşınır ve `SqlStoreContext.OwnsDataSource` sahipliği taşır — dış data source AgentPrism tarafından ASLA dispose edilmez (`ExternalDataSourceTests`, üç sağlayıcıda gerçek sunucuya karşı kanıtlandı). PostgreSQL'de `DataSource` yalnız `NpgsqlDataSource` tipini kabul eder (runtime tip kontrolü; public imzada Npgsql tipi yok) — pgvector deposu zaten bu somut tipi ister ve tek üretim kalitesinde uygulama budur. Ölçüm ayrıca `embedding.md`'nin "aynı connection string iki tarafı tek Npgsql havuzunda buluşturur" iddiasının YANLIŞ olduğunu kanıtladı (`ConnectionPoolSharingTests`: 5+5 eşzamanlı bağlantı, ölçülen backend sayısı tam 10 — havuz Npgsql'de connection string'e değil `NpgsqlDataSource` ÖRNEĞİNE aittir); doküman düzeltildi. `PublicAPI.Shipped.txt` K-603 gereği preview hattı boyunca boş olduğundan kaldırma maliyeti sıfırdır. | — |
| **K-626 — `runs_v1` okuma sözleşmesi görünümü üç sağlayıcıda `EnableReadViews` ile isteğe bağlı yayımlandı; yayımlanan sürüm sütun kaybetmez/yeniden adlandırmaz/daraltmaz, kırıcı değişiklik `runs_v2` olarak açılır; görünüm bir kiracı sınırı DEĞİLDİR (Faz 111)** | 2026-08-26 | Kalıcı bir veri sözleşmesi `Shipped.txt`'in kapsamadığı bir taahhüttür ve geri alması pahalıdır — sözleşme bu yüzden dar başlar (tek görünüm, yalnız üstveri ve türev; hiçbir `ProtectedColumn` girmez) ve Faz 67'nin "knowledge" desenini izleyerek OPT-IN bir migration setine konur (varsayılan kapalı, K1 sıfır sürpriz): bir tüketici hiç açmazsa veritabanında yeni bir nesne görmez. `total_cost` `SqlQueriesBase.CostAddends` ile aynı üç terimi toplar ve K-483'ün null-koruyan kuralını taşır (tüm terimler `NULL` ise toplam `NULL`, sıfır değil); bir terim eklenip görünüme yansıtılmazsa `ReadViewCostTermTests` (veritabanı açmadan, gömülü SQL metni üzerinden) build zamanında kırılır — aynı sınıf hatasının bir daha elle kaçırılmaması. `status_name` görünümde üretilir (tüketici elle eşlemez); `RunStatus`'un sayısal değerleri stabil olduğu için hand-written `CASE` güvenlidir. Görünüm `tenant_id`'yi filtresiz taşır — bu bilinçli bir tasarımdır, güvenlik sınırı yalnız `ITenantContext`/sorgu katmanındadır (K-623) ve uyarı `reference/read-views.md`'de yazılıdır. Kapanışta iki test-altyapısı kusuru bulundu ve düzeltildi: `SqliteTestContext.DisposeAsync()` ve SQL Server `DropSchemaAsync()` yalnız tabloları siliyordu — sarkan bir `runs_v1` görünümü SQLite'ta bir SONRAKİ ilgisiz `ALTER TABLE RENAME` migration'ını, SQL Server'da ise şema silmeyi düşürüyordu; ikisi de görünüm-önce-tablo sırasıyla düzeltildi (`docs/hafiza/sqlite.md`). | Bir sonraki faz `runs_v2` ihtiyacı doğurursa (kırıcı bir sütun değişikliği), veya ikinci bir görünüm (`run_events` gibi korunan içerik taşıyan bir tablo için) talep gelirse yeniden değerlendirilir |
| **K-627 — `RunErrorClass.BudgetExceeded` kaldırıldı; sayısal değer `9` KALICI OLARAK EMEKLİ, asla yeniden kullanılmaz** *(kullanıcı kararı)* | 2026-08-26 | Ölçüldü: üye "the tree or context budget was exceeded" diye beyan ediliyordu ama **hiçbir kod yolu onu üretemiyordu** — `DefaultRunErrorClassifier` hiçbir exception'ı bu sınıfa eşlemiyor ve tree budget tükendiğinde `ChildAgentInvoker` **bilerek** exception atmıyor, modele metin döndürüyor (alt-agent olmadan yanıtı toparlayabilsin diye), yani run başarılı bitiyor. Sınıf taraması on dört üyenin tamamını taradı: üreticisi sıfır olan **tek** üye buydu, yani kusur bir sınıf değil tek vaka. Beyan yine de tüm sevk edilen yüzeye ulaşıyordu — OpenAPI belgesi, TypeScript şeması, generated C# istemcisi ve **iki dil dosyası** (`en`/`tr`) hiç gerçekleşmeyen bir durumu ilan ediyordu; bu Faz 104'ün beyan doğruluğu kapsamıdır. İki düzeltme yönü tartıldı: (A) üyeyi kaldır, (B) üyeyi ürettir. **B reddedildi** çünkü `ChildAgentInvoker`'ın metin döndürme davranışı bilinçli bir tasarımdır ve onu terminal hataya çevirmek mevcut çağıranları sessizce değiştirirdi. **A seçildi**: `PublicAPI.Shipped.txt` preview hattı boyunca boş olduğu için (K-603) kaldırma maliyeti bugün sıfırdır, 1.0 sonrası aynı silme kırıcı olurdu. Değer `9` yeniden numaralandırılmadı ve **boş bırakıldı**: kayıtlı run'lar ve eski istemciler eski anlamı taşımaya devam eder, `9`'u yeniden kullanmak onlara ikinci ve çelişkili bir anlam verirdi. `RunErrorClassContractTests` hem üye kümesini hem `9`'un tanımsız kalmasını sabitler; artık üreticisiz bir üye eklemek sessizce geçemez. Kaldırma sonrası üç üretilmiş yüzey yeniden üretildi (OpenAPI snapshot, `openapi-typescript` şeması, NSwag istemcisi) ve `@agentprism/client` `dist`'i yeniden derlendi — frontend tipleri o pakete bağlı olduğu için bu adım atlanırsa `tsc` kırmızı kalır. | Yeniden açılmaz. Çalıştırma-içi maliyet tavanı (F-166) ayrı bir iştir ve kendi hata sınıfı kararını verir; `9`'u DEĞİL, ya `QuotaExceeded`'i yeniden kullanır ya yeni bir değer alır |
| **K-628 — İstemci taraflı tool (`AddClientTool`) taşıyan bir agent, `POST /replay` ile HİÇBİR `toolMode`'da (`ReplayTools`/`LiveTools`/`NoTools` üçü de) yeniden oynatılamaz; ret `RunReplayService.PrepareAsync` içinde, run hiç başlamadan, agent TANIMININ tool listesi taranarak verilir (Faz 112)** *(kullanıcı kararı)* | 2026-08-26 | Ölçüldü: istemci tool'unun sunucu tarafı gövdesi yoktur ve çağrısı hiçbir zaman kaydedilmez (`RunRecordingAgent.Completion.cs:57` `DrainUnfinished` sonucu `null` bırakır) — bugün böyle bir agent'ın run'ı replay edildiğinde istek `200 OK` dönüyor ve run **sessizce** yarım bitiyordu (hiçbir kontrol, ret veya uyarı yoktu; `grep -rn "ClientTool" src/AgentPrism.Core/Replay/` sıfır isabet veriyordu). Aday metni "kaydı tekrar oynat **veya** reddet" diye iki seçenek sunuyordu; ölçüm oynatma seçeneğini bugünkü kayıtla **imkânsız** buldu (sonuç hiçbir yerde saklanmıyor) — yalnız ret sözleşmesi kuruldu. `NoTools` da reddedilir: o modda hiçbir tool bağlanmasa bile istemci tool'u agent'ın davranışının **belirleyici** parçasıysa sonuç kaynak run'la kıyaslanamaz; tek kural ("varsa reddet") iki kuraldan ("hangi modda ne olur" tablosu) daha ucuz açıklanır. Mekanizma `ApprovalRequired`'ın (K-315/K-435, Faz 47) birebir aynadaki eşidir — `FindApprovalTool`'un yanına `FindClientTool` eklendi, `ToolDescriptor.RunsOnClient` zaten vardı (`ToolRegistry.cs:166`, `registration.Function is not AIFunction`), yeni bir tespit mekanizması yazılmadı. Kontrol İKİ hazırlık koluna da (kalıcı tanım VE katalog/kod agent'ı) eklendi — Faz 47'de yalnız birinci kol kapatılıp katalog kolu delikte kalmıştı (HATA-S4-014), aynı hata burada fonksiyonel testle (`ReplayClientToolEndpointTests`, katalog kolu ayrı case) tekrar önlendi. `ReplayToolMode`'a dördüncü üye AÇILMADI (K-585 zaten kapattı) — yeni bilgi `RunReplayOutcome.ClientToolNotReplayable`'a girdi, HTTP karşılığı `409` (`ApprovalRequired` ile aynı desen). MCP kaynaklı declaration-only tool'un yanlışlıkla `RunsOnClient=true` alması riski ölçüldü ve **doğrulanmadı**: `McpTenantTools.cs:74` yorumunun iddia ettiği gibi MCP tool'u her zaman gerçek bir `AIFunction`'dır (`McpClientTool : AIFunction`), `is not AIFunction` dalı yalnız yanlış yapılandırılmış doğrudan bir kayıtta tetiklenir — `McpTenantToolsTests.An_mcp_tool_is_never_marked_RunsOnClient` bunu şimdi kilitler. | Yeniden açılmaz — oynatma seçeneği yalnız istemci sonucunu tool defterine yazan yeni bir kalıcılık yolu açılırsa (ayrı ve daha pahalı bir iş) yeniden değerlendirilir |
| **K-629 — Sağlayıcı retry kararı için `IProviderRetryClassifier` (üç durumlu: `Unknown`/`Retry`/`DoNotRetry`) yeni public seam olarak `AgentPrism.Abstractions/Providers/` altında açıldı; `DefaultRunErrorClassifier` `internal sealed partial` → `public sealed partial`; `ErrorFingerprint`'in iç hesabı internal kalıp yeni public `RunErrorFingerprint.Compute(string?)` facade'ı eklendi (Faz 113, F-149)** | 2026-08-26 | Aday metni "değiştirilebilir bir seam **tasarla**" diyordu; ölçüm `IRunErrorClassifier`'ın (Faz 44) zaten `TryAddSingleton` ile değiştirilebilir olduğunu, boşluğun yalnız üç somut yerde olduğunu buldu: (A) retry kararının HİÇ genişleme noktası yoktu (`FallbackRetryClassifier` `internal static partial`), (B) yerleşik hata sınıflandırıcısı toptan-ya-da-hiç'ti (`internal sealed partial`, devralınamaz), (C) parmak izi hesabı erişilemezdi. `bool` değil üç durumlu seçildi: yerleşik kural "tanımadığı hata retry OLMAZ" kapalı-küme garantisi taşıyor (`FallbackChatClient.cs`'in kendi XML sözü); `bool` bir tüketici sınıflandırıcısını HER exception için taraf seçmeye zorlar ve bu garantiyi kayıt anında sessizce kırar — `Unknown` yerleşiğe düşmeyi sağlar. `DefaultRunErrorClassifier` kalıtım değil KOMPOZİSYON için public yapıldı (repo `sealed` tercih eder, K3); sıfır bağımlılık taşıdığı için `new()` ile DI'sız kurulabilir. `ErrorFingerprint`'in kendisi değil ince bir `RunErrorFingerprint` facade'ı açıldı (`ErrorFingerprint` `partial` ve `GeneratedRegex` taşıyor — iç detayını sözleşmeye çevirmek yerine tek bir giriş noktası açmak ucuzdur). Kapanışta ayrıca ölçüldü: `WorkflowNodeRetry` (`AgentPrismWorkflowFunctionExtensions.cs:118`) `IRunErrorClassifier`'ı AYNI DI singleton'ından okuyor — tüketicinin kaydı workflow retry'ını da otomatik kapsar, kod değişikliği gerekmedi. `PublicAPI.Shipped.txt` preview hattı boyunca boş olduğundan (K-603) yüzey büyütme maliyeti bugün sıfırdır. | Yeniden açılmaz — `IProviderRetryClassifier`'a dördüncü bir karar değeri eklemek (bugünkü üçün dışında) ayrı bir ölçümle gerekçelenmeli |
| **K-630 — Çalıştırma-içi bütçe kesmesi (`AgentPrismRunBudgetExceededException`, `run_budget_exceeded`) yeni bir `RunErrorClass` üyesi AÇMADAN mevcut `QuotaExceeded` (`4`)'e eşlenir; `9` K-627'nin kararıyla kalıcı emekli kalır (Faz 114, F-166)** *(kullanıcı kararı)* | 2026-08-26 | K-627'nin "sonraki adım" sütunu bu kararı açıkça bu faza devretmişti. İki yön tartıldı: (A) `9`'u yeniden kullan, (B) mevcut `QuotaExceeded`'i genişlet. **A reddedildi**: K-627 `9`'u kalıcı emekli ilan etmişti — kayıtlı run'lar ve eski istemciler o değeri hâlâ eski (hiç üretilmemiş `BudgetExceeded`) anlamıyla taşıyor; yeniden kullanmak onlara ikinci ve çelişkili bir anlam verirdi. **B seçildi**: kullanıcının gördüğü olgu zaten aynıdır — bir harcama tavanı doldu; mevcut istemciler, `RunErrorStatistics` panelleri ve arıza kümeleme kodu değişmeden çalışır, OpenAPI/TypeScript/NSwag/`dist` üretim zinciri hiç koşmaz. Eşleme regex ile DEĞİL, `DefaultRunErrorClassifier.StableIdentities` sözlüğüne tipli bir kimlik (`AgentPrismRunBudgetExceededException.RunBudgetExceededErrorType`) eklenerek yapıldı — `ToolTimeout`/`ContentFiltered` ile aynı desen; bugünkü `QuotaPattern()` mesajda "quota" kelimesi arayan regex'e güvenmek Faz 113'ün düzelttiği kırılganlığın aynısı olurdu. `DefaultRunErrorClassifierTests` mesajında kasıtlı olarak "quota"/"kota" GEÇMEYEN bir örnekle bu eşlemenin regex'e değil kimliğe dayandığını kanıtlar. | Yeniden açılmaz — `9` hâlâ boş; run-içi bütçe kesmesi için ayrı bir sınıf istenirse yeni bir üye (`14`) açılır, `9` asla |
| **K-631 — `ModelProviderRegistry`'nin kurucusu `IRunPricingResolver?` yerine `IServiceProvider?` alır; `RunBudgetChatClient` fiyat çözümleyiciyi boru hattı KURULUM anında (`BuildPipeline` içinde) GEÇ (lazy) çözer (Faz 114, F-166)** | 2026-08-26 | Ölçüldü: `RunPricingResolver`'ın kendisi `IModelProviderRegistry`'ye bağımlıdır (fiyat kataloğunu taramak için) — `ModelProviderRegistry`'nin kurucusuna DOĞRUDAN `IRunPricingResolver?` eklemek, DI konteynerinin çözemeyeceği bir DÖNGÜ üretirdi (`ModelProviderRegistry` ister `IRunPricingResolver` → `RunPricingResolver` ister `IModelProviderRegistry` → henüz inşa edilmemiş AYNI singleton). Planın taslak imzası ("kurucuya sona iki isteğe bağlı parametre eklenir: `IRunPricingResolver?`, `TimeProvider?`") bu döngüyü öngörmüyordu; `faz-uygulama`'nın "planın yapısal iddiasını kabul etmeden ölç" kuralı gereği kod yazılmadan önce ölçüldü ve plan bu noktada düzeltildi. Çözüm: kurucu `IServiceProvider? services` alır (aynı desen `AgentDefinitionCompiler._services`'te zaten vardı), `BuildPipeline` her çağrıldığında (agent DERLEME anında, DI konteyneri tamamen kurulduktan ÇOK SONRA) `_services?.GetService<IRunPricingResolver>()` ile geç çözer — o anda `ModelProviderRegistry` singleton'ı zaten önbelleğe alınmış olduğundan döngü kırılır. | Yeniden açılmaz — benzer bir döngü (bir servis kendi kaydeden registry'ye bağımlıysa) aynı geç-çözümleme desenini tekrarlar |
| **K-632 — `AgentRunBudget`'ın maliyet sayacı `decimal` değil, `long` sabit noktalı nano-birim (`1/1_000_000_000`) + `Interlocked.Add`'tir; `RunRecordingAgent.CompleteAsync`'in run sonu `scope.Budget?.RecordUsage(...)` satırı KALDIRILDI — bütçe muhasebesinin %100'ünü artık `RunBudgetChatClient` taşır (Faz 114, F-166)** | 2026-08-26 | **Açık Soru 2** (kilitsiz `decimal` sayaç): `decimal` için `Interlocked.Add` YOKTUR; altı basamak (mikro-birim) token başına maliyeti (ör. milyon token başı $0.15 = token başı $0.00000015) sıfıra yuvarlardı, dokuz basamak (nano-birim) `long`'un taşma sınırının (~9,2 milyar para birimi) çok altında kalarak bunu önler — tip bugün TAMAMEN kilitsizdir (`AgentRunBudget`'ın kendi XML sözü) ve tek bir alan için kilit koymak o tasarım kararını bozardı. **Açık Soru 1** (çifte sayım): kod okunarak ölçüldü — sıkıştırma (context compaction) özetleme çağrısı da (`ResolveSummarizationChatClient` → `CreateChatClient(definition, binding)` → `_models.CreateChatClient(binding)`) ana agent turlarıyla AYNI `ModelProviderRegistry.BuildPipeline` boru hattından geçer, yani `RunBudgetChatClient` HER gerçek model çağrısını (ana turlar + sıkıştırma) zaten görüyor. Bu, planın B seçeneğinin ("dekoratör kaydeder, `Completion` yalnız GÖRMEDİĞİNİ ekler") öngördüğünden daha güçlü bir sonuçtur: dekoratörün GÖRMEDİĞİ hiçbir şey kalmıyor, bu yüzden `Completion.cs`'in eski satırı "eksik parçayı ekleyen" değil TAMAMEN gereksiz hale geliyor — bırakılsaydı her ağacın harcamasını iki katına çıkarırdı. `RunBudgetAccountingTests` (gerçek çok-turlu tool döngüsü, `ModelProviderRegistry`+`FakeChatClient` üzerinden) bunu `ConsumedTokens`'ın gerçek toplamla TAM eşleştiğini ölçerek kanıtlar. | Yeniden açılmaz — sıkıştırmanın boru hattı konumu değişirse (ayrı bir karar gerektirir) bu varsayım yeniden ölçülmeli |
| **K-633 — `AgentPrism.Client`'ın üretilmiş `System.Text.Json.JsonElement`/`ChatRole`-tipli alanları geriye dönük UYUMSUZ biçimde düzeltildi (`scripts/nswag-postprocess-client.py`); `AgentPrismPublicApiTrackingEnabled=false` olduğu için derleyici bunu işaretlemedi (Faz 115, eval CLI komutu sırasında bulunan önceden var olan kusur)** | 2026-08-27 | Kusur: NSwag, kendi `[JsonConverter]`'ı olan bir değer tipi (bir JSON NESNESİ olarak tanımlanamayan) için boş `{}` şema üretilince, o tipin KISA ADIYLA (`JsonElement`, `ChatRole`) bir POCO sınıfı üretiyor ve bu sınıf AYNI ad alanında GERÇEK tipi gölgeliyordu — 16 alanın TÜMÜ (`EvalCaseResult.Scores` dahil) gerçek veriyle deserialize edilemiyordu (`docs/hafiza/paketleme-ve-dagitim.md`). Düzeltme ZORUNLU olarak tip DEĞİŞTİRİYOR: `EvalCaseResult.Scores` vb. artık gerçek `System.Text.Json.JsonElement`, `ChatMessage.Role` artık `string` (`Microsoft.Extensions.AI.ChatRole` DEĞİL — `AgentPrism.Client` o pakete bilerek bağımlı değildir, `ChatRole`'ün kendi `[JsonConverter]`'ı zaten düz string üretiyor). Bu, klasik anlamda geriye dönük UYUMLU bir düzeltme değildir (tip imzası değişti) ama eski tip zaten İŞLEVSİZDİ (her deserialize denemesi `JsonException` fırlatıyordu) — bu yüzden yeni K-* kaydı gerektiren "kalıcı bir mimari tercih" değil, bir KUSUR DÜZELTMESİdir; kayıt yalnız iz bırakmak için açıldı. `scripts/nswag_postprocess_client_test.py` regresyonu, `docs/hafiza/paketleme-ve-dagitim.md` tuzağı taşır. | Yeniden açılmaz — yeni bir "opak değer tipi" (kendi `[JsonConverter]`'ı olan, boş şema üreten) keşfedilirse `COLLIDING_ANY_TYPES` tablosuna eklenir, yeni bir karar gerekmez |
| **K-634 — Performans tahsis kapısı yalnız TAHSİS EDİLEN BAYTI karşılaştırır (sıfır tolerans), süreyi bilgi olarak kaydeder ama hiçbir şeyi kırmaz; beşinci bağımsız bir kapı DEĞİL, `kapi.py kapanis`'in üç sıcak yol dosyasından biri değiştiğinde koşturduğu koşullu bir adımdır (Faz 116, F-67)** *(kullanıcı kararı)* | 2026-08-27 | Ölçüldü: paylaşılan bir CI makinesinde tahsis edilen bayt aynı kod için deterministiktir (komşu işlerden etkilenmez), duvar saati etkilenir — dar tolerans yanlış kırmızı üretir, geniş tolerans gerçek gerilemeyi kaçırır. Bu yüzden kapı yalnız tahsisi karşılaştırır ve toleransı **sıfırdır**: aynı kodun iki koşumu birebir aynı baytı verdiği `bench/AgentPrism.Benchmarks` üzerinde doğrulandı (üç benchmark, tekrarlanan koşum). Beşinci bağımsız bir kapı ilan etmek `AGENTS.md`'nin "dördü de sıfır uyarı vermelidir" cümlesini değiştirirdi ve her faz kapanışına BenchmarkDotNet koşumu eklerdi; bunun yerine `scripts/kapi.py performans` normal bir alt komut olarak kalır, `kapi.py kapanis` bunu yalnız `RunEventWriter.cs`/`CompiledAgentCache.cs`/`SqlRunStore.cs`/`bench/` değiştiğinde çağırır (`performance_gate_triggered`, `--taban` aralığına göre). Taban çizgisi `bench/baseline.json`'da tutulur; azalan tahsis kırmızı DEĞİLDİR ama uyarı üretir (taban çizgisi bayatladı) — `kapi.py performans --guncelle` ile güncellenir. | Yeniden açılmaz — ayrı bir OS'te tahsis sayısı farklı çıkarsa (fazın temel varsayımı çöker) kapı yeniden tasarlanmalı; bu güne kadar yalnız macOS/Apple Silicon'da doğrulandı, ikinci bir işletim sistemi doğrulaması **açık kalemdir** |
| **K-635 — BenchmarkDotNet 0.15.8 yalnız `bench/AgentPrism.Benchmarks` (`IsPackable=false`) için eklendi; K-007'nin "yeni paket gerekçe ister" barı gerçek restore ile ölçülerek karşılandı (Faz 116, F-67)** | 2026-08-27 | Gerçek restore ile ölçüldü (net10.0, temiz proje, yalnız nuget.org): 22 geçişli paket (K-212'nin Azure AI Foundry'yi erteleten 37 paketinden az). Asıl fark sayı değil yöndür: K-212'nin kaygısı tüketicinin bağımlılık grafiğinin kirlenmesiydi; bu paket `IsPackable=false` bir ölçüm projesindedir ve hiçbir sevk edilen paket ona referans vermez — 22 paket tüketiciye HİÇ ulaşmaz, yük yalnız geliştirme/CI tarafındadır. `Microsoft.Extensions.*`'ın çözümlenen sürümü ölçüldü: repo'nun sabitlediği `10.0.11`'de kaldı (`6.0.0`'a düşmedi) — `dotnet list package --include-transitive` ile doğrulandı. `NU1903` (bilinen CVE) restore'u kırmadı. | Yeniden açılmaz — BenchmarkDotNet'in geçişli ağırlığı ileride önemli ölçüde artarsa (örn. büyük bir majör sürüm) yeniden ölçülmeli |
| **K-636 — `ModelContextProtocol.Extensions.Tasks` 2.2.0 yalnız `AgentPrism.AspNetCore`'a eklendi; net yeni geçişli paket sıfırdır, `AgentPrism.Mcp` (istemci) bu paketi görmez (Faz 117, F-167)** | 2026-08-27 | Gerçek restore ile ölçüldü (net10.0, temiz proje, yalnız nuget.org): yalnız `ModelContextProtocol.AspNetCore` 13 paket çekiyordu, uzantı eklenince 14 — tek net yeni paket kendisidir. Uzantının çektiği on iki geçişli paketin tamamı (`ModelContextProtocol`, `.Core`, `Microsoft.Extensions.*` ailesi) `.AspNetCore` üzerinden zaten grafikteydi. K-057'nin istemci/sunucu ayrımı bozulmadı: `DependencyDirectionTests.Mcp_client_package_does_not_depend_on_server_packages` bu paketi de yasak listesine aldı ve `AgentPrism.Mcp`'nin `.csproj`'unda görünmediğini doğrular. | Yeniden açılmaz |
| **K-637 — MCP task kimliği AgentPrism'in run kimliğidir; SDK'nın `IMcpTaskStore.CreateTaskAsync()`'i çağrının hangi agent/kiracı olduğunu bilmediği için bu eşleşme kayıt-öncesi bir `AsyncLocal` sağlayıcı filtresiyle (`McpTaskRunProvisioningFilter`) kurulur; arka plan yürütmesi kendi `AmbientTenantScope` sarmalını taşır; kiracı-oblivious SDK-içi `tasks/cancel` müdahalesi kabul edilen, kapatılamayan bir sınır olarak belgelenir (Faz 117, F-167) (kullanıcı kararı)** | 2026-08-27 | Plan `IMcpTaskStore.CreateTaskAsync()`'in çağrı bağlamına erişebileceğini varsayıyordu; paket decompile edilerek ölçüldü (2.2.0) ve YANLIŞ çıktı — metot yalnız bir `CancellationToken` alır, SDK asıl tool çağrısını kendi `Task.Run(...)`'ıyla arka planda çalıştırır. Çözüm: `.WithTasks(...)`'tan ÖNCE kayıtlı kendi `CallToolWithAlternateFilters` girdimiz `RunId`+`AgentName`+`TenantId`'yi orijinal istekte üretip `AsyncLocal` ile aşağı akıtır (filtreler kayıt sırasına göre çalışır); bu yönde AsyncLocal'ın güvenilir aktığı gerçek `Task.Delay` ile zorlanan askıya alma dahil ölçüldü. İKİ ayrı kiracı sınırı kusuru fonksiyonel testle bulundu, statik incelemeyle DEĞİL: (1) SDK'nın arka plan `Task.Run`'ı hiçbir `HttpContext` taşımaz, `ITenantContext` sessizce varsayılan kiracıya düşerdi — `CatalogToolCallHandler.HandleAsync` artık `AmbientTenantScope.Begin(...)` ile sarmalanır (mevcut `JobWorkerBackgroundService` deseninin aynısı). (2) SDK'nın `tasks/cancel`'ı, hangi kiracının çağırdığına BAKMADAN, YALNIZ taskId ile anahtarlanan paylaşılan bir `CancellationTokenSource` sözlüğünü koşulsuz iptal eder — AgentPrism'in bu sözlüğe erişimi yoktur, kapatılamaz. Kabul edilen sınır: bir kiracı BAŞKA kiracının task id'sini bilerek onu iptal EDEBİLİR (SDK'nin kendi kusuru), ama ASLA okuyamaz — okuma tarafı (`GetTaskAsync`) hâlâ yalnız çağıranın kendi ambient kiracısını kullanır, bu izolasyonun tek gerçek sınırıdır. `RunBackedMcpTaskStore` `CreateTaskAsync`'te öğrendiği gerçek sahip kiracıyı ayrı bir haritada saklar; orphan-temizlik yazımları (`SetCancelledAsync`/`SetFailedAsync`) çağıranın ambient'ine değil BU haritaya güvenir — SDK'nın kendi post-cancellation catch'i, bizim tenant scope'umuz zaten dispose olmuşken çalıştığı için bu ayrım zorunludur. `McpTaskTenantIsolationTests` (fonksiyonel, dört senaryo) ve gerçek `samples/AgentPrism.Api` koşumu (PostgreSQL + gerçek model çağrısı) ikisini birden kanıtlar. | Yeniden açılmaz — SDK'nın gelecekteki bir sürümü `_cancellationSources`'a kiracı farkındalığı eklerse (kapatılamayan sınır ortadan kalkarsa) not güncellenir ama tasarım değişmez |
| **K-638 — Yargıç başına retry checkpoint'i yeni bir tablo/migration AÇMADAN mevcut `run_scores` satırlarından okur; bu, K-621'in "sonraki adım" sütununun sorduğu soruya (F-152 timeout modeline yeni bir katman ekler mi) HAYIR cevabıdır (Faz 118, F-152) (kullanıcı kararı)** | 2026-08-27 | K-621 timeout'a düşen bir yargıcın gövdesinin arkada sürdüğünü ve geç sonucun SESSİZCE atıldığını kaydetmişti; "sonraki adım" sütunu bu fazın o modele yeni bir katman ekleyip eklemeyeceğini sordu. Ölçüldü: `OnlineEvalJobHandler.JudgeOneAsync` zaten yargıç BAŞINA `scoreStore.UpsertAsync` çağırıyordu ve satır `Author = "judge:{ad}"` taşıyordu — checkpoint için gereken bilgi ZATEN veride duruyordu, eksik olan yalnız döngünün retry'de bu satırı OKUMASIYDI. Çözüm `JudgeRunAsync`'e `skipAlreadyScored` parametresi ekledi (yalnız `context.Job.Attempt > 1` iken `true`); döngüden ÖNCE `run_scores` okunur, `judge:` önekiyle eşleşen yargıçlar atlanır. K-621'in modeli DEĞİŞMEDİ: timeout'a düşen bir yargıç satır bırakmaz, bu yüzden retry'de HÂLÂ yeniden koşar — bu fazın kazancı yalnız BAŞARILI yargıçları kapsar. Okuma hatası atlamayı kapatır (tüm yargıçlar koşar), elle `POST /judge` ucu hiç etkilenmez. Aday metninin "kalıcı model ve üç SQL sağlayıcı migration'ı gerekir" maliyet iddiası bu ölçümle çürüdü — `PublicAPI.*.txt` ve migration dizini değişmedi. | Yeniden açılmaz — çekimser yargıcın (`Score is null`) checkpoint'lenmesi veya timeout'un geç sonucunun kurtarılması ayrı, ölçülmüş bir vaka gerektirir |
| **K-639 — Kiracı sağlayıcı bağlantısında (`BYOK`) `provider` adı, karşılaştırıcı değiştirilerek değil DEĞER NORMALLEŞTİRİLEREK case duyarsız yapılır; kural `TenantProviderBinding.NormalizeProviderName` olarak public'tir ve store hem yazarken hem sorgularken uygular (Yayın denetimi, BL-006)** | 2026-08-27 | Düşen testle yeniden üretildi: `provider` adı sistemde yedi yerde `OrdinalIgnoreCase` ile eşleşirken (`ModelProviderRegistry` sözlüğü, egress politikası, yönetim ucu, fiyat override'ı, sağlık önbelleği, eşzamanlılık sınırlayıcısı) yalnız `ITenantProviderBindingStore` `Ordinal` idi. Yönetici `"OpenAI"` kaydedip agent tanımı `"openai"` dediğinde PostgreSQL/SQLite'ta arama ıskalıyor, ıskalama `null` dönüyor ve `ResolveTenantCredentialAsync` bunu "BYOK yok" sayıp **global setup credential'ına düşüyordu** — kiracının anahtarı hiç kullanılmadan, hata üretmeden, yanlış tarafa fatura yazarak. Bu, aynı metodun 330-332. satırındaki "sessizce global anahtara düşmemelidir" korumasının kapatmadığı ikinci daldı. Karşılaştırıcıyı `LOWER(...)` yüklemine çevirmek REDDEDİLDİ: indeksi kullanılamaz kılar ve birincil anahtarı üç motorda ayrışık bırakır (PostgreSQL/SQLite `"OpenAI"`+`"openai"` satırlarının ikisini birden kabul ederdi, SQL Server varsayılan CI collation'da reddederdi). Değer normalleştirilince düz `=` üç motorda da aynı davranır, indeks korunur, PK yinelemeyi her yerde reddeder. Kural üçüncü tarafın da uygulaması zorunlu olduğu için public'tir; `TenantProviderBindingStoreContract`'ın üç yeni case-mismatch case'i dört implementasyonun hepsinde koşar. Mevcut satırlar üç migration ile katlanır (PostgreSQL 0038, SQLite/SQL Server 0025); SQL Server'da `COLLATE Latin1_General_BIN2` taşıyıcıdır — CI collation altında `provider_name <> LOWER(provider_name)` her zaman false döner ve UPDATE sessizce sıfır satır günceller. | Yeniden açılmaz — `provider` adına harf dışı kimlik kuralı (örn. ad şekli doğrulaması) gerekirse ayrı bir kalem olarak açılır |

---

## 3. Yeni Karar Ekleme Şablonu

Bir iş "kanıt yok → yapma" ile kapatıldığında veya kalıcı bir mimari tercih yapıldığında ilgili tabloya satır ekle:

```
| <karar — ne yapılmadı/yapıldı> | YYYY-AA-GG | <gerekçe — hangi kanıt/ölçüm/kısıt> | <hangi koşul gerçekleşirse yeniden açılır> |
```

Kurallar:
- Gerekçesiz karar ekleme — "istemedik" yeterli değil; neden istenmediği yazılmalı.
- Bir kalem yeniden açıldığında satırı silme; sonuna "**(yeniden açıldı: YYYY-AA-GG, sebep)**" ekle ve işi normal akışta planla.
- Kullanıcı kararlarını `(kullanıcı kararı)` etiketiyle işaretle — bunlar teknik kanıtla değil, ancak kullanıcıyla konuşularak değişir.
